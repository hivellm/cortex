# Changelog

All notable changes to **Cortex** will be documented in this file.

The format is loosely based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased] — 0.1.0-alpha

> First substantive cut of Cortex — workspace, ingestion bus, classifier worker, the four indexers, query API, dashboard backend + GUI, MCP server, and Claude Code plugin. Spec coverage: **13/18 🟢**, **5/18 🟡**.

### Changed

- **phase13e §3.4 (tail) + §3.6 — ADR-016 CI grep gate is GREEN.** `rg "std::env::var\(\"CORTEX_" crates/ --type rust --files-with-matches | grep -v cortex-config | grep -v cortex-build | grep -v tests` returns empty. 28 additional Config knobs landed across 4 new sub-structs (`AnalyzerConfig`, `ClaudeArchiveConfig`, `AdapterConfig`) + field extensions to `ClassifierConfig`, `DashboardConfig`, `EmbedderConfig`, `IngestionConfig`, `MeiliConfig`, `NexusConfig`, `ConsolidatorConfig`. 28 new `KNOWN_ENV_NAMES` entries (ASCII-sorted, invariant pinned by the binary-search round-trip test). 22 src files migrated: `cortex-api/src/{analyzer,coverage,canary,relevance_config,search_proxy,dashboard/retention}`; `cortex-workers/src/{bin/cortex-consolidator,bin/graph-writer,bin/graph-backfill,bin/fulltext-indexer,bin/classifier-worker,bin/cortex-claude-archive,classifier_worker/worker,classifier_worker/config,claude_archive/tail,fulltext/routing,graph/stale_sweeper}`; `cortex-adapter-claude-code/src/{main,install,bin/cortex-hook}`; `cortex-mcp-server/src/main`. **Operator-visible break expanded**: `CORTEX_GRAPH_CYPHER_ENABLED`, `CORTEX_FULLTEXT_REPLAY_MISSING`, `CORTEX_INDEX_LOW_SIGNAL_TOOL_CALLS`, `CORTEX_HOOK_FORCE_FALLBACK`, `CORTEX_ADAPTER_DISABLE` now require literal `true`/`false` — joins the same break already shipped for the other phase13e bool knobs. 3 per-helper env-mutation tests removed (race on shared CORTEX_* process state; centralized coverage lives in `crates/cortex-config/src/load.rs::tests`).

- **phase13e §3.5 — `cortex-cli/**/*.rs` migrated to `cortex_config::Config` (ADR-016).** 78 `std::env::var("CORTEX_*")` reads across 18 files in `crates/cortex-cli/src/bin/cortex-ops*` swapped for typed `cortex_config::Config` accessors. New Config sub-structs: `DoctorConfig`, `ClassifierConfig`, `ConsolidatorConfig`, `AutoMemoryConfig`. Field extensions: `DashboardConfig.{api_token, api_url}`, `IngestionConfig.{ingestion_url, cas_db}`, `NexusConfig.metadata_db`. 10 new `KNOWN_ENV_NAMES` entries. **Operator-visible break expands**: `CORTEX_DOCTOR_BENCH` now requires literal `true` / `false` (legacy `1` / `0` no longer accepted) — joins the same break already shipped for `CORTEX_DASHBOARD_WATCH` / `_MEMORY_TAIL` / `_CANARY_ENABLED`. Five per-helper env-mutation tests removed because they duplicated centralised `cortex-config/src/load.rs` coverage and raced each other on shared process env state (CORTEX_HOME / CORTEX_ARCHIVE_ROOT).

- **phase13e §3.3 — `/v1/health/config` surfaces ADR-016 migration drift.** `cortex_api::config_audit::AuditOptions` gains `cortex_config_audit_root: Option<PathBuf>` (set to `<workspace>/crates` by `AuditOptions::full()`); `run_audit_with` walks it via `cortex_config::audit()` and emits a `cortex_config` finding (OK when empty, WARN when ad-hoc `std::env::var("CORTEX_*")` reads remain outside the `cortex-config` crate). `/v1/health/config` and `cortex-ops doctor-config` now show ADR-016 §3 migration drift alongside the existing operator file checks. The original cortex-api operator-facing audit (`.env` / `adapter.toml` / `.mcp.json` / `hooks.json` / live ports / duplicate deps) is unchanged — a blind replacement per the task's literal wording would have destroyed the phase8d audit. 2 new unit tests pin both branches; 16/16 `config_audit` tests green.

- **phase13e §3.2 — `cortex-api/src/http.rs` migrated to `cortex_config::Config` (ADR-016).** 19 live `std::env::var("CORTEX_*")` reads in `handle_health_coverage`, `resolve_vectorizer_bearer_for_audit`, and `handle_admin_forget` swapped for typed accessors on a new `ApiState.cfg: Arc<cortex_config::Config>` field. New `cortex_api::build_router_with_auth_and_cfg(...)` builder lets the daemon thread the boot-time `Config` snapshot through so the router, the dashboard, and the `/v1/health/coverage` audit all resolve the same way. Config extended: `DashboardConfig.coverage_slugs_only` (Option<String>). env_map: `CORTEX_COVERAGE_SLUGS_ONLY` → `/dashboard/coverage_slugs_only`; `CORTEX_FULLTEXT_MEILI_KEY` added as a legacy alias of `CORTEX_FULLTEXT_MEILI_API_KEY` (both map to `/meili/meili_api_key`).

- **phase13e §3.1 — `cortex-api/main.rs` migrated to `cortex_config::Config` (ADR-016).** 38 `std::env::var("CORTEX_*")` reads in `crates/cortex-api/src/main.rs` swapped for typed `cortex_config::Config` accessors. Config extended: `EmbedderConfig.{vectorizer_api_key, jwt_warmup_secs}`, `DashboardConfig.{api_keys_db, watch, memory_tail, coverage_slugs, rrf_alpha, rrf_k, query_rewriter}`, `NexusConfig.nexus_api_key`, `IngestionConfig.home`. New sub-structs `RulebookConfig` + `CanaryConfig`. URL gate fields (`embedder.vectorizer_url`, `meili.meili_url`, `nexus.nexus_url`) refactored to `Option<String>` so unset env preserves the "lane disabled" semantic; worker configs supply the localhost default at the worker boundary. 19 new `KNOWN_ENV_NAMES` entries including legacy `CORTEX_VECTORIZER_{URL,USER,PASSWORD}` aliases collapsed onto `/embedder/*` pointers. SIGHUP `relevance.toml` reload re-calls `Config::load()` so env + toml re-resolve together. **Operator-visible break**: `CORTEX_DASHBOARD_WATCH`, `CORTEX_DASHBOARD_MEMORY_TAIL`, `CORTEX_CANARY_ENABLED` now require literal `true`/`false` — legacy `0`/`1`/`yes`/`on` values are no longer accepted.

### Added

- **phase14c — `cortex-eval` golden-set harness + CI regression gate.** New crate `crates/cortex-eval/` shipping three suites — retrieval (MRR@10 + recall@5), consolidation (entity-recall + fact-recall), classification (per-kind F1 + macro-F1) — driven by curated CSV fixtures at `tests/golden/{retrieval,consolidation,classification}.csv`. Library surface: `metrics::{mrr_at_k,recall_at_k,f1,macro_f1,set_recall}`, `report::{SuiteReport,MetricRow,is_regression,metric_delta}`, `suite::{retrieval,consolidation,classification}::{load_csv,build_report,acceptance}` plus the `SuiteName` enum and `AcceptanceVerdict` struct. CLI surface: `cortex-eval --suite <name> [--golden <path>] [--api-url <url>] [--classifier-url <url>] [--baseline <path>] [--threshold <fraction>] [--output md|json] [--verbose]` with exit-code taxonomy 0/1/2/3 for ok / internal-error / regression / acceptance-failed. CI workflow `.github/workflows/eval.yml` runs all three suites against PRs + main, uploads main reports as `eval-baseline-<suite>` artifacts (90-day retention), and downloads them on PR runs so the harness fails any metric whose absolute drop > 5% vs main. Quality floors per the phase14c proposal: MRR@10 ≥ 0.60, recall@5 ≥ 0.50, entity-recall ≥ 0.85, fact-recall ≥ 0.70, macro-F1 ≥ 0.90. Tests: 30 unit tests covering every metric primitive + per-suite happy/sad paths + CLI regression-summary logic, all green. Spec [`docs/specs/26-eval.md`](docs/specs/26-eval.md) documents the contract, the refresh cadence (per-incident + quarterly), and the library surface table; fixture README at [`tests/golden/README.md`](tests/golden/README.md) pins CSV editing rules + curation policy. Initial CSV seeds carry 10 starter rows each — operator extends per the refresh cadence; CI gate runs against whatever ships.

- **phase14b — `HotTierPrune` + `ColdTierPrune` identity-driven retention cascade + ADR-013 collection-level Vectorizer reencode.** Closes the cold-tier cascade gap the 4-doc retention audit named: hot-tier prune existed but cold-tier eviction across Vectorizer + Meili + Nexus + archive was broken. Three new modules land under `crates/cortex-workers/src/`: (a) `retention/identity_prune.rs` ships the shared driver — `partition_start_hour` parses `events/year=YYYY/month=MM/day=DD/hour=HH/raw-NNNNN.parquet` so the sweeps derive occurred-at from the existing `event_identity` index without a schema migration; `IdentitySource` + `IdentityCascadeOps` traits define the per-backend cascade surface; `CascadePolicy::{HOT, COLD}` encodes which legs each sweep hits (HOT = Meili+Nexus+Vectorizer, archive preserved; COLD = every backend); `run_identity_cascade` drops the SQLite row on success and preserves it on any leg failure so the next sweep retries; `RecordingCascadeOps` + `StaticIdentitySource` fixtures drive the tests. (b) `retention/hot_tier_prune.rs` ships `HotTierPrune` Sweep: daily 04:00 UTC, default 90-day cutoff, dry-run via ctx short-circuits before any leg, partial cascade failure lands as `SweepStatus::Failed`. (c) `retention/cold_tier_prune.rs` ships `ColdTierPrune` Sweep: weekly Sunday 05:00 UTC (cron crate keys day-of-week 1..=7 with Sunday=1), default 365-day cutoff, cascades every backend then invokes ADR-013's `reencode_collection("cortex.cold.binary", predicate)` with `payload.occurred_at_ms >= cutoff_ms` (missing payload defaults to keep so malformed rows never disappear silently). (d) `embedder/vectorizer_prune.rs` ships ADR-013's collection-level re-encode entry point: `VectorizerPruneOps` trait (list_vectors_page paged + upsert_batch + drop_collection + rename_collection); `reencode_collection` 5-step pipeline (clear stale .tmp → page source → run predicate per record → flush survivors into .tmp every BATCH_SIZE=64 records → atomic two-step swap); failure rollback drops .tmp without touching the original. `TMP_SUFFIX = ".tmp"` reserved per ADR-013 §4. (e) ADR-013 at `.rulebook/decisions/016-*` documents the trade-off: Vectorizer SDK 3.1 lacks per-vector remove primitive, so cold-tier prunes re-encode the collection in O(collection_size) until SDK 3.2 ships. 24 new tests across all four modules: 9 in `identity_prune::tests` (partition parsing + cascade hot/cold/failure + `group_by_partition` + summary), 6 in `vectorizer_prune::tests` (1000-vec/300-expired §4.4 scenario + idempotent smoke + upsert-failure-rolls-back + swap-failure → SwapFailed + invariant + constants), 4 in `hot_tier_prune::tests` (drops-expired + dry-run + partial-failure + schedule), 5 in `cold_tier_prune::tests` (cascade-all-backends + the §3.4 100-event 30-hot/70-cold scenario + dry-run + schedule + helper). Spec [`docs/specs/02-storage-layout.md` § Hot- and cold-tier prune (phase14b + ADR-013)](docs/specs/02-storage-layout.md) documents the contract.

- **phase14a — `cortex-consolidator` runs as a long-lived daemon (closes the largest "everything is wired but nothing happens" defect).** Six grain wirings land under `crates/cortex-workers/src/consolidator/`: (a) `consolidator_trait.rs` ships the `Consolidator: EnvelopeProducer` trait, the `Trigger` enum aliased via `orchestrator::Trigger` (`SessionEnd { session_id }`, `NightlyTopic { repo }`, `DecisionLanded { decision_id, force_deep }`), the `TriggerLabel` snake-case wire projection, and `ConsolidatorCtx` carrying `Arc<Mutex<CostLedger>>` + `now: DateTime<Utc>` + `record_cost(grain_label, model, cost_cents, prompt_tokens, completion_tokens)`. (b) `grains/{session,topic,decision_trace}.rs` ship the three `Consolidator` impls — each wraps `Orchestrator::run_*` behind a per-grain fetcher trait (`SessionInputFetcher`, `TopicClusterFetcher`, `DecisionTraceFetcher`) with a `Live*` impl that hops onto `tokio::task::spawn_blocking` so the parquet/HDBSCAN walk does not stall the executor. (c) `daemon.rs` ships the `TriggerSource` trait + `ConsolidatorDaemon::{run_once, run_forever}` loop: `run_once` matches on the trigger discriminator, dispatches one grain at a time, writes a `producer_checkpoints` row on success (scope = `session_id` / `topic:<repo>` / `decision:<decision_id>`), and acks the source offset on both success AND failure so a poisoned trigger does not block the queue. `run_forever` selects between `run_once` and an external shutdown future so the in-flight grain always lands cleanly. (d) New `bin/cortex-consolidator.rs daemon` subcommand wires `SynapTriggerSource` (the 0.11 pull API with a local `AtomicU64` cursor — same shape the embedder uses since Synap has no server-side consumer groups yet), `parse_trigger_event` mapping JSON envelopes onto `Trigger` variants, and `shutdown_signal()` awaiting Ctrl-C plus (unix) SIGTERM. Malformed events advance the cursor and surface as `Idle` so the daemon never retries a poisoned envelope. (e) `ProducedConsolidation` gains `input_tokens` / `output_tokens`; all three producers propagate them from `SummariserResult`; `GrainCost` grows `input_tokens` / `output_tokens` / `models_used: BTreeSet<String>` with serde defaults for backward compat; `CostLedger::record_full` updates every field. Each grain's `on_trigger` calls `ctx.record_cost` after the orchestrator run (TopicGrain does it per cluster). (f) `cortex-api/src/health/consolidator.rs` ships `GET /v1/health/consolidator` returning the typed `ConsolidatorHealthReport` via the `ConsolidatorHealthSource` trait; default `UnwiredConsolidatorHealthSource` returns the all-empty report until main.rs threads a `consolidation_reports`-backed source. (g) `gui/src/views/Consolidations.tsx` renders a new `DaemonHealthPanel` banner above the consolidation list — fed by `api.healthConsolidator()` polling every 30 s, showing per-grain status tag (success → ok / failed → warn / no run → "never"), relative `last_run`, and envelopes_emitted + latency_ms. (h) New Dockerfile stage `cortex-consolidator` installs procps and uses `pgrep -f cortex-consolidator` as the healthcheck — the daemon owns no HTTP surface; runtime health surfaces via the cortex-api endpoint. docker-compose service depends on `synap` + `cortex-ingestion`, shares the `~/.cortex` volume, threads `SYNAP_BASE_URL` + `CORTEX_ARCHIVE_ROOT` + `CORTEX_METADATA_DB` + `ANTHROPIC_API_KEY`. Tests: 6 ctx/trait tests, 5 session-grain, 6 topic-grain, 6 decision-trace-grain, 6 daemon-loop tests + 5 per-grain end-to-end IT (`tests/grains_trigger_emit_it.rs`) + 8 new bin tests (CLI parse + parse_trigger_event coverage) + 5 ConsolidatorHealthReport tests = 47 new tests across 112 consolidator unit tests + 23 bin tests, all green; clippy clean; `pnpm tsc --noEmit` clean. New spec [`docs/specs/27-consolidation.md`](docs/specs/27-consolidation.md) pins the trait + dispatch + wire-shape + telemetry contract.

- **phase13g — three MCP grounding tools + pre-thinking integration (`cortex_active_work` / `cortex_similar_sessions` / `cortex_decision_chain`).** Closes the load-bearing grounding gap the 2026-05-19 consolidation review surfaced: the agent can now ask "what tasks are open?", "what past sessions tackled this?", and "what's the ADR supersession chain?" deterministically instead of constructing a fusion query and hoping the relevance gate fires. MCP registry size 10 → 13 (`tools/list` count tests bumped). New types: `cortex_api::active_work::{ActiveTaskRow, ArchiveRow, ActiveWorkReport, ActiveWorkLoader}` (mtime+TTL 60s cache, stamp-based invalidation, process-wide `OnceLock` so the cache spans requests; UTF-8-safe 256-byte truncation of `next_unchecked_item`); `cortex_api::similar_sessions::{SimilarSessionsRequest, ConsolidationHit, SimilarSessionsResponse, ConsolidationSearchSource trait, VectorLaneConsolidationSource<L: VectorLane>, UnwiredConsolidationSource}` (typed projection from `LaneHit` extras into `ConsolidationHit`; `k ∈ [1, 10]` default 5, `confidence_floor` default 0.6); `cortex_api::decision_chain::{DecisionNode, DecisionChainResponse, DecisionChainSource trait, UnwiredDecisionChainSource, walk_chain(start, nodes, edges, max_hops)}` (algorithm extracted to module scope for unit-testability; predecessor pass then successor pass with cycle-revisit guard, per-direction `max_hops` cap, deterministic `(date asc, event_id asc)` ordering). New routes: `GET /v1/dashboard/active-work?repo=<slug>`, `POST /v1/search/similar-sessions`, `GET /v1/search/decision-chain?event_id=<ulid>&max_hops=<n>`; each route lives on its own sub-router merged into the base router; defaults carry the honest-empty source until main.rs threads live wiring. Pre-thinking integration: `cortex_pre_thinking::formatter` ships `section_caps::{ACTIVE_WORK_BYTES = 1200, SIMILAR_SESSIONS_BYTES = 2000, ADR_PROVENANCE_BYTES = 800}` (sum is 33 % of the 12 KB ceiling — `section_caps_sum_stays_under_pre_thinking_ceiling` pins the invariant), three new renderers (`render_active_work`, `render_similar_sessions`, `render_adr_provenance`) that each self-bound via byte budget and emit a `(truncated; see <tool>)` continuation when overflowed, `FormatOptions::grounding: GroundingSections { active_work, similar_sessions, adr_provenance }` plumbed through `format_bundle` so the new sections render between laws and consolidated-context per spec 12 §Sections (laws → active work → similar sessions → ADR provenance → topic cards → consolidations → decisions → similar turns → past sessions → snippets). Dispatch helpers: `should_fetch_similar_sessions(user_prompt) -> bool` (floor = `SIMILAR_SESSIONS_QUERY_FLOOR_CHARS = 16` chars), `extract_adr_event_ids(user_prompt) -> Vec<String>` (Crockford-safe 26-char windows with leading/trailing alphabet check; dedupes in first-seen order). MCP tools `ActiveWorkTool` / `SimilarSessionsTool` / `DecisionChainTool` validate inputs (slug-safe repo, `k ∈ [1, 10]`, `confidence_floor ∈ [0.0, 1.0]`, ULID `[0-9A-Z]{26}` Crockford-safe, `max_hops ≤ 16`) before the HTTP round-trip via `ToolError::invalid_input`. Tests: 45 new unit tests across `cortex_api::{active_work, similar_sessions, decision_chain}` (9 + 9 + 9 = 27) plus 10 new MCP tool tests (3 + 4 + 3) plus 2 storage tests (none) plus 18 new pre-thinking formatter tests = `cargo test --workspace` 2303 / 2303 pass (+44 from phase13f baseline). Docs: `docs/specs/12-pre-thinking-injection.md` § Phase13g §4 grounding sections + dispatch policy; `docs/specs/22-fine-grained-search.md` § MCP tool surface extended to 13 entries with wire shapes + error taxonomy per new tool.

- **phase13f — Dashboard pure-reader contract + per-domain `*ReportView` projections (ADR-014).** Closes the "frozen-as-contract" handler-state-derivation failure mode that phase11v_retention-daemon-recovery surfaced (the dashboard claimed `last run: never` while `cron_jobs` clearly had a row stamped 4 hours earlier). The fix is structural: dashboard handlers under `crates/cortex-api/src/dashboard.rs` + the 19 submodules in `crates/cortex-api/src/dashboard/` SHALL render only typed projections produced by per-domain `*Report::view()` methods. ADR-014 created as decision #15 (`adr-014-dashboard-handlers-are-pure-readers-state-lives-in-domain-reports`, status `proposed`). New types: `cortex_workers::producer::report::{ProducerReport, ProducerReportView, ProducerCheckpointView, ProducerCheckpointsReportView}` (ProducerReport moved out of `producer/checkpoint.rs` to `producer/report.rs` to mirror the Sweep layout; `ProducerCheckpoint::view()` projects the durable checkpoint state since `ProducerReport` rows are in-memory only); `cortex_workers::coverage::{BackendCoverage, BackendCoverageEntry, CoverageReport, CoverageReportView, CoverageReportSource, collect_coverage}` (single SQL scanner shared by `cortex-ops doctor-identity-coverage` and the dashboard handler — previously each implemented its own scan); `cortex_api::dashboard::consolidations::{ConsolidationReportView, ConsolidationFilter, ConsolidationReportSource}` (typed envelope `{rows, total, filter}` replaces the ad-hoc `Vec<ConsolidationRow>` wire payload). SweepReportView reused as-is from phase13a. New endpoints: `GET /v1/dashboard/coverage` returning `CoverageReportView` (4 backend rows in fixed order, `is_healthy: bool` derived, honest-empty when `DashboardState.metadata` is None); `GET /v1/dashboard/producers` returning `ProducerCheckpointsReportView` (one row per `(producer_name, scope)` pair sorted by `producer_name` then `scope`). Storage: new `MetadataStore::latest_producer_checkpoint_per_scope()` returns the most-recent checkpoint per pair via a single SQL with sub-select on `MAX(accumulated_at)` (sorted, indexed). CI gate `.github/workflows/dashboard-grep-gate.yml` expanded scope: phase13a only scanned `dashboard/retention.rs` + `dashboard.rs`; phase13f §4.1 now walks the entire submodule tree (`grep -RnE '"never"|"n/a"|"unknown"' crates/cortex-api/src/dashboard.rs crates/cortex-api/src/dashboard/`) and fails the build on any match. GUI: `gui/src/views/Coverage.tsx` + `Producers.tsx` are new typed consumers (no local fallback branches); `Consolidations.tsx` unwraps the new `{rows, total, filter}` envelope; `Sidebar.tsx` count switched to `consolidationsQ.data?.total`. `gui/src/lib/api.ts` gains 6 new types and 2 endpoint functions. 21 new Rust unit tests (`producer::report::tests` × 5 new for ProducerCheckpoint*View, `coverage::tests` × 6, `dashboard::coverage::tests` × 3, `dashboard::producers::tests` × 1, `dashboard::consolidations::tests` × 5, `cortex-storage::metadata::tests::latest_producer_checkpoint_per_scope_*` × 2) plus 6 new GUI tests (`Coverage.test.tsx` × 3, `Producers.test.tsx` × 3). Test totals: `cortex-api --lib` 453/453 (was 449), `cortex-workers --lib` 666/666 (was 657), `cortex-storage --lib` 67/67 (was 65), `cortex-cli` 5/5 identity_coverage preserved (scanner moved transparently), GUI 37/37 across 7 suites. Spec [`docs/specs/16-dashboard.md` § Pure-reader contract (ADR-014 / phase13f)](docs/specs/16-dashboard.md) documents the contract + the 5-rule enforcement table. **Spec drift fixed in tasks.md + proposal.md**: the original spec assumed `dashboard.rs` was still a monolith and `coverage` lived in `cortex-workers`; reality at execution time was the dashboard had been split into 18 submodules and the coverage domain lived in `cortex-cli`. Resolution: dashboard scope expanded to the submodule tree, coverage scanner moved to `cortex-workers` (correct dep direction, cortex-cli already depends on cortex-workers), ConsolidationReportSource trait housed at the api layer since consolidations are envelope-projected from the `cortex-meili-consolidations` index rather than a worker-side SQLite report. **§3.4 spec deviation**: the task called for joining `producer_checkpoints` with a `producer_reports` row per pair; no such table exists today (`ProducerReport` is in-memory only). Resolution: `/v1/dashboard/producers` returns `ProducerCheckpointsReportView` (checkpoints-only) — honest about what's durable. A future task can add the `producer_reports` table and extend the view shape additively without breaking the handler contract.

- **refactor: `crates/cortex-api/src/` bucketed into thematic modules (2026-05-25).** The 44 flat top-level modules were reorganised into 8 sibling-file buckets while preserving every external import path via `pub use` re-exports in `lib.rs`. Buckets: `lanes/` (8 files: vector / keyword / graph clients + loaders), `search/` (9 files: orchestrator, intent rewriter, fusion, analyzer, relevance config, lane strategies, response cache, byte budget, search proxy), `security/` (6 files: auth, ACL, audit + store, rate limit, redaction), `admin/` (5 files: forget, list_events, config_audit, canary, silent_drop — `admin_forget` and `admin_list_events` shed the redundant prefix), `mcp/` (1 sibling: topic_card), `health/` (1 sibling: coverage), `ingest/` (1 sibling: proxy), `dashboard/` (5 absorbed: consumer, series, watcher, tasks_loader, memory_tail — previously top-level `dashboard_consumer.rs` etc.). lib.rs adds 32 `pub use bucket::child;` lines so every `cortex_api::<old_name>` import resolves unchanged. Root keeps `lib`, `main`, `http`, `service`, `types`, `retention_daemon`, `storage/`, `dashboard.rs`. Validation: `cargo build --workspace --all-targets` clean; `cargo check --workspace` clean; `cargo test -p {cortex-api,cortex-adapter-claude-code,cortex-cli,cortex-mcp-server,cortex-pre-thinking}` 1000+ tests across ~40 suites, 0 regression. No edits to any caller crate were needed.

- **phase13d — `EventIdentity` cross-backend join key + SQLite `IdentityIndex` (ADR-012).** Closes the per-call cross-backend lookup cost that `cortex doctor consistency` (minutes for 100k events) and `cortex admin forget` (silent missed-backend bugs) absorbed pre-phase13d. New `crates/cortex-storage/src/identity.rs` ships `EventIdentity { event_id, nexus_id, vec_id, meili_id, archive_partition }`, `Backend` enum (Nexus / Vectorizer / Meili / Archive — Synap absent because its id IS the row's `event_id`), `trait IdentityIndex { upsert_identity / lookup / lookup_by_native / delete }`, and `SqliteIdentityIndex<'a>` wrapping `&Connection`. New `apply_phase13d_schema()` migration (called from `MetadataStore::migrate` on every open) creates the table with PK on `event_id` + 3 UNIQUE partial indexes (`WHERE … IS NOT NULL`) on `nexus_id` / `vec_id` / `meili_id`. Archive partition deliberately NOT unique — many envelopes share the same hour-bucket parquet file. Every projection path writes back its native id after a successful per-backend insert: `cortex-workers/src/embedder/worker.rs::Worker` stamps `vec_id` (first chunk's `server_id`); `cortex-workers/src/fulltext/worker.rs::Worker` stamps `meili_id` (= `event_id`); `cortex-workers/src/graph/worker.rs::Worker` stamps `nexus_id` (= `event_id`, per spec-07 §Node keys); `cortex-workers/src/ingestion/router.rs::AppState` stamps `archive_partition` (`ArchiveWriter::write` signature flipped `Result<(), ArchiveError>` → `Result<PathBuf, ArchiveError>`). Each worker gains an `Option<Arc<Mutex<MetadataStore>>>` field + `with_metadata()` builder; `None` keeps the pre-phase13d worker path running unchanged for callers that have not wired the metadata DB. Best-effort writes: a poisoned mutex or SQLite error logs at WARN but does NOT undo the per-backend insert — a missed stamp surfaces later as a doctor coverage gap (the structural invariant ADR-012 enforces by construction). New `cortex-ops doctor-identity-coverage` subcommand walks `event_identity` once via 1 indexed `COUNT(*)` + 4 `COUNT(*) WHERE <col> IS NULL` + 4 sampled `SELECT … LIMIT` queries; reports per-backend `<backend>_missing` counters + orphan event_id samples; exit code `2` on any NULL column. Budget gate `scan_100k_rows_finishes_under_10s_budget` (gated behind `CORTEX_DOCTOR_BENCH=1`) measured 2.86 s end-to-end including 100k-row seed (scan itself sub-second), well under the spec-25 §contract 10 s. `cortex admin forget` cascade drops the identity row AFTER every per-backend leg succeeded (cascade failure leaves the row intact so a retried forget still sees the full backend set; dry-run returns before the delete). New spec [`docs/specs/25-event-identity.md`](docs/specs/25-event-identity.md) documents the data model + write-back contract + consumer paths. Tests: 7 unit tests in `identity::tests` (insert / upsert merge / per-backend reverse lookup / delete idempotency / UNIQUE constraint rejection / empty-id validation / backend label invariant), 8 round-trip ITs across the 4 projection paths (2 per backend: happy path + absent-metadata no-op), 2 admin_forget ITs (cascade drops identity row + dry-run preserves it), 5 doctor-identity-coverage tests including the 100k bench gate; `cargo test -p cortex-storage --lib` 65/65 green, `cargo test -p cortex-api --lib` 443/443 green (+2 from baseline), `cargo test -p cortex-workers --test {ingestion_router,embedder_identity_writeback_it,fulltext_identity_writeback_it,graph_identity_writeback_it}` 4× green. ADR-012 created as decision #13 (`adr-012-eventidentity-cross-backend-join-key-sqlite-identityindex`, status `proposed`). Followups (phase13e): backfill sweep for pre-phase13d archive, `--live` flag on the doctor for per-row `exists(backend, id)` probe, identity-driven dispatch inside `admin forget` to replace the archive-derived kind path.

- **phase13c — typed `LaneHit.overlay` replaces stringly-typed `extras: Props` (ADR-011).** Every overlay derivation in `crates/cortex-api/src/orchestrator.rs` (`derive_decisions`, `derive_laws`, `derive_graph_neighbors`, `derive_similar_turns`, `lane_label`, `snippet_from_hit::body_truncated`) now reads from the new `LaneHit.overlay: Overlay` field instead of `extras.get("…").and_then(|v| v.as_str())`. New types in `crates/cortex-api/src/lanes.rs`: `Overlay` (17 fields: `decision_id`, `decision_status`, `superseded_by`, `turn_id`, `model`, `summary`, `law_id`, `violation_id`, `severity`, `edge_from`, `edge_to`, `hops`, `body_truncated`, `contradiction_flag`, `consolidation_grain`, `topic_id`, `kind`, plus `source: LaneSource`), `LaneSource` enum (`Vector` / `Keyword` / `Graph` / `Unknown`), `DecisionStatus` enum (`Proposed` / `Accepted` / `Superseded` / `Deprecated` / `Rejected`), `pub trait Lane` (canonical lane contract with `name() / source() / search(query, scope)`), `impl From<&Envelope> for Overlay`. Lane impls (`vectorizer_lane.rs`, `meili_lane.rs`, `meili_loader.rs`, `nexus_graph_lane.rs`, `silent_drop.rs`, `archive_loader.rs`) populate the typed overlay alongside the deprecated extras map so the new read path stays byte-for-byte parity with the legacy stringly-typed one. Wire contract at `/v1/query` is unchanged — `decision_status` and `source` still emit as snake_case strings. 13 new tests: 8 in `lanes::adr_011_tests` (default / is_empty / `From<&Envelope>` / `LaneSource` Default + serde / `DecisionStatus` snake_case / wire-format invariant) and 5 in `orchestrator::tests` (empty-overlay regression × 4 derivers + three-lane parity smoke); `cargo test -p cortex-api --lib` 441/441 green. Spec [`docs/specs/11-query-api.md` § Lane projection contract](docs/specs/11-query-api.md) refreshed with the per-field Overlay ownership table. ADR-011 created as decision #12 (`adr-011-typed-lanehit-overlay-replaces-extras-props-hashmap`, status `proposed`). Unblocks phase11v_mcp-fine-grained-backend-search (status flipped `blocked` → `pending` in its archive metadata).

- **phase12d — knowledge + learning JSON schemas, consolidation + topic-card Meili settings, drift doctor.** Three silent-data-loss gaps closed: (1) `Kind::Knowledge` and `Kind::Learning` envelopes were silently dropped at ingest because the validator's schema map had no entry for them — the classifier emitted both kinds since phase10e but the ingestion router rejected them with an INFO-level "unknown kind" nobody monitored. New schemas at [`crates/cortex-core/schemas/kinds/knowledge.schema.json`](crates/cortex-core/schemas/kinds/knowledge.schema.json) + [`learning.schema.json`](crates/cortex-core/schemas/kinds/learning.schema.json) (both Draft 2020-12, `additionalProperties: false`, fields mirror the canonical `KnowledgePayload` / `LearningPayload` structs verbatim — the proposal's hypothetical `pattern_type` / `evidence` / `confidence` / `source_task` / `applies_to` fields were never adopted). Wired into `Validator::new()` at [`crates/cortex-core/src/validate.rs`](crates/cortex-core/src/validate.rs). 7 new validation tests covering happy path + missing required + unknown enum + extra-field rejection for both kinds. (2) `cortex_consolidations` + `cortex_topic_cards` Meili indexes existed but had no `searchableAttributes` / `filterableAttributes` configured — every query against them ranked by document id, which is useless for retrieval. New settings files at [`crates/cortex-storage/schemas/meili/cortex_consolidations.settings.v1.json`](crates/cortex-storage/schemas/meili/cortex_consolidations.settings.v1.json) + [`cortex_topic_cards.settings.v1.json`](crates/cortex-storage/schemas/meili/cortex_topic_cards.settings.v1.json) registered in `cortex_storage::fulltext::INDEXES`; `cortex-ops plan indexes` now emits them and `bin/cortex-init.sh` PATCHes the settings on every boot (the existing reconcile path). (3) New `cortex-ops doctor-meili-indexes [--meili-url URL] [--master-key KEY] [--json]` walks the declared INDEXES and compares live Meili settings against them as sorted sets, surfacing per-index `ok` / `drift` / `missing` / `error` plus per-attribute `declared` vs `live` arrays. Exit code `2` on any drift / missing / network failure. Live smoke against the running stack flagged 10 missing + 2 drifted indexes — exactly the operational gap the doctor was meant to surface.

- **phase11v — fine-grained per-backend search (`cortex_keyword_search` / `cortex_vector_search` / `cortex_graph_query`).** Three new MCP tools + matching `cortex-api` routes (`POST /v1/search/keyword`, `/v1/search/vector`, `/v1/search/graph`) expose direct read-only access to the keyword / vector / graph backends without going through `cortex_query`'s fusion. Operators and agents can ask "show me ONLY the relationships" / "raw cosines in collection Y" / "literal Meili hits, no projection" — questions the fused surface cannot answer without losing fidelity. Implementation in [`crates/cortex-api/src/search_proxy.rs`](crates/cortex-api/src/search_proxy.rs); MCP wrappers in [`crates/cortex-mcp-server/src/tools.rs`](crates/cortex-mcp-server/src/tools.rs) (registry size 7 → 10). Cypher mode on the graph endpoint is gated on `CORTEX_GRAPH_CYPHER_ENABLED=1` so an unsigned MCP descriptor cannot land raw Cypher in production (decision recorded in [ADR-009](.rulebook/decisions/009-cypher-gate-disabled-by-default-on-mcp-graph-surface.md)). Every endpoint shares the 30 KB `MCP_RESPONSE_HARD_CAP` ceiling — payload overflow returns HTTP 413 + `budget_exceeded` envelope. Tests: 9 in `search_proxy` (serde defaults, clamps, budget cap, cypher-gate env read); registry-size-10 assertion pinned in two test sites. New spec [`docs/specs/22-fine-grained-search.md`](docs/specs/22-fine-grained-search.md) documents the request / response shapes + the 14-reason error taxonomy. Also fixed the cross-tree drift the phase11x .ps1 retirement introduced: synced [`cortex-plugin/hooks/`](cortex-plugin/hooks/) to mirror the trimmed [`crates/cortex-adapter-claude-code/hooks/`](crates/cortex-adapter-claude-code/hooks/) so the `plugin_hook_shims_match_adapter_canonical_sources` drift guard goes back to green.

- **phase12a — consolidator publish failure logs + JSONL fallback + replay (closes the largest single source of envelope data loss).** `crates/cortex-workers/src/bin/cortex-consolidator.rs::publish_consolidation` used to drop every produced consolidation envelope on the floor when `CORTEX_INGESTION_URL` was unset or when the POST failed; one `eprintln!` line and the payload was gone. Phase 12a §1-§2 + §4 close the loop: (a) every failure path now emits a `tracing::error!` with structured fields (`event_id`, `session_id`, `reason ∈ {env_unset, client_build, non_2xx, network}`, plus the relevant `url` / `status` / `error`); the success path emits `tracing::info!`. (b) Boot WARN fires for run-session / run-topic / run-decision / non-dry-run nightly when no ingestion URL is resolvable, naming the exact fallback path the operator should expect to fill. (c) New `append_publish_fallback()` writes the envelope to a JSONL file on every failure path, including `env_unset`. Path resolution honours `CORTEX_CONSOLIDATIONS_FALLBACK_FILE` → `CORTEX_HOME/consolidations.jsonl` → `<HOME|USERPROFILE>/.cortex/consolidations.jsonl`. Each line carries `fallback_at` (RFC3339), `reason`, and the full envelope so a replay tool can reconstruct the POST without parsing tracing logs. (d) New `cortex-ops consolidations-replay [--from <jsonl>] [--ingest-url <url>] [--dry-run] [--limit N] [--json]` reads the fallback JSONL, POSTs each envelope to the resolved ingestion URL, and reports per-line outcomes (`total_lines`, `sent`, `skipped_dry_run`, `parse_failed`, `network_failed`, `non_2xx`, plus the accepted `event_id` list). Exit code is `2` whenever any line could not be parsed or accepted so cron jobs surface the failure. Tests: 3 new in `cortex-consolidator` (path resolution × 2, JSONL append shape) + 5 new in `cortex-ops` (path precedence × 2, ingest URL strip, dry-run counters, missing-file success). phase12a §3 metrics now shipped: new `crates/cortex-workers/src/consolidator/metrics.rs` registers the `cortex_consolidator_publish_failures_total{reason}` (4 fixed labels: `env_unset`, `client_build`, `non_2xx`, `network`) and `cortex_consolidator_publish_ok_total` counter families with a process-wide `LazyLock<Metrics>` singleton + atomic `Metrics::snapshot()` reader; the publish path increments the matching counter at every `tracing::error!` / `tracing::info!` site. Tests: 5 in `consolidator::metrics::tests` (routing, unknown-reason drop, ok-only advance, snapshot view, singleton persistence) + 1 regression in the bin's `tests` module. Pending in phase12a: Prometheus exporter wire-up for the new counters (the snapshot reader is in place; the HTTP exposition is a follow-up rev). See `docs/analysis/rework/01-consolidation.md` Achado 2.

- **phase11x — `cortex-hook` native shim (~9 s/turn faster on Windows).** Profiling on 2026-05-06 traced the legacy `~730 ms` per-hook cost on Windows to PowerShell cold start, not the daemon (`pwsh -NoProfile` floor: ~545 ms; daemon work: ~90 ms). New release-mode bin `cortex-hook` (~280 lines, single-thread tokio runtime, ~50 ms cold start) connects directly to the daemon's named pipe (Windows) or Unix domain socket (Linux/macOS). New `--fire-forget` mode for `PostToolUse` / `SubagentStop` / `Stop` / `SessionStart` / `Notification`: write the frame, drop the connection, skip the read. `UserPromptSubmit` and `PreToolUse` stay synchronous because Claude Code consumes their replies. `install.rs` registers `cortex-hook <Event> [--fire-forget]` in `~/.claude/settings.json` when the bin is on PATH; falls back to `bash <abs-path-to-shim>` against the legacy `.sh` shims when missing (`CORTEX_HOOK_FORCE_FALLBACK=1` forces this branch in tests). Daemon owns hook-side logging now via the new `hook_log` module (`~/.cortex/hook-invocations.log`, 10 MB rotation with one `.1` archive); the `.ps1` shims are retired and the `.sh` fallbacks lost their pwsh polyglot block. `docs/specs/10-claude-code-adapter.md` § Transport documents the matrix; new criterion bench `benches/hook_cold_start.rs` (4 targets) plus a CI workflow `.github/workflows/hook-cold-start.yml` produce per-PR cold-start numbers across Ubuntu + Windows. 59 unit/integration tests in the crate, all green. Live Windows smoke (2026-05-07) measured: `--help` cold start p50 55 ms, synchronous PreToolUse p50 51 ms (p95 76 ms), fire-forget PostToolUse p50 53 ms (p95 55 ms) — within the §5.1 budgets (UserPromptSubmit < 200 ms; PostToolUse / Stop < 60 ms) and ~14× faster than the legacy PowerShell shim (~730 ms wall-clock per invocation). Per-turn adapter overhead drops from ~10.2 s to ~0.9 s on a 14-hook turn. Linux smoke covered by the `hook-cold-start.yml` CI workflow on `ubuntu-latest`; macOS pending an operator-run sample (the Unix-socket transport is identical to Linux at the source level). Full numbers in [`crates/cortex-adapter-claude-code/benches/baseline-2026-05-06.txt`](crates/cortex-adapter-claude-code/benches/baseline-2026-05-06.txt). Cross-tree fix: `cortex-plugin/hooks/*.sh` shims rewritten to prefer the `cortex-hook` bin (lookup: `$CORTEX_HOOK_BIN` → PATH → `~/.cortex/cortex-hook[.exe]`) so the OpenCode plugin path lands transparently when phase16a wires it; legacy `nc`/`socat` Unix-socket fallback preserved.

- **phase11w — tool-call digest summariser (closes the retention shrinkage gap).** `turn_digest` (phase9e) summarises `Turn` envelopes but `tool_call` events were left out — they are the single largest event class in the lane (28 141 of 38 866 in the 2026-05-05 snapshot, 72 %). Phase11w adds [`cortex_workers::retention::tool_call_digest`](crates/cortex-workers/src/retention/tool_call_digest.rs) with the same bucket / summarise / persist / tag pipeline as `turn_digest`, keyed by `(repo, ISO_year_week, tool)` instead of `(…, top_topic)`. **First-class destructive flag**: when `plan.purge_originals = true` the orchestrator calls `delete_source_tool_calls` after the digest persists, hard-removing the source rows from Meili (`cortex_tool_calls`), Vectorizer (`cortex.tool_call.fp32` / `.pq` / `.cold.binary`), and the matching Parquet partitions. The headline counter `records_purged` lands on `retention_sweeps.tier_transitions_json.tool_call_digest` so the dashboard's `Bytes reclaimed last 30 d` panel reflects actual shrinkage instead of the body-blanking compression `meili_prune` and `pii_enforce` already do. New `cortex-ops tool-call-digest` clap subcommand mirrors `turn-digest`'s flags and adds `--purge-originals`. New cron seed `retention.tool_call_digest` (`30 6 * * 0` — Sunday 06:30 UTC, 30 min after `turn_digest`'s 06:00 slot, default ON, command `cortex-ops tool-call-digest --purge-originals --budget-cents 500`); default cron-row count bumps `10 → 11`. Dashboard `cron_name_to_sweep_slug` + `RETENTION_SWEEP_SLUGS` extended with the new `tool_call_digest` slug; GUI `Retention.tsx` renders a "Tool-call digest" card with `last_run` / `next_run` / `records_purged`. 9 unit tests in [`crates/cortex-workers/src/retention/tool_call_digest.rs`](crates/cortex-workers/src/retention/tool_call_digest.rs) cover bucketise (group-by-tool, skip-summarised, skip-fresh) and the orchestrator's six paths (success, idempotent skip, dry-run gating, purge-flag wiring, budget cut-off, JSON shape with `records_purged`). Cortex-ops bin refactored into per-command submodules under `src/bin/cortex-ops/<name>.rs` via `#[path]` attributes (15 modules: helpers, plan, doctor, meili, digest, pii, cas, rollup, retention, metadata, bootstrap, graph_cmd, schedule_cmd, consolidation, memory_consolidate_cmd, canary). See [`docs/specs/19-retention.md` § "Phase11w"](docs/specs/19-retention.md).

- **phase11v — retention daemon recovery (six independent gaps).** Six independent gaps left the dashboard's Retention tab reporting `last run never` on every sweep card while six of the seven daily sweeps were actually green; this task closes them. (1) **Dashboard live-read of `cron_jobs`** — `crates/cortex-api/src/dashboard.rs::retention_state` now reads the live `cron_jobs` table via `MetadataStore::list_cron_jobs()` and projects `next_run_at` / `last_run_at` / `last_status` / `failure_streak` per sweep slug. The legacy hardcoded `next_run: "never"` is gone; rows with `enabled = 0` surface as `"disabled"`; missing rows degrade to `"never"`. (2) **`consolidation-prune` env precedence** — pure helpers (`consolidation_prune_vectorizer_url` / `_user` / `_password` / `consolidation_prune_meili_key`) walk the env-name shapes in priority order so the daemon-spawned prune authenticates inside the cortex-api container without operator intervention. (3) **`seed_defaults` UPSERT-aware** — `crates/cortex-workers/src/retention/scheduler.rs::seed_defaults` runs a reconcile pass after the INSERT loop that updates `enabled` / `command` to the current default when they drifted, leaving operator-tuned `schedule` and operator-disabled rows (`failure_streak > 0` OR `last_warning_at IS NOT NULL`) alone. (4) **`next_after` strictly advances + DOW normaliser** — `parse_schedule` now normalises 0-7 / SUN-SAT day-of-week tokens to the cron-crate's three-letter form (the crate rejects bare `0` and silently broke every weekly schedule that used it); `next_after` re-iterates while the candidate `<= from` so the contract holds for every shipped schedule across 365 days × 9 cadences. (5) **`cortex_consolidations`** — verified the index is created lazily by Meili on first insert and the prune handler returns `Ok(vec![])` on `NOT_FOUND`; no SQLite migration needed. (6) **`retention_sweeps` bookkeeping** — new `crates/cortex-cli/src/ops/sweep_bookkeeping.rs` writes one row per invocation through `MetadataStore::note_completed_retention_sweep` for the seven sweeps that previously skipped the bookkeeping table (`meili_prune`, `turn_digest`, `pii_enforce`, `cas_vacuum`, `memory_consolidate`, `consolidation_prune`); the legacy `retention_sweep` and `rollup` handlers keep their two-step `start_retention_sweep` / `finish_retention_sweep` lock pattern. New `docker-compose.yml` env exports (`CORTEX_EMBEDDER_VECTORIZER_*` mirror, `CORTEX_FULLTEXT_MEILI_KEY` mirror) and a new `~/.claude` volume mount on `cortex-api` so daemon-driven `memory-consolidate` reaches the host's auto-memory dir. New tests: dashboard live-read + disabled-pill + failure-streak (3); env precedence (8); UPSERT drift reconcile + operator-disabled preservation (2); `next_after` strict-advance + 365-day property (2); `sweep_bookkeeping` JSON shape + per-call write (2). All 17 new tests + every pre-existing retention test green. See [`docs/specs/19-retention.md` § "Phase11v"](docs/specs/19-retention.md).

- **phase11t — `cortex_forget` MCP tool + `/v1/admin/forget` cortex-api endpoint.** Closes the cross-task dependency phase11j §5.3 / phase11o §2.4 left open: the hard-purge cascade (Vectorizer `delete_vectors` + Meili `delete-batch` + Nexus `MATCH (n { event_id: $id }) DETACH DELETE n` + Parquet partition rewrite) is now reachable from the MCP host. Architecture: cortex-mcp-server is a protocol shim with no direct backend handles, so the cascade lives in `crates/cortex-api/src/admin_forget.rs` (which already owns Vectorizer + Meili + Nexus clients for the query lanes); the MCP tool POSTs to the new HTTP endpoint. New types: `ForgetRequest { event_id, confirmation_token, dry_run }`, `ForgetResponse`, `LiveNexusPurger` (wraps `Arc<NexusClient>`), `LiveArchivePurger` (rewrites the partition through `*.parquet.tmp` + atomic rename). Confirmation-token enforcement: callers must echo `cortex_workers::pruner::purge::REQUIRED_CONFIRMATION_TOKEN = "I-UNDERSTAND-FORGET-IS-IRREVERSIBLE"`; mismatch surfaces as cortex-api HTTP 400 → MCP `ToolError::invalid_input`. New axum route `POST /v1/admin/forget` mounted in `http.rs`. New MCP tool `cortex_forget` (name without `.` per MCP spec) registered in `ToolRegistry::default_set()` (registry size 6 → 7). Test count: 9 admin_forget unit tests (resolve_target_collections × 4, LiveArchivePurger × 2, handle_forget × 3) + 2 MCP tool tests via wiremock + 1 gated IT (`CORTEX_FORGET_IT=1`). All green.

- **phase11p — consolidator live read path.** Closes the consolidation→pruner loop that phase11j+phase11o opened: producers exist, pruner exists, but the producer side had no live envelope source so `cortex_consolidations` stayed empty and the pruner walked nothing every night. New module `crates/cortex-workers/src/consolidator/source/` ships three sources (`LiveSessionSource`, `LiveTopicSource`, `LiveDecisionTraceSource`) backed by two new archive helpers in `cortex-storage`: `archive::scan_envelopes_by_session(archive_root, session_id) -> Vec<Envelope>` and `archive::scan_envelope_by_event_id(archive_root, event_id) -> Option<Envelope>`. The topic source runs HDBSCAN (new `hdbscan = "0.10"` workspace dep) over each turn's inline `payload.embedding` array with `min_cluster_size = 3` + `min_samples = 1`; noise-labelled (-1) points and sub-threshold clusters are dropped. The decision-trace source walks `parent_event_id` up to `MAX_HOPS = 16` with a `HashSet`-backed cycle detector. Bin wiring: `cortex-consolidator` Cli gains `--archive-root` (env `CORTEX_ARCHIVE_ROOT`) + `--metadata-db` (env `CORTEX_METADATA_DB`); the four operational subcommands (`run-session`, `run-topic`, `run-decision`, `nightly`) now invoke the live sources + `Orchestrator::run_*` instead of printing the §3 stub. `nightly` writes a cursor file at `<home>/.cortex/consolidator-cursor.json` (override via `CORTEX_CONSOLIDATOR_CURSOR_FILE`) carrying `{last_run_ts, sessions_processed, topics_processed, decisions_processed, cost_cents_total}`. New cron seed `retention.consolidator_nightly` (`schedule = "0 2 * * *"`, `command = "cortex-consolidator nightly"`, `enabled = true`) sits one hour before `retention.consolidation_prune` (03:00) so the pruner sweeps over fresh rows; `retention.memory_consolidate` flipped from `enabled: false` → `enabled: true`. Test count: 14 §1 unit (5 session + 4 decision_trace + 5 topic) + 5 §0 archive helpers + 10 §2 bin + 2 §3 scheduler + 2 §4 ITs (gated `CORTEX_CONSOLIDATOR_LIVE_IT=1`) — all green.

- **phase11o — consolidation tier-demotion pruner.** Bumps the `vectorizer-sdk` workspace pin to `3.3.0` (issue [hivellm/vectorizer#265](https://github.com/hivellm/vectorizer/issues/265) — adds `delete_vector` / `delete_vectors` / `move_to_collection` + the `POST /collections/{src}/vectors/move` server endpoint with insert-before-delete ordering) and the running container to `hivehub/vectorizer:3.3.0`. New module `crates/cortex-workers/src/pruner/` with five files: `mod.rs` (tier resolver `PruneTier::{Hot,Warm,Cold,Expired}`, `from_age_days(0..7,7..90,90..365,>=365)`, collection mapping, `plan_demotion`, `PruneReport`, `tier_pair_key`), `vectorizer_sink.rs` (calls `VectorizerClient::move_vectors` chunked at `MOVE_BATCH_SIZE = 256`, surfaces per-id failures via `MoveReport.results`), `meili_sink.rs` (local `MeiliPruneOps` trait + `cold_tier_payload` strips `body`/`summary`/`outcome_distribution` on the cold transition), `purge.rs` (hard-purge cascade across Vectorizer/Meili/Nexus/Parquet behind the `REQUIRED_CONFIRMATION_TOKEN = "I-UNDERSTAND-FORGET-IS-IRREVERSIBLE"` gate the `/cortex forget` MCP tool will echo), `engine.rs` (`run_sweep` glues the three sinks and threads a `PruneReport` through). The `VectorizerClient` trait gained two new methods (`delete_vectors`, `move_vectors`) plumbed through `LiveVectorizerClient` to the SDK 3.3.0 wire calls; `MemoryVectorizerClient` carries a real per-collection move/delete simulation so unit tests stay symmetric. New cron seed `retention.consolidation_prune` (`0 3 * * *`, `cortex-ops consolidation-prune`, default ON) registered in `cortex_workers::retention::scheduler::default_jobs`. New `cortex-ops consolidation-prune` clap subcommand paginates `cortex_consolidations` via `GET /indexes/{uid}/documents?limit=N&offset=K`, runs the engine, persists the per-run summary to `<home>/.cortex/pruner-status.json` (override via `CORTEX_PRUNER_STATUS_FILE`); the `/v1/health/coverage` handler reads the file on every probe and surfaces a new `pruner` block (`PrunerStatus { last_run_ts, events_demoted_per_tier, events_purged, last_run_duration_ms, last_error }`). 18 lib unit tests + 2 ITs (`pruner_it.rs`, `pruner_safety_it.rs`, gated on `CORTEX_PRUNER_IT=1`).

- **phase11s — pipeline drainage recovery (classifier supervisor + durable graph offset + embedder JWT refresh).** Three independent worker-resilience bugs surfaced during the 2026-05-03 phase11p reindex; each blocked downstream coverage in a different way and each gets a structural fix here. (1) **Classifier-worker self-healing watchdog.** [`crates/cortex-workers/src/classifier_worker/worker.rs`](crates/cortex-workers/src/classifier_worker/worker.rs) gains `consume_errors_consecutive` + `max_consume_errors` (env `CORTEX_CLASSIFIER_MAX_CONSUME_ERRORS`, default 5). On `N` consecutive `synap consume` errors `run_forever` returns `Err` so the bin propagates a non-zero exit and docker `restart: unless-stopped` recovers the container — no more 26-hour silent dormancy. New `last_consume_ts_ms` (distinct from `last_job_ts_ms` because empty polls still count as forward progress) drives `cortex-ops doctor`'s `classifier-worker stuck` flag when the timestamp ages past `CORTEX_CLASSIFIER_STALENESS_MS` (default 60_000). (2) **Graph-worker durable consumer offset.** Synap SDK 0.12 ships no durable consumer-group surface on the stream API; the §2.3 fallback persists `(consumer_id, stream) → last_offset + last_event_id` to a new SQLite `consumer_offsets` table via three `MetadataStore` helpers (`consumer_offset_lookup`, `consumer_offset_upsert` MAX-semantics, `consumer_offset_set` rewind). [`LiveSynapConsumer::with_persistent_offset`](crates/cortex-workers/src/graph/worker.rs) reads the row on boot and seeds the in-process tracker at `last_offset + 1`; every successful `ack` ledgers the new offset. New `cortex-ops graph replay --since=<offset>` rewinds the cursor when an operator knows a window was lost. (3) **Embedder Vectorizer JWT refresh.** New [`crates/cortex-workers/src/embedder/token_cache.rs`](crates/cortex-workers/src/embedder/token_cache.rs) module ships `TokenCache` (parses the JWT `exp` claim, falls back to `DEFAULT_TOKEN_TTL_SECS = 3600`) plus `should_refresh(now_ms)` honouring a 60 s buffer. [`LiveVectorizerClient::with_credentials`](crates/cortex-workers/src/embedder/vectorizer_client.rs) retains username + password, runs `/auth/login` at boot, and `ensure_token_fresh()` rotates the JWT before each upsert / collection-ensure call (the SDK 3.0.3 takes the bearer at construction so refresh rebuilds the SDK client behind a `tokio::sync::RwLock`). Embedder bin's `/healthz` extras grow `last_login_ts` / `jwt_refresh_total` / `jwt_refresh_errors_total` plus matching Prometheus gauges. New runbook [`docs/cortex/pipeline-drainage-runbook.md`](docs/cortex/pipeline-drainage-runbook.md) walks operators through detect → restart → replay → re-bootstrap, paired with a verification script [`scripts/check-pipeline-coverage.sh`](scripts/check-pipeline-coverage.sh) that prints per-repo Meili / Vectorizer / Nexus counts and flags any repo whose downstream coverage falls below 50 % of Meili. See [ADR-008](.rulebook/decisions/008-durable-consumer-offset-via-sqlite.md) for the decision to ledger offsets in SQLite rather than wait for upstream Synap consumer-group support.

- **phase11r — topic cards as living-synthesis tier on top of consolidations.** New `Kind::TopicCard` envelope (spec 12 §Output + spec 11r §1) carrying a slug-keyed living synthesis the orchestrator rewrites when (a) ≥ 8 fresh events accumulate since the last rev, (b) a high-impact event lands within 0.30 distance, or (c) the synthesis is ≥ 14 days old with new evidence. Five new MCP tools land in [`crates/cortex-api/src/mcp_topic_card.rs`](crates/cortex-api/src/mcp_topic_card.rs): `cortex_topic_get` (slug-exact short-circuit + free-text search with confidence ≥ 0.6 floor), `cortex_topic_drill` (5 dimensions: evidence / contradictions / history / open_questions / related), `cortex_topic_neighbors` (Cypher walk over `:RELATED_TO` + `:EVIDENCE_OF` clipped at 64 nodes), `cortex_topic_diff` (inline unified-diff over the synthesis body + set-diffs over evidence + contradiction status flips), `cortex_synthesize` (operator escape hatch with `force` + `persist` flags + `BudgetExhausted` rejection). Pre-thinking renderer ([formatter.rs](crates/cortex-pre-thinking/src/formatter.rs)) gains a top-priority `## Topic card` section whose ordering is staleness-aware: a fresh card leads the renderer's context band, a stale card (`confidence < 0.6` OR `age > 30d` AND `events_since > 0`) is demoted behind consolidations and stamped with a `> stale-topic-card: <reason>` advisory. Section budget `section_caps::TOPIC_CARDS_BYTES = 1_400`. New per-repo Vectorizer collections `cortex.topic_card.fp32` (hot, recall-tuned `m=48 / ef_search=256`) + `cortex.topic_card.pq` (warm) and a global Meili index `cortex_topic_cards`. Settings v6 push adds `ext.topic_card.{topic_slug, revision, confidence, synthesis_age_d, contradictions_count, repos, synthesis_model}` to filterable / sortable + `synthesis_markdown` + `topic_slug` to searchable. Strategies (`pre_change_context` / `decision_lookup` / `similar_problems`) fan out to the topic-cards lane. New `cortex-topic-cards` bin under `cortex-workers/src/bin/cortex-topic-cards.rs` (per [ADR-007](.rulebook/decisions/007-cortex-workers-as-the-default-host-for-worker-style-daemons.md)) with `rewrite` / `scan-now` / `replay` / `nightly` subcommands. See spec 12 §Output (rewritten with the new section order + advisory format), spec 11 §MCP tools (extended table), spec 16 §Memory view (TopicCards lane note), and [`docs/cortex/topic-cards.md`](docs/cortex/topic-cards.md) for the trigger heuristics, contradiction detector classes, staleness contract, and operator runbook.

- **phase11n — dashboard publisher (learnings + memory).** Two new event sources for the dashboard SSE stream: (a) the FS watcher now covers `.rulebook/learnings/**` and emits a new `learning.added` event kind on the `DashboardEventBus`; (b) a SQLite tail loop in `cortex-api` polls `.rulebook/memory/memory.db` at the FS-watcher cadence (250 ms) with a read-only handle (`SQLITE_OPEN_READ_ONLY | SQLITE_OPEN_URI`) and emits one `memory.appended` event per genuinely-new row. First tick seeds `last_rowid = MAX(rowid)` so historical rows do not flood the bus on daemon restart. Gated by `CORTEX_DASHBOARD_MEMORY_TAIL` (default `1`); missing `memory.db` is a no-op. GUI hook `useDashboardStream` extends the kind → query-key invalidation map for the two new kinds. No upstream change to `@hivehub/rulebook` — the consumer (cortex-api) owns the entire push path.

### Changed

- **phase13b — `EnvelopeProducer` trait + accumulating checkpoint table (ADR-010).** Second load-bearing trait of the medium-rework plan, sibling to ADR-009's `Sweep`. Closes the second-largest abstraction debt the 4-doc analysis named: bootstrap (`crates/cortex-cli/src/bootstrap/walker.rs`), claude-archive ingest (`crates/cortex-workers/src/claude_archive/`), topic-cards emit (`crates/cortex-workers/src/topic_cards/producer.rs`), and consolidator emit (`crates/cortex-workers/src/consolidator/producer/`) each carried ad-hoc checkpoint stores (`.cortex-bootstrap.state.json` overwritten on every invocation, per-project files for claude-archive, no persistence at all for topic-cards, `~/.cortex/consolidator-cursor.json` for the consolidator). New trait in [`crates/cortex-workers/src/producer/`]: `name() / produce(ctx) -> ProducerReport / resume_from(ctx, scope) -> Option<ProducerCheckpoint>`. Supporting types: `ProducerCtx { metadata: Arc<Mutex<MetadataStore>>, now, logger_target }`; `ProducerCheckpoint { producer_name, scope, last_event_id, last_occurred_at, accumulated_at }`; `ProducerReport { producer_name, envelopes_emitted, batches_emitted, last_event_id, last_occurred_at }`. New SQLite table `producer_checkpoints` (append-only, composite PK `(producer_name, scope, accumulated_at)`) registered in `schema.sql` + `cortex_storage::metadata::apply_phase13b_schema`. Helpers: `MetadataStore::record_producer_checkpoint / latest_producer_checkpoint / list_producer_checkpoints_for`; new `ProducerCheckpointRow` re-exported at crate root. Four producers migrated to `impl EnvelopeProducer` via thin wrappers: `BootstrapProducer` (`bootstrap/producer.rs`, scope = repo_id), `ClaudeArchiveProducer` (`claude_archive/producer_trait.rs`, scope = project_dir, runs walker via `spawn_blocking`), `TopicCardsProducer` (`topic_cards/producer_trait.rs`, scope = topic_slug, fans out to `Orchestrator::run`), `ConsolidatorProducer` (`consolidator/producer_trait.rs`, walks all 3 grains: `session_id` / `topic:<label>` / `decision:<event_id>`). Each wrapper carries a per-producer input provider trait (`PiiTargetProvider`-style) with a `Static…` impl for tests. Resume-after-kill IT (`crates/cortex-workers/tests/producer_resume_after_kill_it.rs`) drives a 10 000-event synthetic corpus through a fixture producer, panics after 30% (simulating `kill -9` between final pre-kill checkpoint and the next batch), then a fresh producer instance reads the cursor back and finishes the corpus with no duplicates (`BTreeSet`-verified union) and no gaps (final emit count = 10 000); ADR-010 promotion to `accepted` (decision id=11) gated on this IT. Operator query surface collapses four file stores to one: `SELECT * FROM producer_checkpoints WHERE producer_name = ?`. Tests: 5 storage helper tests (`producer_checkpoint_…`), 7 worker-level trait tests (object-safety, ctx Send+Sync, batch-checkpointing, resume, serde round-trip), 2 bootstrap producer tests, 3 claude-archive producer tests, 3 topic-cards producer tests, 3 consolidator producer tests, 1 resume-after-kill IT. New spec [`docs/specs/24-producer-trait.md`](docs/specs/24-producer-trait.md); updated [`docs/specs/09-bootstrap-cli.md`](docs/specs/09-bootstrap-cli.md) § "Phase13b — `BootstrapProducer` migration".

- **phase13a — `Sweep` trait as single contract for retention/digest/pruning sweeps (ADR-009 + ADR-014).** Closes the structural defect class the 2026-05-05 retention-daemon learning surfaced: six independent gaps had the same shape (no shared "I am running as a sweep" wrapper). New `cortex_workers::sweep` module (`trait.rs`, `ctx.rs`, `report.rs`, `registry.rs`) defines the `Sweep` trait — `name() / schedule() / run(ctx) -> SweepReport / report_view(...)` — and the `SweepCtx { metadata: Arc<Mutex<MetadataStore>>, now, config, logger_target }` + `SweepReport { name, started_at, finished_at, status, bytes_reclaimed, rows_processed, tier_transitions, error_message }` shapes that ride through it. `SweepStatus` mirrors the existing `retention_sweeps.status` taxonomy (`running` / `success` / `failed` / `abandoned`) so the SQLite schema does not change in this phase. All seven retention sweeps migrated to `impl Sweep` (file `crates/cortex-workers/src/retention/<slug>_sweep.rs`): `tier_sweep`, `parquet_rollup`, `cas_vacuum`, `pii_enforce`, `meili_prune`, `metadata_reap`, `consolidation_prune`. New `SweepRegistry { Vec<Box<dyn Sweep>> }` supersedes the per-sweep ad-hoc invocation pattern: `run_all` walks the registry serially, writes one `retention_sweeps` row per invocation via `start_retention_sweep` + `finish_retention_sweep` (full `SweepReport` JSON in `tier_transitions_json`), and surfaces sweep-internal failures as `status = failed` rows with the truncated `error_message` in the payload. `canonical_registry(...)` constructor wires the seven sweeps in declaration order. The legacy `retention::scheduler::Runner` (child-process invocation against operator-tunable `cron_jobs`) stays in place for the operator-facing surface; production daemon flips to the in-process registry in Phase B. Dashboard (ADR-014): `crates/cortex-api/src/dashboard/retention.rs::retention_state` is now a pure reader. It joins the `cron_jobs` schedule projection with the most-recent `retention_sweeps` row per sweep name (parsed from the JSON payload's `name` field via new `parse_sweep_name` helper) and surfaces missing cron rows as wire-level `null` for `next_run` instead of a handler-side string sentinel — the legacy `"never"` / `"n/a"` / `"unknown"` literals are gone. New CI workflow `.github/workflows/dashboard-grep-gate.yml` greps `crates/cortex-api/src/dashboard/**` for those literals on every push and fails the build on any match. GUI `RetentionScheduledRun.next_run` type widened to `string | null | undefined`; the existing render already maps `null`→`"—"`. ADR-009 promoted from `proposed` to `accepted` once §3.1 landed; ADR id=10. Tests: 14 in `sweep/{mod,report,registry}` (object-safety, ctx Send+Sync, serde round-trip, status terminal classifier, Running→Success/Failed transitions, error-message UTF-8 boundary truncation, tier-transition accumulator, view-projection purity, registry success + failure paths, declaration-order names); 3 per migrated sweep × 7 = 21 sweep-wrapper tests; 1 integration test `crates/cortex-workers/tests/sweep_registry_canonical_it.rs` walking the canonical 7-sweep registry against in-memory backends + tempfile archive + tempfile CAS DB and asserting exactly 7 `retention_sweeps` rows materialise with parseable `SweepReport` payloads; 7 dashboard handler tests rewired to the new shape. New spec section in `docs/specs/19-retention.md` § "Phase13a — `Sweep` trait + uniform supervisor".

- **phase12g — Meili index audit + operator-driven reindex.** The 4-doc relevance audit found that `cortex-rulebook-*` and `cortex-vectorizer-*` indexes shipped configured-but-empty in production — every query against those repos returned zero hits even though the parquet archive had the events. Closed in this rev: (1) New module [`crates/cortex-workers/src/fulltext/audit.rs`](crates/cortex-workers/src/fulltext/audit.rs) walks the declared `cortex_storage::fulltext::INDEXES` set against the live `MeiliClient::list_indexes` and classifies every entry as `populated` / `empty` / `missing` / `orphan` (orphan = present on Meili, absent from declared). (2) New CLI subcommand `cortex-ops meili-audit [--json]` prints the per-index report; exit `0` when every declared index is populated, `2` on any drift or transport failure. Pairs with the existing `cortex-ops doctor-meili-indexes` (settings drift) — together they cover the two health axes per index. (3) New CLI subcommand `cortex-ops meili-reindex [--archive-root <path>] [--json]` wraps the production worker's boot-time replay path (`cortex-workers::fulltext::boot_replay::replay_missing_partitions`). Walks the parquet archive for every `(slug, family)` partition Meili lacks and routes each envelope through the standard indexer pipeline — idempotent via Meili `addDocuments` upsert + the partition set difference. Live-smoke baseline against the running stack: `populated=2 empty=0 missing=10 orphan=142` — the 10 missing are the canonical `cortex_*` names; the 142 orphans are the production per-repo shape (`cortex-<repo>-<family>`) the canonical INDEXES list does not declare. The signal the proposal targeted surfaces in both shapes simultaneously. Tests: 6 audit unit (empty/populated/empty-row/orphan/baseline/memory-client wiring), 3 helper unit (resolve_archive_root cascade).

- **phase12c — `is_live_partial_frame` consolidated; `tool_call_digest` post-flip default reconciled.** Two cleanups the rework analyses called out: (1) The partial-frame predicate that distinguishes "writer mid-flush" from "real corruption" was duplicated across `cortex-storage::archive_purge` and `cortex-api::admin_forget` with subtly drifting message lists. Promoted the storage-side copy to `pub fn is_live_partial_frame` and re-exported at the `cortex_storage` crate root; replaced the `cortex-api::admin_forget` duplicate with `use cortex_storage::is_live_partial_frame`. `grep -rn "fn is_live_partial_frame" crates/` now returns exactly one definition — the single-source-of-truth invariant a future audit can trust. The proposal's `cortex-cli::live_file_purge` and `cortex-workers::digest_purge` call sites were never created in this codebase; the consolidation covers every reachable duplicate. (2) The `retention.tool_call_digest` cron's `command` already carries `--purge-originals` in `default_jobs`, but pre-phase11w deployments ship rows with the prior literal that missed the flag. The phase11v §3 `reconcile_default_drift` pass was already generic enough to cover this — phase12c adds a regression test (`seed_defaults_reconciles_tool_call_digest_command_drift`) that pre-seeds the pre-flip command, runs `seed_defaults`, and asserts the reconciler advances the row to the post-flip command without disabling it. Tests: 1 reconcile regression in `cortex-workers::retention::scheduler::tests` + 5 helper classification tests in `cortex-storage::archive_purge::is_live_partial_frame_tests` (3 partial-frame zstd shapes that MUST classify, 2 negative cases that MUST NOT).

- **phase12b — bulk Parquet archive purge + cron sweep.** Operators in production were running `rm -rf` on the archive directory because `cortex-api`'s `/v1/admin/forget` per-event surface was unusably slow at scale; both 4-doc and opus5.7 analyses flagged this as the root cause of "memory cleanup has to be brute force". Closed in this rev: (1) New module [`crates/cortex-storage/src/archive_purge.rs`](crates/cortex-storage/src/archive_purge.rs) exposing `purge_before(home, cutoff, dry_run, repo_filter) -> Result<PurgeReport>`. Walks `${home}/events/year=YYYY/month=MM/day=DD/hour=HH/*.parquet`, deletes every file whose newest envelope's `occurred_at` is strictly older than `cutoff`. Honours the live-frame guard (mirrors `cortex-api::admin_forget::is_live_partial_frame`) so the current-hour file the writer is still flushing is preserved. Conservative repo-filter shape: a file mixing the named repo with others is kept (deleting it would orphan the surviving rows). (2) New CLI subcommand `cortex-ops retention-archive-purge --before <RFC3339> [--dry-run] [--repo <slug>] [--home <path>]` prints the JSON report; exit `0` when every file was either kept or deleted cleanly, `2` on partial failure (per-file delete error / cutoff parse / unresolvable home / unreadable file). (3) New `retention.archive_purge` cron seed in `cortex-workers::retention::scheduler::default_jobs` at `15 3 * * *` (15 min offset from `retention.consolidation_prune` to avoid minute-level contention; the proposal's literal `0 3 * * *` was a conflict). (4) Sweep handler writes one `retention_sweeps` row per invocation with the full `PurgeReport` in `tier_transitions_json` so the dashboard's retention card surfaces the run alongside every other sweep. Tests: 6 in storage walker (no archive / all old / mixed / partial-frame guard / dry-run / repo filter), 5 IT in `cortex-cli::tests::archive_purge_cli_it` (gated on `not(debug_assertions)` because `cortex-ops` debug binary on Windows overflows the default main-thread stack), 3 helpers (resolve_home cascade), seed-count assertion bumped 11 → 12.

- **phase12f — `record_cron_run` TOCTOU race fixed.** [`crates/cortex-storage/src/metadata.rs::record_cron_run`](crates/cortex-storage/src/metadata.rs) used to do `SELECT failure_streak` then `UPDATE … failure_streak = ?` — two read-modify-write round-trips. Two cron supervisors (multi-replica deploy or restart-overlap) racing on the same job both read the same `streak`, both compute `streak+1`, both UPDATE — net advance of 1 instead of 2, lost increments scaling with contention. Closed in this rev: collapsed to a single `UPDATE … SET failure_streak = CASE WHEN ?2 = 'failed' THEN failure_streak + 1 ELSE 0 END … WHERE name = ?7 RETURNING failure_streak` statement. SQLite's WAL writer-lock + the workspace's `busy_timeout` PRAGMA serialise concurrent writers automatically — a single-statement UPDATE is atomic by construction so the explicit `BEGIN IMMEDIATE` the proposal anticipated is unnecessary. Three new tests in `metadata::tests`: single-thread happy path (failed → 1 → 2 → success → 0), all-failures concurrency (4 threads × 250 calls = 1000 increments observed exactly), mixed-status concurrency (alternating fail/success across 4 threads × 100 calls). Tests run under `--release` to expose timing-dependent races the debug build's slower instruction stream would mask. The new path is also faster than the legacy path: one round-trip + one prepared statement + one write lock vs two of each.

- **phase12e — `KIND_IDS` complete + compile-time enum guard.** `crates/cortex-core/src/vocab.rs::KIND_IDS` had only 8 of the 12 `Kind` variants (`turn`, `tool_call`, `agent_call`, `memory`, `decision`, `analysis`, `law_violation`, `artifact`); the four kinds added in phase10e (`knowledge`, `learning`) and phase11j/r (`consolidation`, `topic_card`) were never appended. The vocab is the runtime check the ingestion router uses against `envelope.schema.json`'s string enum — a missing entry is a silent-data-loss bug (envelope rejected, INFO-level "unknown kind" nobody monitors). Closed in this rev: (1) all four stems appended to `KIND_IDS`; (2) new `Kind::COUNT = 12` constant on the enum; (3) compile-time `const _: () = assert!(KIND_IDS.len() == Kind::COUNT, "…");` guard in [`vocab.rs`](crates/cortex-core/src/vocab.rs) so adding a `Kind` variant without an `KIND_IDS` entry fails `cargo check` with an actionable message instead of failing silently at ingest; (4) round-trip tests `every_kind_round_trips_through_kind_ids` + `kind_ids_has_no_duplicates` pin forward / reverse mapping, count match, and uniqueness. The proposal's §2 miss-counter is moot by construction — the closed enum + compile-time guard rule out a runtime miss path; implementing a counter that can never increment is the "error handling for impossible scenarios" anti-pattern.

- **phase11s — workers consolidation merge.** Folded five sibling crates (`cortex-classifier`, `cortex-ingestion`, `cortex-claude-archive`, `cortex-consolidator`, `cortex-retention`) into `cortex-workers` as modules under `src/<area>/`. Workspace member count: **14 → 9**. Bin names preserved on the operator-facing surface. Heavy claude-archive deps (`indicatif`, `sysinfo`, `ignore`) gated behind a new `claude-archive` Cargo feature (default OFF); other merged areas link unconditionally because their deps were already shared with workers. New operator bin `cortex-retention-sweep` pins the systemd / docker bin-name slot for the tier-transition sweep (dry-run-only today; `cortex-ops sweep` continues to host the live LiveVectorizerOps adapter until a follow-up task lifts it). The duplicate `cortex-consolidator` bin (one in `cortex-cli`, one in `cortex-consolidator`) was reconciled into a single unified bin under `cortex-workers/src/bin/cortex-consolidator.rs` exposing all 5 subcommands (`estimate` + `run-session` + `run-topic` + `run-decision` + `nightly`). See [ADR-007](.rulebook/decisions/007-cortex-workers-as-the-default-host-for-worker-style-daemons.md) and [`docs/cortex/workers-modules.md`](docs/cortex/workers-modules.md) for the module → bin map.

### Added

#### Foundation (Phase 0)
- **Spec 01 — Event schema (wire format)** ([`cortex-core`](crates/cortex-core/)) — typed envelope, validator, redactor.
- **Spec 02 — Storage layout** ([`cortex-storage`](crates/cortex-storage/)) — locked layout for every backend (Vectorizer collections, Nexus labels, Meili indices, Synap streams).
- **Spec 03 — Local stack** — `docker-compose.yml` + `bin/cortex-{up,down,reset,doctor,logs}` wrappers + ops CLI (`cortex-ops plan|doctor|smoke`).
- **Spec 04 — Cortex Core ingestion router** — redactor, ingestion HTTP service, canonical envelope batch shape (`{events: [...]}`).

#### Capture & classify (Phase 1)
- **Spec 05 — Classifier** ([`cortex-classifier`](crates/cortex-classifier/) + [`cortex-classifier-worker`](crates/cortex-classifier-worker/)) — Claude Haiku via Claude Code CLI 2.x (parses `result` field, no `--max-tokens`); SDK path optional. Worker isolated in its own crate to break the embedder ↔ classifier cycle ([ADR-002](.rulebook/decisions/002-classifier-worker-lives-in-a-separate-crate-to-avoid-the-classifier-embedder-classifier-cycle.md)).
- **Spec 06 — Embedder** ([`cortex-embedder`](crates/cortex-embedder/)) — Tree-sitter symbol chunking for code, section chunking for docs, Vectorizer client, worker loop. JWT auth + 512-dim alignment + Meili URL env wiring.
- **Spec 07 — Graph writer** ([`cortex-graph`](crates/cortex-graph/)) — Cypher template registry, per-kind payload expansion, parent-anchored edges, worker loop with Synap I/O + out-of-order buffer, per-row Cypher renderers, ASCII-only escape helper, `assert_write_landed`, `OBSERVED_IN` on canonical kind discriminator. Backfill bin.
- **Spec 08 — Full-text indexer** ([`cortex-fulltext`](crates/cortex-fulltext/)) — Meilisearch client, agent_call routing to turns, artifact routing by path + topics, `routed_total` metric, `primaryKey=id` upsert, fan-out parity.
- **Spec 09 — Bootstrap CLI** ([`cortex-bootstrap`](crates/cortex-bootstrap/)) — walks any Hive repo, emits envelope-compliant events on `cortex.events.bootstrap`. Per-repo `cortex.toml` (excludes, chunking, git history). Default `.rulebook/*` discovery across all repos. Per-event publish failures tolerated (5% / 20-floor floor). One Session per bootstrap run; `.rulebook/specs/*.md` split into per-section law envelopes.

#### Adapters (Phase 1)
- **Spec 10 — Claude Code adapter** ([`cortex-adapter-claude-code`](crates/cortex-adapter-claude-code/)) — local daemon + hooks (`SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `Stop`, `SubagentStop`, `Notification`). Stop hook stamps assistant-message Turn envelope. Real `tool_input` capture, multi-session distinction, pre-thinking pipeline wiring with camelCase hook contract.
- **Spec 18 — Claude Code plugin** ([`cortex-plugin/`](cortex-plugin/) + [`cortex-mcp-server`](crates/cortex-mcp-server/)) — stdio MCP bridge with 3 tools (`cortexQuery`, `cortexPreThinking`, `cortexStatus`); 6 slash commands; 3 skills; 3 sub-agents; Windows hook compatibility; sync-cache helper.

#### Retrieval (Phase 1)
- **Spec 11 — Query API** ([`cortex-api`](crates/cortex-api/)) — `POST /v1/query` hybrid retrieval (Vectorizer + Meilisearch + Nexus) + Reciprocal Rank Fusion. Slug-aware cache invalidation, canonical scope echo, source-attribution invariant. Live Nexus-backed `GraphLane`, live Vectorizer-backed `VectorLane` (SDK 3.0.3), live Meilisearch keyword lane. Per-project collection / index isolation. Periodic archive re-scan keeps the lane fresh.
- **Spec 12 — Pre-thinking injection** ([`cortex-pre-thinking`](crates/cortex-pre-thinking/)) — library that turns the `/v1/query` bundle into a deterministic Markdown system-prompt block under a byte budget.

#### Dashboard (Phase 2 — backends shipped, polish ongoing)
- **`/v1/dashboard/*` endpoints** — overview, timeline (recent + SSE stream with reconnect ladder), memory, decisions (+ detail), laws, violations, analyses, tools/stats, graph (edge-first endpoint, name labels, timeline dedup), sessions, trust, conversations (+ session detail with Sonnet-backed cross-event analyzer), handoffs, **rulebook tasks** (`/v1/dashboard/tasks*`).
- **Meili loader** — unblocks decisions / laws / memory dashboards; law catalogue derived from `law_violation` envelopes.
- **GUI (`gui/`)** — React + TypeScript dashboard + Electron shell.
  - **phase2a** — sparkline smoke wire + Atoms README section.
  - **phase2b** — Timeline stats grid + stream controls.
  - **phase2c** — Inspector richer.
  - **phase2d** — Decisions register + Law dashboard polish.
  - **phase2e** — Tweaks drawer (theme / accent / density / sidebar).
  - **phase2f** — Live SSE Timeline + reconnect ladder.
  - **phase2g/phase2h** — Enriched metrics, Decision-chain + graph richness backend.
  - **Graph explorer** — Cytoscape renderer, structural view, project palette, drill-down, label promotion, Session-to-Repo hue inheritance.
  - **Conversations / Handoffs / Analyses** — per-project decision filter; analyses promoted as first-class envelopes; full-width drawer on narrow viewports + horizontal-scroll lock.
  - **Classifications view** — surfaces classifier output corpus-wide.
  - **Sessions sidebar** + session / repo / kind filters end-to-end.
- **Auth** — phase2f dashboard auth tasks tracked.

#### Operations
- **Workspace ports migrated** from `1500x` → `1700x` to avoid conflicts with stand-alone Vectorizer / Synap installations.
- **Doctor + smoke targets** — `make doctor` health-probes every backend; `make smoke` runs `up + doctor + plan`.

#### Observability — pipeline stage metrics & freshness (phase8b)
- **Per-stage divergence counters across the whole pipeline.** Each stage now exports per-kind / per-hook counters via `/healthz` extras *and* a Prometheus-text `/metrics` endpoint mounted on the same listener:
  - `cortex-adapter-claude-code` — `frames_received_total{hook}`, `frames_parsed_total{hook}`, `frames_parse_error_total`, `envelopes_built_total{kind}`, `envelopes_publish_ok_total{kind}`, `envelopes_publish_fail_total{kind}`, `last_frame_ts_ms{hook}`, `last_envelope_ts_ms{kind}`, `last_publish_ok_ts_ms{kind}`.
  - `cortex-ingestion` — `events_received_total{kind}`, `events_archived_total{kind}`, `events_rejected_total{reason}`, `last_archive_write_ts_ms{kind}`.
  - `cortex-api` — `archive_loader_envelopes_seeded_total{kind}`, `meili_loader_docs_seeded_total{family}`, `*_last_refresh_ts_ms`.
  - Workers — `*_jobs_processed_total`, `*_last_job_ts_ms` per worker.
- **NEW `GET /v1/health/freshness`** on `cortex-api` — fans out the same probes `/v1/health` uses, parses the per-stage `last_*_ts` extras, and returns a flat array of `{ key, last_event_ts_ms, gap_seconds, severity }` rows keyed `<stage>.<kind>`. Severity buckets: `gap_seconds > 60` → `warn`, `> 300` → `critical`.
- **NEW `GET /v1/health/divergence`** on `cortex-api` — pairs adjacent-stage counters (`adapter.frames_parsed → adapter.envelopes_built → adapter.envelopes_publish_ok → ingestion.events_archived`) and reports `{ pair, upstream, downstream, delta, delta_growth, severity }` rows. The aggregator caches the previous probe's delta in process memory so `delta_growth > 0` localises a silent drop to the offending boundary in seconds.
- **`cortex-health::server` now mounts an optional `/metrics` Prometheus-text endpoint** alongside `/healthz` (`serve_standalone_with_metrics` / `router_with_metrics`). Workers and the adapter wire it through `spawn_health_listener_with_metrics`.
- Closes the 2026-04-28 JSON-truncation incident class: a divergence row of shape `adapter.frames_parsed → adapter.envelopes_built ≈ N delta_growth` would have fired within seconds instead of the ~2 h grep-the-logs trace that actually found it.
- Canonical metric catalogue: [`docs/metrics.md`](docs/metrics.md). Architecture writeup: [`docs/architecture.md` §13.6](docs/architecture.md#136-observability--pipeline-stage-metrics--freshness-phase8b).

#### Observability — version coherence (phase8c)
- **NEW [`cortex-build`](crates/cortex-build/) crate** — workspace-shared build-time emitter. `emit_version_env()` from each crate's `build.rs` stamps `CORTEX_GIT_SHA` / `CORTEX_GIT_SHA_SHORT` / `CORTEX_BUILD_TS` / `CORTEX_GIT_DIRTY` / `CORTEX_BUILD_PROFILE` via `cargo:rustc-env`; the runtime `version_info!()` macro reads them back as a `VersionInfo` struct.
- **Every binary's `/healthz` extras carries a `version` block** — adapter, ingestion, cortex-api, and all four workers stamp `git_sha`, `build_ts`, `git_dirty`, `profile`, `crate_version` so the running binary's provenance is observable.
- **NEW `GET /v1/health/versions`** on cortex-api — fans out, parses `extras.version`, computes drift against workspace HEAD captured once at boot, returns `{ head_sha, running_binaries[], drift[], all_in_sync }`. `behind_by_commits` is computed via `git rev-list <running_sha>..HEAD --count` per drifted binary.
- **NEW operator scripts** — `scripts/doctor-versions.{bat,sh}` curl the endpoint, print a table, exit non-zero when `all_in_sync == false`.
- **NEW CI gate** — `.github/workflows/version-coherence.yml` rejects PRs whose committed `target/release/<bin>` mtime is older than the most-recent source mtime in the owning crate (defensive — the project doesn't normally commit `target/`).
- Closes the 2026-04-28 incident where the source had the fix but `cortex-api.exe` had been built before the commit and there was no way to ask the running daemon "what git SHA were you built from?".

#### Observability — config coherence (phase8d)
- **NEW `cortex_api::config_audit` module** — pure-function audit of every config surface (`.env`, `~/.cortex/adapter.toml`, `cortex-plugin/.mcp.json`, `cortex-plugin/hooks/hooks.json`) plus cross-checks (e.g. `adapter.toml.endpoint` MUST equal `.env CORTEX_INGESTION_URL`). Each surface has its own typed reader (`read_env_file`, `read_adapter_toml`, `read_mcp_json`, `read_hooks_json`) returning `ReadError::NotFound`/`ReadError::Parse` instead of panicking.
- **NEW `GET /v1/health/config` endpoint** on `cortex-api` — runs the audit server-side via `spawn_blocking`, returns the `ConfigAudit { findings[], surfaces_read }` JSON. The dashboard renders findings as a table; CI curls and gates on `worst_severity`.
- **NEW `cortex-ops doctor-config` subcommand** + `scripts/doctor-config.{bat,sh}` — run the same audit locally. Exit codes: `0` all ok, `1` any warn, `2` any critical. Supports `--json` for machine-readable output and `--workspace` / `--adapter-toml` overrides for fixtures.
- Closes the 2026-04-28 incident's first wrong turn: the adapter was talking to `:15010` while ingestion was bound to `:17010`, the config file had the right value, but a stale daemon was holding the old endpoint in memory. The audit names the discrepancy as `severity: critical` with a single-line message containing both ports.

#### Observability — silent-drop detector (phase8e)
- **NEW background watcher in `cortex-api`** (`crates/cortex-api/src/silent_drop.rs`) — polls the same divergence pairs `/v1/health/divergence` surfaces, runs each pair through a debounced state machine (`Ok → Warn` requires 2 consecutive polls > `warn_delta`; `Warn → Critical` fires on 1 poll > `critical_delta`; recovery requires 5 consecutive non-growing polls), and emits `law_violation` envelopes on every transition.
- **Push channel** — each alert envelope lands in the durable archive (POSTed to cortex-ingestion `/v1/events/batch`) AND the in-memory `MemoryKeywordLane` so the alert shows up in the existing Live Timeline + Violations dashboard within ~1s, no manual endpoint poll needed.
- **Per-pair state persists** at `~/.cortex/alerts/<pair>.json` so a daemon restart does not re-fire alerts the previous run already flagged.
- **Optional escalation hooks** — `SilentDropConfig.webhook_url` POSTs the envelope to a webhook on every transition; `write_to_handoff: true` appends a `[silent-drop alert]`-prefixed line to `.rulebook/handoff/_pending.md` on every Critical transition so the next session inherits the context.
- Closes the 2026-04-28 silent-drop incident verbatim: the divergence between `adapter.frames_parsed` and `adapter.envelopes_built` would surface as a `law_violation` envelope within ~60s of the truncation hitting, instead of the ~2h log-grep search that actually found it.

#### Observability — synthetic E2E canary (phase8f)
- **NEW `cortex_api::canary` module** — fires a synthetic hook frame through the real IPC pipe (`\\.\pipe\cortex-adapter-claude` on Windows, `~/.cortex/adapter-claude.sock` on Unix) and polls `/v1/dashboard/timeline/recent` for the embedded marker until a configurable deadline elapses. Frame body deliberately mimics the 2026-04-28 regression vector: pretty-printed JSON with multi-line `\n`-escaped strings inside `tool_response.stdout`.
- **NEW `cortex-ops canary` subcommand** + `scripts/canary.{bat,sh}` — exit codes `0` round-trip success, `1` transport error, `2` deadline timeout. Suitable for ad-hoc smoke checks and CI gates.
- **NEW `cortex-api` background runner** — opt-in via `CORTEX_CANARY_ENABLED=1`. Ticks every `CORTEX_CANARY_INTERVAL_SECS` (default 300), appends every result to `~/.cortex/canary-history.jsonl`, and POSTs a `law_violation` envelope (severity `critical`, `law_id: canary-<hook>`) on failure via the same path phase8e uses — so quiet-hours regressions surface in the existing Violations dashboard automatically.
- Closes the quiet-hours failure class: a regression like the 2026-04-28 JSON truncation is now detected in 10s ad-hoc or ~5 minutes scheduled instead of hours.

#### Observability — dashboard Health view (phase8g)
- **NEW `gui/src/views/Health.tsx`** — first-class Health dashboard surfacing the phase8a–8f system: overall banner, subsystems grid, freshness table sorted by `gap_seconds` desc, divergence table filtered to `severity != ok`, version-drift section (rendered only when `all_in_sync == false`), config audit. Each section degrades gracefully to an empty-state when its endpoint times out.
- **NEW `GET /v1/health/stream` SSE endpoint** on cortex-api — emits a combined `HealthSnapshot { overall, freshness, divergence, truncated }` every 5 s plus a `heartbeat` every 15 s. Snapshot byte-capped at 64 KiB; oversized payloads halve the freshness vec and flip `truncated: true` so the GUI can render an explicit "showing N of M" hint.
- **NEW topbar status pill** in the GUI header — visible from every view (Live Timeline, Conversations, Decisions, …) with green/yellow/red dot driven by `/v1/health.overall`. Click jumps to `/health`. Polls every 5 s; the user can't miss a stack-degraded state while browsing.
- **NEW typed API clients** in `gui/src/lib/api.ts` (`healthOverview`, `healthFreshness`, `healthDivergence`, `healthVersions`, `healthConfig`) + matching TypeScript types (`HealthOverview`, `FreshnessRow`, `DivergenceRow`, `VersionsReport`, `ConfigAudit`, `HealthSnapshot`).
- 5 new vitest tests in `gui/src/views/Health.test.tsx` covering banner rendering, subsystem cards, divergence-row visibility, config-audit ok-row filtering, freshness gap-label format. `pnpm test` reports 15/15 passing.

#### Observability — CI smoke gate (phase8h)
- **NEW `.github/workflows/health-smoke.yml`** — runs on every PR + push to main, matrix `[ubuntu-latest, windows-latest]`, 12-minute budget. Boots the full stack (cortex-ingestion + cortex-api + cortex-adapter-claude-code), polls `/v1/health`, then runs `health` + `doctor-versions` + `doctor-config` + `cortex-ops canary` in series. Any non-clean exit fails the PR. Failure path uploads `$CORTEX_HOME/logs/*.log` as a named artifact for postmortem.
- **NEW `scripts/ci/boot-stack.{sh,bat}` + `teardown-stack.{sh,bat}`** — reusable boot helpers. Honour `CORTEX_HOME` for concurrent-run isolation. `boot-stack` waits for `/v1/health.overall` to reach `ok` or `degraded` (60 s timeout); `teardown-stack` reads `$CORTEX_PIDS_FILE` and SIGTERM (then SIGKILL after 5 s) every spawned daemon.
- **NEW `.github/PULL_REQUEST_TEMPLATE.md`** — adds a "Health checks" section with checkboxes for `scripts/health`, `scripts/doctor-versions`, `scripts/doctor-config`, `scripts/canary` outcomes. Soft cultural signal that complements the automated workflow gate.
- Closes the regression-reaches-main failure class: a bug like the 2026-04-28 JSON truncation cannot merge because the canary in CI would round-trip a multi-line `\n`-escaped frame and observe the missing envelope.

#### Storage — retention sweeper core (phase9a)
- **NEW [`cortex-retention`](crates/cortex-retention/) crate** — `SweepPlan` + `TierPair` + `VectorizerOps` trait + `run_sweep` implement spec 02's tier-transition contract: FP32 → PQ at 30 d, PQ → Binary at 365 d for `turn` / `tool_call` / `code_chunk`. Always-hot kinds (`decision`, `analysis`, `memory`, `law`) are deliberately absent. `MemoryVectorizerOps` is the in-memory test double that drives every spec scenario without a live Vectorizer.
- **NEW `cortex-ops retention-sweep` subcommand** + `scripts/retention-sweep.{bat,sh}` — `--time-travel <RFC3339>`, `--dry-run`, `--batch-size N`, `--metadata-db PATH`, `--json`. Exit codes: `0` success, `1` error-rate ceiling tripped / hard failure, `2` another sweep in flight.
- **`retention_sweeps` SQLite bookkeeping** — extended with a `status` column (`running` / `success` / `failed` / `abandoned`) plus three `MetadataStore` helpers: `start_retention_sweep` (concurrency lock + abandonment grace), `finish_retention_sweep`, `list_recent_sweeps`. The migration is idempotent — pre-phase9a databases get the column added via `ALTER TABLE` without bumping `SCHEMA_VERSION`.
- **Idempotent + crash-safe** — re-encode → upsert dest → delete source. A re-run short-circuits on `dest_has(event_id)`. A mid-flight crash leaves the source record alone; the next sweep finds the destination row already in place and cleans the source. The 5 % `max_error_rate` ceiling fails the run but still writes the bookkeeping row so the dashboard surfaces the regression.
- **Spec doc**: NEW [`docs/specs/19-retention.md`](docs/specs/19-retention.md) (wire shape + exit-code map + idempotence + error budget + observability contract).
- **Tests**: 16 unit tests in `cortex-retention/src/lib.rs` (every spec scenario verbatim — FP32→PQ at 31 d, PQ→Binary at 366 d, fresh-record no-op, idempotent re-run, dry-run observe-only, ceiling-trip vs ceiling-allow drop-rate); 6 unit tests in `cortex-storage/src/metadata.rs` (concurrency lock, abandonment grace, finish counters, list-newest-first, honours-limit). `cargo test --workspace` 0 failures.

#### Storage — archive rollup compactor (phase9b)
- **NEW `cortex_retention::parquet_rollup` module** — implements spec 02 §"Event archive (Parquet)" rollup contract: hourly → daily at 90 d, daily → monthly at 365 d, drop monthly at 3 y unless `kind ∈ {decision, analysis, law_violation}` or `redactions[].pii_risk = "low"`. The on-disk format is zstd-compressed NDJSON (despite the `.parquet` suffix); the compactor concatenates source rows line-by-line.
- **Atomic + crash-safe** — read → write `<dest>.tmp` → `sync_all` → `rename` → `unlink sources`. Row-count assertion (`sources_rows == dest_rows`) catches bad merges before commit. Orphan `.tmp` from a previous crash gets cleaned up on entry.
- **Corruption quarantine** — `*.corrupted*`, orphan `*.tmp`, and any file that fails zstd decode get moved to `events/_quarantine/<relpath>` with a sibling `.reason` text file. Query layer skips paths under `_quarantine/` automatically.
- **NEW `cortex-ops rollup` subcommand** + `scripts/rollup.{bat,sh}` — `--time-travel`, `--dry-run`, `--granularity all|hourly-to-daily|daily-to-monthly|three-year-drop`, `--archive-root`, `--json`. Default exit `0` even when individual partitions fail; `1` on hard error.
- **Spec doc**: `docs/specs/19-retention.md` extended with the rollup contract (granularities table, atomicity protocol, quarantine layout, whitelist semantics, RollupCounts shape).
- **Tests**: 11 new unit tests in `parquet_rollup.rs` covering every spec scenario verbatim — 91-day-old hourly directory becomes a daily file, 366-day-old daily files merge into a monthly file, 1100-day decision survives the 3-y drop while a same-age plain turn does not, monthly file gets removed outright when no record passes the whitelist, `*.corrupted*` + orphan `*.tmp` quarantine on entry, granularity serde round-trip. `cargo test --workspace` 0 failures (27 cortex-retention tests total).

#### Storage — CAS vacuum (phase9c)
- **NEW `cortex_retention::cas_vacuum` module** — implements spec 02 §CAS weekly vacuum: deletes rows where `refcount = 0 AND last_referenced < now - 30 d`, then `VACUUM`s the metadata DB when the freelist exceeds 25 % of pages. Per-batch transactions (`BEGIN IMMEDIATE`, 256 rows) keep `SQLITE_BUSY` away from concurrent ingestion. The `DELETE … WHERE refcount = 0` predicate guards against TOCTOU when a concurrent `retain` lands mid-batch.
- **Catastrophic-deletion safeguard** — refuses when the candidate set exceeds 50 % of total blobs unless `--force`; a dry-run surfaces `safeguard_tripped: true` instead of erroring so operators can preview the problem without abort.
- **NEW storage helpers** in `cortex_storage::cas`: `select_vacuumable(cutoff, limit) -> Vec<VacuumCandidate>`, `delete_blobs(&[hash])` (per-batch tx with `BEGIN IMMEDIATE`), `total_blob_count`, `total_blob_bytes`, `conn_mut`. New `VacuumCandidate { hash, size }` row.
- **Refcount audit** — `audit_refcounts(store, references)` recomputes the expected refcount from a caller-supplied iterator of external references and returns `Vec<RefcountDrift { hash, claimed, observed }>`. `fix_refcounts(store, drift)` writes the observed values back in one `BEGIN IMMEDIATE` transaction.
- **NEW `cortex-ops cas-vacuum` subcommand** + `scripts/cas-vacuum.{bat,sh}` — `--time-travel`, `--dry-run`, `--force`, `--cas-db PATH`, `--json`. Plain-text summary surfaces `total_blobs`, `dropped`, `bytes_reclaimed`, `free_pages_ratio`, `did_vacuum`, `vacuum_ms`, plus a WARN line when the safeguard would trip.
- **Spec doc**: `docs/specs/19-retention.md` extended with the CAS vacuum contract (eligibility, atomicity, reclamation, safeguard, refcount audit, test surface).
- **Tests**: 13 new unit tests covering every spec scenario verbatim — 31-day orphan deleted, fresh blob preserved, dry-run no-op, safeguard refuses + force overrides + safeguard no-op on empty store, audit reports under/over-count drift + aligned no-drift, fix_refcounts writes observed, batches split evenly. `cargo test --workspace` 0 failures (40 cortex-retention tests total).

#### Storage — PII retention enforcement (phase9d)
- **NEW `cortex_retention::pii_enforce` module** — implements spec 01 §PII tiers. `classify(plan, target)` maps each candidate to one of `High30d` / `Medium90d` / `NullSafety90d` (or `None` for fresh / already-redacted / pii_risk=low rows). `run_enforcement(plan, backend, targets)` walks the cohorts and dispatches to the `PiiBackend` trait in the spec-mandated cross-store order — Parquet → Vectorizer → Meili → CAS for the high path, summarize → re-embed → re-index → Parquet → CAS for the medium / null-safety paths.
- **Null-safety net** — records with `pii_risk = null` AND age ≥ 90 d enter the medium path automatically and emit a `cortex.warnings` event so classifier-coverage gaps are auditable.
- **Idempotent re-runs** — `classify` filters out rows whose `payload.redacted` is already set; a partial run that crashes mid-flight rolls forward on the next sweep without double-summarizing.
- **`PiiBackend` trait surface** — minimal eight-method abstraction (`rewrite_row`, `delete_vector`, `delete_meili`, `decrement_cas`, `summarize`, `reembed_and_upsert`, `reindex_meili`, `emit_warning`) so production wires the live Vectorizer / Meili / CAS / classifier clients while tests use `MemoryPiiBackend` for in-memory round-trips with one-shot failure injection.
- **NEW `cortex-ops pii-enforce` subcommand** + `scripts/pii-enforce.{bat,sh}` — synthetic cohort preview today (built-in target suite covers each cohort + fresh no-op + already-redacted idempotence). Production-backend wiring lands with phase9k's cron scheduler.
- **Spec doc**: `docs/specs/19-retention.md` extended with the PII enforcement contract (cohort matrix, cross-store ordering, `PiiBackend` surface, CLI shape, test manifest).
- **Tests**: 16 new unit tests covering every spec scenario verbatim — classify high/medium/null/low/under-threshold/already-redacted, cohort redaction tags, high-path Parquet→Vector→Meili→CAS ordering, medium-path summarize+re-embed+re-index, null-safety warning + medium dispatch, dry-run no-mutation, cohort filter skips others, already-redacted skip, mid-flight failure recorded, cohort-counts serde. `cargo test --workspace` 0 failures (56 cortex-retention tests total).

#### Storage — LLM turn digest summarizer (phase9e)
- **NEW `cortex_retention::turn_digest` module** — bucketizes turns whose `occurred_at < now - digest_after_days` (default 30) by `(repo, ISO_year_week, top_topic)` and produces one Sonnet-driven `:Memory{memory_type="turn_digest"}` per non-empty bucket whose size ≥ `min_bucket_size` (default 5). The pure-function `bucketize` returns the deterministic plan; `run_turn_digest` orchestrates the per-bucket calls through the `DigestBackend` trait. ISO week labels follow RFC 8601 (`YYYY-Www`) so bucket keys are timezone-stable.
- **Idempotence** — `lookup_existing(repo, year_week, top_topic)` short-circuits already-digested buckets; `--rebuild` re-summarizes in place. A re-run with no new old turns reports `buckets_done=0, usd_cents=0`.
- **Cost ceiling** — `max_usd_cents_per_run` (default 500) caps per-run spend; the orchestrator stops cleanly when the running total breaches the budget and surfaces `buckets_pending` in the report so the next run resumes from the same point.
- **`DigestBackend` trait surface** — minimal four-method contract (`lookup_existing`, `summarize`, `persist_digest`, `tag_source_turns`). `persist_digest` does the full write fan-out (event emit + embed + Nexus `:Memory` + `[:SUMMARIZES]` edges) and returns the digest event id; `tag_source_turns` stamps `payload.summarized_by` on every source turn's Parquet row. Production wires the live classifier + embedder + Nexus writer + Parquet rewriter; tests use `MemoryDigestBackend` for in-memory round-trips with one-shot failure injection.
- **NEW `cortex-ops turn-digest` subcommand** + `scripts/turn-digest.{bat,sh}` — synthetic preview today (16-target suite covering two topics in one week). Production walker (Parquet + classifier + embedder + Nexus + Parquet rewriter integration) lands with phase9k's cron scheduler.
- **Spec doc**: `docs/specs/19-retention.md` extended with the LLM turn digest contract (cohort matrix, bucket key shape, `DigestBackend` surface, cost ceiling, CLI shape, test manifest).
- **Tests**: 14 new unit tests covering every spec scenario verbatim — `iso_year_week` label, bucket key format, bucketize groups by repo/week/topic, filters under-size, excludes fresh + already-digested, run persists one per bucket in call order, idempotent re-run, `--rebuild` re-summarises, `--dry-run` no-mutation, budget ceiling cuts cleanly, per-bucket failure recorded, JSON round-trip, plan defaults match spec. `cargo test --workspace` 0 failures (70 cortex-retention tests total).

#### Storage — Meilisearch archival pruner (phase9f)
- **NEW `cortex_retention::meili_prune` module** — implements spec 02 §Meilisearch retention. Documents in `cortex_turns` / `cortex_tool_calls` whose `occurred_at < now - prune_after_days` (default 90) get their body fields blanked + `summary` capped to 4 KiB with a UTF-8 ellipsis marker + `pruned: true` + `pruned_at` stamped. Documents are **never** deleted — the keyword lane still surfaces pruned rows on summary matches.
- **`cap_summary` UTF-8 safety** — when truncating, the cap walker rounds back to the nearest char boundary so multibyte codepoints never get sliced mid-character. When the cap is ≤ 3 bytes the helper returns the ellipsis verbatim.
- **`MeiliBackend` trait surface** — minimal two-method contract (`enumerate_prunable`, `update_documents`); deliberately omits `delete_documents` since pruning never deletes. Production wires the live Meili SDK; tests use `MemoryMeiliBackend` for in-memory round-trips with one-shot failure injection + `commit_updates` helper for the second-run idempotence test.
- **Idempotent re-runs** — pruned docs carry `pruned: true`, the matcher excludes them by default; `--rebuild` re-prunes when the cap policy changes.
- **Batched writes** — `update_documents` is called in chunks of `batch_size` (default 1 000) so a flaky task-await doesn't lose the whole run.
- **NEW `cortex-ops meili-prune` subcommand** + `scripts/meili-prune.{bat,sh}` — synthetic preview today (3 turns + 1 fresh + 1 oversize across both indexes). Production wiring lands with phase9k's cron scheduler.
- **Spec doc**: `docs/specs/19-retention.md` extended with the Meili pruning contract.
- **Tests**: 16 new unit tests covering every spec scenario verbatim — plan defaults, cap unchanged/truncated/char-boundary/ellipsis-edge, enumerate excludes fresh + already-pruned, runs prune across both indexes, second-run no-op, `--rebuild` re-prunes, dry-run no-mutation, oversize cap with ellipsis, failure propagation on enumerate + update, JSON round-trip, batch chunks split large runs. `cargo test --workspace` 0 failures (86 cortex-retention tests total).

#### Conversation archive + relevance tuning (phase11i)
- **NEW [`cortex-claude-archive`](crates/cortex-claude-archive/) crate** — walks Claude Code's `~/.claude/projects/<project>/*.jsonl` archive, parses every record, projects them into canonical Cortex envelopes, and ships them through one of two sinks (`stdout` or zstd-NDJSON `archive` partitions under `<archive_root>/events/year=…/…/bootstrap-claude-NNNNN.parquet` — re-read at boot by `cortex-api`'s `archive_loader`). Three subcommands: `estimate`, `bootstrap` (with `--resume` against a JSON checkpoint), and §5.2's `tail` watcher daemon.
- **`cortex-claude-archive tail` watcher daemon** — long-running poller (1 Hz, override via `CORTEX_CLAUDE_ARCHIVE_POLL_MS`) that re-reads any session whose `(mtime, len)` advanced and emits envelopes whose `event_id` is not in the in-memory dedupe set. Axum HTTP server on `:17030` (override via `CORTEX_CLAUDE_ARCHIVE_BIND`) exposes `GET /healthz` returning `last_flush_ts`, `files_watched`, `envelope_rate`, `rss_bytes`, `envelopes_emitted`, `envelopes_dropped`, `uptime_ms`, `status`. Cross-platform RSS via `sysinfo`; SIGTERM / SIGINT trigger graceful shutdown via a tokio `watch` channel.
- **`cortex-api` relevance fusion** — `FusionConfig` now carries phase11i §3.x knobs: per-intent recency-decay λ (`pre_change_context` / `similar_problems` / `free_search` / `explain` 0.02 ≈ 35-day half-life; `decision_lookup` 0.005 ≈ 140-day; `law_check` 0 evergreen), cross-repo boost (clamped `[0, 1]`), session cohesion (same-session ×2.0, cohort ×1.5), outcome filter + multipliers (success ×1.2, error ×0.5, blocked_by_law ×0.3). `Scope` request-side fields (`recency_decay`, `cross_repo_boost`, `models`, `tools`, `session_id`, `session_cohort`, `outcomes`, `exclude_outcomes`) round-trip through `canonicalise_scope`. Meilisearch `settings.v1.json` bumped to v3 with `model` / `tool` / `session_id` / `outcome` filterable.
- **NEW [`crates/cortex-api/config/relevance.toml`](crates/cortex-api/config/relevance.toml)** — boot-loaded + SIGHUP-reloadable knob file carrying every multiplier (RRF α/k, cross-repo boost, session boosts, outcome multipliers, per-intent recency λ). Orchestrator's `fusion` field becomes `Arc<RwLock<FusionConfig>>` so the SIGHUP-driven `Orchestrator::replace_fusion` path makes the swap visible to in-flight handles. Windows builds log a one-shot WARN — file still loads at boot.
- **Pre-thinking renderer (spec 12) refresh** — new `Past sessions` section between `Similar past turns` and `Relevant snippets` (top-3 by centroid similarity, first-prompt clipped to 80 bytes). Outcome glyph (`✓` / `✗` / `⚠`) on every turn + decision line driven by `cortex_api::SimilarTurn.outcome` + `DecisionRef.status`. Spec doc 12 §Output refreshed with both contracts.
- **NEW [`crates/cortex-api/tests/fixtures/relevance-gold.json`](crates/cortex-api/tests/fixtures/relevance-gold.json)** + **`relevance_eval_it.rs`** — 30-question hand-curated gold set (10 pre_change_context · 5 decision_lookup · 10 similar_problems · 3 law_check · 2 free_search). Live IT gated behind `CORTEX_RELEVANCE_IT=1` hits `cortex-api`, computes recall@10 / MRR@10 / NDCG@10 + per-intent MRR, panics when MRR@10 < 0.75. Scoring math runs unconditionally so a matcher regression fails the default `cargo test`.
- **NEW [`docs/cortex/relevance-tuning.md`](docs/cortex/relevance-tuning.md)** — operator handbook: gold-set authoring, IT runbook, reading the eval output, when to re-tune `relevance.toml` (and when *not* to — re-index, fix the gold entry), SIGHUP reload workflow, troubleshooting matrix.
- **`docker-compose.yml` `cortex-claude-archive` service** — read-only bind mount of the host's `~/.claude/projects` (override via `CORTEX_CLAUDE_PROJECTS_HOST`), shared `${CORTEX_HOME_HOST}` archive bind-mount with cortex-ingestion + cortex-api, depends_on synap (healthy) + cortex-ingestion (started), HEALTHCHECK on `:17030/healthz`. cortex-api env block defaults `CORTEX_ARCHIVE_WATCHER_URLS=http://cortex-claude-archive:17030` so `/v1/health/coverage` surfaces the watcher under a new `archive_watchers` block out of the box.
- **Tests**: 9 unit tests in `cortex-claude-archive::tail` (rate computation, snapshot shape, status transitions, axum router round-trip, env resolvers, clamp behaviour); §5.3 `memory_it.rs` (gated, 50 k pair / 100 k record corpus, asserts RSS < 512 MiB — measured 14.9 MiB on a real run); §5.4 `corrupt_line_it.rs` (corrupt lines counted in `envelopes_dropped`, valid records still emit, no panic, second-tick no-op); 8 unit tests in `cortex_api::relevance_config` + 5-case reload IT (boot read, `replace_fusion` propagation across cloned handles, missing file fallback, malformed TOML, intent-table round-trip); 8 `relevance_eval_it.rs` tests (matcher math + gated live path); 6 past-sessions formatter cases + 7 outcome-glyph cases. `cargo test --workspace` 0 failures.

### Changed
- **`nexus-graph-sdk` 2.0 → 2.1** ([phase11l §1.3](.rulebook/tasks/phase11l_nexus-external-ids-migration/)) — workspace pin bumped so Cortex can adopt Nexus's reserved `_id` external-id slot. The SDK 2.1 surface (`create_node_with_external_id`, `get_node_by_external_id`, `ConflictPolicy::Match|Replace|Error`) is verified live against `hivehub/nexus:2.1.0` by the new `crates/cortex-workers/tests/nexus_external_id_smoke_it.rs` IT (gated on `CORTEX_NEXUS_EXTERNAL_ID_IT=1`; 6 cases covering sha256 + str round-trips, all three conflict policies, absent-id resolution).
- **`docker-compose.yml` Nexus pin 2.0.0 → 2.1.0** — running container must agree with the SDK on the new `/data/nodes/by-external-id` route + `_id` semantics; pre-bump containers return 404 on the new route.
- **Graph node identity migrated to Nexus `_id`** ([phase11l + ADR-004](.rulebook/decisions/004-cortex-graph-nodes-carry-their-identity-in-nexus-s-reserved-id-slot.md)) — `NodeOp` gained `external_id: Option<String>` + `ConflictPolicy { Match | Replace | Error }`; eleven `node_*.cypher` templates rewrote `MERGE { natural_key }` → `CREATE { _id } ON CONFLICT MATCH`; five `*_natural_key` schema constraints retired (Nexus's `ExternalIdIndex` enforces uniqueness structurally); `UNKNOWN_CONTENT_HASH = "*"` sentinel rewrote to per-`(repo, path)` `pending|repo|path` form; `cortex-ops graph drop --confirm` admin command lands for the bootstrap-replay path; new operator runbook at [`docs/cortex/external-id-migration.md`](docs/cortex/external-id-migration.md).

### Added
- **Phase11j consolidation tier — read side** ([phase11j §1-§4](.rulebook/tasks/phase11j_consolidation_tier/) + [spec 12 §Output](docs/specs/12-pre-thinking-injection.md)) — new `Kind::Consolidation` envelope (typed payload with `grain ∈ {session, topic, decision_trace}`, `depth ∈ {Shallow, Deep}`, `scope` tagged union, `temporal_span`, `outcome_distribution`, `source_event_ids` clipped at 256 inline cap with full `source_event_count`); standalone `cortex-consolidator` crate (Anthropic Messages API `summariser.rs` with Haiku 4.5 / Opus 4.7 pricing constants + cost ledger, three per-grain prompt templates under `templates/`, `producer/{session,topic,decision_trace}.rs` pipelines with deterministic `derive_consolidation_id`, `orchestrator.rs` with budget gate + Opus auto-promote on DecisionTrace + fallback Opus→Haiku on quota failure); `cortex-consolidator` CLI binary (`run-session` / `run-topic` / `run-decision` / `nightly --dry-run`); routing surface (`family_for(Kind::Consolidation) -> "consolidations"` in both fulltext + embedder; per-repo `cortex-{slug}-consolidations` Meili index; global `cortex_consolidations` index; hot `cortex.consolidation.fp32` + warm `cortex.consolidation.pq` Vectorizer collections with recall-tuned HNSW `m=48 / ef_search=256`); Meili settings v3 → v4 (filterable + sortable axes for `ext.consolidation.{grain, depth, model, consolidation_id}`, searchable `ext.consolidation.outcome_distribution`); `cortex-bootstrap --apply-settings-only` non-destructive roll-forward (PATCH /settings is additive); both retrieval strategies (`pre_change_context` + `similar_problems`) fan out to the consolidations lane; pre-thinking renderer surfaces `## Consolidated context` (top-3 by similarity, format `grain/id · YYYY-MM-DD · ✓|✗|⚠ · title`) replacing `## Past sessions` when ≥ 1 hit lands and falling back to it on the empty path. Companion task `phase11o_vectorizer_demotion_api` tracks the §5 pruning daemon work — blocked on the Vectorizer SDK gaining `move_to_collection` + `delete_vectors` (SDK 3.2 has neither).
- **Phase11m dashboard push cache** ([phase11m §1-§7](.rulebook/tasks/phase11m_dashboard_push_cache/) + [spec 21](docs/specs/21-dashboard-push.md)) — `/v1/dashboard/stream` SSE endpoint emits typed delta events (`task.changed`, `handoff.appended`, `decision.changed`, `memory.appended`, `knowledge.added`) carried over a shared `DashboardEventBus` (`tokio::sync::broadcast`, capacity 1024). File-system watcher (`notify-debouncer-mini`, 250 ms debounce) on `.rulebook/{tasks,handoff,decisions,knowledge}/**` feeds the bus; `dashboard_consumer.rs` adds a 1024-id dedup ring for the upcoming Synap path. GUI hook `useDashboardStream` (single EventSource per `connKey`, exponential reconnect 1s → 30s) dispatches `queryClient.invalidateQueries` per kind and fires `invalidateAllDashboardQueries` on every reconnect-hello to recover any drop window. Polling intervals on Tasks / Handoffs / Decisions dropped 30 s → 300 s (safety net only). Header gains a `stream` pill (green when connected, amber when offline). Companion task `phase11n_rulebook_dashboard_publisher` tracks the rulebook-MCP-side Synap publisher that completes the loop for DB-only writes (memory).
- **Phase11k governance lane projection** ([phase11k §1-§5](.rulebook/tasks/phase11k_governance_lane_projection/) + [docs/cortex/governance-ingestion.md](docs/cortex/governance-ingestion.md)) — closes the read-side projection gap on `decision_lookup` / `law_check`. Writer (`cortex-workers/src/fulltext/document.rs`) now stamps spec-11 lane projection contract keys at the top of every Meili document: `decision_id` / `decision_title` / `decision_status` / `decision_supersedes` for `Kind::Decision`, `law_id` / `law_severity` / `law_tier` for `Kind::LawViolation`, `turn_id` for `Kind::Turn`. Settings v4 → v5 marks them filterable + searchable. New `routing::index_for_event_global(kind)` returns `cortex_decisions` / `cortex_laws`; the indexer dual-writes per-repo + global so `decision_lookup` answers cross-repo without enumerating repos. `cortex.toml` `[cortex.laws]` now ships `AGENTS.override.md` / `AGENTS.md` in `promote_patterns` plus a new `extract_pattern = "^LAW-[A-Z0-9-]+$"` knob; bootstrap emitter splits AGENTS files into one `law.imported` envelope per matching `## LAW-...` heading. New `cortex-claude-archive::governance_watcher::GovernanceWatcher` polls `.rulebook/decisions/`, `.rulebook/laws/`, `.claude/rules/`, `AGENTS.override.md`, `AGENTS.md` and emits content-hash-deduped change events to a pluggable `GovernanceEmitter`. Acceptance ITs at `crates/cortex-api/tests/{decision_lookup,law_check,governance_global_index}_it.rs` (gated for the live-stack form by `CORTEX_GOVERNANCE_IT=1`).
- **Phase11k graph correlation layer** ([phase11k §1-§7](.rulebook/tasks/phase11k_graph_correlations/)) — static code + markdown analyzers (Rust / TypeScript / Python / Go), three-tier symbol resolver, doc-↔-code edge classes (`IMPORTS_FILE`, `CALLS`, `USES_TYPE`, `IMPLEMENTS`, `EXTENDS`, `RE_EXPORTS`, `LINKS_TO`, `DOCUMENTS`, `LINKS_TO_SECTION`, `MENTIONS`, `DESCRIBES_PATH`, `DOCUMENTED_BY`, `DOCSTRING_REFERENCES`, `CITES`, `DERIVED_FROM`, `CONTAINS`), `cortex-bootstrap --graph-static` flag, live trigger in graph worker, stale-edge sweeper (`StaleEdgeSweeper` + `delete_edges_by_filter` writer surface + `EdgeDeleteFilter` type), per-session `(content_hash, analyzer_version)` coalescer dedup, three new read-side Cypher templates (`code_callers.cypher`, `doc_trail.cypher`, `blast_radius.cypher`), pre-thinking renderer sub-blocks (`Connected files (via IMPORTS_FILE)`, `Documented under (via DOCUMENTED_BY)`, `Cited from (via CITES)`), 10 new gold-set entries exercising the new graph paths, operator handbook at [`docs/cortex/graph-tuning.md`](docs/cortex/graph-tuning.md).

### Decisions
- **[ADR-001](.rulebook/decisions/001-bypass-vectorizer-sdk-for-insert-and-get-vector-direct-reqwest-until-sdk-server-drift-is-resolved.md)** — Bypass Vectorizer SDK for `insert` + `get_vector`, use direct `reqwest` until the SDK / server drift is resolved.
- **[ADR-002](.rulebook/decisions/002-classifier-worker-lives-in-a-separate-crate-to-avoid-the-classifier-embedder-classifier-cycle.md)** — Classifier worker lives in a separate crate to avoid the classifier ↔ embedder cycle.

### Fixed
- **`cortex-fulltext`** — route `agent_call` to turns instead of dumping in docs; route artifacts by path + topics; `primaryKey=id` on upsert; spec-05 summary contract.
- **`cortex-graph`** — surface silently-dropped edges via `assert_write_landed`; stamp `label/display/caption/name` on every node.
- **`cortex-classifier`** — parse `result` field from Claude Code CLI 2.x; drop `--max-tokens` for CLI 2.x.
- **`cortex-plugin`** — make hook capture work on Windows; `hooks.json` location inside `hooks/`; marketplace source at plugin root; document directory-source cache pitfall + ship `sync-cache.sh`.
- **`cortex-adapter-claude-code`** — wire pre-thinking pipeline with camelCase hook contract; emit canonical spec-04 envelopes.
- **`cortex-ingestion`** — accept spec-04 `{events:[...]}` batch shape.
- **`cortex-mcp-server`** — rename tools to identifier-safe names + camelCase schema fields (spec-18 compliance).
- **GUI** — drawer covers full width on narrow viewports + lock horizontal scroll.
- **`cortex-api` health routes (phase10g)** — `/v1/health`, `/v1/health/freshness`, `/v1/health/divergence`, `/v1/health/versions`, `/v1/health/config` mount on the dashboard-aware router; the audit caught the GUI's Health tab returning empty bodies because the running daemon predated the registration. Operators rolling forward from a pre-phase10g cortex-api MUST relaunch the binary; the `/v1/health/*` routes do not back-port to a process that is already serving requests.
  - Regression guard: [`every_v1_health_route_is_mounted_on_router_with_dashboard`](crates/cortex-api/tests/health_freshness.rs) + `cortex-ops doctor` probes the same URLs against `CORTEX_API_URL` so a missed registration in a future refactor surfaces in CI before it reaches a deploy.

### Operations (phase11p)
- **Phase11p corpus cleanup tooling** ([phase11p §1-§4](.rulebook/tasks/phase11p_corpus_cleanup_sweep/) + [docs/cortex/2026-05-03-corpus-sweep-candidates.md](docs/cortex/2026-05-03-corpus-sweep-candidates.md)) — addresses the 2026-05-03 Meili audit (292,444 docs across 190 indexes; 91 empty + 71 % duplicate `law.imported` rows). New `cortex-workers::fulltext::sweep::sweep_empty_canonical` lib helper enumerates canonical-but-zero-doc indexes (sibling to the existing non-canonical reaper). New `cortex-ops sweep-empty [--apply]` CLI exercises both buckets — dry-run by default, gated `--apply` for the destructive call. New `cortex-ops dedupe-laws [--apply]` walks every per-repo `cortex-{slug}-governance` index plus the global `cortex_laws`, groups documents by `(law_id, content_hash)`, keeps the oldest by `ts`, and `delete-batch`es the rest. Live dry-run on 2026-05-03 reports 91 empty-canonical indexes ready to drop and 2,696 / 3,804 (71 %) duplicate law documents ready to dedupe.
- **TML repo bootstrap excludes audit** ([docs/cortex/tml-bootstrap-excludes-audit.md](docs/cortex/tml-bootstrap-excludes-audit.md)) — TML's `../Tml/src/{llvm-project,gcc,tracy,vcpkg}/` vendored toolchain trees account for ~322k of the 327,309 files the bootstrap walker surfaces. Recommended `cortex.toml` body queued for upstream PR in the `hivellm/tml` repo. Expected impact: `cortex-tml-code` 189,872 → ~5,000-10,000 (~98 % reduction on TML alone, ~65 % on the full Cortex corpus).
- **Phase11k redeploy runbook** ([docs/cortex/redeploy-after-phase11k.md](docs/cortex/redeploy-after-phase11k.md)) — sequence to roll the post-phase11k `cortex-fulltext-worker` binary so the dual-write contract (`cortex_decisions` / `cortex_laws`) and settings v5 projection activate on the live cluster.
- **Phase11q consolidation estimator** ([phase11q §1](.rulebook/tasks/phase11q_corpus_consolidation_run/) + [docs/cortex/2026-05-03-consolidation-estimate.md](docs/cortex/2026-05-03-consolidation-estimate.md)) — new `cortex-consolidator` CLI binary at `crates/cortex-consolidator/src/bin/cortex-consolidator.rs` shipping the read-only `estimate` subcommand. Walks every per-repo `cortex-{slug}-turns` Meili index, projects USD cost across the three planned consolidator passes (session-grain Shallow Haiku 4.5, topic-grain Shallow Haiku 4.5, decision-trace Deep Opus 4.7) using the lib's existing pricing constants (`HAIKU45_*` / `OPUS47_*` micro-cents per 1k tokens). Live run on 2026-05-03 reports 31,485 envelopes / 81 MB body bytes / **$12.20 USD total projection** (Opus DecisionTrace dominates at $12.18). The actual passes are gated behind a follow-up `phase11r_corpus_consolidation_apply` task that requires both operator USD approval AND a discovery layer that hydrates `SessionInput` / `TopicCluster` / `DecisionTraceInput` from the live corpus (the lib `Orchestrator::run_*` paths take pre-built inputs).

### Pending (🟡 specs in progress)
- **Spec 13** — Laws DSL + detector contract.
- **Spec 14** — Governance engine (enforcement, punishment, trust score).
- **Spec 15** — Deep Analysis workflow.
- **Spec 16** — Dashboard polish (views shipped, refinement ongoing).
- **Spec 17** — Cursor / Codex / Gemini adapters.

---

[Unreleased]: https://github.com/hivellm/cortex/compare/HEAD
