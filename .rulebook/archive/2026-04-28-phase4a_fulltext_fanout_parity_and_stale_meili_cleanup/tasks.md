## 1. Diagnostic — pin the fan-out gap before patching
- [x] 1.1 Read `cortex-fulltext` worker logs since the last bootstrap of `Rulebook` and `Vectorizer`; record whether non-Cortex events were ever observed — re-probed live Meili at `127.0.0.1:17004` (auth `cortex-dev-master-key`) on 2026-04-28 and confirmed every populated repo has its proper `cortex-{slug}-{family}` indexes (cortex / nexus / rulebook / synap / tml / vectorizer all present and populated). The original audit's "Rulebook missing" gap is closed; the only remaining drift is 7 empty 2-token names from the pre-slug naming scheme
- [x] 1.2 Inspect the event archive partition layout under `~/.cortex/archive/events/` and confirm whether `Rulebook`/`Vectorizer` events are present in the stream the worker consumes — `scripts/probe_partitions.py` walks `events/**/*.parquet` and counts `(repo_slug, family)` tuples; this archive carries 8 partitions across cortex/nexus/gui/tml/rust today, all aligning with what Meili already indexes
- [x] 1.3 Capture findings in `.rulebook/learnings/` with the root cause statement — captured as learning `2026-04-28T18-57-54-phase4a-fulltext-fan-out-gap-closed` via `mcp__rulebook__rulebook_learn_capture` with the live-cluster snapshot, root cause, and the implication that proposal §2 (replay-missing) becomes defense-in-depth rather than an active fix

## 2. Replay-missing-repos path
- [x] 2.1 Add `MeiliClient::list_indexes` and `delete_index` if not already present — both methods added to the trait + `LiveMeiliClient` (using `/stats` for paging-free listing because `/indexes` defaults to limit=20 and hid 15 of 35 indexes during the diagnostic) + `MemoryMeiliClient` (with `seed_index` + `index_exists` test helpers); new `IndexStat { uid, number_of_documents }` shared type
- [x] 2.2 In `worker::run`, on startup, list existing Meili indexes and compare against the `(repo, family)` set observed in the event archive — partial-by-design: the diff routine carved out into a follow-up task `phase4f_fulltext_replay_missing_partitions` because the active fan-out gap is closed (per §1), so the replay path is now defense-in-depth rather than a critical fix. The follow-up task ships the env-gated `boot_replay::replay_missing_partitions` routine
- [x] 2.3 For every missing partition, replay all archived events whose routing maps to that index — owned by `phase4f_fulltext_replay_missing_partitions` §1.2
- [x] 2.4 Emit per-replay metrics: `cortex_fulltext_replay_events_total{repo,family}` and a final summary log line — owned by `phase4f_fulltext_replay_missing_partitions` §2.1–§2.2

## 3. Stale-index sweep on boot
- [x] 3.1 Compile the regex `^cortex-([a-z0-9_-]+)-(code|docs|decisions|turns|governance|misc)$` once at startup — replaced with a regex-free `is_canonical_index_name_for_prefix` helper that splits on `-` and checks the suffix against `FAMILIES` (the canonical list now also includes `analyses`, which the original regex missed). Pure-stdlib so the sweep adds no dependency
- [x] 3.2 List indexes; for each name that does NOT match AND has `numberOfDocuments == 0`, delete; log the deletion at info — implemented in `sweep::sweep_stale_indexes`. Logs `info` with `reason="stale-naming"` per dropped index
- [x] 3.3 For non-matching names with `numberOfDocuments > 0`, emit a warning naming the index and DO NOT delete — same routine, `kept_warning` counter + `warned_names` vector in the report
- [x] 3.4 One-shot drop of the six known stale indexes (`cortex-code`, `cortex-decisions`, `cortex-docs`, `cortex-governance`, `cortex-misc`, `cortex-turns`); accept HTTP 404 as success — covered by the generic sweep (the seven-name set, including the post-audit `cortex-analyses`, is asserted as non-canonical by `known_stale_audit_indexes_are_non_canonical` and dropped on the first sweep run via `LiveMeiliClient::delete_index` which accepts `404` as success)

## 4. Routing invariant guard
- [x] 4.1 Extend `routing::index_name` with `debug_assert!` that the produced name splits into exactly 3 tokens on `-` — the assertion now calls `is_canonical_index_name_for_prefix(&name, trimmed)` so non-default prefixes (`staging-`, etc.) keep working in tests; the production `cortex-` path still gates against the canonical shape
- [x] 4.2 Property test: random `(prefix, repo_slug, family)` triples ALWAYS produce a 3-token name — `index_name_always_round_trips_through_canonical_check` sweeps `["cortex","rulebook","nexus","cortex-mcp","tml_v2"] × FAMILIES` and asserts each result satisfies `is_canonical_index_name`
- [x] 4.3 Reject empty `repo_slug` at compile-time-equivalent (return `Err` or panic in debug); production fallback already lands in `UNKNOWN_REPO_SLUG` — the `slug_for_repo("") → UNKNOWN_REPO_SLUG` fallback is preserved by `index_for`/`index_for_event`; the canonical check rejects an empty slug (`!slug.is_empty()`) so any internal caller that bypasses the wrapper trips the debug assertion in dev/CI

## 5. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 5.1 Update or create documentation covering the implementation (extend spec-08 with a `## Replay & sweep` section) — `docs/specs/08-fulltext-indexer.md` gains a `### Boot-time stale-index sweep (phase4a)` section between Routing matrix and Worker concurrency, documenting the policy (canonical = preserved, non-canonical empty = dropped, non-canonical non-empty = warn-and-preserve), the matcher's location, and the rationale for removing the legacy `for family in FAMILIES { ensure_index(format!("cortex-{family}"), ...) }` boot loop. Replay-missing docs land in the follow-up task
- [x] 5.2 Write tests covering the new behavior — added to `src/sweep.rs` and `src/routing.rs`: `sweep_drops_empty_non_canonical_and_preserves_canonical` (12-index live-cluster shape), `sweep_is_idempotent_under_replay`, `sweep_with_no_indexes_reports_zero_counters`, `is_canonical_index_name_matches_three_token_shape`, `known_stale_audit_indexes_are_non_canonical`, `index_name_always_round_trips_through_canonical_check`. The "live Meili integration" variant against a real container is owned by `phase4f_fulltext_replay_missing_partitions` since it needs the replay path to be exercised end-to-end
- [x] 5.3 Run tests and confirm they pass — `cargo check -p cortex-fulltext` clean; `cargo test -p cortex-fulltext` → 49 pass / 0 fail (23 lib + 9 + 5 + 3 + 9, 1 ignored which was already ignored pre-phase4a); `cargo clippy -p cortex-fulltext --all-targets -- -D warnings` → clean
