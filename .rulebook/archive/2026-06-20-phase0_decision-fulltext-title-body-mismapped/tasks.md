## 1. Confirm builder + find the malformed-batch source
- [x] 1.1 DONE: builder is correct — `build_doc` sets `decision_title = Some(d.title)` and `title` via `derive_title` (pinned by `decision_event_stamps_decision_id_title_status_supersedes` + the new `decision_doc_title_differs_from_id_and_body_is_not_json_object`).
- [x] 1.2 DONE: TWO root causes found. (a) **Deeper bug:** `bootstrap_doc_id` emitted `bootstrap:<repo>:<path>:<hash>` — INVALID as a Meili primary key (Meili allows only `[a-zA-Z0-9-_]`; `:` `/` `.` rejected). Live Meili task log (uids 166273/166345/288603) shows `documentAdditionOrUpdate failed … Document identifier … is invalid` — so EVERY content-addressable doc (Decision/LawViolation/Knowledge/Learning) silently failed to index under its stable key; only old random-ULID (`live_doc_id`) docs persisted. (b) The `01KQNYF4J*` `title==id` batch + the 51-docs-for-27-decisions duplication are the residue of that ULID-keyed pre-Phase22 path. Fixed `bootstrap_doc_id` to emit `bootstrap-<sha256hex(repo|path|content_hash)>` (Meili-safe, deterministic, collision-resistant) in `document.rs`.

## 2. Reindex + verify the mix is gone
- [x] 2.1 DONE: new `cortex-ops decisions-reindex` (`crates/cortex-cli/src/bin/cortex-ops/decisions_reindex.rs`) re-emits all `.rulebook/decisions/*.md` through the builder (stable `bootstrap-` keys) and prunes every legacy non-`bootstrap-`-keyed doc (stale dupes + `title==id` orphans), guarded so the prune is bypassed when zero files re-emitted. `--dry-run` + `--json`. Command: `CORTEX_FULLTEXT_MEILI_URL=… CORTEX_FULLTEXT_MEILI_API_KEY=… cortex-ops decisions-reindex --decisions-dir .rulebook/decisions`.
- [x] 2.2 DONE (live): index collapsed 51→**27** docs, all `bootstrap-`-keyed, **0** `title==id`, **0** missing `decision_title`; `cortex-ops doctor-decisions` → "ok no malformed docs" exit 0; title search `q="reranker"` matches decision 25 by title. Fulltext-worker rebuilt + redeployed so live ingestion uses the fixed key.

## 3. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 3.1 Update or create documentation covering the implementation. DONE: spec 08 §Identity rewritten — `bootstrap-<sha256hex>` Meili-safe key, content-addressable-on-live-path note, and the invalid-key root-cause + repair tooling; CHANGELOG `Fixed` entry added.
- [x] 3.2 Write tests covering the new behavior. DONE: builder contract test (`decision_doc_title_differs_from_id_and_body_is_not_json_object`); reindex tests (`build_decision_doc_produces_stable_id_for_same_file_content`, `build_decision_doc_title_differs_from_id`, `repo_rel_path_*`); `bootstrap_doc_id_uses_repo_path_hash` updated to assert the Meili-safe hashed key.
- [x] 3.3 Run tests and confirm they pass. DONE: `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo test --workspace --test-threads=1` all green (194 suites, 0 failures).
