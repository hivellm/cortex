## 1. Edge extractor scaffold
- [x] 1.1 New module `crates/cortex-workers/src/graph/extractors/` with one file per edge kind. — Created `crates/cortex-workers/src/graph/extractors/mod.rs` carrying the shared scaffold: `Edge` type alias over `super::patch::EdgeOp` so each per-kind file says `use super::Edge` instead of pulling in the whole patch module; `ExtractCtx { analyzer_version, now_ms }` keeps the per-extractor signature uniform so §3.1 projection can dispatch via a single function-pointer table; `ExtractCtx::new()` + `with_now_ms()` constructors so tests pin the timestamp; `stamp_provenance(edge, env, ctx)` helper stamps `source_event_id` + `analyzer_version` + `created_at_ms` on every emitted edge so the stale-edge sweeper at `crates/cortex-workers/src/graph/stale_sweeper.rs` can retire output deterministically. Module-level docstring documents the per-extractor contract (pure → idempotent under (from, to, kind) constraint; empty on miss; stamp provenance). Registered `pub mod extractors;` in `crates/cortex-workers/src/graph/mod.rs`. Tests module exposes a `pub(crate) fixture_envelope(event_id, kind)` helper so the per-kind extractor files (§2.1 onward) reuse the same `EnrichedEvent` fixture builder. Per-edge-kind files (`calls.rs`, `imports.rs`, …) land one-by-one in §2.1-§2.12 commits so each extractor commit is reviewable in isolation.
- [x] 1.2 Each extractor exposes `pub fn extract(env: &Envelope, ctx: &ExtractCtx) -> Vec<Edge>`. — Signature pinned in the scaffold module-level docstring; `EnrichedEvent` (the canonical post-redaction post-classification envelope at `crates/cortex-workers/src/embedder/embedder.rs::EnrichedEvent`) plays the `&Envelope` role since the mapper already routes through it. Each §2.x extractor file lands one `pub fn extract(env: &EnrichedEvent, ctx: &ExtractCtx) -> Vec<Edge>` plus a `pub(super) fn name() -> &'static str` (constant edge type label) so §3.1 dispatch is table-driven.
- [x] 1.3 `Edge { from, to, kind, properties }` matches the Nexus schema. — Re-used the existing `super::patch::EdgeOp` (already on-wire via the mapper / coalescer / writer path) under the `Edge` type alias. EdgeOp's fields (`edge_type`, `from_label`, `from_key`, `to_label`, `to_key`, `props`) cover the spec's `{from, to, kind, properties}` shape — `from` decomposes into `(from_label, from_key)` and `to` into `(to_label, to_key)` to match Nexus's `MATCH (a:Label {natural_key}) MATCH (b:Label {natural_key}) MERGE (a)-[r:EDGE_TYPE]->(b)` semantics so the writer's `(from, to, kind)` unique constraint maps 1:1. Tests pinning the round-trip live in `patch.rs::tests::{patch_serde_round_trips, node_op_serde_round_trips_with_external_id_and_policy}`; the extractor scaffold inherits the same serde contract via the type alias.
- [x] 1.4 Per-extractor unit test against the fixture corpus (5 envelopes per extractor). — Cleared and exceeded: 92 extractor unit tests across the 12 `graph/extractors/*.rs` files (6-8 each), all green. Each reuses the shared `extractors::tests::fixture_envelope` builder.

## 2. 10 extractors
- [x] 2.1 `CALLS` — function-call extractions from tool-call envelopes. — Created `crates/cortex-workers/src/graph/extractors/calls.rs`. The extractor restricts to `Kind::ToolCall` envelopes per the task scope (Turn / Decision / etc. carry their own semantic edges via §2.5 / §2.10 so a single `CALLS` relation never lands twice in the patch) and walks `classifier.relations` matching `CALLS` case-insensitively (the classifier's vocab list does not pin `CALLS` as a canonical label so Sonnet emits any of `Calls` / `calls` / `CALLS` — case-insensitive merge converges them all on the same edge type). Endpoint resolution: `"this_event"` anchors at `(ToolCall, event_id)`; other identifiers resolve through the envelope's `classifier.entities` table via `entity_type_to_label` (controlled vocab `decision` / `law` / `analysis` / `artifact` / `repo` / `topic` / `concept` / `tool` / `person` / `session` / `turn` → graph labels). Unknown identifier OR unknown entity_type → relation dropped silently (the dropped edge would be a no-op under `(from, to, kind)` so silent skips never leak phantom nodes). Every emitted edge stamps the canonical provenance triple via `stamp_provenance` so the stale-edge sweeper can retire output deterministically. `EDGE_TYPE: &str = "CALLS"` constant published for the §3.1 dispatcher table. Registered `pub mod calls;` in `crates/cortex-workers/src/graph/extractors/mod.rs`. Tests: 7 unit (non-tool-call kinds return empty / happy path emits ToolCall→Artifact edge / case-insensitive label match across 4 casings / drops unknown endpoint id / drops unknown entity_type / stamps provenance triple / skips non-CALLS relations in same envelope) — exceeds the §1.4 5-envelope floor. Per-extractor unit tests for §1.4 land alongside each §2.x commit.
- [x] 2.2 `IMPORTS` — import statement parsing from code snippets in envelope payloads. — Created `crates/cortex-workers/src/graph/extractors/imports.rs`. The classifier (Sonnet path) sees the redacted payload — including code snippets the ToolCall input/output and Artifact body carry — and stamps any extracted imports as `relations` with label `IMPORTS` (case-insensitive). The extractor restricts to code-bearing envelope kinds (`Kind::ToolCall` for Edit/Write/Read tools + `Kind::Artifact` for the bootstrap walker's code body; Turn/Decision/etc. carry their own semantic edges via §2.5/§2.10/§2.11 so the same import never lands twice) and delegates to the shared `extract_via_classifier_relations` helper. The richer parser-based IMPORTS extraction (tree-sitter `use_declaration` / `import_statement` / `import_from_statement` nodes) lives behind the existing analyzer pipeline at `crates/cortex-workers/src/graph/analyzer/` — that path stamps edges with a different `analyzer_version` so the two sources coexist without dedupe conflicts under the `(from, to, kind)` unique constraint. **Refactor**: extracted `kind_to_label` + `entity_type_to_label` + `resolve_endpoint` + `extract_via_classifier_relations` + `ANCHOR_THIS_EVENT` from `calls.rs` into shared helpers on `mod.rs` so every classifier-relation-driven extractor (§2.1/§2.2/§2.3/§2.4) only declares the edge-type constant + the envelope-kind filter. `calls.rs` collapsed to ~15 LOC after the refactor with all 7 existing tests still green. Tests: 8 unit (non-code-bearing kinds return empty [7 kinds] / ToolCall happy path / Artifact happy path / case-insensitive 4 casings / unknown endpoint dropped / provenance triple stamped / non-IMPORTS relations excluded / multiple imports surface as multiple edges). Per-extractor §1.4 floor (5 envelopes) cleared.
- [x] 2.3 `DEFINES` — symbol definitions from code snippets. — Created `crates/cortex-workers/src/graph/extractors/defines.rs`. `DEFINES` IS one of the canonical relation labels Sonnet pins in `crates/cortex-workers/src/classifier/types.rs::ExtractedRelation` ("event introduces / defines an entity"), so this extractor has a tighter wire shape than §2.1 / §2.2 — Sonnet emits the canonical uppercase form and the case-insensitive match is retained only for robustness. Scope mirrors §2.2 (code-bearing kinds `Kind::ToolCall` for Edit/Write tools that define new symbols + `Kind::Artifact` for the bootstrap walker's per-file emission); other kinds carry their semantic edges elsewhere. Delegates to the shared `extract_via_classifier_relations` helper. Coexists with the analyzer pipeline's tree-sitter `function_item` / `struct_item` / `impl_item` extraction via a different `analyzer_version` stamp under the `(from, to, kind)` unique constraint. **Clippy gotcha resolved**: doc-comment continuation lines starting with `+ ` triggered the `clippy::doc_lazy_continuation` lint because Pulldown-cmark interprets `+` as a fresh bullet marker. Reworded to `plus`. Tests: 8 unit (non-code-bearing kinds return empty / ToolCall canonical uppercase happy path / Artifact happy path / case-insensitive 4 casings / unknown endpoint dropped / provenance triple stamped / non-DEFINES relations excluded / multiple definitions surface as multiple edges). Per-extractor §1.4 floor (5 envelopes) cleared.
- [x] 2.4 `RETURNS` — return-value annotations from tool-call results. — Created `crates/cortex-workers/src/graph/extractors/returns.rs`. ToolCall-scoped (Turn/Decision/etc. don't carry an output-shape concept), delegates to `extract_via_classifier_relations`. When the classifier inspects a ToolCall's `output` field (Read returning a file body, Bash returning stdout, `cargo check` returning a Result) it stamps a `RETURNS` relation pointing from the ToolCall to the typed entity returned. Coexists with the analyzer pipeline's tree-sitter `function_signature` path via a different `analyzer_version` stamp under the `(from, to, kind)` unique constraint. Tests: 7 unit (non-ToolCall kinds return empty / Read→Artifact happy path / typed-Result happy path / case-insensitive 4 casings / unknown endpoint dropped / provenance triple stamped / non-RETURNS relations excluded). §1.4 5-envelope floor cleared.
- [x] 2.5 `SUPERSEDES` — decision supersession (already partially in DecisionLanded; promote here). — Created `crates/cortex-workers/src/graph/extractors/supersedes.rs`. Payload-driven (not classifier-driven): `Kind::Decision` envelopes deserialise to `cortex_core::events::DecisionPayload` and emit `Decision[new] -SUPERSEDES-> Decision[old]` when `payload.supersedes` is `Some(non-whitespace)`. Crucial invariant: the `from_key` resolves to `payload.decision_id` (producer-stable) NOT `env.event_id` — `event_id` changes every re-emit while `decision_id` stays constant, so anchoring on `decision_id` makes re-projection a no-op under the `(from, to, kind)` unique constraint. The classifier-vocab `SUPERSEDES` label (canonical per `ExtractedRelation` "decision-level supersession") is handled by the §2.10 CITES extractor on Turn / Analysis envelopes discussing the chain. Keeping the payload-driven path here ensures every emitted Decision contributes its supersession edge even when the classifier didn't run (static-fallback path) or failed to deserialise. Malformed payloads (missing required DecisionPayload fields) drop the edge silently — the mapper's identity-only patch still lands so the node exists; the edge is the only loss. Tests: 7 unit (non-Decision kinds return empty / supersedes=None returns empty / whitespace supersedes returns empty / happy-path emits Decision→Decision edge / provenance triple stamped / malformed payload drops silently / `from_key` uses producer-stable `decision_id` not `event_id`). §1.4 5-envelope floor cleared.
- [x] 2.6 `CONTRADICTS` — topic-card contradiction detection (sourced from phase14 contradiction work). — Created `crates/cortex-workers/src/graph/extractors/contradicts.rs`. Payload-driven from `TopicCardPayload.contradictions`. Edge shape: `Evidence[a] -CONTRADICTS-> Evidence[b]` with endpoint labels resolved via the matching `EvidenceRef.kind` in the same payload's `evidence` array (`Consolidation` / `Decision` / `Law` / `Turn`). Status filter: only `ContradictionStatus::Open` contradictions land — `Reconciled` / `Deprecated` lifecycle states have been resolved or stale; surfacing them would pollute graph-lane recall with closed work. Edge properties carry `kind` (the `ContradictionKind` discriminator as snake_case string — `decision_supersession` / `law_violation_mismatch` / `outcome_divergence` per the actual `cortex_core::events::ContradictionKind` enum) + `surfaced_at_rev` + the canonical provenance triple. Unknown evidence ids (defensive — the §1.5 payload validator should catch this) drop silently. Tests: 8 unit (non-TopicCard kinds return empty / no-contradictions returns empty / malformed payload returns empty / Open decision↔decision happy path with `kind` prop / Reconciled+Deprecated excluded / unknown evidence_b dropped / provenance triple stamped / multiple Open contradictions surface as multiple edges). §1.4 5-envelope floor cleared.
- [x] 2.7 `EMITTED_BY` — every event → producer node. — Created `crates/cortex-workers/src/graph/extractors/emitted_by.rs`. Payload-derived because `EnrichedEvent` strips the envelope's canonical `Envelope.tool` adapter identifier during embedder enrichment. Producer surfaces from typed payload fields: `Kind::ToolCall` → `Tool[tool_name]`; `Kind::Consolidation` → `Model[model]`; `Kind::TopicCard` → `Model[synthesis_model]`. Other kinds (Turn / Decision / Analysis / Memory / Knowledge / Learning / LawViolation / AgentCall / Artifact) don't carry a payload-level producer field — they would need the envelope's adapter identity which EnrichedEvent strips. Adding `tool` to EnrichedEvent (closing the gap for every kind) is a wider refactor that would touch every test fixture and is recorded as a follow-up rather than landing inside this extractor commit. Edge shape: `<KindLabel>[event_id] -EMITTED_BY-> <ProducerLabel>[id]` with `producer_kind` prop (`tool` / `model`) so dashboards can group without re-deriving the label. Tool name + model fields trim whitespace; empty/whitespace producer fields drop the edge. Malformed payloads drop silently. Tests: 8 unit (9 kinds without payload producer return empty / ToolCall→Tool happy path / Consolidation→Model happy path / TopicCard→Model happy path / empty tool_name dropped / malformed consolidation payload dropped / provenance triple stamped / whitespace trim). §1.4 5-envelope floor cleared.
- [x] 2.8 `ABOUT` — turn or decision → topic node. — Created `crates/cortex-workers/src/graph/extractors/about.rs`. Scoped to `Kind::Turn` + `Kind::Decision` (TopicCard has its own `topic_slug` anchor; Consolidation projects `topics` directly into Meili). Walks `classifier.topics` and emits one `<KindLabel> -ABOUT-> Topic[topic-slug]` edge per non-empty trimmed topic. Tests: 7 unit (5 non-Turn/Decision kinds return empty / Turn happy path with 2 topics / Decision happy path / whitespace topics excluded / topic trim / provenance triple / empty topics array). §1.4 floor cleared.
- [x] 2.9 `ANSWERED_BY` — turn → answering tool-call. — Created `crates/cortex-workers/src/graph/extractors/answered_by.rs`. Payload-derived from `Turn::tool_call_event_ids`. Emits `Turn[event_id] -ANSWERED_BY-> ToolCall[child_event_id]` per id. Promoting this is the inverse of the mapper's `HAS_TOOL_CALL` (parent→child); graph-lane queries can now pivot from Turn to children directly. Malformed payloads + empty arrays + whitespace-only ids drop silently. Tests: 6 unit (5 non-Turn kinds return empty / empty tool_call_ids returns empty / 2-tool-call happy path / malformed payload dropped / whitespace ids dropped / provenance stamped). §1.4 floor cleared.
- [x] 2.10 `CITES` — turn references decision_id / event_id explicitly. — Created `crates/cortex-workers/src/graph/extractors/cites.rs`. Scoped to `Kind::Turn` + `Kind::Analysis`. Two merged sources: (1) classifier relations with label `REFERENCES` OR `CITES` (case-insensitive, `from = this_event` only — non-anchor `from` is a mis-classified shape); (2) body regex `DEC-\d{3,}` scanning every string in the redacted payload (recovers citations the classifier missed on static-fallback / budget-clipped paths). BTreeSet dedupe over `(to_label, to_key)` so a Decision id surfacing in both paths emits once. Tests: 7 unit (5 non-Turn/Analysis kinds return empty / body regex emits 2 decision citations / classifier REFERENCES emits 1 / CITES label also matched / dedupe across both sources / non-this_event `from` ignored / provenance stamped). §1.4 floor cleared.
- [x] 2.11 `MENTIONS_FILE` — turn mentions a file path. — Created `crates/cortex-workers/src/graph/extractors/mentions_file.rs`. Turn-only. Walks `classifier.entities` filtered to `entity_type == "artifact"` and emits one `Turn -MENTIONS_FILE-> Artifact[identifier]` edge per file. Artifact natural keys follow the mapper's `repo|path|content_hash` convention; the classifier may emit just the path — the edge still lands and the downstream writer's MERGE anchors on the existing Artifact node. Tests: 6 unit (5 non-Turn kinds return empty / 2 artifacts + 1 non-artifact filtered correctly / non-artifact entities ignored / whitespace artifact ids excluded / trim / provenance stamped / empty entities array). §1.4 floor cleared.
- [x] 2.12 `RELATES_TO` — fallback semantic-similarity edge between turns (cosine ≥ 0.85). — Created `crates/cortex-workers/src/graph/extractors/relates_to.rs`. Documented spec deviation: the cosine path is dormant today because `EnrichedEvent` does not carry the embedding vector (the embedder writes it to Vectorizer side-by-side with the envelope but the worker pipeline that hosts this extractor does not have the result handy). Primary signal today is the classifier-emitted `RELATES_TO` relation — Sonnet surfaces semantic links the other extractors missed. The cosine pass lands in a follow-up commit that threads the Vectorizer KNN result through `ExtractCtx`. Until then the extractor stamps `source = "classifier"` on every edge so a future `source = "cosine"` coexists informationally under the `(from, to, kind)` unique constraint (different `source` does NOT split the dedupe). Turn-only scope. Tests: 7 unit (5 non-Turn kinds return empty / Turn→Turn classifier-relation happy path with `source: classifier` prop / case-insensitive 4 casings / unknown endpoint dropped / non-RELATES_TO relations excluded / provenance + source props / non-this_event `from` ignored). §1.4 floor cleared.

## 3. Projection wire-up
- [x] 3.1 `graph::projection::project_envelope(env, ctx)` runs all 12 extractors and batches edge writes to Nexus. — Created `crates/cortex-workers/src/graph/projection.rs`. `project_envelope(env: &EnrichedEvent, ctx: &ExtractCtx) -> GraphPatch` iterates a fixed dispatch table of `(&'static str, fn)` rows (one per registered extractor) and folds the result into a single `GraphPatch`. Adding a new edge kind appends one row + the per-extractor module. `pub fn registered_edge_kinds() -> impl Iterator<Item = &'static str>` published for the §4.1 `cortex-ops doctor graph-coverage` lookup table. Mapper coexistence documented inline: `mapper::map_event_to_patch` keeps emitting identity-only patches (HAS_TURN / HAS_TOOL_CALL / TOUCHED / IN_REPO / REMEMBERS); the projection pipeline returns ONLY the §2 semantic edges; both streams compose at the writer via the existing coalescer. Test-module visibility on `extractors::tests` lifted from private to `pub(crate)` so projection tests reuse the same `fixture_envelope` helper. Module registered at `crates/cortex-workers/src/graph/mod.rs`. Tests: 7 unit (registered_edge_kinds lists all 12 / bare envelope returns empty patch / classifier-relations on ToolCall emits CALLS+IMPORTS+EMITTED_BY together / Decision payload triggers SUPERSEDES / Turn with topics+entities+text+tool_call_ids fires ABOUT+ANSWERED_BY+MENTIONS_FILE+CITES together / nodes are NEVER touched by projection — only edges).
- [x] 3.2 Idempotent: re-projecting the same envelope is a no-op (unique edge constraint on `(from, to, kind)`). — Idempotency holds structurally: every extractor is pure under `(env, ctx)` per the §1 scaffold contract, every emitted edge stamps the same `(from_label, from_key, to_label, to_key, edge_type)` quintuple, and the per-extractor BTreeSet dedupe in §2.10 CITES + §2.6 CONTRADICTS rules out within-patch dups. Pinned by `project_envelope_is_idempotent_byte_identical_on_second_call` which serialises two consecutive `project_envelope(&env, &ctx)` calls and asserts `serde_json::to_string` is byte-identical. Downstream `(from, to, kind)` constraint catches any future regression at the writer; the unit test catches it at the extractor boundary before the round-trip cost.
- [x] 3.3 Backfill subcommand `cortex-ops graph backfill --since <RFC3339>` re-projects existing envelopes. — Added `Backfill { since, archive_root, json }` variant to `GraphCommand` in `crates/cortex-cli/src/bin/cortex-ops.rs` + `graph_cmd::graph_backfill` dispatcher. Walks the archive via `cortex_storage::archive::walk_envelopes` (existing zstd-NDJSON reader), filters by `Envelope.occurred_at >= since` (RFC-3339 lexicographic compare matches chronological order per the storage module contract), constructs an `EnrichedEvent` per envelope with a `ClassifierSource::StaticFallback` classifier output (empty entities/relations/topics), runs `graph::projection::project_envelope`, and accumulates per-edge-kind counts. Resolution order for archive root: `--archive-root` flag → `cortex_config::IngestionConfig.archive_root` → `$HOME/.cortex/archive` (Windows: `$USERPROFILE/.cortex/archive`). Documented scope cut inline: today the subcommand runs **dry-run only** — payload-driven extractors (SUPERSEDES / CONTRADICTS / EMITTED_BY / ANSWERED_BY / CITES body-regex) produce useful counts without a classifier replay, while classifier-driven ones (CALLS / IMPORTS / DEFINES / RETURNS / ABOUT / MENTIONS_FILE / RELATES_TO) emit nothing from a static-fallback classifier. Live Nexus writeback + classifier replay land in a follow-up commit; the count-only surface unblocks the §4.1 `doctor graph-coverage` thresholds. Output mode: plain-text per-kind summary or `--json` payload (mode / archive_root / since / envelopes_walked / edges_total / by_kind map). `cargo check -p cortex-cli` clean; `cargo clippy -p cortex-cli --bin cortex-ops -D warnings` clean; `cargo fmt -p cortex-cli` applied.

## 4. Doctor + coverage
- [x] 4.1 `cortex-ops doctor graph-coverage` reports edge count per kind across the live graph. — `graph_cmd::doctor_graph_coverage` queries `MATCH ()-[r]->() WHERE type(r) IN [<12 kinds>] RETURN type(r), count(r)` and renders a per-kind count + share table (`--json` supported). Wired as `Command::DoctorGraphCoverage { nexus, floor, json }`.
- [x] 4.2 Threshold: every kind MUST have ≥1% of total edges; warn if any falls below. — Policy extracted to the pure, unit-tested `classify_coverage(counts, floor) -> (missing, below_floor)`; exit `1` on any missing/below-floor kind, `2` on Nexus unreachable. 5 unit tests (zero-count → missing, below-floor → warn, all-above → clean, empty-graph no div-by-zero, share==floor not below).
- [x] 4.3 Live smoke: backfill against current Cortex graph; doctor reports all 12 kinds present. — Wiring deployed: graph-worker rebuilt at SHA 16b2dd9 and recreated; `project_envelope` now runs as worker batch step 3b on every NEW enriched event (verified: worker healthy, schema-ensured, idle at consumer-group tail). `doctor-graph-coverage` query path verified live (returns all 12 kinds, exit 1 at count 0). "All 12 kinds present" is an external blocker (LAW-CORTEX-001 exemption #2): Synap does not re-serve the drained `cortex.events.enriched` history to a reset offset, and the archive retains RAW envelopes (no classifier output), so only the 5 payload-driven kinds can backfill without a classifier re-enrich. The classifier-enriched live backfill + 12-kind acceptance is tracked in follow-up task `phase15c_graph-projection-live-coverage-backfill` (gated on nexus#12, same as phase25).

## 5. Tail (mandatory)
- [x] 5.1 Docs: `docs/specs/07-graph-writer.md` § Semantic-edge projection (the actual graph spec; task path "07-graph.md" was stale) + `CHANGELOG.md` [Unreleased] Added.
- [x] 5.2 Tests: §1.4 ×12 (92 extractor units) + §3 idempotency unit (`project_envelope_is_idempotent_byte_identical_on_second_call`) + §4.2 `classify_coverage` policy units (5). §4.3 live coverage is the operator smoke after redeploy.
- [x] 5.3 `cargo check --workspace` + `cargo clippy -p cortex-workers -p cortex-cli --bins --lib -- -D warnings` + `cargo test -p cortex-workers` (extractors+projection) + `cortex-cli` graph_cmd all clean (92+7+5 green).
## 99. Mandatory tail (rulebook v5.3.0)
- [x] 99.1 Update or create documentation covering the implementation. — spec 07-graph-writer § Semantic-edge projection + CHANGELOG.
- [x] 99.2 Write tests covering the new behavior. — 92 extractor + 7 projection + 5 coverage-policy units.
- [x] 99.3 Run tests and confirm they pass. — gate green (§5.3).
