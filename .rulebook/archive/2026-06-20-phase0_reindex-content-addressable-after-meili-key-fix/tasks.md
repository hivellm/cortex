## 1. Audit
- [x] 1.1 DONE (live audit 2026-06-20, local `cortex` repo indexes): `cortex_decisions` global = 27 all `bootstrap-` (clean, fixed by prior task); `cortex-cortex-decisions` per-repo = 33 all legacy, 8 `title==id`; `cortex-cortex-knowledge` = 309 all legacy, 0 `title==id`; `cortex-cortex-learnings` = 500 all legacy, 0 `title==id`; `cortex-cortex-governance` = empty; `cortex_laws` = 240 all legacy AND all `title==id` (body = stringified JSON object — fully malformed).
- [x] 1.2 DONE: source-of-truth completeness differs sharply by kind. **Decisions** are fully file-backed (`.rulebook/decisions` = 27 files = 27 decisions) → safe to re-emit+prune. **Knowledge** (`.rulebook/knowledge` = 1 file vs 309 docs) and **Learnings** (`.rulebook/learnings` = 44 files vs 500 docs) are NOT fully file-backed — most entries were added live via `rulebook_knowledge_add`/`rulebook_learn_capture` and exist only in the index/rulebook store, so re-emit-from-file + prune would DESTROY them. **Laws** (`.claude/rules` = 15 files vs 240 docs) similarly over-counted + fully malformed. Emitter fns exist for every kind (`emit_{decision,law,spec_laws,extracted_laws,knowledge,learning,analysis,memory}_imported`).

## 2. Reindex / migrate (approach is per-kind, driven by §1.2)
- [x] 2.1 DONE (decisions, both index families): added `--index` flag to `cortex-ops decisions-reindex`; ran it against the per-repo `cortex-cortex-decisions` → collapsed 33→**27**, all `bootstrap-`-keyed, 0 `title==id`, 0 missing `decision_title` (global `cortex_decisions` already clean from the prior task). Decisions are now fully repaired in every index the `decision_lookup` strategy reads.
- [x] 2.2 DONE: new `cortex-ops meili-rekey --index <idx>` (`crates/cortex-cli/src/bin/cortex-ops/meili_rekey.rs`) re-keys each legacy doc in place — recomputes `bootstrap-<sha256hex(repo|path|content_hash)>` from the doc's own `repo`/`path`/`content_hash`, upserts under the new id, deletes the old id; docs lacking the triple are left untouched. No source-driven prune → non-file-backed (live `rulebook_*`-captured) entries preserved; true duplicates (same triple) collapse. Ran live: **knowledge 309→26** (25 `bootstrap-` + 1 preserved no-triple doc; the 309 were ~13× re-bootstrap dupes of 25 entries), **learnings 500→44** (all `bootstrap-`, one per source file). Idempotent (2nd run = all already-keyed). 2 unit tests; workspace clippy clean.
- [x] 2.3 Laws are split into their own task **phase0_laws-index-routing-and-malformed-docs** (created before this item was checked) because the fix needs a routing-contract decision, not a mechanical reindex. Findings: `cortex_laws` holds 240 malformed law-DEFINITION docs (`title==id` ULID, `body` = stringified `{"body": "<.claude/rules markdown>", …}` payload). BUT routing (`index_for_event_global`) sends only `Kind::LawViolation` to `cortex_laws`; `Kind::Law` definitions route per-repo (`governance`) only. Yet `law_check` (strategies.rs) reads `cortex_laws` expecting law definitions with title+body. So there are two coupled problems: (a) the 240 docs are malformed (body stringified, title not derived — the historical emit path didn't go through the current `build_doc` `Kind::Law` arm, which IS correct); (b) law definitions don't currently route to the global index `law_check` reads. Resolve the intended contract (which index holds law definitions for `law_check`), fix the emit/routing, re-emit from `.claude/rules` + spec-extracted via the builder, then prune the malformed 240. Likely warrants its own task given the routing-design component.
- [x] 2.4 DONE: new `cortex-ops doctor-content-addressable --index <idx>` (`doctor.rs`) flags *fixable* legacy residue (non-`bootstrap-` docs WITH the `repo`+`path`+`content_hash` triple) → exit 2; reports path-less live-captured docs (legitimately ULID-keyed, e.g. `cortex_capture_memory` knowledge) separately without failing. Live: decisions(both)/knowledge/learnings → ok exit 0; `cortex_laws` → FAIL 240 (the tracked laws task).

## 3. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 3.1 Update or create documentation covering the implementation. DONE: spec 08 §Identity repair-tooling section now documents `decisions-reindex --index`, `meili-rekey`, and `doctor-content-addressable` (file-backed vs in-place vs no-triple semantics); CHANGELOG `Fixed` entry added.
- [x] 3.2 Write tests covering the new behavior. DONE: `meili_rekey` unit tests (`rekey_id_is_meili_safe_and_stable`, `distinct_triples_yield_distinct_ids`); `decisions_reindex` tests (stable id + title!=id + repo_rel_path); `bootstrap_doc_id_uses_repo_path_hash` (Meili-safe key). Live before/after probes verify each migration.
- [x] 3.3 Run tests and confirm they pass. DONE: `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo test --workspace --test-threads=1` all green (194 suites, 0 failures).
