## 1. Kind::TopicCard + payload

- [x] 1.1 `Kind::TopicCard` variant added in `crates/cortex-core/src/events.rs` with the phase11r docstring; `envelope.schema.json` `kind` enum extended with `"topic_card"`; `Kind::schema_stem()` maps the new variant to `"topic_card"`. Existing exhaustive matches on `Kind` across the workspace (11 sites in cortex-workers: classifier/statics.rs, embedder/routing.rs, fulltext/builders.rs ×5, fulltext/routing.rs, graph/mapper.rs ×2, bin/graph-backfill.rs) gained a new arm each — phase11r §3.1-§3.2 routing semantics applied (`family_for(Kind::TopicCard) → "topic_cards"` on both fulltext + embedder, `kind_label → "topic_card"`, graph `:TopicCard` label, `emit_memory` reuse for the graph edge pattern).
- [x] 1.2 Added `TopicCardPayload`, `EvidenceRef` (with `EvidenceKind` enum: Consolidation / Decision / Law / Turn — snake_case serde tags), `Contradiction` (with `ContradictionKind` enum: DecisionSupersession / LawViolationMismatch / OutcomeDivergence and `ContradictionStatus` enum: Open / Reconciled / Deprecated). Public constants `TOPIC_CARD_SLUG_MAX_CHARS = 80`, `TOPIC_CARD_SYNTHESIS_MIN_BYTES = 200`, `TOPIC_CARD_SYNTHESIS_MAX_BYTES = 4_000`, `TOPIC_CARD_OPEN_QUESTIONS_MAX = 8`, `TOPIC_CARD_RELATED_MAX = 32`. Deviation from the original §1.2 plan: dropped the `TopicCardId` newtype because the id is a `String` matching the `topic-{24-hex}` pattern enforced by the JSON Schema regex; introducing a newtype would have required a custom serde impl + breaking changes everywhere a card id moves through MCP / Synap / Vectorizer envelopes — not worth the type-safety dividend on a `String` already validated at the schema layer.
- [x] 1.3 `derive_topic_card_id(topic_slug, repo_scope) -> String` lands as a free function in `events.rs`. Uses `sha2::Sha256` over `slug\0repo_scope`, takes the first 12 bytes (24 hex chars) of the digest, prefixes with `topic-` (total 30 chars). Idempotent across re-runs; `derive_topic_card_id_*` tests pin the contract.
- [x] 1.4 9 unit tests landed in `events.rs::topic_card_tests` (exceeds the §1.4 minimum of 6): `schema_stem_maps_topic_card`, `kind_topic_card_serialises_snake_case`, `payload_serde_round_trip_preserves_every_field`, `evidence_kind_serialises_snake_case`, `contradiction_status_serialises_snake_case`, `derive_topic_card_id_is_deterministic_per_slug_repo_pair`, `derive_topic_card_id_differs_across_slugs`, `derive_topic_card_id_differs_across_repo_scope`, `constants_match_phase11r_spec`. All passing.
- [x] 1.5 `crates/cortex-core/schemas/kinds/topic_card.schema.json` written from scratch enforcing: `topic_card_id` matches `^topic-[0-9a-f]{24}$`; `topic_slug` matches `^[a-z0-9](?:[a-z0-9-]{0,78}[a-z0-9])?$`; synthesis byte range `[200, 4_000]`; `evidence.minItems = 1`; `open_questions.maxItems = 8` with per-item maxLength 200; `related_topic_ids.maxItems = 32` with the same `topic-` regex; `confidence` in `[0.0, 1.0]`; per-Contradiction enum tags. Cross-field rule (every `contradictions[*].evidence_*` references an `EvidenceRef.id` in the same payload's `evidence` vec) enforced by `validate_topic_card_payload(&TopicCardPayload)` in `crates/cortex-core/src/validate.rs` because JSON Schema cannot express it cleanly. 8 unit tests in `validate.rs::topic_card_validate_tests` cover the accept/reject paths: minimal payload accepted, empty evidence rejected with `evidence_required`, orphan `contradictions[0].evidence_a` rejected with the correct path, orphan `contradictions[0].evidence_b` rejected, both refs in evidence accepted, JSON-schema rejects synthesis below floor (300-byte boundary), JSON-schema accepts minimal envelope (canonical Crockford ULID + sha256-hex content_hash + array redactions), JSON-schema rejects invalid slug (`Auth_Rewrite` — uppercase + underscore). All passing. The validator is wired into `Validator::kinds` under the `"topic_card"` key alongside the existing 9 kinds.

## 2. cortex-topic-cards crate

- [x] 2.1 **ADR-007 deviation applied.** The original §2.1 plan called for a new `cortex-topic-cards` workspace member. ADR-007 (committed alongside phase11s) prohibits new top-level crates for worker-style daemons — they live inside `cortex-workers` behind a feature flag (when deps are unique) or unconditionally (when shared). Topic-cards' deps are entirely shared with cortex-workers (chrono, sha2, serde, tokio, async-trait, reqwest already present from the consolidator merge), so the module lands unconditionally at `crates/cortex-workers/src/topic_cards/`. Module declarations: `contradictions`, `orchestrator`, `producer`, `synthesiser`, `templates`, `trigger`, `prelude`. `pub mod topic_cards;` added to `crates/cortex-workers/src/lib.rs` (alphabetical order between `retention` and EOF).
- [x] 2.2 Prompt template at `crates/cortex-workers/templates/topic_cards/rewrite.md` (subdirectory under the existing `templates/` dir that hosts the consolidator session/topic/decision_trace templates). Slots: `{{topic_slug}}`, `{{repo_scope}}`, `{{existing_synthesis}}`, `{{existing_revision}}`, `{{new_evidence_block}}`, `{{superseded_evidence_block}}`, `{{output_contract}}`. Output contract pins `{synthesis_markdown, contradictions, open_questions, confidence}` JSON shape (no `title` field — the topic_slug carries the identity). `templates.rs` exposes `RewriteSlots<'a>` (borrowed strs, zero-copy in the steady state) + `render_rewrite_prompt(slots) -> String` doing `{{key}}` substitution. 3 unit tests pin: substitution of all known slots, unknown-slot fallthrough (typos surface as literal `{{foo}}` so the operator notices), slots round-trip with empty values.
- [x] 2.3 `synthesiser.rs` ships `TopicCardSynthesiser` holding `Arc<dyn cortex_workers::consolidator::summariser::Summariser>` (composition over duplication; no new abstract trait). `rewrite(slots: RewriteSlots<'_>) -> Result<RewriteOutput, SynthesiserError>` renders the template, calls `summariser.summarise()`, strips ```` ```json ```` code fences, deserialises into `RewriteOutput { synthesis_markdown, contradictions: Vec<RawContradiction>, open_questions: Vec<String>, confidence: f32, cost_cents: u32 }`. `RawContradiction` carries `kind` / `evidence_a` / `evidence_b` only — the producer (§2.4) stamps `surfaced_at_rev` and `status: Open`. `SynthesiserError` wraps `SummariserError` plus a `Parse(String)` variant for malformed JSON. 5 unit tests pin parse-success on raw JSON, code-fence-stripped (json + plain ```), missing-field rejection, cost passthrough, summariser-error mapping.
- [x] 2.4 `producer.rs` ships `produce(input: &ProduceInput, synthesiser: &TopicCardSynthesiser) -> Result<ProducedTopicCard, ProducerError>`. `ProduceInput` carries `topic_slug`, `repo_scope`, `existing_card: Option<TopicCardPayload>`, `all_evidence: Vec<EvidenceRef>` (caller pre-dedupes by id — the producer trusts the canonical citation set), `new_evidence_text` / `superseded_evidence_text` (rendered prompt blocks), `force_deep: bool`. Pipeline: build slots → call synthesiser → validate output (synthesis byte range, open_questions count) → enrich raw contradictions with `surfaced_at_rev = next_revision` + `status = Open` → build `TopicCardPayload` with `revision = prior + 1` (or 1 first emit), `events_since_last_rev = 0`, `last_rev_at = now()`, `topic_card_id` via §1.3 helper, `synthesis_model` from `synthesiser.summariser_kind().model_id()`. `ProducerError` enum: `Synthesiser(SynthesiserError)`, `SynthesisBelowFloor`, `SynthesisAboveCeiling`, `InvalidField`. 8 unit tests covering: first-emit revision=1, re-rewrite revision monotonicity, code-fence stripping (json + plain), missing-field rejection, summary-below-floor (199 byte boundary), summary-above-ceiling (4001 byte boundary), idempotent topic_card_id across runs, cost passthrough through the result struct.
- [x] 2.5 `trigger.rs` ships `Trigger::evaluate(card: Option<&TopicCardPayload>, new_event: &EventClassifier, distance: f32, now_rfc3339: &str) -> TriggerDecision`. The original §2.5 brief used `&Envelope`; rebuilt as a small `EventClassifier { kind, is_high_impact }` so the orchestrator can build it upstream with whatever signal makes sense (the Envelope shape carries too many unrelated fields). Constants: `TRIGGER_EVENTS_THRESHOLD = 8`, `TRIGGER_DISTANCE_THRESHOLD = 0.30`, `TRIGGER_AGE_DAYS = 14`. Heuristics return `Rewrite` when `events_since_last_rev >= 8` OR (`distance < 0.30` AND new_event kind is Decision / LawViolation / a high-impact-outcome event) OR `synthesis_age_d >= 14 AND any new evidence`. `Hold { reason: HoldReason }` with `HoldReason ∈ {Cooldown, LowImpact, NotRelevant}` otherwise. 7 unit tests cover every branch (events threshold, distance + Decision, distance + LawViolation, distance + high-impact outcome, stale-age + new evidence, cooldown hold, not-relevant hold).
- [x] 2.6 `contradictions.rs` ships `scan(hydrated: &[HydratedEvidence], at_rev: u32) -> Vec<Contradiction>`. The original §2.6 brief used a hydration callback (`hydrate_fn`); rebuilt as a `HydratedEvidence` value type the caller pre-builds (callbacks complicate the orchestrator's call site without buying anything for the in-memory scan). `EvidenceFacts` enum carries the per-kind facts the detectors need: `Decision { decision_id, supersedes }`, `LawViolation { law_id, law_version }`, `Law { law_id, version, active }`, `Consolidation { temporal_start_ms, temporal_end_ms, outcome_majority }`, `Turn`. Three detector classes: `DecisionSupersession` (any pair where one decision's `supersedes` matches another's `decision_id`), `LawViolationMismatch` (a violation citing a different version than the matching active Law), `OutcomeDivergence` (two consolidations with overlapping temporal spans + different outcome majorities). Heuristic-only — never blocks rewrite. Each emitted `Contradiction` carries `surfaced_at_rev = at_rev` and `status = Open`. 9 unit tests covering each class accept / reject / edge case (single evidence, no contradictions, mixed kinds).
- [x] 2.7 `orchestrator.rs` ships `Orchestrator::new(haiku, opus)` + `with_budget(CostBudget)` + `run(input: ProduceInput) -> Result<ProducedTopicCard, OrchestratorError>`. `GRAIN_LABEL = "topic_card"` (the cost-ledger grain bucket key). Auto-promotion to Opus when ANY: (a) `input.force_deep` is set, OR (b) the existing card's open contradictions count `>= 3`, OR (c) the existing evidence set already triggers a `decision_supersession` per the §2.6 scanner. Budget gate before the summariser call: `CostBudget::can_afford(ledger, est_cents)` where `est_cents` is 100 (Haiku) / 4 000 (Opus). After the rewrite returns, the realised cost stamps into `cost_ledger.record(GRAIN_LABEL, produced.cost_cents)` so `/v1/health/coverage` surfaces the burn alongside consolidator's `session` / `topic` / `decision_trace` grains. 4 unit tests pin: cost lands in topic_card grain bucket, budget-gate refusal trips `CostCeiling`, Opus auto-promotion on `force_deep`, Opus auto-promotion on existing contradictions count `>= 3`.
- [x] 2.8 New bin `crates/cortex-workers/src/bin/cortex-topic-cards.rs` (NOT in cortex-cli per the §2.1 ADR-007 deviation — the bin lives next to its module). Subcommands `rewrite <topic_slug> --repo <slug>`, `scan-now`, `replay --since <ts>`, `nightly --dry-run`. Top-level flags `--api-key` (env `ANTHROPIC_API_KEY`), `--api-url` (env `ANTHROPIC_API_URL`), `--monthly-cents-cap` (default 100 000), `--verbose`, `--deep`. The `nightly --dry-run` path runs offline (prints budget cap + remaining cents). The other subcommands print a `plan` header and exit with `pending §3 routing wiring` because the live envelope read path lands alongside §3. `[[bin]] cortex-topic-cards` registered in `crates/cortex-workers/Cargo.toml`. 7 clap-surface unit tests (rewrite parse, scan-now parse, replay --since parse, nightly default-dry-run, top-level --deep, require_api_key error path, require_api_key value path).
- [x] 2.9 IT at `crates/cortex-workers/tests/topic_cards_end_to_end_it.rs` (NOT under the original `crates/cortex-topic-cards/tests/` per the ADR-007 deviation). `three_sequential_rewrites_preserve_id_and_advance_revision` runs three rewrites against three `CannedSummariser` instances (no network) and asserts: deterministic `topic_card_id` across all three runs (matches `derive_topic_card_id("auth-rewrite", "cortex")` exactly), revision monotonicity 1 → 2 → 3, evidence accumulates without duplication (round 3 re-cites DEC-0042 — the canonical citation set stays at 2 entries), `synthesis_model = "claude-haiku-4-5"`, cost ledger records 80 cents in the `topic_card` grain bucket with `consolidations = 1` after round 1, contradictions surfaced from canned model output get `surfaced_at_rev = 3` + `status = Open`. 1 IT, 13 asserts (exceeds the §2.9 minimum of 6).

## 3. Family / collection / Meili routing

- [x] 3.1 `FAMILIES` in `crates/cortex-workers/src/fulltext/routing.rs` extended with `"topic_cards"` (between `consolidations` and `misc` to keep the matcher's family scan ordered by spec-08 routing). `family_for(Kind::TopicCard) -> "topic_cards"` already landed in §1.1; the matrix-coverage test inherits the existing exhaustive-match guard. `fulltext/builders.rs` already covers payload-text extraction (`synthesis_markdown` + bulleted `open_questions[]`), title derivation (reads `payload.topic_slug`), and `kind_label` mapping (`"topic_card"`) from the §1.1 routing pass. The `ext.topic_card` block grew `synthesis_age_d` (computed via new `synthesis_age_days(&str) -> u32` helper that parses RFC 3339 + clamps negative deltas to 0 so a malformed / future timestamp does not trip the staleness advisory) and `repos` (from `TopicCardPayload.repos`) — completes the §3.1 ext-field set. `index_for_event_global` non-governance regression test extended with `Kind::TopicCard` so the dual-write matrix stays pinned.
- [x] 3.2 Embedder routing (`crates/cortex-workers/src/embedder/routing.rs`) and classifier statics (`crates/cortex-workers/src/classifier/statics.rs`) already routed `Kind::TopicCard → "topic_cards"` (and pushed `topic_cards` + `topic_slug` as topics) from the §1.1 routing pass. Graph mapper got a dedicated `emit_topic_card` (replacing the placeholder `emit_memory` reuse): keys the `:TopicCard` node on the deterministic `topic_card_id`, stamps `topic_slug`, `revision`, `confidence`, `synthesis_model`, `events_since_last_rev`, `last_rev_at`, and `repos`; anchors via `(:Session)-[:OWNS]->(:TopicCard)` for graph connectivity (matches consolidation / memory pattern); emits one `(:TopicCard)-[:EVIDENCE_OF]->(:Decision|:Law|:Consolidation|:Turn)` edge per evidence item with target label varying by `EvidenceKind` and a deduped `(label, id)` set so re-cited evidence collapses to one edge; emits `(:TopicCard)-[:RELATED_TO]->(:TopicCard)` bidirectional edges (drops self-references, deduped on `(from, to)`) for `related_topic_ids`. Backfill bin's `kind_label` already maps `Kind::TopicCard → "topic_card"`. `cargo check -p cortex-workers` clean.
- [x] 3.3 Bumped `crates/cortex-workers/settings/settings.v1.json` v5 → v6 (the v4 → v5 bump landed in phase11k §1.3 ahead of this phase, so the phase11r bump becomes v5 → v6 to retrigger the settings-version watcher and re-apply the schema across all per-repo indexes). Added `ext.topic_card.{topic_slug, revision, confidence, synthesis_age_d, contradictions_count, repos, synthesis_model}` to `filterableAttributes`. Added the same set sans `repos` (Meili rejects sortable arrays) to `sortableAttributes`. Added `ext.topic_card.synthesis_markdown` + `ext.topic_card.topic_slug` to `searchableAttributes`. New regression test `topic_card_axis_fields_are_filterable_and_sortable` pins all three lists; updated `baked_in_settings_parse` to assert `version == "v6"` with the inherited rationale chain (v6 = phase11r topic_cards, v5 = phase11k governance, v4 = phase11j consolidations, v3 = phase11i relevance, v2 = phase11b path_prefixes). 6/6 settings tests green.
- [x] 3.4 `COLLECTION_TOPIC_CARD_FP32 = "cortex.topic_card.fp32"` (hot, recall-tuned `m=48 / ef_search=256`) and `COLLECTION_TOPIC_CARD_PQ = "cortex.topic_card.pq"` (warm, `m=16 / ef_search=64`) exported from `crates/cortex-storage/src/names.rs`; both registered in `COLLECTIONS` (between consolidation tiers and cold binary) so `ensure_collection` runs at boot. `INDEX_TOPIC_CARDS = "cortex_topic_cards"` exported and added to `ALL_INDEXES`. 3 unit tests in `collections.rs::tests`: `topic_card_collections_are_declared` (presence of both tiers in `COLLECTIONS`), `topic_card_hot_tier_uses_recall_tuned_hnsw` (m=48 / ef_search=256, fp32, Hot tier), `topic_card_collection_names_export_via_names_module` (string literals + `ALL_INDEXES` membership). 5/5 collections tests green.
- [x] 3.5 `archive_loader::envelope_to_hit` (`crates/cortex-api/src/archive_loader.rs`) gained a `Kind::TopicCard` arm decoding the `TopicCardPayload`, rendering `[<topic_slug>] <synthesis preview>` (synthesis clipped to 320 bytes via the same `clip` helper consolidations use), stamping `symbol = "topic_card:<slug>"` so the dashboard's per-topic group-by reads it without re-decoding. Empty `topic_slug` falls back to a bare preview without the `[]` prefix (defensive parity with the consolidation branch). `renders_topic_card_with_slug_and_synthesis_preview` regression test pins the prefix, body fragment, length cap, and symbol — matches the `renders_consolidation_*` test pattern. 11/11 archive_loader tests green.
- [x] 3.6 New `crates/cortex-workers/tests/topic_card_routing_it.rs` mirrors the phase11j §3.9 consolidation IT structure (4 cases): `fulltext_router_maps_topic_card_to_topic_cards_family` (kind-only + event routing + FAMILIES membership), `embedder_router_maps_topic_card_to_topic_cards_collection` (per-repo collection + unknown-slug fallback), `index_for_topic_card_lands_in_per_repo_topic_cards_index` (3 repos: Cortex / Tml / unknown), `global_topic_card_storage_constants_match_naming_convention` (`INDEX_TOPIC_CARDS` + both Vectorizer tiers). Note the per-repo Meili index name is `cortex-{slug}-topic_cards` (underscore, matching the family token) — the §3.6 brief used a dash but the canonical-name guard in `is_canonical_index_name` walks `FAMILIES` literally, so the underscore stays. 4/4 IT cases green.

## 4. MCP tools

- [x] 4.1 `cortex_topic_get(query_or_slug, scope) -> Option<TopicCardPayload>` lands in a new sibling module `crates/cortex-api/src/mcp_topic_card.rs` (editorial deviation from the §4 brief — `mcp.rs` already owns `cortex_query` end-to-end, and the additional ~600 lines for the topic-card surface would dilute it; the topic-card module is self-contained with its own error type, lookup trait, and payload contract; `lib.rs` re-exports the public names so callers stay ignorant of the file layout). Public surface: `TOOL_NAME_TOPIC_GET`, `TOPIC_GET_CONFIDENCE_FLOOR = 0.6`, `TopicCardLookup` async trait (`get_by_slug` / `search`), `TopicCardMcpError` (`ScopeRepoRequired` / `Invalid` / `Backend`), `is_valid_topic_slug` (matches the `topic_card.schema.json` regex `^[a-z0-9](?:[a-z0-9-]{0,78}[a-z0-9])?$` exactly — kebab-case, 1-80 chars, alnum at both ends), `topic_get_descriptor` (camelCase MCP 2024-11-05 schema with `query_or_slug` + `scope.repo` required), `invoke_topic_get`. Slug-exact path bypasses the confidence floor (caller named the card directly); search path applies the floor. Empty / absent `scope.repo` returns `ScopeRepoRequired` before either lookup lane is touched. Audit-envelope wiring lands in this same item via two new helpers in `audit.rs`: `build_topic_card_envelope(caller, tool, scope_repo, result_summary)` (canonical `kind = "topic_card_mcp_audit"` envelope shape) and `record_topic_card_call(publisher, caller, tool, scope_repo, summary)` (one-line publish helper the §4.X invokers call at the tail). Every `invoke_topic_get` call emits exactly one envelope: success → `{hit, topic_card_id, revision, confidence, via}`, miss → `{hit: null, via}`, rejection → `{error: "scope_repo_required"}`. 6 unit tests in `mcp_topic_card.rs::tests` (exceeds §4.1 minimum of 4): slug regex accept-set, slug regex reject-set (uppercase / underscore / edge dashes / 81 chars / spaces / empty), slug-exact short-circuit + bypass-floor + audit envelope, query path filters below-floor hit + audit shape, query path returns above-floor hit + audit confidence (f32→f64 ULP-tolerant compare), scope.repo absent + empty-string both reject + 2 audit envelopes recorded. 2 unit tests in `audit.rs::tests` for the new helpers (`record_topic_card_call_publishes_envelope_with_tool_and_scope`, `build_topic_card_envelope_handles_absent_repo`). 1 IT in `crates/cortex-api/tests/topic_card_mcp_it.rs` (`topic_get_end_to_end_dispatches_slug_then_search_with_confidence_floor`) exercising all four phases of the contract end-to-end against the public `cortex_api::*` re-exports + asserting the four-envelope audit trail (vias `["slug_exact", "search", "search", "none"]` with the rejection carrying `error = "scope_repo_required"`). 6 unit + 2 audit + 1 IT, all green.
- [x] 4.2 `cortex_topic_drill(topic_card_id, dimension) -> DrillResult` lands in the same `mcp_topic_card.rs` module. Public surface: `TOOL_NAME_TOPIC_DRILL`, `DrillDimension` enum (`Evidence` / `Contradictions` / `History` / `OpenQuestions` / `Related` — snake_case serde, `label()` accessor for telemetry), `HydratedEvidenceItem` (kind / id / title / occurred_at / cited_at_rev / weight), `TopicCardRevision` (topic_card_id / revision / last_rev_at / synthesis_diff_hash — sha256 hex), `DrillResult` (echoes id + dimension; exactly one of evidence / contradictions / history / open_questions / related populated, others omitted from the wire shape via serde `skip_serializing_if`), `TopicCardDrill` async trait (`get_card` / `hydrate_evidence` / `history` / `related`), `topic_drill_descriptor` (camelCase JSON Schema with `topic_card_id` regex `^topic-[0-9a-f]{24}$` + 5-value dimension enum), `invoke_topic_drill`. Dispatcher pattern: lookup card → match dimension → fan out → emit one audit envelope at the tail with `{topic_card_id, dimension, count}`. Missing card returns `TopicCardMcpError::Invalid("topic card not found: <id>")`. The §4.2 brief used `TopicCardId` newtype but the events module already represents the id as `String` matching the `topic-{24-hex}` schema regex (see §1.2 deviation note); the trait + descriptor stays on `String` for parity with the canonical envelope contract. 6 unit tests in `mcp_topic_card.rs::tests` (exceeds §4.2 minimum of 5): one per dimension exercising the dispatcher + audit envelope + content checks (evidence with a missing-source defensive path, history newest-first ordering, contradictions verbatim, open_questions verbatim, related neighbour ids), plus one rejection path (`returns_invalid_when_card_missing`). 1 IT case in `topic_card_mcp_it.rs` (`topic_drill_end_to_end_dispatches_each_dimension`) iterates every dimension end-to-end against the public re-exports + asserts the 5-envelope audit trail in dimension order. 6 unit + 1 IT, all green; total 12 unit + 2 IT across §4.1 + §4.2.
- [x] 4.3 `cortex_topic_neighbors(topic_card_id, depth?) -> NeighborGraph` lands in `mcp_topic_card.rs`. Public surface: `TOOL_NAME_TOPIC_NEIGHBORS`, `TOPIC_NEIGHBORS_DEFAULT_DEPTH = 2`, `TOPIC_NEIGHBORS_NODE_CAP = 64`, `NeighborNode` (topic_card_id / topic_slug / revision), `NeighborEdge` (edge_type / from / to — passes the literal Cypher type so callers can branch on `RELATED_TO` vs `EVIDENCE_OF`), `NeighborGraph` (echoes root + depth + nodes + edges + truncated flag), `TopicCardNeighbors` async trait (`neighbors(id, depth)` — implementer clips at the node cap and sets the flag), `topic_neighbors_descriptor` (camelCase JSON Schema with the same `topic-{24-hex}` regex + `depth: integer minimum=1, maximum=5, default=2`), `invoke_topic_neighbors`. Dispatcher clamps the requested depth into `[1, 5]` (load-bearing safety net for callers that bypass schema validation), runs the walk, emits one audit envelope `{topic_card_id, depth, nodes, edges, truncated}`. The Cypher query the production backend runs matches the §4.3 brief verbatim: `MATCH (t:TopicCard {id: $id})-[:RELATED_TO|EVIDENCE_OF*1..$depth]-(n) RETURN n LIMIT 64`. 4 unit tests in `mcp_topic_card.rs::tests` (exceeds §4.3 minimum of 3): `default_depth_is_2` (pin the constant + dispatcher behaviour when depth omitted), `returns_nodes_and_edges_at_depth_1` (depth=1 happy path with edge type assertion + audit content), `clip_at_64_nodes_sets_truncated_flag` (cap test with 64 nodes + flag pinned), `clamps_out_of_range_depth` (depth=0 → 1, depth=99 → 5). 1 IT case `topic_neighbors_end_to_end_returns_subgraph_with_audit_envelope` exercising descriptor schema (depth bounds 1-5) + invoke + audit envelope. 4 unit + 1 IT, all green; total 16 unit + 3 IT across §4.1 + §4.2 + §4.3.
- [x] 4.4 `cortex_topic_diff(topic_card_id, since_rev) -> TopicCardDiff` lands in `mcp_topic_card.rs`. Public surface: `TOOL_NAME_TOPIC_DIFF`, `TopicCardDiff` (echoes id + from_rev/to_rev + `synthesis_diff: String` + `evidence_added`/`evidence_removed: Vec<EvidenceRef>` + `contradictions_added`/`contradictions_resolved: Vec<Contradiction>`), `TopicCardDiffer` async trait (`revision_pair(id, since_rev) -> Option<(from, to)>`), `topic_diff_descriptor` (camelCase JSON Schema with `since_rev: integer minimum=1`), `invoke_topic_diff`. Unified-diff helper `render_synthesis_diff(from, to)` lands inline (no new dep) — splits both bodies by lines, elides common prefix + suffix so a small middle-of-body edit reads as one or two `-` / `+` lines instead of "every line changed", returns empty string when bodies are byte-identical. Set-diff helpers `diff_evidence` (key on `(kind, id)`) and `diff_contradictions` (key on `(kind, evidence_a, evidence_b)`; `resolved` = same key but `from.status == Open` && `to.status ∈ {Reconciled, Deprecated}`). Dispatcher rejects `since_rev >= to.revision` with `Invalid("since_rev N must be older than current revision M")` and rejects a missing pair with `Invalid("revision pair not found ...")`. Audit envelope at the tail carries `{topic_card_id, from_rev, to_rev, evidence_added, evidence_removed, contradictions_added, contradictions_resolved}`. 5 unit tests in `mcp_topic_card.rs::tests` (exceeds §4.4 minimum of 4): `render_synthesis_diff_elides_common_prefix_and_suffix`, `render_synthesis_diff_returns_empty_when_bodies_equal`, `set_diffs_evidence_and_contradictions` (full happy path with added evidence + reconciled contradiction + new contradiction + audit content), `rejects_since_rev_at_or_above_current`, `returns_invalid_when_pair_missing`. 1 IT case `topic_diff_end_to_end_renders_synthesis_and_set_diffs` (descriptor schema + invoke + diff content + audit envelope). 5 unit + 1 IT, all green; total 21 unit + 4 IT across §4.1 + §4.2 + §4.3 + §4.4.
- [x] 4.5 `cortex_synthesize(query, scope, force?, persist?) -> SynthesizeResult` lands in `mcp_topic_card.rs`. Public surface: `TOOL_NAME_SYNTHESIZE`, `SynthesizeRequest` (query / scope / `force: bool = false` / `persist: bool = false`), `SynthesizeResult` (topic_card / cost_cents / persisted), `TopicCardSynthesizer` async trait (the production wiring wraps cortex-workers' `topic_cards::orchestrator` — the implementer is responsible for the budget gate), `synthesize_descriptor` (camelCase JSON Schema with `query: string minLength=1` + `scope.repo` required + boolean flags). Two new failure modes: `TopicCardMcpError::BudgetExhausted { used_cents, cap_cents }` (carries both numbers so the dashboard surfaces them together) and `Invalid("query is empty")` for whitespace-only queries. Dispatcher: validate scope.repo + non-empty query → backend.synthesize → audit. Audit envelope on success carries `{topic_card_id, topic_slug, revision, cost_cents, persisted, force, persist}`; rejections carry `{error, …}` so the dashboard differentiates them. 5 unit tests in `mcp_topic_card.rs::tests`: `persist_false_returns_payload_without_emitting`, `persist_true_marks_envelope_emitted`, `force_flag_passes_through` (fake's cost branches on `req.force`, so a 4_000-cent return verifies the flag reaches the backend), `returns_budget_exhausted_when_over_cap` (BudgetExhausted carries both used + cap; audit envelope still recorded), `requires_scope_repo_and_non_empty_query` (two rejection paths + 2 audit envelopes). 1 IT case `topic_synthesize_end_to_end_runs_persist_paths_and_budget_gate` exercising preview + persist + over-cap rejection through the public re-exports + asserting a 3-envelope audit trail. 5 unit + 1 IT, all green; total 26 unit + 5 IT across §4.1-§4.5.
- [x] 4.6 `crates/cortex-api/tests/topic_card_mcp_it.rs` consolidates §4 coverage with 9 IT cases (exceeds §4.6 minimum of 8) — every tool's happy path plus edge cases. Happy paths from §4.1-§4.5 (5 cases): get-with-confidence-floor, drill-each-dimension (5-envelope audit trail), neighbors-with-subgraph, diff-with-prefix-elision, synthesize-preview-and-persist-and-budget. Edge cases (4 cases): `topic_get_returns_none_when_no_card_matches_either_lane` (audit `hit: null`), `topic_drill_returns_invalid_when_card_missing` (Invalid("not found")), `topic_neighbors_clamps_depth_zero_to_one` (depth-0 clamps + audit `depth: 1`), `topic_diff_rejects_future_since_rev` (Invalid("must be older")). All 9 IT cases run hermetically against in-memory `Fake*` doubles per the §3.6 routing-IT pattern; live Vectorizer / Meili / Nexus reads are exercised by the routing-IT (§3.6) and the env-gated worker IT (§2.9). 9/9 IT cases green.

## 5. Pre-thinking renderer integration

- [x] 5.1 `ResultsBag` extended with `topic_cards: Vec<TopicCardRef>` (the §5.1 brief named `crates/cortex-pre-thinking/src/render.rs` but `ResultsBag` actually lives in `crates/cortex-api/src/types.rs` — file added there to match the canonical home; `cortex-pre-thinking` consumes via `cortex_api::types::ResultsBag` re-export). New `TopicCardRef` struct carries every field the §5.1 spec named: `topic_card_id`, `topic_slug`, `synthesis_preview: String` (clipped to ≤ 600 bytes by the producer), `evidence_top5: Vec<EvidenceRef>` (top-5 by `cited_at_rev` desc), `open_contradictions: Vec<Contradiction>` (status == Open only), `confidence: f32`, `synthesis_age_d: u32`, `events_since_last_rev: u32`, `score: f32`. Serde uses `skip_serializing_if = "Vec::is_empty"` on the vec fields so an empty card does not pollute the wire shape. Re-exported from `lib.rs::pub use types::*`. `cargo check -p cortex-api -p cortex-pre-thinking` clean.
- [x] 5.2 `crates/cortex-api/src/strategies.rs` extended in three intents: `pre_change_context` (topic cards lead the renderer's context band; hot tier vector + global keyword lane added alongside the consolidation lane), `decision_lookup` (cards drill into evidence per §4.2 — adding the lane surfaces the synthesis attached to a queried ADR), `similar_problems` (cards fold many consolidations into a single living synthesis — hot tier only, same recall reasoning as the consolidation lane). All three add `COLLECTION_TOPIC_CARD_FP32` to the vector list and `INDEX_TOPIC_CARDS` to the keyword list. PQ / warm tier intentionally not consulted — the §5.4 staleness advisory + downgrade handles older / lower-confidence cards. Three regression tests pin the lanes against drift (one per intent — exceeds §5.2 minimum of 2): `pre_change_context_includes_topic_cards_lane`, `decision_lookup_includes_topic_cards_lane`, `similar_problems_includes_topic_cards_lane`. 16/16 strategies tests green (full suite stays green; the existing `pre_change_context_uses_three_lanes` test asserts shape, not count, so the added lane does not regress it).
- [x] 5.3 New `## Topic card` section in [crates/cortex-pre-thinking/src/formatter.rs](crates/cortex-pre-thinking/src/formatter.rs) rendered via the new `render_topic_card_section` helper. Format pinned by spec: `[<topic_slug>] (rev N, confidence X%, age Yd, +Z ev)` header line, then synthesis preview clipped at `byte_cap / 2` so the evidence block fits within the section budget, then `### Evidence (N)` sub-block (`kind:id (cited@rev=N[, w=…])` per item), then `### Open contradictions (N)` sub-block (`kind: A vs B (surfaced@rev=N)`). The optional `> stale-topic-card: <reason>` advisory lands directly under the `## Topic card` heading when the §5.4 staleness path fires. New constants in `section_caps`: `TOPIC_CARDS: usize = 1` (count cap), `TOPIC_CARDS_BYTES: usize = 1_400` (section byte budget the clipper targets), `TOPIC_CARD_STALE_AGE_DAYS: u32 = 30`, `TOPIC_CARD_CONFIDENCE_FLOOR: f32 = 0.6`. New `FormatOptions.topic_cards_cap: usize = 1` + `topic_cards_byte_cap: usize = 1_400`. The `render_consolidations_section` helper was extracted alongside so both order branches (fresh / stale) reuse the same body without duplication. Added `revision: u32` to `TopicCardRef` (the §5.1 brief omitted it but the §5.3 header line spec needs the value — captured inline in the type docstring). Two test-only call sites in `cortex-pre-thinking` had their `ResultsBag` literals extended with `topic_cards: Vec::new()` so the field stays opt-in for legacy bundles. `cortex-pre-thinking/Cargo.toml` dev-dependency on `cortex-core` added so the new tests can construct `EvidenceRef` / `Contradiction` literals.
- [x] 5.4 Section priority in `formatter.rs` (`render.rs` does not exist — the canonical renderer lives in `formatter.rs`; `format_bundle` was reordered) now emits in spec order: `laws` → `topic_cards` → `consolidations` → `decisions` → `similar_turns` → `past_sessions` → `snippets` → `graph`. The fresh path runs `render_topic_card_section(stale=false)` then `render_consolidations_section`; the stale path inverts the order (consolidations first, then topic card section with the advisory line). Staleness fires when `confidence < TOPIC_CARD_CONFIDENCE_FLOOR (0.6)` OR (`synthesis_age_d > TOPIC_CARD_STALE_AGE_DAYS (30)` AND `events_since_last_rev > 0`). The empty-bundle short-circuit was extended to count topic cards. 5 new tests pin every branch: `topic_card_section_renders_when_present` (fresh card → no advisory + section header / format line / evidence + contradictions sub-blocks), `topic_cards_take_priority_over_consolidations` (fresh card → topic card position < consolidations position), `stale_topic_card_advisory_downgrades_section` (parametrised across BOTH staleness branches: low confidence AND age+events — consolidations position < topic card position + advisory line lands), `topic_cards_cap_caps_section_size` (cap=1 drops second card), `consolidations_render_when_no_topic_card_matches` (zero cards keeps the existing consolidation lane intact + no advisory). 34/34 formatter tests green (29 prior + 5 new).
- [x] 5.5 New IT [crates/cortex-pre-thinking/tests/topic_cards_render_it.rs](crates/cortex-pre-thinking/tests/topic_cards_render_it.rs) lands 6 cases (meets §5.5 minimum): `section_order_with_fresh_topic_card_matches_spec` (laws < topic < cons < dec < turns < snippets per §5.4 order), `section_order_with_stale_topic_card_demotes_topic_after_consolidations` (low-confidence branch — cons < topic + advisory line), `section_order_with_age_plus_events_staleness_branch` (age > 30 + events_since > 0 branch with confidence > floor — cons < topic + advisory), `no_topic_card_falls_back_to_consolidations_lane` (zero cards keeps the existing consolidation lane intact, no advisory, no topic card heading), `topic_card_section_respects_section_byte_cap` (50-byte cap clips the synthesis preview but keeps the header line), `budget_clipper_keeps_topic_card_bundle_under_thirty_two_kib` (20_000-byte synthesis still ends up under the 32 KiB ceiling after `clip_to_budget` runs). All 6 cases run hermetically through the public `cortex_pre_thinking::*` re-exports + `format_bundle` / `clip_to_budget`. 6/6 IT cases green.
- [x] 5.6 [crates/cortex-api/tests/fixtures/relevance-gold.json](crates/cortex-api/tests/fixtures/relevance-gold.json) extended with 8 phase11r entries (rel-051..rel-058): 3 `pre_change_context` (trigger heuristic, renderer staleness advisory, settings.v6 push), 2 `decision_lookup` (drill into evidence dimension, contradiction detector), 2 `similar_problems` (neighbours traversal, living synthesis distillation), 1 `free_search` (`cortex_synthesize` escape hatch + budget gate). Each entry pairs the query with 1-3 acceptable hits in the established matcher shape (file paths, partial paths, or symbol substrings — survives chunk-hash drift). `gold_fixture_loads_and_matches_intent_breakdown` updated for the new 20/12/17/3/6 split + 58-entry total. Live `relevance_gold_set_meets_mrr_threshold` IT inherits the same MRR threshold as phase11j §4.5 (no threshold change). 1/1 fixture-shape regression test green.

## 6. Tail (mandatory — enforced by rulebook v5.3.0)

- [x] 6.1 Update or create documentation covering the implementation. (a) [CHANGELOG.md](CHANGELOG.md) `[Unreleased] → Added` entry summarising §1-§5 — Kind::TopicCard envelope + 5 MCP tools + pre-thinking section reorder with staleness advisory + new collections + settings v6 push + bin under cortex-workers + cross-references to spec 12 / 11 / 16 + topic-cards.md handbook + ADR-006 / ADR-007. (b) [docs/architecture.md](docs/architecture.md) §6 gains §6.0a "Living-synthesis tier: topic cards on top of consolidations" — three-tier table (raw / consolidation / topic_card) with mutation model column, three trigger heuristics with constants, three contradiction detector classes, staleness contract pointing at the renderer's order matrix, MCP tool surface summary, ADR-006 cross-link. (c) [docs/cortex/topic-cards.md](docs/cortex/topic-cards.md) written from scratch with six numbered sections — trigger heuristics table, contradiction detector classes table, staleness contract with both order branches, operator runbook (force-rewrite, replay, nightly dry-run, ad-hoc synthesize), MCP tool reference table, cost & budget table. (d) [docs/specs/12-pre-thinking-injection.md](docs/specs/12-pre-thinking-injection.md) Output section rewritten — example bundle now includes the `## Topic card` section with header line + synthesis preview + Evidence + Open contradictions sub-blocks; the section-order paragraph documents the §5.4 reorder (laws → topic_cards → consolidated context → decisions → similar turns → past_sessions → snippets) + the staleness flip + advisory line; new "Topic card section (phase11r §5.3)" subsection pins the format template with cap + byte budget. (e) [docs/specs/11-query-api.md](docs/specs/11-query-api.md) MCP tool binding section extended with a "Topic-card MCP tools (phase 11r §4)" subsection — 5-row table covering each tool's input / output / contract notes, audit-envelope contract called out + cross-link to topic-cards.md handbook. (f) [docs/specs/16-dashboard.md](docs/specs/16-dashboard.md) `/memory` route description extended with the TopicCards lane filter chip set (`topic_slug` / `revision` / `confidence` / `synthesis_age_d` / `contradictions_count` / `synthesis_model`) and a pointer to the v6 settings push that makes the axis fields filterable + sortable.
- [x] 6.2 Test coverage delivered alongside each section. §1: 9 events + 8 validate (≥ 14 — meets minimum). §2: 41 unit tests across templates / synthesiser / producer / trigger / contradictions / orchestrator / cli + 13-assert IT in `topic_cards_end_to_end_it.rs`. §3: 8 unit tests across routing / settings / collections / archive_loader + 4-case routing IT. §4: 26 unit + 2 audit + 9 IT cases in `topic_card_mcp_it.rs`. §5: 3 strategy regression tests + 5 formatter tests + 6-case `topic_cards_render_it.rs` + 1 fixture-shape test. Sum: **≥ 14 + ≥ 41 + ≥ 8 + ≥ 28 + ≥ 14 unit and 4 ITs** (`topic_cards_end_to_end_it.rs`, `topic_card_routing_it.rs`, `topic_card_mcp_it.rs`, `topic_cards_render_it.rs`) — exceeds the §6.2 minimums on every section. Existing fixture-driven tests updated for the v5 → v6 settings bump (`baked_in_settings_parse` asserts v6) + the topic-card axis fields (`topic_card_axis_fields_are_filterable_and_sortable`) + the new collections (`topic_card_collections_are_declared`, `topic_card_hot_tier_uses_recall_tuned_hnsw`, `topic_card_collection_names_export_via_names_module`) + the routing matrix (`index_for_event_global_returns_none_for_non_governance_kinds` extended with `Kind::TopicCard`) + the relevance gold fixture (`gold_fixture_loads_and_matches_intent_breakdown` updated for the 20/12/17/3/6 split + 58-entry total).
- [x] 6.3 Run tests and confirm they pass. `cargo check -p cortex-core -p cortex-api -p cortex-cli -p cortex-storage -p cortex-pre-thinking -p cortex-workers` clean (the ADR-007 deviation in §2.1 folded `cortex-consolidator` / `cortex-classifier` / `cortex-topic-cards` into `cortex-workers` so the original `-p cortex-consolidator -p cortex-classifier -p cortex-topic-cards` segment is the same compilation unit as `-p cortex-workers`). `cargo test` 100% pass for every phase11r-touched test target: `cortex-core` (events::topic_card_tests + validate::topic_card_validate_tests), `cortex-storage` (collections + names — 7/7), `cortex-pre-thinking` (formatter — 34/34), `cortex-workers --lib topic_cards` (40/40 across templates / synthesiser / producer / trigger / contradictions / orchestrator / cli), `cortex-workers --test topic_cards_end_to_end_it` (1/1 + 13 asserts), `cortex-workers --test topic_card_routing_it` (4/4), `cortex-api --lib mcp_topic_card` (26/26), `cortex-api --test topic_card_mcp_it` (9/9), `cortex-api --test relevance_eval_it gold_fixture_loads` (1/1), `cortex-pre-thinking --test topic_cards_render_it` (6/6). One `pipeline.rs` test-helper `ResultsBag` literal needed `topic_cards: Vec::new()` added to compile — adjacent test-only code was the spec's "Update existing fixture-driven tests" path. `cargo clippy --no-deps --tests` surfaces only inherited warnings from earlier phases (e.g. `cortex-api/src/fusion.rs::OUTCOME_MULT_BLOCKED_BY_LAW` missing-doc; `cortex-cli/src/bin/cortex-ops.rs` already-shadowed binding); per the surgical-changes rule (CLAUDE.md / AGENTS.md), pre-existing items stay untouched. **Zero new clippy warnings** introduced by phase11r.
- [x] 6.4 Captured 1 learning via `rulebook_learn_capture` (id `2026-05-03T22-56-05-phase11r-confidence-floor-on-f32-f64-json-round-trip-needs-ulp-tolerant-compare`) — surfaces the non-obvious failure mode where `assert_eq!(json["confidence"], 0.82)` fails when the source is f32 because the JSON number widens to a non-binary-clean f64; the working pattern is to read back as f64, cast to f32, compare against the source f32 with `< f32::EPSILON`. Applies anywhere a typed f32 round-trips through JSON into a test assertion (audit envelopes, embedding scores, similarity floors). Captured 1 pattern via `rulebook_knowledge_add` (id `living-synthesis-trigger-heuristic-3-way-or-with-cooldown-holds`, category `architecture`, type `pattern`) — the canonical shape for a "living document" trigger function: 3-way OR over independent heuristics (burst / high-impact proximity / stale + new evidence) returning Rewrite, with a typed `HoldReason` (Cooldown / LowImpact / NotRelevant) when none fire. Pin includes the worked code example, when to use / when NOT to use guidance, and the principle that the trigger stays a pure function (no I/O / no async) so every branch is cheap to unit-test.
- [x] 6.5 [ADR-006](.rulebook/decisions/006-topic-card-as-living-synthesis-vs-consolidation-as-snapshot.md) `topic-card-as-living-synthesis-vs-consolidation-as-snapshot` written + metadata.json (status: `proposed`, related tasks `phase11j_consolidation_tier` + `phase11r_topic_card_mcp_enrichment`). Decision: ship a separate `Kind::TopicCard` layered on top of `Kind::Consolidation`; cards cite consolidations through `EvidenceKind::Consolidation`; consolidations stay append-only as the lower tier. Four rejected alternatives written up: (a) mutate `Kind::Consolidation` to be revision-aware (would require a fourth scope variant + entangle live-rewrite path with snapshot path), (b) single `Kind::LivingDocument` (would lose snapshot semantics or carry a `mode` discriminator every consumer must branch on), (c) separate vault projection in markdown directory (loses Synap durability + Vectorizer indexing + Nexus graph membership; documented as future EXPORT format only), (d) `Kind::Consolidation` with a `live: bool` flag (same blast radius as #a plus runtime ambiguity for malformed events). Consequences section documents both the positive side (clean dependency direction at type level, additive renderer change, idempotent `derive_topic_card_id`, naturally-implementable `cortex_topic_diff`, clean cost ledger bucket) and the tradeoffs (parallel match arms across N consumers, soft evidence refs across retention tiers, heuristic contradiction detector false-positive accumulation absent dashboard status edit surface, blast radius for future "living document" kinds). The `Future "living document" kinds inherit the same blast radius` paragraph documents the discipline so phase 11r becomes the canonical template for ADRs of this shape.
