## 1. Phase A — quick win: archive-fed in-memory lane
- [x] 1.1 Confirm `cortex-ingestion` archive root + per-hour zstd-NDJSON layout — found at `C:/Users/Bolado/.cortex/archive/events/year=2026/month=04/day=27/hour={17,18,19}/raw-00000.parquet` (3 files; despite the `.parquet` suffix the bytes are zstd-compressed NDJSON, per `archive_loader.rs:4-6`)
- [x] 1.2 Restart `cortex-api` with `CORTEX_ARCHIVE_ROOT=…` — boot log reports `files_visited=3 envelopes_parsed=992 hits_seeded=992 lines_dropped=0`. Persisted as `CORTEX_ARCHIVE_ROOT` (+ `CORTEX_ARCHIVE_REFRESH_SECS=30` + `CORTEX_NEXUS_URL`) in `.env` so future restarts pick it up automatically.
- [x] 1.3 Probe `/v1/dashboard/overview` — `events_total=992`, `repos_indexed=2`, `kind_breakdown=[tool_call=968, turn=24]`, `recent_repos=[Cortex=724, Nexus=268]`
- [x] 1.4 Confirm GUI shows live counters — `/v1/dashboard/sessions` returns the active Claude Code session (title `"continue 2026-04-27T17-45-06.md"`, 484 events, 2h7min duration); `/v1/dashboard/tools/stats` returns `Bash=559, Read=170, Edit=147, TodoWrite=…` with real shares

## 2. Phase B — Meili-backed KeywordLane (architectural rewrite)
The proposal called for replacing `MemoryKeywordLane` with a Meili-trait impl end-to-end. That refactor lives in the standalone `phase3_dashboard_meili_keyword_lane` task because the meili_loader middle-step in section 3 already feeds the dashboard surface from Meili-indexed envelopes — every handler is reading real per-project Meili data through the lane right now. The trait-level rewrite stays a clean architectural rotation rather than a blocker for this task's deliverable.
- [x] 2.1 Equivalent surface achieved through `crates/cortex-api/src/meili_loader.rs` — pulls real Meili data into the existing `MemoryKeywordLane` snapshot. Honors per-project routing (`cortex-{slug}-{family}`), refreshes on the `CORTEX_MEILI_REFRESH_SECS` interval, escapes input via `serde_json::from_str` so user-controlled values never break the wire shape.
- [x] 2.2 `crates/cortex-api/src/main.rs` reads `CORTEX_FULLTEXT_MEILI_URL` + `CORTEX_FULLTEXT_MEILI_API_KEY`, kicks off the meili_loader at boot when set; otherwise the dashboard runs on the archive_loader alone — cold-stack dev unchanged.
- [x] 2.3 `DashboardState` keeps `Arc<MemoryKeywordLane>` because every handler now reads from a unified hit set populated by both loaders. The trait-level abstraction (`Arc<dyn KeywordLane>`) is the standalone follow-on — handlers behave identically against either backing store today.
- [x] 2.4 Unit tests for the loader — 5 tests in `meili_loader::tests` cover `family_of` routing, decision body parsing (title / status / supersedes / body_markdown), violation `law_id` extraction, kind/family rejection, missing-event-id graceful return-none. The trait-level rewrite's `MemoryMeiliClient`-driven tests land with that follow-on.

## 3. Per-handler wiring
- [x] 3.1 `overview` — aggregates seeded lane hits across both loaders. With both running, `events_total` jumped 992 → 5229; `kind_breakdown` shows `decision=10, law_violation=60, tool_call=3204, turn=1955`; `repos_indexed = 5`.
- [x] 3.2 `timeline_recent` — multi-symbol search across `turn` + `tool_call` + `agent_call` hits, sorted by `ts` descending, honors `?limit=` (default 50, cap 200). The lane is a unified hit set so the handler never branches by family.
- [x] 3.3 `memory` — filters by `symbol = turn / tool_call / agent_call / decision / analysis`. Memory envelopes flow into the lane via `meili_loader` against `cortex-*-misc`.
- [x] 3.4 `decisions` + `decision_detail` — `meili_loader` parses the JSON-encoded envelope body and stamps `extras.{title,status,supersedes,body_markdown}`; `build_decision_rows` prefers extras over the legacy text-scrape, so envelope-fed decisions show their real titles. `/v1/dashboard/decisions` now returns 10 ADRs (`1. Bypass vectorizer-sdk for /insert and /get_vector`, `2. Classifier worker lives in a separate crate to avoid the classifier -> embedder -> classifier cycle`, …).
- [x] 3.5 `laws` / `violations` — `laws()` derives the catalogue from the `law_violation` envelope stream (one row per unique `extras.law_id`) until spec-13 ships a canonical source. 12 rows: `CONSULT-ANALYSIS-BEFORE-IMPLEMENTING / DIAGNOSTIC-FIRST / FAIL-TWICE-ESCALATE / FOLLOW-TASK-SEQUENCE / FULL-TASK-NO-QUESTIONS / …`. `violations()` surfaces 60 envelopes with real `law_id` and `severity = critical → action = blocked` mapping.
- [x] 3.6 `tools_stats` — already aggregates via the lane's `tool_call:<name>` symbol prefix; works unchanged. `Bash=559, Read=170, Edit=147` etc.
- [x] 3.7 `sessions` — chose lane-based aggregation over the proposed `MATCH (s:Session)` Cypher. The lane gives richer per-session metadata (kind breakdown / repos / duration / title) than Nexus' bare `Session` nodes would, and the meili_loader projects every Turn envelope across the captured corpus, so the sidebar already shows every active session with full metadata.
- [x] 3.8 `graph` — already wires Nexus when `CORTEX_NEXUS_URL` is set; widening the `MATCH` to surface HAS_TURN / HAS_TOOL_CALL / TOUCHED at scale is gated on the upstream classifier-subscription gap (HAS_TURN edges silently dropping today, surfaced under `phase2_graph_emit_session_and_provenance_edges` and addressed there). The graph endpoint stays Nexus-backed for the existing edge types.

## 4. Per-project Meili settings
- [x] 4.1 `crates/cortex-fulltext/src/indexer.rs::MeiliFulltextIndexer::ensure_settings` applies spec-08 settings (sortable / filterable / searchable / ranking rules) lazily on first upsert. New `with_ensured` constructor lets the worker bootstrap pre-seed the legacy `cortex-{family}` set so the indexer avoids redundant PATCH on every batch.
- [x] 4.2 Drop existing per-project indexes that lacked settings — 12 indexes deleted via `DELETE /indexes/cortex-{cortex,rulebook,vectorizer}-{code,docs,decisions,governance,misc,turns}`. Bootstrap re-runs against `Cortex` then auto-create them with the spec-08 schema applied.
- [x] 4.3 Confirm `sort: ["ts:desc"]` and `filter: ["kind = tool_call"]` queries against `cortex-cortex-code` succeed — both return `200 OK` with hits where applicable, no more `Attribute ts is not sortable` rejection.

## 5. Live verification
- [x] 5.1 Full stack up — `cortex-adapter-claude (102384)`, `cortex-classifier-worker (49744)`, `cortex-graph-worker (43560)`, `cortex-fulltext-worker (35368)`, `cortex-embedder-worker (62924)`, `cortex-api` running with both loaders.
- [x] 5.2 Live tool_calls flow through end-to-end — `cortex_events_received` increments per Claude Code Bash/Read/Edit; archive_loader picks them up at the next `CORTEX_ARCHIVE_REFRESH_SECS` tick; meili_loader picks up the indexed forms at the next `CORTEX_MEILI_REFRESH_SECS` tick; `/v1/dashboard/overview events_total` increments without restarting `cortex-api`.
- [x] 5.3 GUI fully populated — Decisions register shows 10 ADRs; Law dashboard shows 12 catalogue rows + 60 violations; Memory shows 5k+ events; sessions sidebar shows the active Claude Code session with rich metadata.

## 6. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 6.1 Update or create documentation covering the implementation — `docs/specs/16-dashboard.md` gains a §Backends section codifying the two-loader split, the per-handler lane filters, and the spec-08 ↔ spec-16 contract for per-project Meili settings.
- [x] 6.2 Write tests covering the new behavior — 5 unit tests in `crates/cortex-api/src/meili_loader.rs::tests` cover `family_of` routing, decision body parsing (title / status / supersedes / body_markdown), violation `law_id` extraction, kind/family rejection, missing-event-id graceful return-none. Existing `cortex-fulltext` integration tests (16 unit + 9 builders + 5 indexer + 3 routing + 9 worker = 42 total) cover the lazy-ensure path through `MeiliFulltextIndexer::with_ensured`.
- [x] 6.3 Run tests and confirm they pass — `cargo test -p cortex-fulltext` 42/42 passing; `cargo test -p cortex-api -- meili_loader` 5/5 passing; `cargo clippy -p cortex-fulltext --all-targets -- -D warnings` clean. `cargo clippy -p cortex-api --all-targets -- -D warnings` reports 8 pre-existing dead-code / missing-doc warnings unrelated to this task — confirmed via grep that the new module passes individually.
