## 1. Cold-archive path (tail watcher)
- [x] 1.1 Root-cause + fix the `files_watched=0` mount/root mismatch — DONE (commit 4706f08): mount host projects at `/data/claude-projects/projects`. Verified files_watched 0→203, 15052 envelopes emitted.
- [x] 1.2 DONE (2026-06-22): investigated. `cortex-api`'s `archive_loader` (`crates/cortex-api/src/lanes/archive_loader.rs`) seeds an IN-MEMORY `MemoryKeywordLane` at boot (+ every 30 s) so `/v1/query` can answer over captured events — an explicit stopgap "until the spec-06/07/08 indexer pipeline is wired". It does NOT re-index into Meili/Nexus. The live Meili/Nexus indexes are fed only by the live indexer workers (embedder/fulltext/graph) consuming the Synap stream. So the restored cold archive feeds the QUERY path (in-memory lane), while the restored live path (§2) feeds Meili/Nexus — two independent paths, both now flowing.

## 2. Live indexing path (adapter → ingestion → Synap → classifier)
- [x] 2.1 DONE (2026-06-21): root cause = the host adapter daemon (`cortex-adapter-claude daemon`) was simply NOT running. It listens on the named pipe `\\.\pipe\cortex-adapter-claude`, receives hook frames from `cortex-hook`, builds envelopes, and POSTs them to `cortex-ingestion:17010`. `adapter-daemon.log` showed it healthy (real session `14APAEZ9…` events + pre-thinking bundles flowing) until it stopped Jun 20 13:46 (likely reboot/terminal close). The repeated `ipc handler failed: os error 232` WARNs are benign fire-forget disconnects (cortex-hook writes + exits without awaiting the reply), NOT the cause — they coexisted with healthy ingestion on Jun 20. The earlier "system broken" impression was mostly the false-positive IPC canary spam (fixed + disabled separately, commits 74cea0b/b9314d4).
- [x] 2.2 DONE (2026-06-21): restarted `cortex-adapter-claude daemon` on the host. Verified live via the daemon `/healthz` (:17011) counters after restart: `frames_received_total={PostToolUse:5,PreToolUse:5}`, `frames_parse_error_total=0`, `envelopes_publish_ok_total={tool_call:18,turn:3}`, `envelopes_publish_fail_total={}` — the full path `cortex-hook → pipe → daemon → cortex-ingestion` is flowing again with ZERO publish failures. CAVEAT (durability): the daemon was launched as a background process this session; it is NOT set to autostart, so it will stop again on reboot/logoff. Operator action needed for permanent recurrence-prevention: register `cortex-adapter-claude daemon` as a login/startup task (or run `cortex-adapter-claude install` if that wires autostart). The §3 freshness watchdog also guards against silent recurrence.

## 3. Watchdog (prevent silent recurrence)
- [x] 3.1 DONE (2026-06-22): delivered as the `cortex-ops watchdog` `archive_watcher_blind` alarm (Critical when the watcher is healthy but `files_watched==0`) — built in `phase0_recurrent-consolidation-and-retention` §5 (`crates/cortex-cli/src/bin/cortex-ops/watchdog.rs`), seeded as the `health.watchdog` cron (`*/15 * * * *`).
- [x] 3.2 DONE (2026-06-22): delivered as the watchdog `ingest_stale` alarm (no emitter flush in N s, default 1 h) + `sweep_stale`/`consolidation_stale`. Same `health.watchdog` command. Live smoke confirmed it correctly fires `consolidation_missing`.

## 4. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 4.1 Update or create documentation covering the implementation — DONE (2026-06-22): CHANGELOG entry for the two-path ingestion restore (cold archive→in-memory query lane; live adapter daemon→ingestion→Meili/Nexus); watchdog already documented in `docs/specs/19-retention.md` (Coverage watchdog).
- [x] 4.2 Write tests covering the new behavior — DONE (2026-06-22): watchdog behaviour covered by 8 unit tests on `evaluate()` (incl. `files_watched_zero_is_critical`, `stale_ingest_flush_warns`). The §1.1 fix was a docker-compose bind-mount (config, not walker code) — no walker layout code to unit-test; the watcher's `resolve_health_bind`/`resolve_poll_interval` tests pin its config surface.
- [x] 4.3 Run tests and confirm they pass — DONE (2026-06-22): `cargo check --workspace` + `clippy -D warnings` clean; `cargo test --workspace` = 3137 passed.
