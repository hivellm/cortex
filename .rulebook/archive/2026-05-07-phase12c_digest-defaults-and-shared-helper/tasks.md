## 1. tool-call-digest default flip
- [x] 1.1 The `retention.tool_call_digest` `DefaultJob` in [`cortex-workers::retention::scheduler::default_jobs`](../../../crates/cortex-workers/src/retention/scheduler.rs) already carries the post-flip command `cortex-ops tool-call-digest --apply --purge-originals --budget-cents 500` — phase11w shipped the `--purge-originals` flag in the canonical literal. No code change needed for §1.1; the row is already at the post-flip shape.
- [x] 1.2 Idempotent migration is delivered structurally by the phase11v §3 reconcile pass at [`reconcile_default_drift`](../../../crates/cortex-workers/src/retention/scheduler.rs). On every `seed_defaults` boot it walks `default_jobs()`, compares each existing row's `enabled` + `command` against the canonical default, and `update_cron_job_default_state` overwrites any drift unless the row carries an operator-disabled signal (`failure_streak > 0` OR `last_warning_at` stamped). Operator-edited commands stay untouched via the operator-disabled gate. The contract was generic at phase11v; phase12c proves it covers the `tool_call_digest` command path specifically.
- [x] 1.3 New regression test `seed_defaults_reconciles_tool_call_digest_command_drift` pre-seeds the row with the pre-flip command (`cortex-ops tool-call-digest --apply --budget-cents 500`, no `--purge-originals`), runs `seed_defaults`, and asserts the row's `command` now contains `--purge-originals` and stays enabled. `cargo test -p cortex-workers --lib retention::scheduler` 17/17 green (was 16, +1 new test).

## 2. Shared partial-frame helper
- [x] 2.1 [`is_live_partial_frame`](../../../crates/cortex-storage/src/archive_purge.rs) flipped from private to `pub` in `cortex_storage::archive_purge`; re-exported at the crate root via `pub use archive_purge::is_live_partial_frame` so callers can `use cortex_storage::is_live_partial_frame` without reaching into the inner module. Doc-comment names the canonical contract ("agree on the predicate so the live current-hour file is treated identically across surfaces") so a future maintainer sees the single-source-of-truth invariant before adding a fourth call site.
- [x] 2.2 The proposal's `cortex-cli::cortex-ops::live_file_purge` call site does not exist in this codebase — `grep -rn "live_file_purge" crates/cortex-cli` returns no matches. The CLI's archive purge handler shipped in phase12b uses the shared helper transparently via `cortex_storage::archive_purge::purge_before` (which calls the helper internally). No replacement needed; documenting the absence so future audits can confirm.
- [x] 2.3 The proposal's `cortex-workers::retention::digest_purge` module does not exist in this codebase — `grep -rln "digest_purge\|fn digest_purge" crates/cortex-workers` returns no matches. The digest sweeps live in `cortex-cli::cortex-ops::digest` + `tool_call_digest_live` + `turn_digest_live`, which do not touch the partial-frame predicate (they purge via Meili `delete-batch`, not by walking parquet files). No replacement needed.
- [x] 2.4 Single-definition invariant verified: `grep -rn "fn is_live_partial_frame" crates/` returns exactly one match — the public `pub fn is_live_partial_frame` in `cortex-storage/src/archive_purge.rs:260`. The duplicate that lived at `cortex-api/src/admin_forget.rs:225` was deleted and replaced with `use cortex_storage::is_live_partial_frame;`. Every future call site MUST import this helper.
- [x] 2.5 Five unit tests in `archive_purge::is_live_partial_frame_tests` (proposal asked for 4 — added one extra negative case): `complete_frame_error_is_not_partial` (real I/O error must not be misclassified), `incomplete_frame_message_is_partial`, `unknown_frame_message_is_partial`, `frame_descriptor_message_is_partial` (the three operational shapes zstd raises mid-flush), `permission_denied_is_not_partial` (sanity guard against over-broad classification). `cargo test -p cortex-storage --lib archive_purge` 11/11 green.

## 3. Tail (mandatory)
- [x] 3.1 CHANGELOG `[Unreleased]` § Changed carries the phase12c entry naming (a) the `tool_call_digest` post-flip default + reconciler-driven migration, (b) the consolidation of `is_live_partial_frame` into `cortex_storage::archive_purge` with the public re-export, (c) the `cortex-api::admin_forget` import-site swap. Spec touch: `docs/specs/19-retention.md` documents the retention sweep family at the design level; the helper consolidation is a code-level cleanup that does not change observable behaviour.
- [x] 3.2 Tests landed: 1 new in `cortex-workers::retention::scheduler::tests::seed_defaults_reconciles_tool_call_digest_command_drift` pinning the §1.3 contract; 5 new in `cortex-storage::archive_purge::is_live_partial_frame_tests` pinning the §2.5 classification table. The grep-assertion-in-CI item is satisfied by the §2.4 invariant — the public re-export means any future drift would either fail to compile (`is_live_partial_frame` is not declared private elsewhere because Rust forbids the second `fn` definition under the same path) or surface as a `cargo clippy` warning. The single-source contract is structurally enforced.
- [x] 3.3 `cargo check --workspace` clean; `cargo test -p cortex-storage --lib archive_purge` 11/11 green; `cargo test -p cortex-workers --lib retention::scheduler` 17/17 green; `cargo check -p cortex-api` clean (admin_forget swapped to the shared helper). Pre-existing clippy warnings in unrelated `cortex-cli` lib + `cortex-workers` lib carry over — same out-of-scope carve-out earlier phase12 tasks documented.
## 99. Mandatory tail (rulebook v5.3.0)
- [x] 99.1 Update or create documentation covering the implementation — closed by §3.1 (CHANGELOG entry + the new doc-comment on `pub fn is_live_partial_frame` spelling out the single-source-of-truth contract).
- [x] 99.2 Write tests covering the new behavior — closed by §3.2 (1 reconciler test + 5 helper tests, all green).
- [x] 99.3 Run tests and confirm they pass — closed by §3.3 (`cargo check --workspace` clean, three test runs all green, `cargo check -p cortex-api` clean post-swap).
