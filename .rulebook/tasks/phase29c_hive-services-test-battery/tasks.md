## 1. Vectorizer 3.6.0 (Thunder) update
- [x] 1.1 Bump `vectorizer-sdk` 3.5.0 → 3.6.0 (workspace pin + lockfile); `cargo check --workspace --all-targets` for API breakage. — zero breakage; also took nexus:3.0.0-alpha per explicit user order (SDK stays 2.5 — 3.x unpublished; 2.5-client↔3.0-server interop verified live; `NEXUS_AUTH_REQUIRED_FOR_PUBLIC=false` documented opt-out for the 3.0 non-loopback auth gate).
- [x] 1.2 Compose pin `hivehub/vectorizer:3.6.0-fastembed`; pull + recreate — `/health` 3.6.0; data survived in full: 181 collections / 133,081 vectors (early 11/15,100 reading was boot lazy-load, not loss → vectorizer#391).
- [x] 1.3 Rebuilt all 7 cortex images on SDK 3.6 + redeployed; embedder consuming/upserting cleanly (probe event embedded + searchable, top-1 0.875).

## 2. Test battery (live stack)
- [x] 2.1 Vectorizer: REST login + collections + corpus search green. Thunder RPC exercised with the CORRECT client (`vectorizer_sdk::rpc::RpcClient::connect_url`): dial `vectorizer://127.0.0.1:17005` → PING→PONG → HELLO+JWT (AUTH handshake, authenticated=true, 115 caps) → collection.list=181 (matches REST) → search.by_text top-1 0.8754 (matches REST path). Learning: the REST facade does NOT speak `vectorizer://` by design in 3.6 (login = REST-only `POST /auth/login`; RPC sessions authenticate via HELLO token) — facade given `vectorizer://` dies with an opaque reqwest builder error → DX issue vectorizer#392. Error-log sweep clean.
- [x] 2.2 Nexus 3.0.0-alpha: write round-trip green (48/48 backfill via SDK 2.5); `_id` projection STILL null (nexus#29 — repro comment added); undirected `(a)-[r]-(b)` FIXED in 3.0 (2.5 regression gone; Cortex's two-directed-pass BFS workaround retained, harmless); CONTAINS + STARTS WITH + ENDS WITH all green against known fixtures (`a.path CONTAINS "PLANS"` → rows; ENDS WITH → 26); communities/label queries green. Error-log sweep clean.
- [x] 2.3 Synap 1.3: scratch-room lifecycle green (get_or_create→publish→consume byte-array round-trip→stats coherent). Restart resilience: `docker restart cortex-synap` → workers NOT restarted; graph worker self-healed its durable stale cursor (56789 → head 0) within one poll and consumed the probe; BUT the restart exposed the SAME starvation class in the in-memory consumers (classifier/fulltext/embedder cursors beyond the recreated room head; raw room count=1/consumed=0) — Cortex bug #2, fix ported to all three consumers + ITs (see §4.2).
- [x] 2.4 Umbrella: `doctor-smoke` exit 0, 37 READ tools exercised. Warns triaged: 3 workers "degraded (idle)" = the §2.3 starvation (clears with the fix); `cortex_session_timeline` + `cortex_files_touched` timed out (slow endpoints — observation, tracked as future perf item); reranker/claude-adapter skips are unconfigured-optional. Full pipeline flow verified live: probe 01KZ8Z9HQYY23XQQ8VZS5498X1 → Meili + Vectorizer + graph (`(Session)-[:REMEMBERS]->(Memory)` projected on Nexus 3.0.0-alpha).

## 3. Upstream issue filing (for every CONFIRMED bug)
- [x] 3.1 Filed after double-reproduction + Cortex-misuse rule-out: vectorizer#391 (boot lazy-load makes `GET /collections` partial listing indistinguishable from data loss), vectorizer#392 (REST facade accepts `vectorizer://` base_url, fails with opaque reqwest builder error — asymmetric with RpcClient's clear http:// rejection), nexus#29 comment (3.0.0-alpha repro: `n._id` still null + release-notes prominence suggestion for the new auth gate). The undirected zero-rows regression needed NO filing — fixed in 3.0.0-alpha. Both starvation bugs were Cortex-side (ours), fixed in-repo.

## 4. Tail (docs + tests — check or waive with tailWaiver)
- [x] 4.1 Docs: spec 03 pin rows (vectorizer 3.6.0-fastembed with Thunder RPC port label; nexus 3.0.0-alpha with the auth-gate opt-out note), CHANGELOG entry (update round + battery findings + both starvation fixes), findings recorded inline in §1–§3 above.
- [x] 4.2 Regression tests: `synap_room_selfheal_it.rs` `stale_offset` module grew from 1 to 4 ITs — durable-ledger heal (graph, `consumer_offset_set` force-rewind after the MAX()-guard lesson) + in-memory heal per consumer (fulltext, embedder, classifier — classifier IT also asserts the sibling room's tracker is untouched by the per-room stale bound).
- [x] 4.3 Gates green: cargo check, clippy --workspace --all-targets -D warnings, selfheal IT 6/6; full workspace suite run before commit (see commit).
