## 1. cortex-classifier → cortex-workers/src/classifier_lib/

- [ ] 1.1 Move `crates/cortex-classifier/src/**` to `crates/cortex-workers/src/classifier_lib/`. Rename the existing `cortex-workers/src/classifier_worker/` only if a name collision arises during the move (the daemon glue that consumes the lib stays under its current path otherwise). Update the moved files' module declarations + crate-relative `crate::` imports so the tree resolves under `cortex_workers::classifier_lib::*`.
- [ ] 1.2 Add `pub mod classifier_lib;` and `pub use classifier_lib as classifier;` in `crates/cortex-workers/src/lib.rs` so external imports resolve as `cortex_workers::classifier::*`. Keep `_lib` internal (no cross-crate path uses the underscore form).
- [ ] 1.3 Move `crates/cortex-classifier/tests/*` to `crates/cortex-workers/tests/`, prefixing each filename with `classifier_` (`prompt.rs` → `classifier_prompt.rs`). Update test imports from `cortex_classifier::*` to `cortex_workers::classifier::*`.
- [ ] 1.4 Remove `cortex-classifier` from the workspace `members` array in the root `Cargo.toml`. Drop the `cortex-classifier = { path = "../cortex-classifier" }` path-dep entry from `crates/cortex-workers/Cargo.toml`. Replace every `use cortex_classifier::*` inside `cortex-workers` with the new `crate::classifier::*` (or `crate::classifier_lib::*`) path.
- [ ] 1.5 Verify with `cargo check -p cortex-workers` zero errors, then `cargo test -p cortex-workers` 100 % pass for the classifier-related test files. Delete `crates/cortex-classifier/` only after both gates are green.

## 2. cortex-ingestion → cortex-workers/src/ingestion/ + bin

- [ ] 2.1 Move `crates/cortex-ingestion/src/**` (lib code only — bin handled in §2.2) to `crates/cortex-workers/src/ingestion/`. Update internal `crate::` paths.
- [ ] 2.2 Move `crates/cortex-ingestion/src/bin/cortex-ingestion.rs` to `crates/cortex-workers/src/bin/cortex-ingestion.rs`. Add `[[bin]] name = "cortex-ingestion"` + `path = "src/bin/cortex-ingestion.rs"` to `crates/cortex-workers/Cargo.toml`. Bin name preserved.
- [ ] 2.3 Add `pub mod ingestion;` to `crates/cortex-workers/src/lib.rs` re-exporting the ingestion API surface as `cortex_workers::ingestion::*`.
- [ ] 2.4 Move `crates/cortex-ingestion/tests/*` to `crates/cortex-workers/tests/` with the `ingestion_` filename prefix. Repoint test imports.
- [ ] 2.5 Drop `cortex-ingestion = { path = "../cortex-ingestion" }` from `crates/cortex-cli/Cargo.toml:38`. Repoint every `use cortex_ingestion::*` in `cortex-cli` to `cortex_workers::ingestion::*`. If `cortex-cli` did not already declare a path dep on `cortex-workers`, add `cortex-workers = { path = "../cortex-workers" }`.
- [ ] 2.6 Update the comment-only mention in `crates/cortex-health/Cargo.toml:17` from `cortex-ingestion` to `cortex-workers (ingestion module)`.
- [ ] 2.7 Remove `cortex-ingestion` from the workspace `members` array in the root `Cargo.toml`.
- [ ] 2.8 Verify `cargo check -p cortex-workers -p cortex-cli -p cortex-health` zero errors, `cargo test -p cortex-workers -p cortex-cli` 100 % pass on the ingestion-related tests. Verify `cargo build --bin cortex-ingestion` produces a runnable binary identical in surface to the old one (`--help` output diff = empty). Delete `crates/cortex-ingestion/` only after gates are green.

## 3. cortex-claude-archive → cortex-workers/src/claude_archive/ + bin (feature-gated)

- [ ] 3.1 Add a `claude-archive` feature flag in `crates/cortex-workers/Cargo.toml` `[features]` section. Pull the heavy deps (`notify`, `notify-debouncer-mini`, `cron`, `flate2`, `zstd`, `rusqlite`) under `claude-archive = ["dep:notify", "dep:notify-debouncer-mini", "dep:cron", "dep:flate2", "dep:zstd", "dep:rusqlite"]` (mark the deps `optional = true` where they are not already used unconditionally by other workers modules — verify each dep against the current `cortex-workers/Cargo.toml` before flipping).
- [ ] 3.2 Move `crates/cortex-claude-archive/src/**` (lib code only) to `crates/cortex-workers/src/claude_archive/`. Update internal `crate::` paths. Wrap the module declaration in `cortex-workers/src/lib.rs` with `#[cfg(feature = "claude-archive")] pub mod claude_archive;`.
- [ ] 3.3 Move `crates/cortex-claude-archive/src/bin/cortex-claude-archive.rs` to `crates/cortex-workers/src/bin/cortex-claude-archive.rs`. Add `[[bin]] name = "cortex-claude-archive"` + `path = "src/bin/cortex-claude-archive.rs"` + `required-features = ["claude-archive"]` to `crates/cortex-workers/Cargo.toml`. Bin name preserved.
- [ ] 3.4 Move `crates/cortex-claude-archive/tests/*` to `crates/cortex-workers/tests/` with the `claude_archive_` filename prefix. Gate each test file with `#![cfg(feature = "claude-archive")]` at the top so the suite still passes when the feature is off.
- [ ] 3.5 Remove `cortex-claude-archive` from the workspace `members` array in the root `Cargo.toml`.
- [ ] 3.6 Verify `cargo check -p cortex-workers` (default features) zero errors AND zero new warnings, `cargo check -p cortex-workers --features claude-archive` zero errors, `cargo test -p cortex-workers --features claude-archive` 100 % pass on claude_archive_* tests. Verify `cargo build --bin cortex-claude-archive --features claude-archive` produces a runnable binary; `cargo build --bin cortex-claude-archive` (without feature) MUST fail cleanly with the standard `required-features` clap message. Delete `crates/cortex-claude-archive/` only after gates are green.

## 4. cortex-consolidator → cortex-workers/src/consolidator/ + bin (feature-gated)

- [ ] 4.1 Audit the duplicate bin entry in `crates/cortex-cli/Cargo.toml:26-27` (`[[bin]] name = "cortex-consolidator"` + `path = "src/bin/cortex-consolidator.rs"`). Open the trampoline file at `crates/cortex-cli/src/bin/cortex-consolidator.rs` and the lib-side bin at `crates/cortex-consolidator/src/bin/cortex-consolidator.rs` side-by-side. Three outcomes: (a) trampoline only → record finding in commit message and plan to delete the cli-side file in §4.4; (b) divergent code → reconcile by porting any cli-side logic into the workers-side bin in §4.3, recording the diff in the §6.5 ADR; (c) cli-side adds CLI ergonomics (clap subcommands wrapping the lib) → keep the cli-side as a thin shell-out wrapper, mark the workers bin as the canonical operator entry point. Item §4.1 fails until exactly one of the three outcomes is recorded inline as a `// findings:` comment in the eventual commit body for §4.4.
- [ ] 4.2 Add a `consolidator` feature flag in `crates/cortex-workers/Cargo.toml` gating the Anthropic-API-shaped deps the consolidator adds (`reqwest` already lives at workspace root — no new pull; `chrono` likewise; verify each before adding). Make the feature `default = ["consolidator"]` ON because phase11j and phase11r both depend on it; `claude-archive` stays OFF by default to keep the operator-facing daemons opt-in.
- [ ] 4.3 Move `crates/cortex-consolidator/src/**` (lib code only) to `crates/cortex-workers/src/consolidator/`. Update internal `crate::` paths. Wrap the module declaration in `cortex-workers/src/lib.rs` with `#[cfg(feature = "consolidator")] pub mod consolidator;`. Move `crates/cortex-consolidator/src/bin/cortex-consolidator.rs` to `crates/cortex-workers/src/bin/cortex-consolidator.rs`. Add `[[bin]] name = "cortex-consolidator"` + `path = "src/bin/cortex-consolidator.rs"` + `required-features = ["consolidator"]` to workers Cargo.toml. Bin name preserved. Apply the §4.1 reconciliation outcome in this step.
- [ ] 4.4 Drop `cortex-consolidator = { path = "../cortex-consolidator" }` from `crates/cortex-cli/Cargo.toml:40`. Drop the duplicate `[[bin]] name = "cortex-consolidator"` entry on lines 26-27 + the file `crates/cortex-cli/src/bin/cortex-consolidator.rs` (per §4.1 outcome). Repoint every `use cortex_consolidator::*` in any consumer (cortex-cli, future phase11r authoring) to `cortex_workers::consolidator::*`. Move `crates/cortex-consolidator/tests/*` to `crates/cortex-workers/tests/` with the `consolidator_` filename prefix.
- [ ] 4.5 Remove `cortex-consolidator` from the workspace `members` array in the root `Cargo.toml`.
- [ ] 4.6 Verify `cargo check -p cortex-workers -p cortex-cli` zero errors, `cargo test -p cortex-workers -p cortex-cli` 100 % pass on consolidator_* + cli tests. Verify `cargo build --bin cortex-consolidator` produces a runnable binary identical in surface to the old one (`--help` diff = empty). Delete `crates/cortex-consolidator/` only after gates are green.

## 5. cortex-retention → cortex-workers/src/retention/ + new sweep bin

- [ ] 5.1 Move `crates/cortex-retention/src/**` to `crates/cortex-workers/src/retention/`. Update internal `crate::` paths. Add `pub mod retention;` to `crates/cortex-workers/src/lib.rs` re-exporting as `cortex_workers::retention::*`.
- [ ] 5.2 Move `crates/cortex-retention/tests/*` to `crates/cortex-workers/tests/` with the `retention_` filename prefix. Repoint test imports from `cortex_retention::*` to `cortex_workers::retention::*`.
- [ ] 5.3 Drop `cortex-retention = { path = "../cortex-retention" }` from `crates/cortex-api/Cargo.toml:25` and `crates/cortex-cli/Cargo.toml:39`. If either crate did not already declare a path dep on `cortex-workers`, add `cortex-workers = { path = "../cortex-workers" }`. Repoint every `use cortex_retention::*` in cortex-api and cortex-cli to `cortex_workers::retention::*`.
- [ ] 5.4 Add a new `cortex-retention-sweep` bin under `crates/cortex-workers/src/bin/cortex-retention-sweep.rs` that exposes the retention sweep that today runs via the `cortex-cli sweep` subcommand. Bin spec: parses `--dry-run` (default false), `--scope <repo>`, `--max-age-days <N>`, `--verbose`; reads `CORTEX_RETENTION_*` env vars consistent with the cortex-cli surface; emits the same audit envelopes the cortex-cli code path emits today; exits non-zero on any partial failure. Add `[[bin]] name = "cortex-retention-sweep"` + `path = "src/bin/cortex-retention-sweep.rs"` to workers Cargo.toml.
- [ ] 5.5 Update `cortex-cli` `sweep` subcommand to delegate to the same `cortex_workers::retention::run_sweep(...)` entry point the new bin uses (NOT shell out — call the lib API in-process so error surfaces and audit envelopes stay identical). Verify `cortex-cli sweep --help` and `cortex-retention-sweep --help` diff is limited to the binary header line.
- [ ] 5.6 Remove `cortex-retention` from the workspace `members` array in the root `Cargo.toml`.
- [ ] 5.7 Verify `cargo check -p cortex-workers -p cortex-api -p cortex-cli` zero errors, `cargo test -p cortex-workers -p cortex-api -p cortex-cli` 100 % pass on retention_* + api + cli tests. Verify `cargo build --bin cortex-retention-sweep` produces a runnable binary; `cortex-cli sweep` against a fixture corpus produces an output byte-equivalent to the pre-merge baseline. Delete `crates/cortex-retention/` only after gates are green.

## 6. Tail (mandatory — enforced by rulebook v5.3.0)

- [ ] 6.1 Update or create documentation covering the implementation — CHANGELOG `[Unreleased] → Changed` entry covering the workspace-member 14 → 9 reduction and the new feature-flag scheme; `docs/architecture.md` §3 (services) updated to fold the five merged crates into the cortex-workers entry, with a footnote pointing to ADR-007; `docs/specs/04-cortex-core.md` (or whichever spec enumerates the workspace dep graph) updated to reflect the new module hierarchy under `cortex-workers/src/`; `docs/specs/05-classifier.md`, `09-bootstrap-cli.md` updated where they cite the now-merged crate names; `docs/cortex/workers-modules.md` written from scratch documenting the module → bin map (which feature gates which bin, which `cortex_workers::*` paths the public surface exposes).
- [ ] 6.2 Write tests covering the new behavior — sum of relocated tests across §1-§5 (existing tests pass byte-equivalently after import path update). Plus 4 new ITs: `crates/cortex-workers/tests/feature_gates_it.rs` (asserts `cargo check -p cortex-workers --features claude-archive` succeeds, `cargo build --bin cortex-claude-archive` without the feature fails with the expected `required-features` message); `crates/cortex-workers/tests/bin_surface_parity_it.rs` (runs `--help` on each of the 9 bins and pins stdout against a checked-in golden file so the merge does not silently drop a CLI flag); `crates/cortex-workers/tests/module_re_export_it.rs` (compile-time test asserting `cortex_workers::{classifier, ingestion, claude_archive, consolidator, retention}` paths resolve to the moved types); `crates/cortex-cli/tests/sweep_parity_it.rs` (runs `cortex-cli sweep --dry-run` against a fixture and pins the audit-envelope output against a golden file).
- [ ] 6.3 Run tests and confirm they pass — `cargo check --workspace --all-features` clean. `cargo test --workspace` 100 % pass with the new ITs. `cargo clippy --workspace --no-deps --tests -- -D warnings` zero warnings (existing pre-existing items inherited from earlier phases stay untouched per surgical-changes rule; if any pre-existing item now lives inside cortex-workers because it was in a moved crate, document the inheritance in the commit body without fixing it inside this task — fix-forward stays on the original task that introduced it). `cargo build --workspace --all-features --bins` clean. `cargo build --workspace --bins` (default features) clean — verifies the default-feature surface still produces every operator bin EXCEPT the `claude-archive`-gated one.
- [ ] 6.4 Capture ≥ 1 learning via `rulebook_learn_capture` covering any non-obvious edge case discovered during the merge (likely candidates: feature-flag interaction with `required-features` on the `claude-archive` bin, the `cortex-cli sweep` ↔ `cortex-retention-sweep` parity contract, the duplicate-bin reconciliation outcome from §4.1). Capture ≥ 1 pattern via `rulebook_knowledge_add` covering "worker-style daemons live in cortex-workers behind a feature flag — do not mint a new top-level crate". Measure cold-build time for the workspace before the merge (`cargo clean && time cargo build --workspace --all-features --bins`) and after, record both numbers in the learning entry so the build-time regression / improvement is empirical rather than estimated.
- [ ] 6.5 ADR-007 `cortex-workers-as-the-default-host-for-worker-style-daemons` created (status: proposed) — pins the rule that future worker-style daemons land in `cortex-workers` behind a feature flag rather than minting a new workspace member; documents the §4.1 duplicate-bin reconciliation outcome (which path won, why, what was discarded); lists the rejected alternatives (keep five separate crates for compile parallelism, mint a new `cortex-daemons` umbrella crate, push everything into `cortex-cli` as subcommands); documents the blast radius for future merges so the decision compounds. Related tasks: phase11j + phase11r + phase11s.
