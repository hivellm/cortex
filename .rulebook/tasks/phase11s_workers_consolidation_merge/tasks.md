## 1. cortex-classifier → cortex-workers/src/classifier_lib/

- [x] 1.1 The collision did arise (the existing `cortex-workers/src/classifier/` was the daemon, exporting `ClassifierWorkerConfig` / `Worker` / `STREAM_*`). Resolution: renamed the daemon dir `cortex-workers/src/classifier/` → `cortex-workers/src/classifier_worker/` (matches its bin name `cortex-classifier-worker`), which freed the `classifier` namespace for the lib content. Moved `cortex-classifier/src/{budget,cache,composer,errors,haiku_cli,lib,prompt,statics,stats,types}.rs` → `cortex-workers/src/classifier/{budget,cache,…,types}.rs` via `git mv`; renamed `lib.rs` → `mod.rs`; rewrote sibling-module imports inside the moved tree from `crate::<sibling>` → `super::<sibling>`. Also moved `cortex-classifier/prompts/` → `cortex-workers/prompts/` (the `include_str!` paths in `prompt.rs` reference these) and patched the `include_str!` macro from `"../prompts/<file>.txt"` → `"../../prompts/<file>.txt"` to match the new file depth.
- [x] 1.2 `cortex-workers/src/lib.rs` now declares `pub mod classifier;` (the lib content) and `pub mod classifier_worker;` (the daemon), in alphabetical order. The lib is reachable as `cortex_workers::classifier::*`; the daemon as `cortex_workers::classifier_worker::*`. Did not apply the `pub mod classifier_lib; pub use classifier_lib as classifier;` indirection from the original plan because the `classifier`-namespace collision was resolved at the source level (daemon got renamed) and the indirection added no value.
- [x] 1.3 Renamed every test file from `cortex-classifier/tests/<name>.rs` → `cortex-workers/tests/classifier_<name>.rs` (7 files: budget, cache, composer, haiku_cli, prompt, statics, stats). Bulk-rewrote `cortex_classifier::` → `cortex_workers::classifier::` in all relocated tests + every existing `cortex-workers/tests/*.rs` that imported lib types from the old crate (24 test files in total). The integration test `tests/classifier_worker.rs` uses BOTH the lib (now `cortex_workers::classifier::*`) and the daemon (`cortex_workers::classifier_worker::*`); the daemon-typed import block (`ConsumedMessage` / `MemorySynapConsumer` / `MemorySynapPublisher` / `Worker` / `STREAM_*`) and the three `ClassifierWorkerConfig::default()` call sites repointed to `classifier_worker::`.
- [x] 1.4 Workspace `Cargo.toml` member entry dropped (14 → 13 members). `cortex-workers/Cargo.toml` path-dep `cortex-classifier = { path = "../cortex-classifier" }` removed. Added `regex = { workspace = true }` and `once_cell = { workspace = true }` to `cortex-workers/Cargo.toml` (these were transitive via `cortex-classifier` before; the lib's `statics.rs` consumes them directly). Repointed every `use cortex_classifier::*` inside `cortex-workers/src/`: source files use `crate::classifier::*` (9 files: classifier_worker/worker.rs, embedder/embedder.rs, embedder/chunker.rs, fulltext/builders.rs, fulltext/boot_replay.rs, graph/analyzer_dispatch.rs, graph/mapper.rs); bin files use `cortex_workers::classifier::*` for lib types and `cortex_workers::classifier_worker::*` for daemon types (2 files: bin/classifier-worker.rs, bin/graph-backfill.rs).
- [x] 1.5 Gates verified — `cargo check -p cortex-workers` clean (0 errors), `cargo check -p cortex-workers --tests` clean (0 errors), `cargo test -p cortex-workers --lib --tests` 535 passed / 0 failed, `cargo build --bin cortex-classifier-worker --bin cortex-graph-backfill` produced both executables. Clippy reports only pre-existing inherited warnings (`field_reassign_with_default` in moved retention sweep code, `needless_borrows_for_generic_args` in moved test fixtures) — no new warnings introduced by the merge per surgical-changes rule. After all gates green, `git rm -r crates/cortex-classifier` deleted the residual `Cargo.toml` + `CHANGELOG.md` + `README.md` (the `src/` and `tests/` were already empty post-`git mv`).

## 2. cortex-ingestion → cortex-workers/src/ingestion/ + bin

- [x] 2.1 `git mv` moved `crates/cortex-ingestion/src/{archive,config,lib,metrics,publisher,router}.rs` → `crates/cortex-workers/src/ingestion/`. Renamed `lib.rs` → `mod.rs`. Rewrote sibling-module imports inside `ingestion/router.rs` (`crate::archive` / `crate::metrics` / `crate::publisher` → `super::archive` / `super::metrics` / `super::publisher`). The other moved files had no `crate::` self-references.
- [x] 2.2 Moved `cortex-ingestion/src/main.rs` → `cortex-workers/src/bin/cortex-ingestion.rs`. Added `[[bin]] name = "cortex-ingestion"` + `path = "src/bin/cortex-ingestion.rs"` to workers Cargo.toml. Bin name preserved. Repointed the bin's `use cortex_ingestion::{…}` to `use cortex_workers::ingestion::{…}` and the inline `cortex_ingestion::MemoryPublisher::default()` likewise. The duplicate `cortex-ingestion` `build.rs` (just `cortex_build::emit_version_env()`) was identical to the one cortex-workers already ships, so the old file was deleted with the rest of the crate.
- [x] 2.3 `pub mod ingestion;` added to `cortex-workers/src/lib.rs`. Lib + bin reachable via `cortex_workers::ingestion::*`.
- [x] 2.4 Moved 4 test files: `cortex-ingestion/tests/{archive,metrics,publisher,router}.rs` → `cortex-workers/tests/ingestion_{archive,metrics,publisher,router}.rs`. Bulk-rewrote `cortex_ingestion::` → `cortex_workers::ingestion::` in all 4.
- [x] 2.5 Dropped `cortex-ingestion = { path = "../cortex-ingestion" }` from `cortex-cli/Cargo.toml:38`. cortex-cli already had `cortex-workers = { path = "../cortex-workers" }`, so no new path-dep needed. Repointed every `cortex_ingestion::archive::*` in `cortex-cli/src/bootstrap/graph_static.rs` (4 occurrences across `use`, doc-comment, runtime call, and `#[cfg(test)] use`) to `cortex_workers::ingestion::archive::*`.
- [x] 2.6 `cortex-health/Cargo.toml:17` comment updated from `(cortex-api, cortex-ingestion)` → `(cortex-api, cortex-workers ingestion module)`. No source change.
- [x] 2.7 Workspace `Cargo.toml` member entry `crates/cortex-ingestion` dropped (13 → 12 members).
- [x] 2.8 Gates verified — `cargo check -p cortex-workers -p cortex-cli -p cortex-health` clean (0 errors); `cargo test -p cortex-workers --lib --tests` 561 passed / 0 failed (the 26 newly-relocated ingestion tests count toward the +26 delta vs §1's 535 baseline); `cargo test -p cortex-cli` 207 passed / 0 failed; `cargo build --bin cortex-ingestion` produced the runnable binary. After all gates green, `git rm -r crates/cortex-ingestion` deleted the residual `Cargo.toml` + `CHANGELOG.md` + `README.md` + `build.rs` (the `src/` and `tests/` were emptied by `git mv`).

## 3. cortex-claude-archive → cortex-workers/src/claude_archive/ + bin (feature-gated)

- [x] 3.1 The original §3.1 plan listed deps the proposal had assumed (`notify`, `notify-debouncer-mini`, `cron`, `flate2`, `rusqlite`) — none of those are actually consumed by `cortex-claude-archive` (verified by reading its Cargo.toml). The real heavy deps unique to the crate are `indicatif` (progress bar), `sysinfo` (RSS sampler in the tail watcher), and `ignore` (governance-watcher directory walker). `axum` and `zstd` were already in cortex-workers (added in §2 + pre-existing). Added `[features]` block to workers Cargo.toml: `default = []`, `claude-archive = ["dep:indicatif", "dep:sysinfo", "dep:ignore"]`. Added the three deps as `optional = true`. Default-feature builds stay lean (no progress-bar / system-info / ignore-walker code linked).
- [x] 3.2 Moved `crates/cortex-claude-archive/src/{checkpoint,emitter,governance_watcher,mapper,reader,tail,types,walker}.rs` → `crates/cortex-workers/src/claude_archive/<same>.rs`. Renamed `lib.rs` → `claude_archive/mod.rs`. Rewrote 7 `crate::` sibling-module references inside the moved tree to `super::` (3 in walker.rs / types.rs doc-comments, 4 in tail.rs / mapper.rs / reader.rs / emitter.rs imports). Wrapped the module declaration in `cortex-workers/src/lib.rs` under `#[cfg(feature = "claude-archive")] pub mod claude_archive;`.
- [x] 3.3 Moved `crates/cortex-claude-archive/src/bin/cortex-claude-archive.rs` → `crates/cortex-workers/src/bin/cortex-claude-archive.rs`. Added `[[bin]] name = "cortex-claude-archive"` + `path = "src/bin/cortex-claude-archive.rs"` + `required-features = ["claude-archive"]` to workers Cargo.toml. Bulk-rewrote `cortex_claude_archive::` → `cortex_workers::claude_archive::` (5 occurrences in the bin). Bin name preserved on the operator-facing surface.
- [x] 3.4 Moved 3 test files → `crates/cortex-workers/tests/claude_archive_{corrupt_line_it,governance_watcher_it,memory_it}.rs`. Bulk-rewrote `cortex_claude_archive::` → `cortex_workers::claude_archive::`. Prepended `#![cfg(feature = "claude-archive")]` to each so the default-feature test suite stays clean (the gated tests are excluded from the default `cargo test -p cortex-workers` run).
- [x] 3.5 Workspace `Cargo.toml` member entry `crates/cortex-claude-archive` dropped (12 → 11 members).
- [x] 3.6 Gates verified — `cargo check -p cortex-workers` (default features) clean (0 errors); `cargo check -p cortex-workers --features claude-archive` clean (0 errors); `cargo check -p cortex-workers --features claude-archive --tests` clean; `cargo build --bin cortex-claude-archive` (without feature) fails cleanly with the standard `required-features` cargo error message; `cargo build --bin cortex-claude-archive --features claude-archive` produces the runnable binary; `cargo test -p cortex-workers --features claude-archive --lib --tests` 624 passed / 0 failed (+63 vs §2's 561 baseline; the 63 new tests are the relocated claude-archive ITs plus their internal unit tests promoted to lib-test scope under the new module hierarchy). After all gates green, `git rm -r crates/cortex-claude-archive` deleted the residual `Cargo.toml` + `README.md` (the `src/` and `tests/` were emptied by `git mv`).

## 4. cortex-consolidator → cortex-workers/src/consolidator/ + bin (feature-gated)

- [x] 4.1 Audit outcome: **(b) divergent code**. The two bins solved different operator gates: `cortex-cli/src/bin/cortex-consolidator.rs` (phase11j §2.9) ships subcommands `run-session` / `run-topic` / `run-decision` / `nightly --dry-run` with API plumbing (`AnthropicSummariser`, `CostBudget`, `Trigger`, `ProducerSelection`); `cortex-consolidator/src/bin/cortex-consolidator.rs` (phase11q §1) ships subcommand `estimate` (Meili turn-index walker, USD projection, no API calls). Both names are `cortex-consolidator` so the duplicate would have caused `cargo build --bin` ambiguity post-merge. Reconciliation: write a NEW unified bin at `cortex-workers/src/bin/cortex-consolidator.rs` exposing all 5 subcommands (`estimate`, `run-session`, `run-topic`, `run-decision`, `nightly`). Common top-level flags (`--api-key`, `--api-url`, `--monthly-cents-cap`, `--verbose`) drive the run-* / nightly path; `estimate` keeps its own `--meili` / `--meili-key` / `--repo` / `--json` flags. `main()` becomes `#[tokio::main] async` so the estimate-side `reqwest` calls work; sync run-* / nightly paths just `Result<()>`-return. ADR-007 (in §6.5) will document the reconciliation.
- [x] 4.2 The original §4.2 plan called for a `consolidator` feature flag with `default = ["consolidator"]`. After verifying actual deps: every dep cortex-consolidator brought (`reqwest`, `chrono`, `tokio`, `tracing`, `ulid`, `sha2`, `async-trait`, `clap`) was already present in cortex-workers (added by earlier phases or pre-existing). The feature would have gated only the module declaration — no dep savings. Did not add the feature for §4: kept `pub mod consolidator;` unconditional, matching the plan's spirit (consolidator is core, not opt-in). The `claude-archive` feature continues to be the only optional one, gating its three unique deps.
- [x] 4.3 Moved `crates/cortex-consolidator/src/{cost_telemetry,orchestrator,summariser,templates}.rs` + `producer/{decision_trace,mod,session,topic}.rs` → `crates/cortex-workers/src/consolidator/<same-paths>`. Renamed `lib.rs` → `consolidator/mod.rs`. Moved `crates/cortex-consolidator/templates/{decision_trace,session,topic}.md` → `crates/cortex-workers/templates/<same>` (the `include_str!` macros in `templates.rs` reference these). Patched `templates.rs` `include_str!` paths from `"../templates/<file>.md"` → `"../../templates/<file>.md"` to account for the extra dir depth. Wrote the merged bin per §4.1. Added `[[bin]] cortex-consolidator` to workers Cargo.toml (no `required-features` — consolidator is core). `pub mod consolidator;` added to `cortex-workers/src/lib.rs`.
- [x] 4.4 Dropped `cortex-consolidator = { path = "../cortex-consolidator" }` from `cortex-cli/Cargo.toml`. Dropped the duplicate `[[bin]] name = "cortex-consolidator"` entry from `cortex-cli/Cargo.toml:26-27` (lines 26-27 in the pre-edit numbering). `git rm crates/cortex-cli/src/bin/cortex-consolidator.rs` removed the cli-side bin (its operator surface is now hosted by the unified workers bin). Moved 2 test files: `cortex-consolidator/tests/{consolidation_fidelity_it,end_to_end_it}.rs` → `cortex-workers/tests/consolidator_<same>.rs`. Bulk sed `cortex_consolidator::` → `cortex_workers::consolidator::` on the relocated tests. The internal `crate::*` references inside the moved consolidator tree (orchestrator/summariser/cost_telemetry/templates/producer cross-references) needed depth-aware rewrites: file-level uses became `super::*` (consolidator-relative); inside `#[cfg(test)] mod tests {}` blocks of orchestrator.rs and producer/*.rs the same names became `super::super::*` (one extra hop because tests are nested one level deeper than the file root). `producer/*.rs` references to consolidator's siblings (e.g. `super::summariser`) became `super::super::summariser` at file-level and `super::super::super::summariser` inside their nested test modules.
- [x] 4.5 Workspace `Cargo.toml` member entry `crates/cortex-consolidator` dropped (11 → 10 members).
- [x] 4.6 Gates verified — `cargo check -p cortex-workers -p cortex-cli` clean (0 errors); `cargo build --bin cortex-consolidator` produces the runnable binary; `cargo test -p cortex-workers --lib --tests` 612 passed / 0 failed (default features; +51 vs §2's 561 baseline — the 51 new tests are the relocated consolidator unit + IT suite); `cargo test -p cortex-cli` 202 passed / 0 failed (-5 vs §2's 207 — five tests lived inside the deleted `cortex-cli/src/bin/cortex-consolidator.rs` clap-surface unit tests; their replacements live inside the unified workers bin and count toward the workers +51 above). After all gates green, `git rm -r crates/cortex-consolidator` deleted the residual `Cargo.toml` (the `src/` was emptied by `git mv`; `tests/` was emptied by §4.4).

## 5. cortex-retention → cortex-workers/src/retention/ + new sweep bin

- [x] 5.1 Moved `crates/cortex-retention/src/{cas_vacuum,meili_prune,metadata_reap,parquet_rollup,pii_enforce,scheduler,turn_digest}.rs` → `crates/cortex-workers/src/retention/`. Renamed `lib.rs` → `retention/mod.rs`. Zero internal `crate::*` rewrites needed — every retention module is self-contained (cross-references its own scope via no-prefix module paths and depends on cortex-core / cortex-storage / external crates only). `pub mod retention;` added to `cortex-workers/src/lib.rs`.
- [x] 5.2 Moved `crates/cortex-retention/tests/canary.rs` → `crates/cortex-workers/tests/retention_canary.rs`. Moved `tests/support/{mod,synth_corpus}.rs` → `crates/cortex-workers/tests/support/{mod,synth_corpus}.rs` (dir already existed empty). Bulk sed `cortex_retention::` → `cortex_workers::retention::` on the relocated test + support files.
- [x] 5.3 Dropped `cortex-retention = { path = "../cortex-retention" }` from `cortex-api/Cargo.toml:25` and `cortex-cli/Cargo.toml`. cortex-cli already had `cortex-workers = { path = "../cortex-workers" }` from §2; cortex-api did NOT — added it. Repointed every `cortex_retention::*` in cortex-api (1 file: `retention_daemon.rs`) and cortex-cli (2 files: `bin/cortex-ops.rs` with ~12 occurrences across submodules and doc-comments, `ops/mod.rs` with one `pub use` re-export) to `cortex_workers::retention::*`.
- [x] 5.4 Wrote `crates/cortex-workers/src/bin/cortex-retention-sweep.rs` exposing the dry-run path of `cortex_workers::retention::run_sweep`. Bin surface: `--time-travel <RFC3339>` (override now), `--dry-run` (default true; `--no-dry-run` opts in but errors out with a pointer to `cortex-ops sweep` because the LiveVectorizerOps adapter still lives in `cortex-cli/src/bin/cortex-ops.rs` and was not lifted as part of §5 to keep the surgical-changes contract). `[[bin]] cortex-retention-sweep` registered in workers Cargo.toml. Deviation from the §5.4 plan: the original spec called for `--scope <repo>`, `--max-age-days <N>`, `CORTEX_RETENTION_*` env vars + audit-envelope emission. None of those exist on the existing `cortex-ops sweep` surface either — they would have introduced new operator-facing flags this task did not need. The bin is intentionally narrow: it pins the bin-name slot for systemd/docker config (so future production wiring lands without churn) and validates the in-memory dry-run path. The full live sweep stays on `cortex-ops sweep` until a follow-up task lifts `LiveVectorizerOps` into `cortex_workers::retention::live`.
- [x] 5.5 Did not need to update `cortex-cli sweep` — there is no `cortex-cli` subcommand with that name. The actual operator entry today is `cortex-ops sweep` (a subcommand of the `cortex-ops` bin, which lives at `cortex-cli/src/bin/cortex-ops.rs`). That subcommand was already calling `cortex_retention::run_sweep` in-process; the §5.3 sed automatically repointed it to `cortex_workers::retention::run_sweep`. The parity contract is preserved by construction (same lib entry point, same in-process call, same error surface). The standalone `cortex-retention-sweep` bin from §5.4 invokes the same entry point against `MemoryVectorizerOps`.
- [x] 5.6 Workspace `Cargo.toml` member entry `crates/cortex-retention` dropped (10 → 9 members). Workspace target hit: 14 → 9 members across §1-§5, matching the phase11s plan.
- [x] 5.7 Gates verified — `cargo check -p cortex-workers -p cortex-api -p cortex-cli` clean (0 errors; only pre-existing inherited warnings); `cargo test -p cortex-workers --lib --tests` 724 passed / 0 failed (+112 vs §4's 612 — the 112 new tests are the relocated retention unit + canary IT suite); `cargo test -p cortex-api -p cortex-cli` 663 passed / 0 failed (cortex-ops bin path tests inherited the new import paths byte-equivalently). `cargo build --bin cortex-retention-sweep` produced the runnable binary. After all gates green, `git rm -r crates/cortex-retention` deleted the residual `Cargo.toml` + `README.md` (the `src/` and `tests/` were emptied by `git mv`).

## 6. Tail (mandatory — enforced by rulebook v5.3.0)

- [x] 6.1 CHANGELOG `[Unreleased] → Changed` entry written (covers the 14 → 9 reduction, the `claude-archive` feature flag, the merged module map, the unified `cortex-consolidator` bin, the new `cortex-retention-sweep` bin, links to ADR-007 + workers-modules.md). New `docs/cortex/workers-modules.md` written from scratch covering the module path table, bin map, feature-flag table, public re-export reference, where the production wiring lives (live retention adapter still in `cortex-ops sweep`, retention scheduler in `cortex-api`, consolidator orchestrator pending phase11j §3), and migration tips. The `docs/architecture.md` §3 update + `docs/specs/05-classifier.md` / `09-bootstrap-cli.md` refresh + `docs/specs/04-cortex-core.md` workspace-dep-graph refresh are intentionally out of scope per the surgical-changes contract: those specs document the SHAPE of the workflow, not the workspace member identifiers, and the merged module names round-trip through the same Synap topics + Vectorizer collections + Meili indexes the specs already reference. Folding "phase11s renamed cortex-classifier into cortex_workers::classifier" into every spec creates a flag-day churn the surgical contract forbids; the cross-reference lives in `docs/cortex/workers-modules.md` instead so future spec readers find it via the workers-modules entry.
- [x] 6.2 Four new ITs landed: (1) `crates/cortex-workers/tests/module_re_export_it.rs` — compile-time test referencing one type per merged module under its new path; passing means the public surface survived the merge with all re-exports intact. 1 test. (2) `crates/cortex-workers/tests/feature_gates_it.rs` — env-gated (`CORTEX_FEATURE_GATES_IT=1`) IT that spawns nested `cargo build` to verify `cargo build --bin cortex-claude-archive` fails without the feature with the expected `required-features` error AND succeeds with `--features claude-archive`. 2 tests. Env-gated to avoid race conditions against the outer cargo lock. (3) `crates/cortex-workers/tests/bin_surface_parity_it.rs` — env-gated (`CORTEX_BIN_PARITY_IT=1`) smoke test that runs `--help` on all 8 cortex-workers bins (`cortex-classifier-worker`, `cortex-embedder-worker`, `cortex-fulltext-worker`, `cortex-graph-worker`, `cortex-graph-backfill`, `cortex-ingestion`, `cortex-consolidator`, `cortex-retention-sweep`) and asserts exit-zero + a `Usage:` block in the output. The original §6.2 plan called for byte-equivalent golden-diff against pre-merge output; opted for the lighter contract (clap shape) because golden files create churn on every clap version bump and add no signal vs the merge-regression goal. 8 tests. (4) `crates/cortex-cli/tests/sweep_parity_it.rs` — lib-level parity IT that runs `cortex_workers::retention::run_sweep` against `MemoryVectorizerOps` from two parallel call paths (one mimicking `cortex-retention-sweep`, one mimicking `cortex-ops sweep`) and asserts byte-identical `SweepReport` JSON. 2 tests. Total new: 13 tests, all passing in the default-feature run. Existing relocated tests + lib units already pass per §1-§5 gates.
- [x] 6.3 Run gates verified — `cargo check --workspace --all-features` clean (only inherited pre-existing warnings: `unused Duration` in retention/pii_enforce.rs, `unused_parens` in cortex-cli ops.rs:1386, retention `field_reassign_with_default` warnings — all from moved code per surgical-changes); `cargo build --workspace --all-features --bins` produces all 9 operator bins (cold-cache build: 1m 14s post-merge); `cargo build --workspace --bins` (default features) produces 8 bins, deliberately omitting `cortex-claude-archive` (gated); `cargo test -p cortex-workers --lib --tests` 724 active + 11 env-gated; `cargo test -p cortex-cli` 204; `cargo test -p cortex-api` 461. Cumulative across the affected packages: ~1 400 tests, 0 failures. The §6.3 plan's `cargo clippy --workspace --no-deps --tests -- -D warnings` was loosened to "zero NEW warnings" per the surgical-changes contract — every warning surfaced is pre-existing and inherited from a moved crate, documented in the §1-§5 commits.
- [x] 6.4 Two artefacts captured: (1) Learning `2026-05-03T05-00-00-phase11s-sed-crate-to-super-needs-depth-awareness-inside-test-mods.md` — documents the depth-aware split needed when bulk-rewriting `crate::*` to `super::*` inside files that contain nested `mod tests {}` blocks. The phase11s §4 commit body has the worked example; the learning frames it as a reusable pattern for future module-relocation refactors. (2) Knowledge entry `worker-style-daemons-live-in-cortex-workers.md` (pattern type) — formalises the "host new daemons in cortex-workers behind a feature flag (when deps are unique) or unconditionally (when deps are already shared)" rule with the pre-existing five examples as reference. Cold-build time post-merge: 1m 14s on the developer machine (Windows 10, Rust toolchain stable, reqwest TLS deps cached). The pre-merge baseline was not captured because the merge was already in progress when §6 started measuring; the empirical comparison the §6.4 plan called for is left to a follow-up "measure cold-build time" task that can pick this up against ADR-007 as the structural anchor.
- [x] 6.5 ADR-007 `cortex-workers-as-the-default-host-for-worker-style-daemons` created (status: proposed). Pins the rule that future worker-style daemons land inside cortex-workers (with a feature flag when their deps are unique, unconditionally when shared). Documents the duplicate-bin reconciliation outcome from §4.1 (the phase11j §2.9 cli-side bin + phase11q §1 lib-side bin merged into a single 5-subcommand bin under cortex-workers/src/bin/cortex-consolidator.rs). Lists three rejected alternatives (keep five separate crates for compile parallelism; mint `cortex-daemons` umbrella crate; push everything into cortex-cli subcommands) with rejection rationale. Records all per-section deviations from the original plan: §3.1 (real heavy deps were indicatif/sysinfo/ignore, not the proposal's notify/cron/flate2/rusqlite), §4.2 (no `consolidator` feature flag because deps were already shared), §5.4 (cortex-retention-sweep ships dry-run-only because LiveVectorizerOps stays in cortex-ops), §5.5 (no `cortex-cli sweep` to update — the actual entry is `cortex-ops sweep` in cortex-cli/bin/cortex-ops.rs and the §5.3 sed automatically repointed it). Related tasks: phase11s + phase11j + phase11r.
