## 1. Implementation
- [x] 1.1 Upstream: file a hivellm/synap issue requesting a room-generation discriminator in `stream.stats` (`created_at` epoch or a monotonic `generation` id) — today's payload (name, message_count, max_offset, min_offset, total_published, total_consumed, subscriber_count, dropped) cannot distinguish "wiped room refilled to N events" from "healthy caught-up room with N events". Filed: https://github.com/hivellm/synap/issues/257
- [x] 1.2 Shipped in all four `LiveSynapConsumer`s: stats probe promoted from empty-poll-only to ANY poll with offset>0 (still bounded once per interval — this also closes the lagging-consumer wipe, where non-empty polls silently deliver cross-generation offsets); `last_seen_published` baseline = max(remembered, probed, cursor); `probed_published < baseline` ⇒ warn + cursor reset to 0 (+ graph durable `consumer_offset_set` rewind); `with_stale_check_interval` builder as the honest test/tuning hook. ITs 8/8 in `synap_room_selfheal_it.rs`, including `published_decrease::equality_at_stale_check_documents_residual_gap_then_decrease_heals_via_remembered_baseline` (documents the residual 1==1 hole as upstream-blocked) and `published_decrease::lagging_consumer_nonempty_poll_still_probes_and_heals_on_published_decrease`. clippy --workspace -D warnings clean.
- [ ] 1.3 ⏸ blocked: upstream — awaiting a hivellm/synap release exposing generation/created_at in `stream.stats` (synap#257). Then replace the heuristics with the definitive generation comparison and drop the interim bookkeeping.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — CHANGELOG entry (blind spots, fix shape, residual hole + synap#257); `docs/cortex/continuity-loop-runbook.md` already routes the symptom class here.
- [x] 2.2 Write tests covering the new behavior — 2 new `published_decrease` ITs (equality residual-gap documentation + lagging-consumer non-empty-poll heal) atop the 3 per-consumer stale-heal ITs.
- [x] 2.3 Run tests and confirm they pass — `synap_room_selfheal_it` 8/8; `cargo clippy --workspace --all-targets -D warnings` clean.
