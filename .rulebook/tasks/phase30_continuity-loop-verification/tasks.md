## 1. Verification and wiring
- [x] 1.1 Written: `crates/cortex-pre-thinking/tests/cross_session_continuity_it.rs` — live-gated (skips when :17000 down), drives the REAL `pipeline::run` against the running stack; the "prior session" is the newest `/v1/consolidations/recent` hit (every consolidation is a past session's distillate), the "fresh session" is a new session_id asking about that hit's topic; asserts the consolidation id/title lands in the bundle. Runs `#[ignore]`d (red by known bug, see 1.2): `cargo test -p cortex-pre-thinking --test cross_session_continuity_it -- --ignored`.
- [x] 1.2 It fails — root cause is a THREE-layer dead wire in the retrieval read path, verified live: (1) keyword lane queries the extinct global `cortex_consolidations` Meili index while writes go per-repo (`cortex-cortex-consolidations`, 62 docs); (2) vector lane queries `cortex.consolidation.fp32` which does not exist in the live Vectorizer (404); (3) NOTHING in cortex-api constructs `ConsolidationRef` — all hits become snippets, `results.consolidations` is permanently empty, so the renderer's "Consolidated context" section only ever rendered in unit fixtures. Not embedding lag, not scope mismatch — structural. Fix scoped as follow-up task `phase30b_consolidations-read-path` (created; the IT's `#[ignore]` names it and un-ignoring is that task's acceptance gate).
- [x] 1.3 Wired end-to-end in the adapter: `SyncClient::active_work` (GET `/v1/dashboard/active-work?repo=<slug>`, 300ms timeout, fail-open) + `HookKind::SessionStart` dispatcher arm (repo slug from cwd via the pre-thinking scope helper; ≤8-row Markdown block; `HookResponse::session_start_context` stamps the correct `hookEventName`) + `install.rs` SessionStart flipped `fire_forget: true → false` (the shim now READS the daemon response — without this the computed `additionalContext` was dropped at the transport layer; re-`install` migrates settings). Adapter suite 82/82 incl. 4 new SessionStart dispatcher ITs (happy path, fail-open, empty, row cap) + install command-table test updated.
- [x] 1.4 `docs/cortex/continuity-loop-runbook.md` — two-channel model (active-work at SessionStart; consolidated context per prompt), staged triage checklist in write order, the phase30b known-break called out explicitly (complaints about forgotten conclusions are that bug until it lands), symptom→owner quick-reference table.

## 2. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 2.1 Update or create documentation covering the implementation — `docs/specs/10-claude-code-adapter.md`: SessionStart hook-map row (active-work surfacing semantics + fail-open), settings JSON example, command table (SessionStart → synchronous, migration note); plus `docs/cortex/continuity-loop-runbook.md` (§1.4) documents both channels operationally.
- [x] 2.2 Write tests covering the new behavior — 4 SessionStart dispatcher ITs (mock api: happy path with ids+next fragments, fail-open on unreachable, empty on zero tasks, 8-row cap) + `settings_register_cortex_hook_bin_with_fire_forget_per_event` updated for the synchronous SessionStart contract; all separate from the §1.1 consolidation IT.
- [x] 2.3 Run tests and confirm they pass — cortex-adapter-claude-code 82/82; cortex-pre-thinking green (1 deliberately-ignored red IT gated on phase30b); full workspace gate re-run green pre-archive.
