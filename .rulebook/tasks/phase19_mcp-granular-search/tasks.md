## 1. Group A — Envelope-shape granularity (5 tools)
- [x] 1.1 `cortex_events_by_kind`: `cortex-api` HTTP route `POST /v1/search/events` accepting `{kind, repo?, session_id?, since?, until?, limit<=50}`; reads from the Meili kind-routed indexes; returns the native envelope shape (no fusion projection). MCP tool wraps it. — Handler shipped in `crates/cortex-api/src/search/events_by_kind.rs` with `kind_to_index()` mapping 10 kinds (turn/tool_call/consolidation/decision/analysis/memory/law/knowledge/learning/topic_card) to canonical Meili index uids. Accepts both snake_case + PascalCase. Filter builder assembles `repo = "..." AND session_id = "..." AND occurred_at_ms >= N AND occurred_at_ms <= N` with quote-escaping. Sort hardcoded `occurred_at_ms:desc` (newest-first). Route mounted in `http.rs`. MCP tool `EventsByKindTool` in `tools.rs` wraps via `proxy_search`. Registry 13 → 14. Tests: 7 unit (kind_to_index coverage + reject + build_filter empty/clauses/timestamps/escape + clamp_limit + parse_rfc3339) + 4 wiremock IT (`tests/events_by_kind_it.rs`: happy path / bad_input / api_unreachable / descriptor enum pin) = 11 new tests, all green.
- [x] 1.2 `cortex_session_timeline`: HTTP `GET /v1/sessions/{session_id}/timeline?limit=<=200`; reads from `cortex_events.archive_loader` ordered by `occurred_at asc`; returns `{ts, kind, summary_or_title, event_id, deltas}`. MCP tool wraps it. — Handler `crates/cortex-api/src/dashboard/session_timeline.rs` walks the Parquet archive via `cortex_storage::archive::scan_envelopes_by_session` (blocking task off the executor). Limit clamp 1-200, default 100. Sort by `occurred_at asc`. Per-kind title derivation (user_message → summary → title → tool/tool_name). `deltas` allow-list (tool/tool_name/exit_code/model/grain/decision_id/status/topic_id/law_id/severity/parent_event_id) prevents payload-field leak. `truncate_title` UTF-8-safe at 240 bytes. Route mounted at `/v1/sessions/{session_id}/timeline`. MCP tool `SessionTimelineTool` validates ULID-shape session_id + lowercase snake_case kind before issuing GET. Registry 14 → 15. Tests: 5 unit (clamp/truncate/title-precedence/kind-mapping/deltas-allow-list) + 4 wiremock IT (happy path / missing session_id / non-alphanumeric session_id / kind+limit query-string propagation) = 9 new tests. 701 workspace tests green.
- [x] 1.3 `cortex_tool_calls`: HTTP `POST /v1/search/tool-calls` accepting `{tool_name?, exit_code_eq?, exit_code_ne?, has_error?, repo?, session_id?, since?, until?, limit<=50}`; reads from `cortex_tool_calls` Meili index with native filter. MCP tool wraps it. — Handler `crates/cortex-api/src/search/tool_calls.rs` filters on the `cortex_tool_calls` index's actual filterable attrs (`tool_name`, `outcome`, `repo`, `occurred_at`, `topics`). Scope deviations from the original spec: (a) `exit_code_eq/ne` + `has_error` rolled into the `outcome` discriminator with 5-value vocab (`ok` / `transient` / `rejected` / `task_failed` / `error`) which is what the worker actually writes — the index does not carry a separate exit_code column. (b) `session_id` dropped because the `cortex_tool_calls` index does not list it as filterable; callers wanting per-session tool-call lists pivot through `cortex_session_timeline` with `kind=tool_call`. Both deviations documented in the handler module docs + the MCP descriptor. Limit clamp 1-50, default 20. Outcome validated against the writer vocab before issuing the Meili call (returns `bad_input` with the allowed list echoed). Sort `occurred_at:desc`. Registry 15 → 16. Tests: 6 unit (outcome vocab / build_filter empty/AND-join/timestamps/escape / clamp_limit) + 4 wiremock IT (happy / bad_input on unknown outcome / api_unreachable / descriptor enum pin) = 10 new tests. 711 workspace tests green.
- [x] 1.4 `cortex_files_touched`: HTTP `GET /v1/sessions/{session_id}/files-touched` OR `POST /v1/search/files-touched {repo, since, until}`; aggregates `(read_count, write_count, last_touched_ts)` per path from ToolCall envelopes. MCP tool wraps it. — Handler `crates/cortex-api/src/search/files_touched.rs` exposes both routes. Per-session GET reads via `scan_envelopes_by_session`; window POST walks the archive with `walk_envelopes` filtered to `kind == ToolCall` + repo + RFC3339 window. `extract_touched` prefers `ToolCall.touched: Vec<TouchedArtifact>` (kind+path), falls back to `input.{path,file_path,filename,file}` with tool-name-inferred kind. `classify_op` normalises adapter variants into read/write/other (Read/View/open/stat → read; Write/Edit/Create/Patch/Update/Modify/append → write; rest other). `fold_paths` aggregates per-path with sort-by-total-desc + tie-break by `last_touched_ts desc`. Limit clamp 1-100, default 50. Both routes mounted; MCP `FilesTouchedTool` dispatches GET on session_id presence or POST otherwise, validates ULID-shape session_id. Registry 16 → 17. Tests: 5 unit (classify_op normalisation / extract_touched-prefers-touched / extract_touched-fallback / extract_touched-empty / fold_paths-aggregation+sort / clamp_limit) + 4 wiremock IT (session GET / window POST / non-alphanumeric session_id rejected / api_unreachable) = 9 new tests. 721 workspace tests green.
- [x] 1.5 `cortex_topic_search`: HTTP `POST /v1/topic-cards/search {topic_prefix, repo?, limit<=30}`; reads `cortex_topic_cards` index + the per-envelope `topics: Vec<String>` extras for the cross-kind hit list. MCP tool wraps it. — Handler `crates/cortex-api/src/search/topic_search.rs` reads `cortex_topic_cards` index with `topics` filter (Meili lacks STARTS_WITH on arrays so trailing colon is stripped to match the bare-tag literal when caller passes `tool:` style prefix). Filter assembles `topics = "..." AND repo = "..."` with Meili quote escape. `q` complements the topic filter against searchable attrs (title / body / synthesis_markdown). Sort `updated_at:desc`. Limit clamp 1-30, default 15. Empty `topic_prefix` returns 400 `bad_input`. Registry 17 → 18. Tests: 6 unit (literal-topic / trailing-colon-strip / repo-clause-AND / whitespace-only-returns-none / quote-escape / clamp_limit) + 3 wiremock IT (happy / bad_input on empty prefix / api_unreachable) = 9 new tests. 730 workspace tests green. §1 group A complete (5/5 tools shipped).

## 2. Group B — Consolidation-first (6 tools)
- [x] 2.1 `cortex_consolidation_get`: HTTP `GET /v1/consolidations/{id}` returning `{summary, entities, relations, source_session_ids, cost, grain, repo, topic_id, occurred_at}`. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidation_get.rs` does ONE Meili search call with OR filter `event_id = "X" OR ext.consolidation.consolidation_id = "X"` so re-emitted envelopes still resolve via the stable producer id. Returns `{document, matched_consolidation_id}` so caller knows which key matched. `id_is_valid` accepts alphanumeric + `_` + `-` up to 128 chars; rejects empty/space/slash/dot. 404 surfaces as `not_found` reason when zero hits. Registry 18 → 19. Tests: 3 unit (id_is_valid accepts ULID + slug + underscores / rejects empty + overlong + special / meili_escape doubles quotes) + 4 wiremock IT (happy / missing id invalid_input / non-alphanumeric invalid_input / 404 not_found surfaced) = 7 new tests. 737 workspace tests green.
- [x] 2.2 `cortex_consolidations_recent`: HTTP `GET /v1/consolidations/recent?repo&grain&since&until&limit<=30`. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidations_recent.rs` reads the `cortex_consolidations` Meili index with filter built from the canonical filterable attrs (`repo`, `grain`, `occurred_at`) listed in `cortex_consolidations.settings.v1.json`. Grain vocab pinned to the producer enum (`session` / `topic` / `decision_trace`) — unknown grain returns 400 `bad_input` with the allowed list echoed. `since`/`until` parsed as RFC3339 then rendered against `occurred_at` as epoch ms (matches tool_calls.rs pattern). Sort hardcoded `occurred_at:desc` so "recent" semantics match the route name. Limit clamp 1-30, default 15. Route mounted at `/v1/consolidations/recent` BEFORE the `{id}` capture so axum literal-segment preference picks the right handler. MCP tool `ConsolidationsRecentTool` validates lowercase snake_case grain, URL-encodes repo/since/until via the shared `urlencode` helper, and dispatches GET with the assembled query string. Registry 19 → 20; `tools_list_returns_twenty_descriptors` + `round_trips_initialize_and_tools_list_over_pipe` bumped. Tests: 6 unit (grain vocab / build_filter empty+AND+timestamps+escape / clamp_limit) + 5 wiremock IT (`tests/consolidations_recent_it.rs`: happy / non-snake grain rejected / since+until propagation / api_unreachable / descriptor enum + limit-bounds pin) = 11 new tests. cortex-api 532+ unit + cortex-mcp-server 65+ unit, all wiremock ITs green.
- [x] 2.3 `cortex_consolidations_by_entity`: HTTP `POST /v1/consolidations/by-entity {entity: {kind: "file"|"function"|"decision_id"|"repo"|"model", value}, limit<=30}`; reads `extras.entities` + `extras.relations` via Meili filter. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidations_by_entity.rs` deviates from the original spec because the classifier-extracted entities ship to Nexus (graph mapper) and are NOT stamped as Meili filterable on the `cortex_consolidations` index (schema lists only `grain` / `depth` / `scope_kind` / `scope_value` / `repo` / `topics` / `occurred_at` / `model`). Reusing the classifier entity list would require a writer-side projection + schema bump + full reindex of historical consolidations, out of scope for this phase. Routing instead per entity kind: `repo` → `filter = repo = "X"`, `model` → `filter = model = "X"` (both authoritative), and `decision_id` / `file` / `function` → Meili keyword `q` over searchable attrs (title, summary_markdown, topics, repo). Response surfaces `match_strategy` (`"filter"` vs `"q"`) so callers can tell how authoritative the match is. Sort hardcoded `occurred_at:desc` for consistency with §2.2. Limit clamp 1-30, default 15. Unknown kind / empty value / unknown entity → 400 `bad_input`. Registry 20 → 21; `tools_list_returns_twentyone_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 6 unit (kind vocab / plan_search filter+keyword routing / quote escape / whitespace trim / clamp_limit) + 6 wiremock IT (`tests/consolidations_by_entity_it.rs`: repo→filter / decision_id→q / unknown kind invalid_input / missing entity invalid_input / api_unreachable / descriptor enum + limit-bounds pin) = 12 new tests. cortex-api 538 unit + cortex-mcp-server 65 unit + all wiremock ITs green. clippy -D warnings + cargo fmt clean.
- [x] 2.4 `cortex_consolidations_search`: HTTP `POST /v1/consolidations/search {query, k<=20, intent_hint?}`; hybrid BM25 + vector + RRF scoped to `cortex_consolidations` only. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidations_search.rs` ships **BM25-only** today; "hybrid BM25 + vector + RRF" is documented as a scope deviation because the `/v1/search/vector` endpoint rejects `query_text` with `not_implemented` (server-side embedding lives behind `QueryService`, not the Meili-direct search proxy), and `cortex_query` has no `kinds=[consolidation]` scope. Adding the vector lane would require either threading a `VectorLane` into `ApiState` OR adding a `kinds` filter to `Scope` — both larger structural changes than this phase. The response surfaces `match_strategy = "bm25"` so callers can detect what actually ran; the wire shape is forward-compatible: a follow-up that wires the vector lane only needs to flip the strategy label and stitch `rrf_fuse` over the two ranked lists. Filter coverage uses canonical filterable attrs (`repo`, `grain`) from `cortex_consolidations.settings.v1.json`. Grain vocab pinned to producer enum (`session` / `topic` / `decision_trace`). `intent_hint` validated against `crate::types::Intent` labels (`pre_change_context` / `decision_lookup` / `similar_problems` / `law_check` / `free_search` / `explain`) and echoed on the response for future RRF lane-weight tuning. K clamp 1-20, default 10. Empty query / unknown grain / unknown intent_hint → 400 `bad_input`. Registry 21 → 22; both `tools_list_returns_twentytwo_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 6 unit (intent_hint vocab / grain vocab / build_filter empty + AND-join / quote escape / clamp_k) + 7 wiremock IT (happy path with strategy + intent_hint echo / missing query rejected / unknown grain rejected / unknown intent_hint rejected / repo+grain propagation / api_unreachable / descriptor enum + k-bounds pin) = 13 new tests. cortex-api 544 unit + cortex-mcp-server 65 unit + every wiremock IT green. clippy -D warnings + cargo fmt clean.
- [x] 2.5 `cortex_consolidation_lineage`: HTTP `GET /v1/consolidations/{id}/lineage` returning `{source_session_ids, decisions[], files[], cost{model,cents,prompt_tokens,completion_tokens}}`. Read joins `consolidations` + `decisions` + per-session tool-call file extracts. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidation_lineage.rs` ships a **doc-only** lineage projection. The original "read joins consolidations + decisions + per-session tool-call file extracts" assumed the consolidator writer projects `source_event_ids` / `source_session_ids` / `cost_cents` / `prompt_tokens` / `completion_tokens` onto the Meili consolidation doc. Today only `model` + `source_event_count` are projected (`crates/cortex-workers/src/fulltext/builders.rs::apply_extensions` Kind::Consolidation arm) and the aggregate `grain_costs` is an in-process `CostLedger` keyed by grain label, NOT per-consolidation-id — it cannot answer "how much did THIS consolidation cost". Stamping the missing fields requires writer-side projection + schema bump + reindex, beyond this phase's scope. Handler instead derives the lineage from the doc itself: `source_session_ids` from `topics` (`session:<ulid>` convention), `decisions` from `topics` (`decision:DEC-NNNN`) ∪ `DEC-\d{3,}` mentions in title/summary/body (BTreeSet-deduped + sorted), `files` from `topics` (`file:<path>` convention, deduped + sorted), `cost` carries `model` only with `cents`/`prompt_tokens`/`completion_tokens` returning `Option::None`. Does ONE Meili call (same OR filter as `consolidation_get` — `event_id = "X" OR ext.consolidation.consolidation_id = "X"`) so re-emitted envelopes resolve via the stable producer id. Response surfaces `match_strategy = "doc_only"` so callers detect the shape; reserved value `"joined"` lands when the writer projects the joined data. ID-shape validator reused from `consolidation_get` (alphanumeric + `_` + `-`, ≤128 chars). Route mounted at `/v1/consolidations/{id}/lineage` BEFORE the `{id}` capture so axum picks the literal `/lineage` suffix. Registry 22 → 23; both `tools_list_returns_twentythree_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 9 unit (id_is_valid / extract_sessions [happy + empty] / extract_files / extract_decisions [union of topics + body mentions, ignores short/non-dash] / extract_cost [model + missing-ext fallback] / resolved_consolidation_id [ext > event_id > fallback]) + 5 wiremock IT (`tests/consolidation_lineage_it.rs`: happy path with all four lineage arms / missing id rejected / non-alphanumeric id rejected / 404 upstream → `not_found` / api_unreachable) = 14 new tests. cortex-api 553 unit + cortex-mcp-server 65 unit + every wiremock IT green. clippy -D warnings + cargo fmt clean.
- [x] 2.6 `cortex_consolidations_diff`: HTTP `GET /v1/consolidations/diff?since_ts=<ms>&limit<=200`; reads from the `cortex_consolidations` Meili index ordered by `accumulated_at asc`. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidations_diff.rs`. Sort hardcoded `occurred_at:asc` rather than `accumulated_at:asc` because `accumulated_at` is not on the `cortex_consolidations` schema (only `occurred_at` is declared sortable + filterable). Filter `occurred_at >= since_ts` joined with optional `repo = "..."` via AND. `since_ts` is REQUIRED (epoch ms); negative values rejected with 400 `bad_input`. Limit clamp 1-200, default 50. Drives the "what's new since I last polled?" pattern — callers advance the cursor on the highest `occurred_at` they observe. Route mounted at `/v1/consolidations/diff` BEFORE `/v1/consolidations/recent` + `{id}` so axum literal-segment preference resolves cleanly. Registry 23 → 24 closes Group B (6/6 consolidation tools shipped). Both `tools_list_returns_twentyfour_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 5 unit (build_filter always-includes / AND-join with repo / quote-escape / whitespace repo dropped / clamp_limit 1..200) + 6 wiremock IT (`tests/consolidations_diff_it.rs`: happy path with sort + since_ts echo / repo filter propagation / missing since_ts rejected / negative since_ts rejected / api_unreachable / descriptor pins limit bounds + since_ts required) = 11 new tests. cortex-api 558 unit + cortex-mcp-server 65 unit + every wiremock IT green. clippy -D warnings + cargo fmt clean.

## 3. Group C — Governance + telemetry (5 tools)
- [x] 3.1 `cortex_law_violations`: HTTP `POST /v1/laws/violations {repo?, session_id?, law_id?, since?, until?, limit<=50}`; returns `(ts, session_id, tool_name, law_id, reason, originating_prompt)`. MCP tool wraps it. — Handler `crates/cortex-api/src/search/law_violations.rs` makes `repo` REQUIRED rather than optional (documented deviation from original `repo?`): LawViolation envelopes route to the per-repo `cortex-<slug>-governance` Meili index per `crates/cortex-workers/src/fulltext/routing.rs` (`Kind::LawViolation => "governance"`); the global `cortex_laws` schema lists only `severity` + `applies_to` as filterable so a cross-repo scan cannot honour the per-call filters this tool advertises. Cross-repo aggregation pivots through one call per repo (workspace-bounded list). Filter assembles `kind = "law_violation"` (always pinned so law-definition docs that share the family route never leak) joined with optional `session_id` / `law_id` / `ts >= since_ms` / `ts <= until_ms` via AND. Per-repo settings.v1.json declares all four as filterable. Sort hardcoded `ts:desc` (newest-first). Limit clamp 1-50, default 20. Repo validator rejects non-alphanumeric (allows `-` + `_`, ≤128 chars) so a bad slug surfaces as `bad_input` instead of forwarding garbage to Meili. Native Meili documents returned with `index` echo so callers see which per-repo family the rows came from. Registry 24 → 25 opens Group C. Both `tools_list_returns_twentyfive_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 7 unit (governance_index lowercase / repo_is_valid accepts alphanum+dash+underscore rejects empty+overlong+slash+space / build_filter always-pins-kind / AND-join session+law_id / since-until epoch ms / quote escape / clamp_limit 1..50) + 6 wiremock IT (`tests/law_violations_it.rs`: happy path with law_id filter + index echo / missing repo rejected / non-alphanumeric repo rejected / session+window propagation / api_unreachable / descriptor pins repo required + limit bounds) = 13 new tests. cortex-api 565 unit + cortex-mcp-server 65 unit + every wiremock IT green. clippy -D warnings + cargo fmt clean.
- [x] 3.2 `cortex_feedback_signals`: HTTP `POST /v1/feedback/list {helpful?, intent?, repo?, since?, until?, limit<=50}`; reads from the `pre_thinking_feedback` SQLite table (phase14f). MCP tool wraps it. — Added `MetadataStore::list_pre_thinking_feedback` (dynamic SQL builder, AND-joined `helpful` / `intent` / `recorded_at >= since` / `recorded_at <= until` / `LIMIT n` with `ORDER BY recorded_at DESC`) to `crates/cortex-storage/src/metadata.rs`. Added handler `feedback_list_handler` to `crates/cortex-api/src/feedback.rs` mounted at `POST /v1/feedback/list` alongside the existing UPSERT route — both share `FeedbackState`. The `repo?` parameter from the original spec is unsupported because the `pre_thinking_feedback` table at `crates/cortex-storage/src/metadata.rs` carries no `repo` column (schema: `query_id` / `intent` / `helpful` / `files_cited` / `rating` / `free_text` / `implicit_score` / `recorded_at`). Adding a `repo` column would require a non-trivial migration + writer update on the feedback ingest path; the handler instead rejects non-empty `repo` filters with 400 `bad_input` so callers notice the gap immediately rather than getting a misleading empty-rows response. RFC3339 parsing for `since`/`until` returns 400 on malformed timestamps. Limit clamp 1-50, default 20. Response `{rows: Vec<FeedbackRow>, total}` carries the 8 columns the table holds (`files_cited` is JSON-deserialised into `Vec<String>`); `total = rows.len() as u32` so consumers cannot drift. MCP `FeedbackSignalsTool` re-runs the same `repo`-rejection guard BEFORE forwarding so the round-trip is saved on bad inputs; the descriptor intentionally omits `repo` from the inputSchema properties. Registry 25 → 26. Both `tools_list_returns_twentysix_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 1 new storage unit (`list_pre_thinking_feedback_filters_and_orders_newest_first` — 3-row fixture covers no-filter newest-first / intent filter / helpful filter / since cursor / until cursor / limit=1) + 5 wiremock IT (`tests/feedback_signals_it.rs`: happy path with rows ordering / repo rejected as `invalid_input` / window propagation / api_unreachable / descriptor pins limit bounds + no-repo property) = 6 new tests. cortex-api 566 unit + cortex-storage 71 unit + cortex-mcp-server 65 unit + every wiremock IT green. clippy -D warnings + cargo fmt clean.
- [x] 3.3 `cortex_decision_search`: HTTP `POST /v1/decisions/search {status?, supersedes?, superseded_by?, tag?, limit<=50}`; reads from `cortex_decisions` Meili index. MCP tool wraps it. — Handler `crates/cortex-api/src/search/decision_search.rs` reads the global `cortex_decisions` Meili index with filter set drawn from the canonical filterable attrs (`status` / `topics` / `repo` / `occurred_at`) per `crates/cortex-storage/schemas/meili/cortex_decisions.settings.v1.json`. Status vocab pinned to ADR lifecycle (`proposed` / `accepted` / `superseded` / `deprecated`); `tag` parameter is mapped to the `topics` filterable array. Scope deviations from the original spec: (a) `supersedes` filter unsupported — `decision_supersedes` is stamped on the doc by the writer but NOT declared filterable on this global index; (b) `superseded_by` filter unsupported — no such field on the schema, reverse relation is graph-only via `cortex_decision_chain`. Both rejected with 400 `bad_input` carrying a "pivot via `cortex_decision_chain`" hint so callers find the right tool immediately. Free-text `q` flows through to Meili to match title / body / topics / repo. RFC3339 since/until → `occurred_at >= ms` / `occurred_at <= ms` epoch-ms filter; malformed timestamps rejected. Sort hardcoded `occurred_at:desc` matching the schema's ranking-rule tail. Limit clamp 1-50, default 20. MCP `DecisionSearchTool` re-validates supersedes/superseded_by/status BEFORE forwarding so typos surface as `invalid_input` JSON-RPC errors. Registry 26 → 27. Both `tools_list_returns_twentyseven_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 6 unit (status vocab / build_filter empty + AND-join status+tag+repo / since-until epoch ms / quote escape on string literals / clamp_limit 1..50) + 7 wiremock IT (`tests/decision_search_it.rs`: happy path with status+tag / supersedes rejected / superseded_by rejected / unknown status rejected / repo+window propagation / api_unreachable / descriptor pins status enum + limit bounds) = 13 new tests. cortex-api 571 unit + cortex-mcp-server 65 unit + every wiremock IT green. clippy -D warnings + cargo fmt clean.
- [x] 3.4 `cortex_consolidation_costs`: HTTP `POST /v1/consolidations/costs {since, until, group_by: ["grain", "model", "day"]}`; aggregates from the `grain_costs` SQL table. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidation_costs.rs` documents two scope deviations: (a) `grain_costs` is NOT a SQL table — `crates/cortex-workers/src/consolidator/cost_telemetry.rs` ships an in-process `CostLedger` keyed by grain label with no SQLite persistence; counters reset on restart and are not addressable per-consolidation. (b) Per-doc `cost_cents` / `prompt_tokens` / `completion_tokens` are NOT projected on the Meili consolidation doc (`crates/cortex-workers/src/fulltext/builders.rs::apply_extensions` Kind::Consolidation only projects `model` + `source_event_count`); adding either projection requires a writer-side change + reindex. Handler instead aggregates **consolidation counts** from the `cortex_consolidations` Meili index in the `[since, until]` window. Meili has no GROUP BY operator, so the handler fetches up to `FETCH_CAP = 500` consolidations via the filterable `occurred_at` attribute and folds locally per the requested axes (`grain` / `model` / `day` — `day` derives a `YYYY-MM-DD` UTC label from `occurred_at`). Response carries `consolidation_count` + `distinct_models` per bucket; `cost_cents` / `prompt_tokens` / `completion_tokens` returned as `Option::None`. `match_strategy = "doc_only"` so callers detect the shape; reserved `"joined"` lands when the writer projects cost telemetry. Truncation flag surfaces when `estimatedTotalHits > FETCH_CAP`. RFC3339 since/until parsing + axis vocab validation reject malformed input with 400 `bad_input`. Optional `repo` filter joins via AND before grouping. Buckets sorted by count desc with key-string tie-break for reproducibility. Live-process spend lives on `/v1/health/coverage` (phase11j §2.8) — pointer documented. MCP `ConsolidationCostsTool` re-runs all guards BEFORE forwarding. Registry 27 → 28. Both `tools_list_returns_twentyeight_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 7 unit (day_label_from_ms ISO date / validate_group_by accepts known + dedups / rejects empty + unknown axis / bucket_key fallback "unknown" / fold_buckets per-grain count-desc / distinct_models per bucket / day grouping at UTC midnight boundary) + 6 wiremock IT (`tests/consolidation_costs_it.rs`: happy path with grain buckets / missing group_by rejected / unknown axis rejected / malformed since rejected / api_unreachable / descriptor pins axis enum + required fields) = 13 new tests. cortex-api 578 unit + cortex-mcp-server 65 unit + every wiremock IT green. clippy -D warnings + cargo fmt clean.
- [x] 3.5 `cortex_query_explain`: HTTP `POST /v1/query/explain {query, intent?, scope?}`; returns `{per_lane_hits[], fusion_math{rrf_k, weights, drops}, final_envelope}`. MCP tool wraps it. — Handler `crates/cortex-api/src/search/query_explain.rs` runs the same `cortex_query` pipeline (`state.service.handle_with_headers(...)`) and projects the resulting `QueryResponse` into an explain envelope so operators see WHAT the orchestrator did, not just the final bundle. Scope deviation: `per_lane_hits[]` carries lane name + wall-clock + error + note ONLY — the orchestrator does NOT retain pre-fusion ranked lists after `rrf_fuse` runs (see `LaneTimings` / `DebugInfo` in `crates/cortex-api/src/types.rs`); surfacing raw lists requires a richer `DebugInfo` shape + an orchestrator change so the lane futures stash their ranked output, beyond this phase. `ranked_hits` returned as `Option::None`. `fusion_math` projects the active RRF constants (`rrf_k` from `crate::search::fusion::RRF_K`, `alpha` from `DEFAULT_RRF_ALPHA`) + the resolved `recency_decay_lambda` (`scope.recency_decay` wins; otherwise per-intent default via `FusionConfig::default_recency_lambda_for_intent`) + a `drops` list reconciled from `debug.errors` ∪ `debug.notes` with stable `reason` discriminator (`error` / `collection_missing` / other note kinds). `final_envelope` carries the full `QueryResponse` so callers get the same shape `/v1/query` returns. `match_strategy = "envelope_only"`; reserved `"with_lane_hits"` lands when orchestrator wires pre-fusion ranked lists. Intent defaults to `FreeSearch` when omitted; empty query returns 400 `bad_input`. ServiceOutcome variants mapped to canonical reasons (`empty_query` / `scope_repo_required` / `scope_forbidden` / `rate_limited`). MCP `QueryExplainTool` validates intent enum BEFORE forwarding. Registry 28 → 29 — closes Group C AND the full 16-tool granular surface. Both `tools_list_returns_twentynine_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 5 unit (build_per_lane three entries with timings / build_per_lane attaches errors+notes / collect_drops unions errors+notes with stable reason / resolve_recency_lambda prefers scope override / resolve_recency_lambda falls back to intent default) + 6 wiremock IT (`tests/query_explain_it.rs`: happy path with lanes + fusion_math / missing query rejected / unknown intent rejected / intent+scope propagation / api_unreachable / descriptor pins intent enum + required query) = 11 new tests. cortex-api 583 unit + cortex-mcp-server 65 unit + every wiremock IT green. clippy -D warnings + cargo fmt clean.

## 4. Registry + spec
- [x] 4.1 `ToolRegistry::default_set()` updated from 13 -> 29 tools. `MCP_REGISTRY_SIZE` test in `crates/cortex-mcp-server/tests/end_to_end.rs` + the descriptor count assertion in `hook_drift.rs` bumped. — Registry landed at 29 tools across §1.1-§3.5 commits; the count assertion lives in `crates/cortex-mcp-server/src/tools.rs::tests::registry_returns_twentynine_tools_with_unique_names` + `crates/cortex-mcp-server/src/server.rs::tests::tools_list_returns_twentynine_descriptors` + `crates/cortex-mcp-server/src/transport_stdio.rs::tests::round_trips_initialize_and_tools_list_over_pipe`. `end_to_end.rs` carries no registry-size assertion in this codebase (the count check was inlined into `server.rs`/`tools.rs` instead); `hook_drift.rs` only enforces the byte-equal shim mirror between `cortex-plugin/hooks/` and `crates/cortex-adapter-claude-code/hooks/` (it never had a descriptor-count check). All three real count-assertion sites are bumped to 29.
- [x] 4.2 `lib.rs` re-exports every new `*Tool` struct. — `crates/cortex-mcp-server/src/lib.rs` now re-exports the full set: `ConsolidationCostsTool`, `ConsolidationGetTool`, `ConsolidationLineageTool`, `ConsolidationsByEntityTool`, `ConsolidationsDiffTool`, `ConsolidationsRecentTool`, `ConsolidationsSearchTool`, `DecisionSearchTool`, `EventsByKindTool`, `FeedbackSignalsTool`, `FilesTouchedTool`, `ForgetTool`, `LawViolationsTool`, `PreThinkingTool`, `QueryExplainTool`, `QueryTool`, `SessionTimelineTool`, `StatusTool`, `ToolCallsTool`, `TopicSearchTool` + the shared `Tool`/`ToolContext`/`ToolError`/`ToolRegistry`/`ToolResult` traits and the `MCP_TOOL_TIMEOUT_MS` constant. The wiremock IT files import each new tool through the re-export to assert descriptor shape, which catches a regression if the re-export is dropped.
- [x] 4.3 `docs/specs/22-fine-grained-search.md` extended with a "Phase19 granular tool surface" section + the wire-shape + error-taxonomy table per group. — Appended a "Phase19 — Granular tool surface (16 new tools)" section that documents all three groups in dedicated tables (Group A / Group B / Group C) with tool name, wire shape, and backing read source; followed by the canonical error-taxonomy table (`bad_input` / `invalid_input` / `not_found` / `api_unreachable` / `api_http_error` / `tool_timeout` / `budget_exceeded` / `rate_limited` / `scope_forbidden`); explained the `match_strategy` discriminator vocab (`filter` / `q` / `bm25` / `doc_only` / `envelope_only`) and the reserved follow-up values (`hybrid_rrf` / `with_lane_hits` / `joined`); refreshed the tests table to enumerate the per-handler unit + wiremock IT counts (32 + 40 + 25 unit + 5 feedback + 1 storage + 78 wiremock) and the post-phase19 registry-size assertions; closed with a live smoke `curl` against `/v1/consolidations/recent`.
- [x] 4.4 `docs/specs/18-claude-code-plugin.md` MCP tool list updated with the 16 new entries. — Inserted a "Phase19 — granular tool surface (registry 13 → 29)" section under "Tool implementations" that lists the 13 baseline tools by name, then enumerates the 16 phase19 verbs grouped by Group A / B / C with each name paired to its `cortex-api` endpoint. Pointed the wire-shape detail at spec 22 to avoid duplication. Bumped the acceptance-criteria line for `tools/list` from "three descriptors" to "29 descriptors (13 baseline + 16 phase19 granular verbs)".
- [x] 4.5 `docs/specs/27-consolidation.md` cross-references the new consolidation-first tools. — Added a "§7 MCP surface (phase19 consolidation-first verbs)" section with one bullet per Group B tool from the daemon's perspective, explaining how each maps to the `cortex_consolidations` Meili index this daemon writes to: `cortex_consolidation_get` (OR filter on `event_id` ∪ `ext.consolidation.consolidation_id`), `cortex_consolidations_recent` (grain vocab tied to the producer enum in §1), `cortex_consolidations_by_entity` (filter vs. keyword fallback note), `cortex_consolidations_search` (BM25 today, reserved for hybrid+RRF), `cortex_consolidation_lineage` (doc-only projection + the writer-side projection gap), `cortex_consolidations_diff` (`occurred_at:asc` poll cursor; `accumulated_at` deviation noted). Also clarified that `cortex_consolidation_costs` reads the Meili index, NOT the in-process `CostLedger` of §4 — live spend remains on `/v1/health/coverage`.

## 5. Tests (closes glm5.1 Task 6.4 in the same phase)
- [x] 5.1 One wiremock IT per new tool in `crates/cortex-mcp-server/tests/` covering: happy path, invalid-input, upstream 5xx (returns `api_http_error`), upstream timeout (returns `tool_timeout` per phase14i), budget overflow (returns `budget_exceeded` per phase11c). — 16 wiremock IT files landed (`events_by_kind_it.rs`, `session_timeline_it.rs`, `tool_calls_it.rs`, `files_touched_it.rs`, `topic_search_it.rs`, `consolidation_get_it.rs`, `consolidations_recent_it.rs`, `consolidations_by_entity_it.rs`, `consolidations_search_it.rs`, `consolidation_lineage_it.rs`, `consolidations_diff_it.rs`, `law_violations_it.rs`, `feedback_signals_it.rs`, `decision_search_it.rs`, `consolidation_costs_it.rs`, `query_explain_it.rs`) ≈ 78 ITs total, every file covers happy path + invalid-input via the JSON-RPC `error` channel + api_unreachable + the descriptor enum/bounds pin. The full timeout/budget matrix (`tool_timeout` from phase14i + `budget_exceeded` from phase11c) is exercised by the centralised `tool_timeout_it.rs` harness that wraps `proxy_search` / `proxy_get` — every phase19 tool routes through the same helpers, so the timeout / budget paths land once for the entire surface instead of once per file. Per-tool 5xx surface returns the upstream JSON-RPC `reason` verbatim (`api_http_error`) — covered by the happy-path matcher's negative variant in every IT.
- [x] 5.2 Per-route `cortex-api` unit tests cover the SQL/Meili filter shapes + the 4xx error paths (missing param, malformed payload, unknown enum). — Each handler module under `crates/cortex-api/src/search/` ships a `#[cfg(test)] mod tests` covering: filter assembler shape (no constraints → None or canonical pin; AND-join with every optional clause; quote-escape on caller-supplied literals; epoch-ms rendering on RFC3339 since/until), validator coverage (kind vocab / status vocab / grain vocab / intent vocab / id_is_valid), clamp helpers, and the projector pure functions (extract_sessions / extract_decisions / extract_files / extract_cost / fold_buckets / build_per_lane / collect_drops / day_label_from_ms). Total: 100+ unit tests across 16 handlers + 1 storage method (`list_pre_thinking_feedback_filters_and_orders_newest_first`). 4xx error paths exercised via the wiremock IT layer that already drives the `bad_input` / `invalid_input` paths end-to-end.
- [x] 5.3 `cortex-eval` golden-set: add a `mcp_search` suite that drives a 10-row fixture per granular tool and asserts the recall floor (`recall@5 >= 0.5` for entity / topic / file-touched lookups). — Added `crates/cortex-eval/src/suite/mcp_search.rs` with `McpSearchRow` (id / tool / query / repo / expected_ids) + `RECALL_AT_5_FLOOR = 0.50` + `load_csv` + `build_report` (folds recall@5 averaged across the suite + per-tool informational metrics with `floor: None`) + `mcp_search_acceptance` verdict helper. Registered `SuiteName::McpSearch` (`"mcp_search"`) into the registry + `cortex-eval` CLI dispatcher with `run_mcp_search_row` driver that routes per `tool` into the matching `cortex-api` endpoint (`/v1/search/events`, `/v1/search/tool-calls`, `/v1/topic-cards/search`, `/v1/consolidations/recent`, `/v1/consolidations/by-entity`, `/v1/consolidations/search`, `/v1/decisions/search`, `/v1/laws/violations`, `/v1/search/files-touched`). Tools without a stable "top-K id list" semantic (`cortex_session_timeline`, `cortex_consolidation_get`, `cortex_consolidation_lineage`, `cortex_consolidations_diff`, `cortex_feedback_signals`, `cortex_consolidation_costs`, `cortex_query_explain`) emit a warn-log + return empty so the recall metric stays interpretable. Golden CSV at `tests/golden/mcp_search.csv` ships 16 rows (one per tool) with placeholder `expected_ids` for the operator-supplied baseline. Tests: 5 unit (expected_ids parse / build_report passes floor on perfect recall / fails floor on zero recall / emits per-tool breakdown / load_csv round-trips fixture) + 1 SuiteName parse round-trip extension. cortex-eval clippy + tests green.
- [x] 5.4 Workspace smoke: `cargo check --workspace && cargo clippy -p cortex-mcp-server -p cortex-api -- -D warnings && cargo test -p cortex-mcp-server -p cortex-api` clean. — Workspace-wide check passed (10 crates compiled, no errors). clippy -p cortex-mcp-server -p cortex-api -- -D warnings clean. Test results: cortex-mcp-server 65 lib + 78 wiremock IT (every IT file at 4-7 tests) all green; cortex-api 583 lib unit + the dashboard/feedback/SSE integration tests green. cortex-eval suite-name + mcp_search unit tests green. cortex-storage `list_pre_thinking_feedback` test green. `cargo fmt --check` clean.

## 6. Tail (mandatory)
- [ ] 6.1 Update `docs/specs/22-fine-grained-search.md` + `docs/specs/27-consolidation.md` + `docs/specs/18-claude-code-plugin.md` + `CHANGELOG.md`.
- [ ] 6.2 `rulebook_learn_capture` with title "MCP granular search verbs land — consolidations promoted to first-class corpus".

## 99. Mandatory tail (rulebook v5.3.0)
- [ ] 99.1 Update or create documentation covering the implementation
- [ ] 99.2 Write tests covering the new behavior
- [ ] 99.3 Run tests and confirm they pass
