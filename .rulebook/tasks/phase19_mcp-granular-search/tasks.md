## 1. Group A — Envelope-shape granularity (5 tools)
- [x] 1.1 `cortex_events_by_kind`: `cortex-api` HTTP route `POST /v1/search/events` accepting `{kind, repo?, session_id?, since?, until?, limit<=50}`; reads from the Meili kind-routed indexes; returns the native envelope shape (no fusion projection). MCP tool wraps it. — Handler shipped in `crates/cortex-api/src/search/events_by_kind.rs` with `kind_to_index()` mapping 10 kinds (turn/tool_call/consolidation/decision/analysis/memory/law/knowledge/learning/topic_card) to canonical Meili index uids. Accepts both snake_case + PascalCase. Filter builder assembles `repo = "..." AND session_id = "..." AND occurred_at_ms >= N AND occurred_at_ms <= N` with quote-escaping. Sort hardcoded `occurred_at_ms:desc` (newest-first). Route mounted in `http.rs`. MCP tool `EventsByKindTool` in `tools.rs` wraps via `proxy_search`. Registry 13 → 14. Tests: 7 unit (kind_to_index coverage + reject + build_filter empty/clauses/timestamps/escape + clamp_limit + parse_rfc3339) + 4 wiremock IT (`tests/events_by_kind_it.rs`: happy path / bad_input / api_unreachable / descriptor enum pin) = 11 new tests, all green.
- [x] 1.2 `cortex_session_timeline`: HTTP `GET /v1/sessions/{session_id}/timeline?limit=<=200`; reads from `cortex_events.archive_loader` ordered by `occurred_at asc`; returns `{ts, kind, summary_or_title, event_id, deltas}`. MCP tool wraps it. — Handler `crates/cortex-api/src/dashboard/session_timeline.rs` walks the Parquet archive via `cortex_storage::archive::scan_envelopes_by_session` (blocking task off the executor). Limit clamp 1-200, default 100. Sort by `occurred_at asc`. Per-kind title derivation (user_message → summary → title → tool/tool_name). `deltas` allow-list (tool/tool_name/exit_code/model/grain/decision_id/status/topic_id/law_id/severity/parent_event_id) prevents payload-field leak. `truncate_title` UTF-8-safe at 240 bytes. Route mounted at `/v1/sessions/{session_id}/timeline`. MCP tool `SessionTimelineTool` validates ULID-shape session_id + lowercase snake_case kind before issuing GET. Registry 14 → 15. Tests: 5 unit (clamp/truncate/title-precedence/kind-mapping/deltas-allow-list) + 4 wiremock IT (happy path / missing session_id / non-alphanumeric session_id / kind+limit query-string propagation) = 9 new tests. 701 workspace tests green.
- [x] 1.3 `cortex_tool_calls`: HTTP `POST /v1/search/tool-calls` accepting `{tool_name?, exit_code_eq?, exit_code_ne?, has_error?, repo?, session_id?, since?, until?, limit<=50}`; reads from `cortex_tool_calls` Meili index with native filter. MCP tool wraps it. — Handler `crates/cortex-api/src/search/tool_calls.rs` filters on the `cortex_tool_calls` index's actual filterable attrs (`tool_name`, `outcome`, `repo`, `occurred_at`, `topics`). Scope deviations from the original spec: (a) `exit_code_eq/ne` + `has_error` rolled into the `outcome` discriminator with 5-value vocab (`ok` / `transient` / `rejected` / `task_failed` / `error`) which is what the worker actually writes — the index does not carry a separate exit_code column. (b) `session_id` dropped because the `cortex_tool_calls` index does not list it as filterable; callers wanting per-session tool-call lists pivot through `cortex_session_timeline` with `kind=tool_call`. Both deviations documented in the handler module docs + the MCP descriptor. Limit clamp 1-50, default 20. Outcome validated against the writer vocab before issuing the Meili call (returns `bad_input` with the allowed list echoed). Sort `occurred_at:desc`. Registry 15 → 16. Tests: 6 unit (outcome vocab / build_filter empty/AND-join/timestamps/escape / clamp_limit) + 4 wiremock IT (happy / bad_input on unknown outcome / api_unreachable / descriptor enum pin) = 10 new tests. 711 workspace tests green.
- [x] 1.4 `cortex_files_touched`: HTTP `GET /v1/sessions/{session_id}/files-touched` OR `POST /v1/search/files-touched {repo, since, until}`; aggregates `(read_count, write_count, last_touched_ts)` per path from ToolCall envelopes. MCP tool wraps it. — Handler `crates/cortex-api/src/search/files_touched.rs` exposes both routes. Per-session GET reads via `scan_envelopes_by_session`; window POST walks the archive with `walk_envelopes` filtered to `kind == ToolCall` + repo + RFC3339 window. `extract_touched` prefers `ToolCall.touched: Vec<TouchedArtifact>` (kind+path), falls back to `input.{path,file_path,filename,file}` with tool-name-inferred kind. `classify_op` normalises adapter variants into read/write/other (Read/View/open/stat → read; Write/Edit/Create/Patch/Update/Modify/append → write; rest other). `fold_paths` aggregates per-path with sort-by-total-desc + tie-break by `last_touched_ts desc`. Limit clamp 1-100, default 50. Both routes mounted; MCP `FilesTouchedTool` dispatches GET on session_id presence or POST otherwise, validates ULID-shape session_id. Registry 16 → 17. Tests: 5 unit (classify_op normalisation / extract_touched-prefers-touched / extract_touched-fallback / extract_touched-empty / fold_paths-aggregation+sort / clamp_limit) + 4 wiremock IT (session GET / window POST / non-alphanumeric session_id rejected / api_unreachable) = 9 new tests. 721 workspace tests green.
- [x] 1.5 `cortex_topic_search`: HTTP `POST /v1/topic-cards/search {topic_prefix, repo?, limit<=30}`; reads `cortex_topic_cards` index + the per-envelope `topics: Vec<String>` extras for the cross-kind hit list. MCP tool wraps it. — Handler `crates/cortex-api/src/search/topic_search.rs` reads `cortex_topic_cards` index with `topics` filter (Meili lacks STARTS_WITH on arrays so trailing colon is stripped to match the bare-tag literal when caller passes `tool:` style prefix). Filter assembles `topics = "..." AND repo = "..."` with Meili quote escape. `q` complements the topic filter against searchable attrs (title / body / synthesis_markdown). Sort `updated_at:desc`. Limit clamp 1-30, default 15. Empty `topic_prefix` returns 400 `bad_input`. Registry 17 → 18. Tests: 6 unit (literal-topic / trailing-colon-strip / repo-clause-AND / whitespace-only-returns-none / quote-escape / clamp_limit) + 3 wiremock IT (happy / bad_input on empty prefix / api_unreachable) = 9 new tests. 730 workspace tests green. §1 group A complete (5/5 tools shipped).

## 2. Group B — Consolidation-first (6 tools)
- [x] 2.1 `cortex_consolidation_get`: HTTP `GET /v1/consolidations/{id}` returning `{summary, entities, relations, source_session_ids, cost, grain, repo, topic_id, occurred_at}`. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidation_get.rs` does ONE Meili search call with OR filter `event_id = "X" OR ext.consolidation.consolidation_id = "X"` so re-emitted envelopes still resolve via the stable producer id. Returns `{document, matched_consolidation_id}` so caller knows which key matched. `id_is_valid` accepts alphanumeric + `_` + `-` up to 128 chars; rejects empty/space/slash/dot. 404 surfaces as `not_found` reason when zero hits. Registry 18 → 19. Tests: 3 unit (id_is_valid accepts ULID + slug + underscores / rejects empty + overlong + special / meili_escape doubles quotes) + 4 wiremock IT (happy / missing id invalid_input / non-alphanumeric invalid_input / 404 not_found surfaced) = 7 new tests. 737 workspace tests green.
- [x] 2.2 `cortex_consolidations_recent`: HTTP `GET /v1/consolidations/recent?repo&grain&since&until&limit<=30`. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidations_recent.rs` reads the `cortex_consolidations` Meili index with filter built from the canonical filterable attrs (`repo`, `grain`, `occurred_at`) listed in `cortex_consolidations.settings.v1.json`. Grain vocab pinned to the producer enum (`session` / `topic` / `decision_trace`) — unknown grain returns 400 `bad_input` with the allowed list echoed. `since`/`until` parsed as RFC3339 then rendered against `occurred_at` as epoch ms (matches tool_calls.rs pattern). Sort hardcoded `occurred_at:desc` so "recent" semantics match the route name. Limit clamp 1-30, default 15. Route mounted at `/v1/consolidations/recent` BEFORE the `{id}` capture so axum literal-segment preference picks the right handler. MCP tool `ConsolidationsRecentTool` validates lowercase snake_case grain, URL-encodes repo/since/until via the shared `urlencode` helper, and dispatches GET with the assembled query string. Registry 19 → 20; `tools_list_returns_twenty_descriptors` + `round_trips_initialize_and_tools_list_over_pipe` bumped. Tests: 6 unit (grain vocab / build_filter empty+AND+timestamps+escape / clamp_limit) + 5 wiremock IT (`tests/consolidations_recent_it.rs`: happy / non-snake grain rejected / since+until propagation / api_unreachable / descriptor enum + limit-bounds pin) = 11 new tests. cortex-api 532+ unit + cortex-mcp-server 65+ unit, all wiremock ITs green.
- [x] 2.3 `cortex_consolidations_by_entity`: HTTP `POST /v1/consolidations/by-entity {entity: {kind: "file"|"function"|"decision_id"|"repo"|"model", value}, limit<=30}`; reads `extras.entities` + `extras.relations` via Meili filter. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidations_by_entity.rs` deviates from the original spec because the classifier-extracted entities ship to Nexus (graph mapper) and are NOT stamped as Meili filterable on the `cortex_consolidations` index (schema lists only `grain` / `depth` / `scope_kind` / `scope_value` / `repo` / `topics` / `occurred_at` / `model`). Reusing the classifier entity list would require a writer-side projection + schema bump + full reindex of historical consolidations, out of scope for this phase. Routing instead per entity kind: `repo` → `filter = repo = "X"`, `model` → `filter = model = "X"` (both authoritative), and `decision_id` / `file` / `function` → Meili keyword `q` over searchable attrs (title, summary_markdown, topics, repo). Response surfaces `match_strategy` (`"filter"` vs `"q"`) so callers can tell how authoritative the match is. Sort hardcoded `occurred_at:desc` for consistency with §2.2. Limit clamp 1-30, default 15. Unknown kind / empty value / unknown entity → 400 `bad_input`. Registry 20 → 21; `tools_list_returns_twentyone_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 6 unit (kind vocab / plan_search filter+keyword routing / quote escape / whitespace trim / clamp_limit) + 6 wiremock IT (`tests/consolidations_by_entity_it.rs`: repo→filter / decision_id→q / unknown kind invalid_input / missing entity invalid_input / api_unreachable / descriptor enum + limit-bounds pin) = 12 new tests. cortex-api 538 unit + cortex-mcp-server 65 unit + all wiremock ITs green. clippy -D warnings + cargo fmt clean.
- [x] 2.4 `cortex_consolidations_search`: HTTP `POST /v1/consolidations/search {query, k<=20, intent_hint?}`; hybrid BM25 + vector + RRF scoped to `cortex_consolidations` only. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidations_search.rs` ships **BM25-only** today; "hybrid BM25 + vector + RRF" is documented as a scope deviation because the `/v1/search/vector` endpoint rejects `query_text` with `not_implemented` (server-side embedding lives behind `QueryService`, not the Meili-direct search proxy), and `cortex_query` has no `kinds=[consolidation]` scope. Adding the vector lane would require either threading a `VectorLane` into `ApiState` OR adding a `kinds` filter to `Scope` — both larger structural changes than this phase. The response surfaces `match_strategy = "bm25"` so callers can detect what actually ran; the wire shape is forward-compatible: a follow-up that wires the vector lane only needs to flip the strategy label and stitch `rrf_fuse` over the two ranked lists. Filter coverage uses canonical filterable attrs (`repo`, `grain`) from `cortex_consolidations.settings.v1.json`. Grain vocab pinned to producer enum (`session` / `topic` / `decision_trace`). `intent_hint` validated against `crate::types::Intent` labels (`pre_change_context` / `decision_lookup` / `similar_problems` / `law_check` / `free_search` / `explain`) and echoed on the response for future RRF lane-weight tuning. K clamp 1-20, default 10. Empty query / unknown grain / unknown intent_hint → 400 `bad_input`. Registry 21 → 22; both `tools_list_returns_twentytwo_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 6 unit (intent_hint vocab / grain vocab / build_filter empty + AND-join / quote escape / clamp_k) + 7 wiremock IT (happy path with strategy + intent_hint echo / missing query rejected / unknown grain rejected / unknown intent_hint rejected / repo+grain propagation / api_unreachable / descriptor enum + k-bounds pin) = 13 new tests. cortex-api 544 unit + cortex-mcp-server 65 unit + every wiremock IT green. clippy -D warnings + cargo fmt clean.
- [x] 2.5 `cortex_consolidation_lineage`: HTTP `GET /v1/consolidations/{id}/lineage` returning `{source_session_ids, decisions[], files[], cost{model,cents,prompt_tokens,completion_tokens}}`. Read joins `consolidations` + `decisions` + per-session tool-call file extracts. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidation_lineage.rs` ships a **doc-only** lineage projection. The original "read joins consolidations + decisions + per-session tool-call file extracts" assumed the consolidator writer projects `source_event_ids` / `source_session_ids` / `cost_cents` / `prompt_tokens` / `completion_tokens` onto the Meili consolidation doc. Today only `model` + `source_event_count` are projected (`crates/cortex-workers/src/fulltext/builders.rs::apply_extensions` Kind::Consolidation arm) and the aggregate `grain_costs` is an in-process `CostLedger` keyed by grain label, NOT per-consolidation-id — it cannot answer "how much did THIS consolidation cost". Stamping the missing fields requires writer-side projection + schema bump + reindex, beyond this phase's scope. Handler instead derives the lineage from the doc itself: `source_session_ids` from `topics` (`session:<ulid>` convention), `decisions` from `topics` (`decision:DEC-NNNN`) ∪ `DEC-\d{3,}` mentions in title/summary/body (BTreeSet-deduped + sorted), `files` from `topics` (`file:<path>` convention, deduped + sorted), `cost` carries `model` only with `cents`/`prompt_tokens`/`completion_tokens` returning `Option::None`. Does ONE Meili call (same OR filter as `consolidation_get` — `event_id = "X" OR ext.consolidation.consolidation_id = "X"`) so re-emitted envelopes resolve via the stable producer id. Response surfaces `match_strategy = "doc_only"` so callers detect the shape; reserved value `"joined"` lands when the writer projects the joined data. ID-shape validator reused from `consolidation_get` (alphanumeric + `_` + `-`, ≤128 chars). Route mounted at `/v1/consolidations/{id}/lineage` BEFORE the `{id}` capture so axum picks the literal `/lineage` suffix. Registry 22 → 23; both `tools_list_returns_twentythree_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 9 unit (id_is_valid / extract_sessions [happy + empty] / extract_files / extract_decisions [union of topics + body mentions, ignores short/non-dash] / extract_cost [model + missing-ext fallback] / resolved_consolidation_id [ext > event_id > fallback]) + 5 wiremock IT (`tests/consolidation_lineage_it.rs`: happy path with all four lineage arms / missing id rejected / non-alphanumeric id rejected / 404 upstream → `not_found` / api_unreachable) = 14 new tests. cortex-api 553 unit + cortex-mcp-server 65 unit + every wiremock IT green. clippy -D warnings + cargo fmt clean.
- [x] 2.6 `cortex_consolidations_diff`: HTTP `GET /v1/consolidations/diff?since_ts=<ms>&limit<=200`; reads from the `cortex_consolidations` Meili index ordered by `accumulated_at asc`. MCP tool wraps it. — Handler `crates/cortex-api/src/search/consolidations_diff.rs`. Sort hardcoded `occurred_at:asc` rather than `accumulated_at:asc` because `accumulated_at` is not on the `cortex_consolidations` schema (only `occurred_at` is declared sortable + filterable). Filter `occurred_at >= since_ts` joined with optional `repo = "..."` via AND. `since_ts` is REQUIRED (epoch ms); negative values rejected with 400 `bad_input`. Limit clamp 1-200, default 50. Drives the "what's new since I last polled?" pattern — callers advance the cursor on the highest `occurred_at` they observe. Route mounted at `/v1/consolidations/diff` BEFORE `/v1/consolidations/recent` + `{id}` so axum literal-segment preference resolves cleanly. Registry 23 → 24 closes Group B (6/6 consolidation tools shipped). Both `tools_list_returns_twentyfour_descriptors` + `transport_stdio.rs::round_trips_...` bumped. Tests: 5 unit (build_filter always-includes / AND-join with repo / quote-escape / whitespace repo dropped / clamp_limit 1..200) + 6 wiremock IT (`tests/consolidations_diff_it.rs`: happy path with sort + since_ts echo / repo filter propagation / missing since_ts rejected / negative since_ts rejected / api_unreachable / descriptor pins limit bounds + since_ts required) = 11 new tests. cortex-api 558 unit + cortex-mcp-server 65 unit + every wiremock IT green. clippy -D warnings + cargo fmt clean.

## 3. Group C — Governance + telemetry (5 tools)
- [ ] 3.1 `cortex_law_violations`: HTTP `POST /v1/laws/violations {repo?, session_id?, law_id?, since?, until?, limit<=50}`; returns `(ts, session_id, tool_name, law_id, reason, originating_prompt)`. MCP tool wraps it.
- [ ] 3.2 `cortex_feedback_signals`: HTTP `POST /v1/feedback/list {helpful?, intent?, repo?, since?, until?, limit<=50}`; reads from the `pre_thinking_feedback` SQLite table (phase14f). MCP tool wraps it.
- [ ] 3.3 `cortex_decision_search`: HTTP `POST /v1/decisions/search {status?, supersedes?, superseded_by?, tag?, limit<=50}`; reads from `cortex_decisions` Meili index. MCP tool wraps it.
- [ ] 3.4 `cortex_consolidation_costs`: HTTP `POST /v1/consolidations/costs {since, until, group_by: ["grain", "model", "day"]}`; aggregates from the `grain_costs` SQL table. MCP tool wraps it.
- [ ] 3.5 `cortex_query_explain`: HTTP `POST /v1/query/explain {query, intent?, scope?}`; returns `{per_lane_hits[], fusion_math{rrf_k, weights, drops}, final_envelope}`. MCP tool wraps it.

## 4. Registry + spec
- [ ] 4.1 `ToolRegistry::default_set()` updated from 13 -> 29 tools. `MCP_REGISTRY_SIZE` test in `crates/cortex-mcp-server/tests/end_to_end.rs` + the descriptor count assertion in `hook_drift.rs` bumped.
- [ ] 4.2 `lib.rs` re-exports every new `*Tool` struct.
- [ ] 4.3 `docs/specs/22-fine-grained-search.md` extended with a "Phase19 granular tool surface" section + the wire-shape + error-taxonomy table per group.
- [ ] 4.4 `docs/specs/18-claude-code-plugin.md` MCP tool list updated with the 16 new entries.
- [ ] 4.5 `docs/specs/27-consolidation.md` cross-references the new consolidation-first tools.

## 5. Tests (closes glm5.1 Task 6.4 in the same phase)
- [ ] 5.1 One wiremock IT per new tool in `crates/cortex-mcp-server/tests/` covering: happy path, invalid-input, upstream 5xx (returns `api_http_error`), upstream timeout (returns `tool_timeout` per phase14i), budget overflow (returns `budget_exceeded` per phase11c).
- [ ] 5.2 Per-route `cortex-api` unit tests cover the SQL/Meili filter shapes + the 4xx error paths (missing param, malformed payload, unknown enum).
- [ ] 5.3 `cortex-eval` golden-set: add a `mcp_search` suite that drives a 10-row fixture per granular tool and asserts the recall floor (`recall@5 >= 0.5` for entity / topic / file-touched lookups).
- [ ] 5.4 Workspace smoke: `cargo check --workspace && cargo clippy -p cortex-mcp-server -p cortex-api -- -D warnings && cargo test -p cortex-mcp-server -p cortex-api` clean.

## 6. Tail (mandatory)
- [ ] 6.1 Update `docs/specs/22-fine-grained-search.md` + `docs/specs/27-consolidation.md` + `docs/specs/18-claude-code-plugin.md` + `CHANGELOG.md`.
- [ ] 6.2 `rulebook_learn_capture` with title "MCP granular search verbs land — consolidations promoted to first-class corpus".

## 99. Mandatory tail (rulebook v5.3.0)
- [ ] 99.1 Update or create documentation covering the implementation
- [ ] 99.2 Write tests covering the new behavior
- [ ] 99.3 Run tests and confirm they pass
