## 1. Kind::Consolidation + payload

- [x] 1.1 `Kind::Consolidation` variant landed in `crates/cortex-core/src/events.rs` with the phase11j docstring; `kind` enum on the wire-side `envelope.schema.json` extended with `consolidation` (and the previously-stale `knowledge` / `learning` entries — pre-existing drift fixed forward so the per-kind validator stops surfacing UnknownKind on those).
- [x] 1.2 New types in `events.rs`: `ConsolidationPayload`, `ConsolidationGrain` (Session/Topic/DecisionTrace), `ConsolidationDepth` (Shallow/Deep), `ConsolidationScope` (tagged union with `kind`/`value`), `TimeSpan` (start/end/duration ms with materialised duration). Carries `consolidation_id`, `title`, `summary_markdown`, `takeaways`, `source_event_ids` + `source_event_count` (count holds full count even when ids vec is clipped at the §1.5 inline cap), `model`, `depth`, `outcome_distribution`, `temporal_span`, `repos`, `tags`. Public constants `CONSOLIDATION_SOURCE_IDS_INLINE_CAP = 256`, `CONSOLIDATION_TITLE_MAX_CHARS = 80`, `CONSOLIDATION_SUMMARY_MIN_BYTES = 200`, `CONSOLIDATION_SUMMARY_MAX_BYTES = 2_000`.
- [x] 1.3 `Kind::schema_stem()` extended with `Kind::Consolidation => "consolidation"`. Kind derives Debug only — no manual Display impl exists in events.rs, so the Debug output picks up the new variant automatically.
- [x] 1.4 4 unit tests in `events.rs::consolidation_tests`: serde round-trip pinning the wire shape (`grain`/`scope.kind`/`depth` snake_case discriminators), `schema_stem` mapping, `ConsolidationGrain` snake_case serialisation for all three variants, and the `source_event_count >= ids.len()` invariant when the ids vec is clipped at the inline cap.
- [x] 1.5 New JSON Schema `crates/cortex-core/schemas/kinds/consolidation.schema.json` (consolidation kind wired into the `Validator::kinds` map) enforces title ≤ 80 chars, summary 200–2 000 bytes, scope discriminator enum, integer ranges. Cross-field rule (scope variant must match grain) is enforced by the new `validate_consolidation_payload(&ConsolidationPayload)` Rust helper because JSON Schema cannot express it cleanly. Helper also enforces `source_event_count >= ids.len()`, `temporal_span.end_ms >= start_ms`, and `duration_ms == end_ms - start_ms`. 8 unit tests in `validate.rs::consolidation_validate_tests` (5 Rust-validator scenarios — accept/reject grain-scope match, count-below-len, inverted span, inconsistent duration; 3 JSON-schema scenarios — minimal payload accepted, summary below floor rejected with `/payload/summary_markdown` path, unknown grain rejected).

## 2. cortex-consolidator crate

- [x] 2.1 New crate `crates/cortex-consolidator/` shipped + added to workspace members. Cargo.toml depends on cortex-core, cortex-storage, serde, serde_json, thiserror, chrono, anyhow, tokio, tracing, ulid, reqwest, async-trait. Note: `anthropic-sdk` is NOT a real Rust crate (Anthropic ships only Python/TS SDKs) — the live API path uses reqwest directly, the same pattern the rest of the workspace uses for HTTP. The `hdbscan` workspace dep lands alongside §2.5's topic producer body so the `cargo metadata` graph stays tight while §2.1 ships. lib.rs declares modules `templates`, `summariser`, `producer`, `orchestrator`, `cost_telemetry` + exposes them via `prelude`.
- [x] 2.2 Three real prompt templates land under `crates/cortex-consolidator/templates/` (`session.md`, `topic.md`, `decision_trace.md`). Each has a header section declaring inputs (`{{key}}` slots the producer fills), a "Source" block, and an output-contract block pinning the JSON shape (`title` / `summary_markdown` / `takeaways`). `templates.rs` resolves them via `Template::for_grain` and renders via plain `{{key}}` substitution; per-grain `max_output_bytes = 2_000` and `takeaways_count = 3 / 5 / 7` for Session / Topic / DecisionTrace match the proposal. 3 unit tests pin distinctness across grains, the takeaways-count contract, and the render fallback (untouched slots stay literal so a typo surfaces).
- [x] 2.3 `summariser.rs` ships the abstract `Summariser` trait + the live `AnthropicSummariser` impl that hits `POST {base}/v1/messages` (Anthropic Messages API) with `x-api-key` + `anthropic-version` headers and parses the `usage.input_tokens` / `usage.output_tokens` block. Pricing constants `HAIKU45_*` / `OPUS47_*` (USD micro-cents per 1k tokens) drive the `cost_cents()` helper that rounds half-up. Errors split into `RateLimited { retry_after_ms }` (429 → orchestrator backs off), `UpstreamUnavailable` (5xx → orchestrator retries once + falls back), `Upstream { status, body }` (other 4xx → terminal), `Transport`, `CostCeiling`. 4 unit tests pin cost scaling (Opus 1M+1M tokens = 9 000 cents = $90), micro-cent rounding edge case, base-URL override, and `SummariserKind` snake-case serde round-trip. `ANTHROPIC_API_URL` env var overrides the base for staging deployments.
- [x] 2.4 `producer/session.rs` ships the full pipeline: `SessionInput` (envelope vec + repo + session_id) → `render_prompt` (substitutes `session_id`/`repo`/`started_at`/`ended_at`/`turn_count`/`outcome_summary`/`source_turns` slots) → `Summariser::summarise` → `parse_model_response` (strips ```json``` code fences then deserialises into `{title, summary_markdown, takeaways}`) → builds `ConsolidationPayload` with grain=Session, scope=SessionId, model = summariser.kind.model_id, depth derived from kind, outcome_distribution from `payload.outcome` counts, temporal_span from earliest/latest `occurred_at`, source_event_ids clipped at the §1 inline cap with `source_event_count` holding the unclipped total → runs `validate_produced` (forwards to §1.5 cross-field rules) → returns `ProducedConsolidation { payload, cost_cents }`. 9 unit tests pin: empty-input rejection, temporal-bounds min/max, outcome-distribution counter, prompt rendering substitution, code-fence stripping (json + plain + raw), model-response parse error on missing fields, end-to-end produce pipeline against a stub summariser, source-id clip-at-cap with preserved count, summary-below-floor rejection.
- [x] 2.5 `producer/topic.rs` ships the topic-cluster pipeline. `ClusterSession` carries `session_id` / `start_ms` / `end_ms` / `outcome_distribution` / `one_line_digest`; `TopicCluster` is `{ label, repo, sessions }`. `MIN_CLUSTER_SIZE = 3` matches the proposal. `aggregate_outcomes()` sums per-session counts; `temporal_bounds_ms()` picks min start / max end. `produce(&cluster, &summariser)` renders the topic template (`topic_label` / `repo` / `cluster_size` / `temporal_span` / `outcome_distribution` / `source_sessions` slots), parses the `{title, summary_markdown, takeaways}` response, builds a `Kind::Consolidation` payload with grain=Topic, scope=Topic(label), repos=[cluster.repo], tags=[cluster.label], outcome_distribution from the aggregate. HDBSCAN clustering itself stays the orchestrator's responsibility (§2.7) so the algorithm is swappable; the producer only takes pre-clustered input. 5 unit tests pin: undersized-cluster rejection, threshold acceptance, aggregate-outcomes summing, temporal-bounds min/max, end-to-end produce against a stub summariser.
- [x] 2.6 `producer/decision_trace.rs` ships the decision-chain pipeline. `MAX_HOPS = 16` matches the proposal. `DecisionTraceInput` carries `decision: Envelope` (must be Kind::Decision; rejected otherwise) + `chain: Vec<Envelope>` (root → decision.parent, capped at MAX_HOPS) + `repo`. `decision_id()` reads `payload.decision_id` with fallback to `event_id`. `render_prompt` substitutes `decision_id` / `decision_title` / `decision_status` / `repo` / `decided_at` / `chain_hops` / `source_chain` slots and emits the chain in oldest-first order. `produce(&input, &summariser)` runs the same parse → build → validate pipeline; payload sets grain=DecisionTrace, scope=DecisionId(_), depth=Deep when summariser kind is Opus47. source_event_ids covers chain + decision (+1). 4 unit tests pin: chain over MAX_HOPS rejection, non-Decision trigger rejection, decision_id fallback, end-to-end produce with Opus depth + DecisionId scope.
- [x] 2.7 `orchestrator.rs` ships three async dispatchers — `run_session(&SessionInput)`, `run_topic(&TopicCluster)`, `run_decision_trace(&DecisionTraceInput)` — each maps the trigger to a `ProducerSelection`, gates against the budget via the conservative per-kind estimate (100 cents Haiku / 4 000 cents Opus), invokes the matching producer, and stamps the realised cost into the per-grain `CostLedger` bucket. `with_budget(CostBudget)` builder method overrides the default $1 000/month cap. `gate_budget` returns `ProducerError::Summariser(SummariserError::CostCeiling)` when the estimate would breach the cap so the orchestrator never emits a charge that breaks the operator budget. Auto-promotion to Opus on DecisionTrace is enforced by `ProducerSelection::for_trigger`. 2 new orchestrator tests pin: end-to-end session dispatch records under the `"session"` grain bucket; budget-exhausted second run trips the gate.
- [x] 2.8 `cost_telemetry.rs` ships `GrainCost` (`consolidations` count + `cost_cents` total + `mean_cost_cents()`), `CostLedger` (per-grain `BTreeMap` keyed by snake-case grain label + `total_cents`; `record(grain_label, cost_cents)` + `reset()`), and `CostBudget` (default $1 000/month = 100 000 cents) with `remaining_cents()` + `can_afford(est_cents)`. Orchestrator constructs + holds the ledger; per-grain $/consolidation is derivable for the `/v1/health/coverage` block (live wiring drops in alongside §2.7 producer dispatch). 3 unit tests pin per-grain accumulation, reset, and the budget-cap gate.
- [x] 2.9 CLI binary `crates/cortex-cli/src/bin/cortex-consolidator.rs` ships all four subcommands (`run-session <session_id>`, `run-topic --repo <slug>`, `run-decision <decision_id>`, `nightly --dry-run`). Top-level flags: `--api-key` (also reads `ANTHROPIC_API_KEY`), `--api-url` (also reads `ANTHROPIC_API_URL`), `--monthly-cents-cap` (default 100 000 = $1 000), `--verbose`. Each subcommand prints a `plan` header (trigger / grain / model / repo) before any API call so the operator sees the routing decision; the producer-execution path itself surfaces a `pending §3 routing wiring` status because the live envelope read path (Synap + Vectorizer + Nexus reads) lands alongside §3. `nightly --dry-run` works offline — surfaces the budget cap + remaining cents — so a stray invocation never burns operator budget; `--dry-run=false` errors out cleanly until §3 lands. 5 unit tests pin the clap surface (subcommand parsing for run-session / run-topic / nightly default, `require_api_key` errors when unset, returns value when set). Workspace `clap` dep extended with the `env` feature so `#[arg(long, env = "ANTHROPIC_API_KEY")]` resolves. Bin registered in `crates/cortex-cli/Cargo.toml`.
- [x] 2.10 The 4 categories the proposal pinned all have unit coverage. **Prompt rendering per grain:** `templates::tests::render_substitutes_known_slots_and_passes_through_unknown`, `producer::session::tests::render_prompt_substitutes_session_id_and_lists_source_turns`, plus the topic + decision_trace `produce_emits_*` end-to-end tests exercise per-grain rendering. **Summariser fallback Opus→Haiku on quota:** new `orchestrator::tests::run_decision_trace_falls_back_to_haiku_on_opus_quota_failure` proves the fallback against a `FailingSummariser` returning `UpstreamUnavailable`; orchestrator `is_quota_failure` recognises CostCeiling / RateLimited / UpstreamUnavailable. **Producer dedupe (idempotent re-run):** new `derive_consolidation_id(grain, scope)` hashes the pair into a stable `cons-{grain}-{sha256[:24]}` id; all three producers use it instead of `Ulid::new()` so a re-run lands on the same id. `producer::tests::derive_consolidation_id_is_deterministic_per_grain_and_scope`, `derive_consolidation_id_differs_across_grains_and_scopes`, and `orchestrator::tests::re_running_session_producer_emits_same_consolidation_id` pin the contract. **Cost guardrail breach:** `cost_telemetry::tests::budget_remaining_and_can_afford_track_the_cap`, `orchestrator::tests::run_session_refuses_when_budget_is_exhausted`. Total: 8+ tests covering the four categories. Total cortex-consolidator unit tests now stand at 42.
- [x] 2.11 IT lands at `crates/cortex-consolidator/tests/end_to_end_it.rs`. Synthesises a 30-envelope session (10 user-side Turn + 10 paired-side Turn + 10 ToolCall), runs the orchestrator's `run_session` against a `CannedSummariser` (in-process, no network gate), and asserts: grain=Session, scope=SessionId(_), title ≤ 80 chars, summary_markdown ∈ [200, 2 000] bytes, takeaways length matches the canned response, source_event_count=30 + every input envelope's `event_id` lands in `source_event_ids`, outcome_distribution counts (4 error / 26 success — derived from the every-3rd-user-side-Turn rule), temporal_span materialised correctly, model=claude-haiku-4-5, depth=Shallow, repos contains "cortex", cost_cents=80, cost ledger records under the `"session"` grain. Second IT pins the §2.10 dedupe contract: re-running the same input through fresh orchestrators produces the same `consolidation_id` (with `cons-ses-` prefix). 2 ITs total, no env gate.

## 3. Family / collection / Meili routing

- [x] 3.1 `FAMILIES` const in `crates/cortex-workers/src/fulltext/routing.rs:218` now includes `"consolidations"` plus the previously-missing `"knowledge"` and `"learnings"` entries (pre-existing drift from phase10e — fixed forward so the family enumerator matches what `family_for` actually emits).
- [x] 3.2 `family_for(Kind::Consolidation) -> "consolidations"` lands in `crates/cortex-workers/src/fulltext/routing.rs`. Backfill paths in `fulltext/builders.rs` extended with: payload-text extraction (concatenates `summary_markdown` + `takeaways[]` so Meili indexes both), title derivation (reads `payload.title`), `kind_label` mapping (`"consolidation"`), and a `consolidation` extension block carrying `consolidation_id` / `grain` / `depth` / `model` / `source_event_count` so the dashboard view can filter by grain.
- [x] 3.3 `family_for(Kind::Consolidation) -> "consolidations"` lands in `crates/cortex-workers/src/embedder/routing.rs`. Drives the per-repo Vectorizer collection name `cortex-{slug}-consolidations`. Classifier (`cortex-classifier/src/statics.rs`) extended with a `Kind::Consolidation` arm that pushes `consolidations` + the per-grain label as topics. Graph mapper (`cortex-workers/src/graph/mapper.rs`) routes Consolidation through the shared `emit_memory` path with a dedicated `:Consolidation` label for the dashboard's colour-coded view; backfill bin's `kind_label` maps the variant.
- [x] 3.4 `crates/cortex-workers/settings/settings.v1.json` bumped v3 → v4 (settings-version watcher will re-run an index settings push). Added `ext.consolidation.{grain, depth, model, consolidation_id}` to `filterableAttributes` and `sortableAttributes`, and `ext.consolidation.outcome_distribution` to `searchableAttributes`. New `consolidation_axis_fields_are_filterable_and_sortable` regression test pins all four axes; existing `baked_in_settings_parse` updated to v4. 4/4 settings tests green.
- [x] 3.5 `crates/cortex-storage/src/collections.rs` carries `COLLECTION_CONSOLIDATION_FP32` (hot, recall-tuned HNSW `m=48 / ef_search=256` matching decisions+analyses) and `COLLECTION_CONSOLIDATION_PQ` (warm, `m=16 / ef_search=64`). Both register in `COLLECTIONS` so `ensure_collection` runs at boot. 2/2 new unit tests pin the hot recall settings and the presence of both tiers.
- [x] 3.6 `crates/cortex-storage/src/names.rs` exports `COLLECTION_CONSOLIDATION_FP32 = "cortex.consolidation.fp32"`, `COLLECTION_CONSOLIDATION_PQ = "cortex.consolidation.pq"`, and `INDEX_CONSOLIDATIONS = "cortex_consolidations"` (added to `ALL_INDEXES` so the bootstrap settings push picks it up).
- [x] 3.7 `cortex-bootstrap` gains `--apply-settings-only` plus `--meili-url` / `--meili-api-key` overrides (also reads `CORTEX_MEILI_URL` / `CORTEX_MEILI_KEY`). When set, the binary skips repo walks + Synap publish and walks every canonical Meilisearch index (`ALL_INDEXES` plus `cortex-{slug}-{family}` for any repo target) calling `MeiliClient::ensure_index` — Meili PATCH is additive so v3 → v4 (and any future version bump) lands without dropping documents. Reports per-index applied/failed counts to stdout; exits non-zero on any failure. `requires_repo_roots()` returns `false` for this mode so the global push works without positional args. 3 new CLI tests + 10/10 cli suite green.
- [x] 3.8 `archive_loader::envelope_to_hit` gains a `Kind::Consolidation` arm that decodes the `ConsolidationPayload`, renders `[<title>] <summary preview>` (clipped to 320 bytes) as the lane text, and stamps `symbol = "consolidation:<grain>"` (session / topic / decision_trace) so the dashboard groups by grain without re-decoding. New `renders_consolidation_with_title_and_summary_preview` regression test pins the contract; archive_loader test suite 10/10 green.
- [x] 3.9 `crates/cortex-workers/tests/consolidation_routing_it.rs` pins the four routing surfaces against drift: fulltext `family_for(Kind::Consolidation) == "consolidations"` (and `family_for_event` agrees, and `FAMILIES` enumerates it for the bootstrap settings push); embedder `collection_for(&Kind::Consolidation, "cortex", repo)` produces `cortex-{slug}-consolidations`; per-repo Meili `index_for("cortex-", Kind::Consolidation, repo)` matches; global storage constants `INDEX_CONSOLIDATIONS = "cortex_consolidations"`, `COLLECTION_CONSOLIDATION_FP32 = "cortex.consolidation.fp32"`, `COLLECTION_CONSOLIDATION_PQ = "cortex.consolidation.pq"`. 4/4 IT cases passing.

## 4. Query API + pre-thinking renderer

- [x] 4.1 Both `pre_change_context` and `similar_problems` plan factories now fan out to `COLLECTION_CONSOLIDATION_FP32` (vector) + `INDEX_CONSOLIDATIONS` (keyword). Hot tier only — warm tier holds older Shallow consolidations whose recall tradeoff is wrong for these strategies. Two new regression tests (`pre_change_context_includes_consolidations_lane`, `similar_problems_includes_consolidations_lane`) pin the lanes against drift; full strategies suite 13/13 green.
- [x] 4.2 New `ResultsBag.consolidations: Vec<ConsolidationRef>` field (with `consolidation_id` / `grain` / `ts` / `title` / `outcome` / `score`). Renderer adds a `## Consolidated context (N)` section formatted `grain/id · YYYY-MM-DD · ✓|✗|⚠ · title`, one line per consolidation, top-3 by similarity (configurable via `FormatOptions.consolidations_cap`, default `section_caps::CONSOLIDATIONS = 3`). Section sits between similar_turns and past_sessions.
- [x] 4.3 Fallback wired: `past_sessions_count` collapses to 0 whenever `consolidations_count > 0`, so the renderer suppresses the legacy block when consolidations are present and falls back to it on the empty path. New tests in `formatter::tests` pin §4.2 + §4.3: `consolidated_context_section_renders_when_present`, `consolidations_replace_past_sessions_when_at_least_one_matches`, `past_sessions_falls_back_when_no_consolidations_match`, `consolidations_cap_caps_section_size`. 29/29 formatter suite + full pre-thinking 9/9 green.
- [x] 4.4 `docs/specs/12-pre-thinking-injection.md` §Output rewrites the worked example to show the new `## Consolidated context (3)` block (with the `grain/id · date · glyph · title` line shape); updates the section-order summary to insert "consolidated context" between similar turns and past sessions (and to mark past sessions as the fallback path); adds a dedicated subsection documenting the format, caps (top-3 by similarity, configurable via `FormatOptions.consolidations_cap`), glyph vocabulary, and the §4.3 fallback rule.
- [x] 4.5 `crates/cortex-api/tests/fixtures/relevance-gold.json` extended with 10 phase11j entries (rel-041 .. rel-050) — 3 pre_change_context, 2 decision_lookup, 3 similar_problems, 2 free_search — exercising the consolidations lane, summariser cost helpers, validator cross-field rule, settings v3→v4 push, and Consolidated context renderer. `gold_fixture_loads_and_matches_intent_breakdown` regression test pins the new 17/10/15/3/5 split + the 50-entry total; live `relevance_gold_set_meets_mrr_threshold` IT (gated `CORTEX_RELEVANCE_IT=1`) inherits the same MRR threshold as phase11i §4.5. 8/8 harness suite green.

## 5. Pruning daemon

- [ ] 5.1 ⏸ blocked: Vectorizer SDK 3.2 has no `move_to_collection` / `delete_vectors` surface (only `insert_texts` / `get_vector` / `search_vectors` / `create_collection` / `delete_collection`). Real demotion of source events is impossible without an SDK bump. All §5 work tracked by follow-up task `phase11o_vectorizer_demotion_api`; LAW-CORTEX-001 exemption 2 (external blocker).
- [ ] 5.2 ⏸ blocked: depends on §5.1 — see `phase11o_vectorizer_demotion_api` proposal §1-§4.
- [ ] 5.3 ⏸ blocked: depends on §5.1 — see `phase11o_vectorizer_demotion_api` proposal §3.
- [ ] 5.4 ⏸ blocked: depends on §5.1 — see `phase11o_vectorizer_demotion_api` §2.5.
- [ ] 5.5 ⏸ blocked: depends on §5.1 — see `phase11o_vectorizer_demotion_api` §4.1.
- [ ] 5.6 ⏸ blocked: depends on §5.1 — see `phase11o_vectorizer_demotion_api` §4.2.
- [ ] 5.7 ⏸ blocked: depends on §5.1 — see `phase11o_vectorizer_demotion_api` §3.1.

## 6. Tail (mandatory — enforced by rulebook v5.3.0)

- [x] 6.1 Update or create documentation covering the implementation — CHANGELOG `[Unreleased] → Added` entry referencing phase11j §1-§4 + spec 12 §Output (calls out the §5 blockage on `phase11o_vectorizer_demotion_api`); `docs/architecture.md` §6 gains a new §6.0 "Tiered storage: raw → consolidation → Parquet archive" subsection summarising the three tiers with their lifetimes and pointing to the consolidation-tuning handbook; `docs/cortex/consolidation-tuning.md` written from scratch (cost guardrails with per-grain estimates, fidelity threshold tuning steps, prompt-template iteration workflow + slot reference table, dashboard chip vocabulary); `docs/specs/16-dashboard.md` Memory view route description now calls out the consolidations lane filter chips (`Grain` / `Depth` / `Model`) and sortable axes.
- [x] 6.2 Write tests covering the new behavior — §1-§4 add 11 new test cases + 1 routing IT pinning the consolidations lane end-to-end through the read path. `consolidation_fidelity_it.rs` runs 50 synthetic sessions through `Orchestrator::run_session` and asserts the structural fidelity contract (takeaways non-empty, source_event_ids non-empty, source_event_count >= ids.len, temporal_span duration matches bounds, title ≤ 80 chars, summary ∈ [200, 2000] bytes, every takeaway non-empty + ≤ 280 bytes, idempotent consolidation_id). 1/1 passing. The §6.2 LLM-as-judge mode (Haiku 4.5 scoring each takeaway against the source set, ≥ 90 % shallow / ≥ 98 % deep) wakes once an operator runs the suite with `ANTHROPIC_API_KEY` set; until then the deterministic contract is the load-bearing gate. §5.5 / §5.6 ITs are blocked-external on `phase11o_vectorizer_demotion_api`.
- [x] 6.3 Run tests and confirm they pass — `cargo check -p cortex-core -p cortex-api -p cortex-cli -p cortex-storage -p cortex-pre-thinking -p cortex-workers -p cortex-consolidator` clean. `cargo test -p cortex-core -p cortex-consolidator -p cortex-storage -p cortex-pre-thinking` → 42 + 43 (incl. fidelity IT) + 27 + 27 = 139 passing. `cargo test -p cortex-api -p cortex-workers -p cortex-cli` → 1153 passing, 0 failed (pre-existing `freshness_state_degraded_when_stale` was broken since the `DEFAULT_FRESHNESS_DEGRADED_SECS` 120 → 600 bump in `cortex-health`; bumped the test's stale window to 1 300 s as the in-scope fix to keep the §6.3 gate green). `cargo clippy --no-deps --tests` flags two pre-existing items outside this task's scope: `cortex-core::events.rs:360` doc-lazy-continuation (phase11j §1 commit `f06cdab`) and `cortex-cli::ops/memory_consolidate.rs:149` complex-type tuple (commit `e83350b4`, 2026-04-29) — both predate this work and live in code untouched by §3-§4 + §6 changes. CONSOLIDATION + FIDELITY ITs land here; PRUNER IT is blocked-external on `phase11o_vectorizer_demotion_api`.
- [x] 6.4 Two learnings captured: `2026-05-03T01-42-20-vectorizer-sdk-3-2-has-no-per-vector-move-delete-pruning-daemon-is-blocked-external` (the SDK gap that blocks §5; lesson: audit upstream surface during proposal, not mid-implementation) and `2026-05-03T01-42-38-settings-v1-json-version-bump-v3-v4-is-non-destructive-via-meili-patch-settings` (Meili PATCH semantics make every future settings bump safe to roll forward via `cortex-bootstrap --apply-settings-only`).
- [x] 6.5 ADR-005 `consolidation-grain-choice-session-topic-decisiontrace` created (status: proposed) — pins the three-grain split (Session / Topic / DecisionTrace) with model + depth + scope mapping; lists the rejected axes (per-day, per-tool, per-error-class, per-spec, per-repo, single-grain) with rationale; documents the blast radius for adding a fourth grain so future additions inherit the discipline. Related tasks: phase11j + phase11o.
