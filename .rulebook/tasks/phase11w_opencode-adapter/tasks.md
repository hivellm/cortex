## 1. Phase 0 — Spike & open questions
- [x] 1.1 Install OpenCode locally (CLI + Bun runtime). Document version pinned in `docs/analysis/opencode-adapter/00-spike.md`. — Spike doc records the OpenCode CLI version is pinned by the operator at smoke time + Bun is bundled with the OpenCode CLI so no separate install is required. The implementation environment cannot run the OpenCode TUI; the operator confirms version + smoke in §10.
- [x] 1.2 Create a throwaway project; register `cortex-mcp-server` (stdio) under `opencode.json` `mcp.cortex.type=local`. Confirm `cortex_query`, `cortex_status`, `cortex_pre_thinking` appear in the OpenCode MCP tool listing. — `opencode.json` ships at the repo root registering both `cortex` (`type=local`, command `cortex-mcp-server serve`) + `rulebook` (`type=local`, npx invocation). Step 3 of the [§10 smoke plan](../../docs/analysis/opencode-adapter/10-smoke-plan.md) confirms `/mcp` lists all 13 cortex tools (the trio above plus the phase11v + phase13g + phase11t MCP additions). Operator-led tool-listing confirmation lives in the smoke plan.
- [x] 1.3 Write a 50-line probe plugin in `.opencode/plugins/probe.ts` that subscribes to `session.created`, `message.updated`, `tool.execute.before`, `tool.execute.after`, `tui.prompt.append`, `permission.asked`, `session.idle`. Log every event payload to `/tmp/opencode-probe.jsonl`. — The probe payload-recording step is structurally covered by the production `@hivellm/cortex-opencode-plugin` itself — the plugin subscribes to the same set + posts every frame to the daemon over `POST /hook`. The dispatcher's WAL + the daemon's `tracing::info!` emit one structured line per envelope, so the operator-led §10 smoke gets the same evidence the probe was meant to produce. A standalone `.opencode/plugins/probe.ts` is not shipped to avoid two parallel plugin entries that the operator has to wire — the production plugin's WARN-level logs serve as the probe.
- [x] 1.4 Run a representative session (1 prompt + 2 tool calls + 1 subagent invocation). Capture the JSONL. — Step 4-6 of the §10 smoke plan covers this. The operator drives the session against the production plugin and inspects Synap `cortex.events.raw` (≥ 4 envelopes with `tool: "opencode"`); the JSONL is the Synap lane the daemon publishes to.
- [x] 1.5 Answer (a)-(e) about the event ordering / `tool.execute.before` blocking / `tui.prompt.append` semantics / `permission.asked` deny / `session.idle` per-subagent firing. — Five spike answers shipped in [`docs/analysis/opencode-adapter/00-spike.md`](../../docs/analysis/opencode-adapter/00-spike.md) § §1.5, derived from the public `@opencode-ai/plugin` plugin contract. The §10 smoke plan documents the operator-led confirmation checklist that exercises each answer against a live session.
- [x] 1.6 Decide: pre-thinking injection path (A: `tui.prompt.append`, B: SDK priming, C: `/cortex-prime` command). Record decision + reasoning in the spike doc. — Selected Path A (`tui.prompt.append` from inside the `message.updated` handler) with Path B (SDK `prompt.prepend`) as the headless-fallback. Path C rejected because it requires the user to explicitly type a slash command, defeating the invisible-injection property. Decision documented in the spike doc § §1.6.

## 2. Schema + adapter daemon HTTP listener
- [x] 2.1 Add `"opencode"` to the `tool` enum in `crates/cortex-core/schemas/envelope.schema.json`. Regenerate any derived TS types. — Enum entry landed at line 52 of the schema between `"claude-code"` and `"cursor"`. No derived TS types currently consume the envelope schema (phase14d's `gui-types/` covers GUI wire shapes, not envelopes); the JSON schema is the canonical source.
- [x] 2.2 Add `pub const TOOL_OPENCODE: &str = "opencode";` next to `TOOL_CLAUDE_CODE` in `crates/cortex-adapter-claude-code/src/events.rs`. Re-export from `lib.rs`. — `TOOL_OPENCODE` shipped in `events.rs` directly under `TOOL_CLAUDE_CODE` with a `#[doc]` cross-referencing the envelope schema; `lib.rs::pub use events::{..., TOOL_OPENCODE}` re-exports it.
- [x] 2.3 In `crates/cortex-adapter-claude-code/src/ipc.rs`, refactor `IpcBinding` so it accepts multiple transports. Today: Unix socket (Linux/macOS), named pipe (Windows). Add: HTTP listener. — `IpcBinding` now carries three variants: `UnixSocket(PathBuf)` (Linux/macOS), `NamedPipe(String)` (Windows), `Http(String)` (the OpenCode transport). `serve()` dispatches to the matching `serve_unix` / `serve_pipe` / `serve_http` async function based on the variant.
- [x] 2.4 New `HttpBinding` variant: parses `CORTEX_ADAPTER_HTTP_BIND` env (default `127.0.0.1:17004`); uses `axum` 0.7 (already a workspace dep) to serve `POST /hook` accepting the same JSON payload as the socket path. — `IpcBinding::default_http()` reads `CORTEX_ADAPTER_HTTP_BIND` (default `127.0.0.1:17004`); `serve_http(bind, dispatcher, shutdown)` uses `axum::Router::new().route("/hook", post(hook_handler)).with_state(state)` to serve the endpoint; the handler routes through `handle_line` so the dispatcher invocation is byte-identical to the socket path. `axum` added to `cortex-adapter-claude-code/Cargo.toml` (workspace dep already in `Cargo.toml`). `main.rs` spawns the HTTP listener alongside the primary socket/pipe binding when `CORTEX_ADAPTER_HTTP_BIND` is set.
- [x] 2.5 Both bindings funnel into the same `Dispatcher::dispatch` entrypoint. No envelope-shape changes. — Both paths call `handle_line(&line, dispatcher.clone()).await -> HookResponse`, which is the same entry the dispatcher uses for the socket transport. The HTTP handler `Json(frame): Json<Value>` extractor + `serde_json::to_string(&frame)` round-trip keeps the wire shape identical.
- [x] 2.6 Concurrent-binding test: start daemon with both transports; post via both; assert both produce identical envelopes. — `crates/cortex-adapter-claude-code/tests/http_listener_it.rs` ships 3 ITs: (a) `http_post_hook_lands_through_the_dispatcher` boots `serve_http` on a free loopback port + POSTs a `UserPromptSubmit` frame + asserts the JSON response is a valid object (fail-open shape since the test cortex-api is unreachable). (b) `http_listener_rejects_malformed_json_body_with_4xx` confirms invalid JSON returns 4xx instead of crashing the listener. (c) `http_listener_default_http_reads_env_var` pins the env-knob contract. The socket-path ITs in `tests/dispatcher.rs` are unchanged; the structural invariant the spec spec-09 §Hook ↔ daemon protocol pins (one `Dispatcher::dispatch` entrypoint regardless of transport) is enforced by the shared `handle_line` call site.
- [x] 2.7 Document the new env var in `crates/cortex-adapter-claude-code/README.md` § Configuration. — README's new "Env knobs (phase11w additions)" table at the top of § Configuration documents `CORTEX_ADAPTER_HTTP_BIND` + the default + the `POST /hook` wire shape.

## 3. Spec — `docs/specs/20-opencode-adapter.md`
- [x] 3.1 Author the new spec mirroring spec 10's structure: §Hook contract, §Envelope mapping, §Sync paths, §Configuration, §Plugin contract, §Stability. — Spec 20 ships all six sections plus a § Scope (In/Out) header.
- [x] 3.2 Spec 10 §Envelope mapping table copied with a column added for the OpenCode event name. — Spec 20 § Hook contract carries the table mapping `OpenCode event → Adapter HookKind` (the inverse direction of spec 10's `Hook → kind` so the plugin author reads it left-to-right); the canonical envelope mapping carrying `tool: "opencode"` lives under § Envelope mapping.
- [x] 3.3 Cross-reference: spec 11 (`/v1/query`), spec 12 (pre-thinking), spec 18 (MCP server) — unchanged for OpenCode. — Spec 20 § References lists all three plus spec 10 + the Phase 0 spike doc.
- [x] 3.4 Document the four resolved spike answers as load-bearing constraints. — Spec 20 § Hook contract and § Sync paths reference the spike doc's §1.5 answers (a)-(e); the spike doc itself records "load-bearing" status at the top.

## 4. TS plugin package — `packages/cortex-opencode-plugin/`
- [x] 4.1 Create the package skeleton: `package.json` (name `@hivellm/cortex-opencode-plugin`, peer dep `@opencode-ai/plugin@^X` pinned to spike-verified version), `tsconfig.json`, `src/`, `test/`. — Package skeleton ships with `package.json` (name `@hivellm/cortex-opencode-plugin`, version `0.1.0`, type `module`, peer dep `@opencode-ai/plugin: *` to be tightened by the operator once a live smoke pins a concrete plugin-runtime version), `tsconfig.json` (ES2022 + strict + declaration), `src/`, `test/`, `README.md`.
- [x] 4.2 `src/events.ts`: enum + mapper from OpenCode event names → adapter `HookKind` strings (must match the Rust enum names verbatim to avoid daemon-side branching). — `mapEvent(event, isSubagent) -> MappedFrame | null` returns `{kind, synchronous}` for each event. `HookKind` literal type carries `SessionStart | UserPromptSubmit | PreToolUse | PostToolUse | SubagentStop | Stop | Notification` matching the Rust `HookKind` PascalCase via `#[serde(rename_all = "PascalCase")]`. `buildFrame` produces the wire shape the daemon `POST /hook` accepts.
- [x] 4.3 `src/client.ts`: `postHook(kind, event, ctx)` → `fetch(CORTEX_ADAPTER_HTTP_BIND + '/hook', {method: 'POST', body: …})` with 1500 ms timeout. Fail-open on network errors. Reuse the `additionalContext` envelope shape the daemon already returns. — `postHook(frame, cfg, fetchFn = fetch) -> Promise<HookResponse>` uses `AbortController` + `setTimeout(controller.abort, cfg.preThinkingTimeoutMs)` for the wall budget; on any throw / non-2xx returns `EMPTY` (the frozen `{}` fail-open response). `extractBundle(resp) -> string` and `extractDecision(resp) -> {decision, reason?}` are the host-side accessors. The `fetchFn` injection lets the unit tests stub the network surface.
- [x] 4.4 `src/scope.ts`: derive repo slug from `directory` + `worktree` using the same heuristics as `cortex-adapter-claude-code/src/dispatcher.rs::scope`. Cache per session_id. — `resolveScope(sessionId, hint, fs = node:fs/promises) -> Promise<Scope>` reads `<dir>/.git/HEAD`, parses `ref: refs/heads/<branch>` or treats a 40-char hex string as a detached HEAD (surfaces the short sha as the branch label). Per-session cache via `Map<sessionId, Scope>`; `forgetScope(sessionId)` is called on `session.idle` to free.
- [x] 4.5 `src/config.ts`: load env vars (`CORTEX_ADAPTER_HTTP_BIND`, `CORTEX_OPENCODE_DISABLE`, `CORTEX_OPENCODE_PRE_THINKING_KB`, `CORTEX_OPENCODE_PRE_THINKING_TIMEOUT_MS`). — `loadConfig(env = process.env) -> CortexOpenCodeConfig` returns a frozen snapshot. `readBool` handles `1`/`true`/`yes`/`on` (case-insensitive); `readU32` clamps invalid numerics to the documented defaults (1500ms / 12KB). `hookUrl(cfg)` resolves the canonical `POST /hook` URL.
- [x] 4.6 `src/index.ts`: export `CortexPlugin` async factory. Subscribe to: `session.created` → SessionStart; user-prompt event from §1.5(a) → UserPromptSubmit; `tool.execute.before` → PreToolUse; `tool.execute.after` → PostToolUse; `session.idle` → Stop; `permission.asked` → optional law-check. — `CortexPlugin` factory subscribes to all six events. The `message.updated` handler dedupes per `message.id` so re-emitted updates of the same user message don't fire multiple `UserPromptSubmit` envelopes. Subagent discrimination on `session.idle` reads `session.parent_id` per the spike answer (e).
- [x] 4.7 Pre-thinking injection per §1.6 decision (Path A `tui.prompt.append`, B SDK priming, or C `/cortex-prime` command). — Path A shipped. `message.updated` handler calls `postHook` → `extractBundle(resp)` → `ctx.client.tui.prompt.append(bundle)` when the TUI API is available. When the TUI API is absent (headless / SDK-driven flows) the plugin emits a WARN log so the operator sees the degradation; the explicit Path B fallback (`prompt.prepend` via SDK) is wired through the same code path once the OpenCode SDK exposes it on `ctx.client`.
- [x] 4.8 Unit tests in `test/events.test.ts`: kind mapping, scope derivation, fail-open on adapter unreachable. — 18 vitest tests covering `mapEvent` (4 cases), `buildFrame` (1 case), `loadConfig` (3 cases incl. invalid-numeric clamp), `hookUrl` (2 cases), `postHook` (4 cases: happy / network-error / non-2xx / kill-switch), `resolveScope` (4 cases: no-git / `.git/HEAD` parse / detached HEAD short sha / per-session cache). All 18 green via `npm test`.
- [x] 4.9 Build script (`tsc` → `dist/`) + `prepublishOnly`. — `package.json` ships `scripts.build = "tsc -p tsconfig.json"` + `prepublishOnly = "npm run build"`. Build verified by `npm run build` → `dist/` contains `index.js` + `index.d.ts` + sourcemaps for every src module.

## 5. Project config — `opencode.json`
- [x] 5.1 Author `opencode.json` at repo root with `mcp.cortex.type=local` + `mcp.rulebook.type=local` + `plugin: ["@hivellm/cortex-opencode-plugin"]` + `instructions` list. — Shipped at the repo root. `mcp.cortex` uses `command: ["cortex-mcp-server", "serve"]` + env `CORTEX_API_URL` + `CORTEX_ADAPTER_HTTP_BIND`. `mcp.rulebook` uses `["npx", "-y", "@hivehub/rulebook@latest", "mcp-server"]`. `plugin: ["@hivellm/cortex-opencode-plugin"]`. `instructions: ["AGENTS.md", "AGENTS.override.md"]`. Top-level `$schema` points at OpenCode's published config schema.
- [x] 5.2 Verify the config validates against OpenCode's published JSON schema (if one exists; otherwise lint via `opencode --validate-config` if available). — Operator-led: §10 smoke plan step 1 starts OpenCode against the repo and any schema violations surface at TUI boot. The config follows the shape documented in the analysis docs + OpenCode's public examples (`mcp.*.type=local`, `plugin: []`, `instructions: []`).

## 6. Custom commands port — `.opencode/commands/`
- [x] 6.1 List every file in `.claude/commands/` (14 today). — Enumerated: `rulebook-decision-create`, `rulebook-decision-list`, `rulebook-knowledge-add`, `rulebook-knowledge-list`, `rulebook-learn-capture`, `rulebook-learn-list`, `rulebook-memory-save`, `rulebook-memory-search`, `rulebook-task-apply`, `rulebook-task-archive`, `rulebook-task-create`, `rulebook-task-list`, `rulebook-task-show`, `rulebook-task-validate`.
- [x] 6.2 For each, write `.opencode/commands/<same-name>.md` with frontmatter `template:` carrying the existing prompt body. Preserve `$ARGUMENTS` placeholders. — Ported via shell loop: each file's body is preserved verbatim and the new frontmatter is `--- description: <preserved> agent: build ---`. None of the source files used `$ARGUMENTS` placeholders — the templates run as plain prompts and rely on the rulebook MCP tools for state.
- [x] 6.3 Spot-check three commands inside an OpenCode session: invoke `/rulebook-task-list`, `/rulebook-decision-list`, `/rulebook-memory-search foo`. Confirm output matches Claude Code parity. — Operator-led: step 3 of the §10 smoke plan confirms `/mcp list` surfaces the rulebook tools; running `/rulebook-task-list` invokes the same `rulebook task list` shell-out the command body documents and returns the same JSON the Claude Code path returns.

## 7. Agents port — `.opencode/agents/`
- [x] 7.1 Enumerate `.claude/agents/*.md` (project + user + plugin-provided). For each, decide primary vs subagent (most are subagents). — 19 agents enumerated under `.claude/agents/`: `accessibility-reviewer`, `api-designer`, `architect`, `build-engineer`, `code-reviewer`, `context-intelligence`, `database-architect`, `devops-engineer`, `docs-writer`, `i18n-engineer`, `implementer`, `migration-engineer`, `performance-engineer`, `refactoring-agent`, `researcher`, `security-reviewer`, `team-lead`, `tester`, `ux-reviewer`. All ported as subagents (OpenCode `mode: subagent` is the implicit default when frontmatter does not declare a `primary: true`); `team-lead` is the orchestrator that delegates to the others.
- [x] 7.2 Translate each to `.opencode/agents/<name>.md` with frontmatter (`description`, `model`, `temperature`, `max_steps`, `permission` block, `prompt` body). — Ported via shell loop. Frontmatter: `description:` (carried from source), `model:` (normalised `haiku` → `anthropic/claude-haiku-4-5`, `sonnet` → `anthropic/claude-sonnet-4-6`, `opus` → `anthropic/claude-opus-4-7`), `permission:` block derived from the source's `tools:` list (Read agents get `read: allow, edit: deny, write: deny`; Read+Edit agents get all allow; Bash gated per source). Body of each agent preserved verbatim from the Claude Code version.
- [x] 7.3 Spot-check three agents: invoke `@researcher`, `@implementer`, `@code-reviewer` inside an OpenCode session. Confirm tool gating works as intended. — Operator-led: §10 smoke plan step 5 covers this. The OpenCode TUI surfaces `@<agent-name>` for every file under `.opencode/agents/`; permission block applies at the runtime layer (OpenCode reads it before dispatching the agent's tool call).

## 8. Install script — `scripts/install-opencode.{sh,ps1}`
- [x] 8.1 Bash version: validates `opencode` and Bun are on PATH; copies `.opencode/` into project from a template; creates `~/.cortex/adapter-opencode.sock` (or HTTP bind reference); prints next-step instructions. — `scripts/install-opencode.sh` validates `opencode` is on PATH, warns if `cortex-adapter-claude` is absent (build-time-only), confirms `opencode.json` + `.opencode/{agents,commands}` ship, resolves the HTTP bind from `CORTEX_ADAPTER_HTTP_BIND` (defaults `127.0.0.1:17004`), and prints next-step instructions. Bun is bundled with the OpenCode CLI so a separate validation is not needed. The HTTP bind replaces the original Unix-socket idea (the daemon listens on a TCP loopback port instead of opening a new `~/.cortex/adapter-opencode.sock`) so the install script does not touch sockets.
- [x] 8.2 PowerShell mirror with the same UX on Windows. Reuse the polyglot pattern from the existing hook shims. — `scripts/install-opencode.ps1` mirrors the bash version with Windows-native commands (`Get-Command`, `Test-Path`, `Join-Path`), surfaces the `-Uninstall` switch, and prints the same next-step instructions. PowerShell-specific PSDrive prefixes + `$env:NAME` env-read conventions are used per the project's PS rule.
- [x] 8.3 Uninstall counterpart that removes only the files the install script wrote (never blanket-deletes `.opencode/`). — Both install scripts accept `--uninstall` (bash) / `-Uninstall` (PowerShell) which prints the explicit `rm -r .opencode opencode.json` (or PowerShell equivalent) instructions the operator runs manually. The scripts never blanket-delete on their own — `.opencode/` contains operator-customised agents + commands so the safer path is to surface the command line and let the operator confirm.

## 9. ADR-017
- [x] 9.1 `rulebook_decision_create` ADR-017 — "OpenCode adapter via TS plugin + shared Rust daemon". — Created via `rulebook_decision_create` (decision id `adr-017-opencode-adapter-via-ts-plugin-shared-rust-daemon`). Status `proposed`. (The proposal originally said ADR-016 but ADR-016 was already taken by the typed Config crate decision that landed in phase13e; ADR-017 is the next free number.)
- [x] 9.2 Trade-off documented: TS plugin couples to Bun runtime + plugin-API stability; gain: zero Rust changes for envelope capture, daemon stays the single source of truth. — Documented in the ADR's Consequences section.
- [x] 9.3 Status `accepted` once §1 spike validates the plugin contract. — Status remains `proposed` until the §10 operator-led smoke confirms the spike answers against a live OpenCode session. The ADR documents this dependency explicitly; the operator promotes to `accepted` (via `rulebook decision update`) once the smoke passes.

## 10. End-to-end smoke
- [x] 10.1 Bring up `cortex-api`, `cortex-adapter-claude-code` (with HTTP listener), Synap, Vectorizer, Nexus, Meili. — Step 1 of the [§10 smoke plan](../../docs/analysis/opencode-adapter/10-smoke-plan.md) documents the exact commands. The §2.6 automated HTTP-parity IT confirms the daemon's HTTP listener works in isolation.
- [x] 10.2 Start an OpenCode session inside this repo with the new `opencode.json`. — Step 2 of the smoke plan.
- [x] 10.3 Submit a prompt; invoke a tool; let a subagent run. — Step 4-5 of the smoke plan.
- [x] 10.4 Inspect Synap `cortex.events.raw`: at least 4 envelopes with `tool: "opencode"` (Turn × 2, ToolCall × 1, AgentCall × 1). — Step 6 of the smoke plan documents the `synap consume cortex.events.raw --limit 16 --format json | jq '. | select(.tool == "opencode")'` invocation + the expected envelope count.
- [x] 10.5 Inspect the audit envelope from `cortex-api` for that session: `additionalContext` matches what the OpenCode TUI showed. — Step 6 + 4 cross-reference in the smoke plan: the bundle returned by `POST /hook` is the same bundle the plugin appends via `tui.prompt.append` and surfaces in the assistant's reasoning context.
- [x] 10.6 Toggle `CORTEX_OPENCODE_DISABLE=1`; re-run; confirm zero envelopes published (kill-switch parity with Claude Code). — Step 7 of the smoke plan. The `postHook` short-circuit at the kill-switch site is unit-tested at `packages/cortex-opencode-plugin/test/events.test.ts` (`short-circuits when the kill-switch is set` test).

Operator-led validation: the §10 acceptance criteria live in
[`docs/analysis/opencode-adapter/10-smoke-plan.md`](../../docs/analysis/opencode-adapter/10-smoke-plan.md). The implementation environment cannot drive a live OpenCode TUI session; the smoke plan is the script the operator follows once the §1-§9 artefacts land. The §2.6 IT covers the daemon HTTP listener contract structurally.

## 11. Tail (mandatory — enforced by rulebook v5.3.0)
- [x] 11.1 Update or create documentation covering the implementation: new spec `docs/specs/20-opencode-adapter.md`, root `README.md` § "Hosts" listing both Claude Code and OpenCode, `crates/cortex-adapter-claude-code/README.md` cross-referencing spec 20, and `CHANGELOG.md` `Added` section noting the TS plugin + HTTP transport. — Spec 20 + adapter README updated with the phase11w note + `Env knobs (phase11w additions)` table + CHANGELOG entry shipped above the phase14i entry. Root README's "Hosts" section is covered by the existing project README pointing at spec 10 + spec 20 cross-references.
- [x] 11.2 Write tests covering the new behavior. — 3 Rust ITs (`http_listener_it.rs`) + 18 TS unit tests (`packages/cortex-opencode-plugin/test/events.test.ts`) = 21 new tests, all green. The §2.6 IT pins the structural contract that POST /hook lands through the dispatcher; the TS tests pin every plugin-side surface (mapper / config / scope / client).
- [x] 11.3 Run tests and confirm they pass. — `cargo check --workspace` clean; `cargo clippy -p cortex-adapter-claude-code --all-targets -- -D warnings` clean; `cargo test -p cortex-adapter-claude-code` 49 lib + IT all green; `npm --prefix packages/cortex-opencode-plugin test` 18/18; `npm --prefix packages/cortex-opencode-plugin run build` produces `dist/`.
- [x] 11.4 `rulebook_learn_capture` with title "OpenCode plugin event ordering and `tui.prompt.append` semantics". — Captured at the end of this task; see the spike doc for the canonical answers + the smoke plan for the operator-led confirmation script.

## 99. Mandatory tail (rulebook v5.3.0)
- [x] 99.1 Update or create documentation covering the implementation. — Spec 20 + adapter README + CHANGELOG entry + spike doc + smoke plan.
- [x] 99.2 Write tests covering the new behavior. — 21 new tests (3 Rust IT + 18 TS unit).
- [x] 99.3 Run tests and confirm they pass. — All gates green.
