## 1. Implementation
- [x] 1.1 Keyword lane repointed: `pre_change_context` / `similar_problems` now fan out via `repo_scoped(req, "consolidations")` → `cortex-<slug>-consolidations` (the write-side reality; the global `cortex_consolidations` uid 404s live while `cortex-cortex-consolidations` holds 62 docs). Both strategy unit tests updated to assert the per-repo uid.
- [x] 1.2 Assembly shipped: the Meili lane projects the nested `ext.consolidation.{consolidation_id,grain}` bag + curated `title` into lane extras (Consolidation never got the top-level flattening Decision/LawViolation have), and the orchestrator partitions consolidation-kind hits OUT of the fused snippet stream (`partition_consolidation_hits` — no double-listing) and maps them to `ConsolidationRef` (`derive_consolidations`, cap 8 = `CONSOLIDATIONS_ASSEMBLY_CAP`, looser than the renderer's 3 so the response carries spares). `outcome` degrades to `None` — the spec-08 fulltext builder stamps none today, matching the field's documented contract. Verified against a live doc: `ext.consolidation.consolidation_id`, `grain`, top-level `kind`/`title`/`ts` all present as read.
- [x] 1.3 Vector lane decision: **removed, not wired.** `cortex.consolidation.fp32` does not exist in the live Vectorizer and NO consolidation collection exists at all (checked all 181 live collections) — consolidations are never embedded. Rationale: keyword lane + assembly restores the loop today; wiring an embedding path would be an unrequested feature (simplicity-first) and half-wiring a phantom collection is what produced this bug class in the first place. Topic-card collections left untouched (real and live). Strategy tests now assert the collection's ABSENCE with the rationale inline.
- [x] 1.4 Acceptance GREEN against the live stack. Two defects surfaced only here, both fixed: **(a) ordering bug** — assembly ran AFTER `fused.truncate(req.limit)`, so consolidations (ranked below code/docs) were cut before the partition saw them; `results.consolidations` stayed empty live despite §1.1-§1.2 being correct. `req.limit` bounds SNIPPETS; consolidations carry their own caps → partition moved above the truncate, pinned by a new IT. **(b) test-design defect** — the IT asserted that one SPECIFIC document (the newest `/v1/consolidations/recent` hit) wins retrieval, which is a live-BM25 relevance property, not a wiring property (measured: even an exact-title query surfaces a different, equally valid consolidation); worse, it compared identifier spaces that never overlap (`/recent` reports the event ULID, a rendered row carries `cons-<grain>-…`). Re-aimed at the real invariant: the bundle carries a `## Consolidated context (N)` section whose rows hold real `cons-` ids. Live proof — rendered bundle: `## Consolidated context (1)` / `1. session/cons-ses-… · ⚠ · Cortex rework analysis: …`, and `/v1/query` returns `consolidations: 1` on a cache-miss run.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — `docs/specs/12-pre-thinking-injection.md` gained a "Retrieval wiring (phase30b — the section was structurally dead until this fix)" subsection under Consolidated context: the three breaks, the per-repo index, the removed vector lane with rationale, the field-source map, the assembly cap, and the acceptance gate.
- [x] 2.2 Write tests covering the new behavior — 2 strategy tests repointed (per-repo uid asserted, phantom collection absence asserted); 5 orchestrator unit tests (kind-label recognition incl. the phase28 §6.2 PascalCase class, partition preserves fusion order and empties consolidations out of the snippet stream, wire-field mapping + cap, skip-on-missing-id, title fallback to body); 2 new integration tests in `crates/cortex-api/tests/consolidations_assembly_it.rs` driving the REAL orchestrator with lane doubles — assembly + no-double-listing, and the ordering regression (`consolidations_survive_a_limit_smaller_than_the_fused_candidate_list`, which fails on the pre-fix truncate order).
- [x] 2.3 Run tests and confirm they pass — live acceptance IT green (exit 0, no skip); cortex-api suite exit 0 (25 orchestrator tests, new assembly IT 2/2); clippy `--workspace --all-targets -D warnings` and the full workspace suite re-run green with cargo's own exit code captured (never through a trailing grep).

## 3. Follow-up observed (not in scope here)
- Retrieval relevance for consolidations is weak: an exact-title query does not rank that consolidation first over the 62-doc live corpus. The loop is wired and rendering, but which distillate surfaces is coarse. Natural owner: the phase32 ranking/embedding work (consolidations are keyword-lane-only by the §1.3 decision, so they never get semantic scoring today).
