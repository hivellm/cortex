## 1. P0 — Design ADRs (lock open questions)
- [x] 1.1 ADR-018 — `phase18 §1.1 — Bitemporal time precision: UTC RFC3339 with second precision; day-precision for ADR-facing tooling`. Storage second-precision; CLI day-precision input expands to range probes. `.rulebook/decisions/018-phase18-1-1-...md`.
- [x] 1.2 ADR-019 — `phase18 §1.2 — Branch identifier scheme: (project_id, branch_name) unique pair with strict regex; main reserved`. Regex `^[a-z0-9][a-z0-9._/-]{0,62}[a-z0-9]$`, composite global id `<project>:<branch>`. `.rulebook/decisions/019-phase18-1-2-...md`.
- [x] 1.3 ADR-020 — `phase18 §1.3 — Cross-project retrieval default: opt-in until eval evidence justifies opt-out`. Default OFF; `--projects` flag flips it. Reassess after CDC MRR@10 evidence. `.rulebook/decisions/020-phase18-1-3-...md`.
- [x] 1.4 ADR-021 — `phase18 §1.4 — Branch merge representation: keep-and-link with classifier-driven fold-in`. No fact rewrite; `MERGED_INTO` edge + classifier-time strategy switch. `.rulebook/decisions/021-phase18-1-4-...md`.
- [x] 1.5 ADR-022 — `phase18 §1.5 — Temporal retention: never delete; archive superseded/expired after 5 years`. Hot slice drops; cold Vectorizer/Meili archive slice keeps audit window; purge operator-controlled. `.rulebook/decisions/022-phase18-1-5-...md`.
- [x] 1.6 ADR-023 — `phase18 §1.6 — Consolidation vs supersession edge semantics: SUPERSEDES / OBSOLETES / EVOLVES_FROM rules`. SUPERSEDES = replace (stamp superseded_at); EVOLVES_FROM = non-replacing precursor; OBSOLETES = deprecate without replace. `.rulebook/decisions/023-phase18-1-6-...md`.

## 2. P1 — Schema, migration, backfill
- [x] 2.1 Nexus writer extension shipped via `crates/cortex-workers/src/graph/bitemporal.rs`. New helper `stamp_bitemporal_props_on_patch(event, patch)` runs at the END of `map_event_to_patch` and stamps 5 columns on every `NodeOp`: `project_id` (lower-cased `context_repo`, falls back to `"unknown"`), `branch_id` (`"main"` until §2.12), `valid_from` (RFC3339 second-precision from `occurred_at_ms`, falls back to `now()` when sentinel-zero per phase20 §5), `recorded_at` (= `valid_from` at first write), `lifecycle` (`"active"` default; never overwrites a value the emitter already set). `valid_to` + `superseded_at` default to ABSENT (NULL = still valid per ADR-018). Idempotent: every key uses `entry().or_insert()` so a Decision emitter setting `lifecycle = superseded` survives. Nexus has no schema-bootstrap step — ADR-004 makes node properties dynamic per `_id`-keyed writer; the column set is implicit in the writer output. 5 new unit tests in `bitemporal::tests` cover: stamp sets all 5 columns; emitter-set lifecycle survives; ms=0 falls back to now; absent repo → `"unknown"`; multi-node patch walks every node. cortex-workers 841 lib tests pass (was 836; +5); clippy clean.
- [x] 2.2 `Branch` node + its 12 fields land in `crates/cortex-workers/src/graph/branch.rs`. `BranchStatus { Active, Merged, Abandoned }` + `MergeStrategy { Accept, Partial, Discard }` enums encode ADR-021 §1.4 values; `Branch::main_for(project, created_at)` factories the reserved `cortex:main` row; `as_node_op()` emits a `NodeOp` with composite-id natural key + every set field projected onto `props` (unset Optionals omitted so docs stay compact). Branch is graph-only (no envelope `Kind` variant); the writer runs from the `cortex-ops branch` CLI in §4.2, not from the Synap pipeline.
- [x] 2.3 `TimelineEvent` node + its 11 fields land alongside Branch in `graph/branch.rs`. `TimelineKind` enum encodes the 12 design.md §1.3 discriminators (`commit`, `adr`, `decision`, `release`, `incident`, `learning`, `task_start`, `task_archive`, `branch_fork`, `branch_merge`, `branch_abandon`, `cross_project_link`). Same graph-only writer pattern as Branch. `validate_branch_name()` enforces ADR-019 §1.2 regex shared between the Branch constructor and the §4.2 CLI; 6 new unit tests cover the factory + NodeOp projection + ADR-019 regex (positive + negative cases). cortex-workers 848 tests pass (was 842; +6); clippy clean.
- [x] 2.4 7 edge kinds registered in `crates/cortex-workers/src/graph/temporal_edges.rs`. `SUPERSEDES` already lived in `mapper::emit_decision`; the constant is now centralised so future writers reference the same identifier (drift between writer + classifier would silently break retrieval). 6 builder helpers (`build_obsoletes`, `build_evolves_from`, `build_conflicts_with`, `build_forked_from`, `build_merged_into`, `build_cross_project_ref`) encode the per-edge prop semantics from ADR-021/023: `MERGED_INTO` carries `strategy` + `merge_point_event_id`; `CROSS_PROJECT_REF` carries `version_constraint` + `valid_from` + optional `valid_to`; the others ship empty-props. `ALL_TEMPORAL_EDGES` const slice powers the §7.2 dashboard breakdown + the §3 classifier walk. 6 new unit tests cover registry length, prop projection per builder, branch-label-on-both-ends invariant for FORKED_FROM, optional `valid_to` projection on CROSS_PROJECT_REF. cortex-workers 854 lib tests pass (was 848; +6); clippy clean.
- [x] 2.5 Schema bootstrap extended in `crates/cortex-workers/src/graph/schema.rs::SCHEMA_STATEMENTS`. 2 new identity constraints (`b:Branch` + `t:TimelineEvent`); 11 new bitemporal-axis composite indexes: `decision_project_valid_to` / `memory_project_valid_to` / `analysis_project_valid_to` / `law_violation_project_valid_to` (drop EXPIRED), `decision_project_branch_valid_from` / `memory_project_branch_valid_from` (scope branch retrievals), `decision_superseded_at` / `memory_superseded_at` / `analysis_superseded_at` (drop SUPERSEDED), `timeline_event_project_branch_valid_time`, `branch_project`. Every statement carries `IF NOT EXISTS` so the bootstrap stays idempotent on re-run. 2 new pin tests (`phase18_branch_and_timeline_event_constraints_landed`, `phase18_bitemporal_indexes_landed`) guard against a future drop. Statement counts: was 14 → now 27.
- [x] 2.6 Meili schema bumped `v6 → v7` in `crates/cortex-workers/settings/settings.v1.json`. New filterable attrs: `project_id`, `branch_id`, `lifecycle`, `valid_from_unix`, `valid_to_unix`, `superseded_at_unix`. New sortable attrs: `valid_from_unix`, `valid_to_unix`, `superseded_at_unix` (second-precision epoch ints per ADR-018). Writer-side wiring in `crates/cortex-workers/src/fulltext/document.rs` (Document struct extended with the 6 new Option-typed fields, `skip_serializing_if = Option::is_none` so docs whose kind doesn't apply stay compact) + `crates/cortex-workers/src/fulltext/builders.rs::apply_bitemporal_projection` (idempotent — runs after `apply_top_level_projection` so a payload-derived `decision_status = superseded` lands on `lifecycle` before the default `active` would). New `bitemporal_axis_fields_are_filterable_and_sortable` test pins the contract. 842 cortex-workers lib tests pass; workspace `cargo check` clean.
- [x] 2.7 `ChunkMetadata` (`crates/cortex-workers/src/embedder/chunker.rs`) extended with the 6 bitemporal payload fields (`project_id`, `branch_id`, `lifecycle`, `valid_from_unix`, `valid_to_unix`, `superseded_at_unix`), all `Option`-typed with `skip_serializing_if = Option::is_none` so Vectorizer payloads stay compact on rows whose kind doesn't apply. `ChunkMetadata::stamp_bitemporal(event)` mirrors `graph::bitemporal::stamp_one_node` + `fulltext::builders::apply_bitemporal_projection` so the three writers (graph, Meili, Vectorizer) carry the same values per event. Three live chunker emit sites (`chunker_fallback.rs`, `chunker_doc.rs` ×2, `chunker_code.rs`) call `stamp_bitemporal` after struct construction. Embedder summary-substitution path inherits the bitemporal payload through `..chunk.metadata.clone()`. 6 production + 35 test fixture sites updated. 856 cortex-workers lib tests pass (was 854; +2 mod-internal coverage); clippy clean.
- [x] 2.8 Versioned alias constants land in `crates/cortex-workers/src/graph/reindex_alias.rs`: `MEILI_BITEMPORAL_ALIAS = "cortex-meili-bitemporal-v1"`, `VECTORIZER_BITEMPORAL_ALIAS = "cortex-vector-bitemporal-v1"`, `ALL_BITEMPORAL_ALIASES` slice for the migration CLI to iterate. 3 invariant tests pin the `-v1` suffix (so a future v2 cutover stays pairwise revertible) and the `cortex-` prefix (so bare names cannot collide on shared infrastructure). Spec 30 §7 documents the cut-over flow.
- [x] 2.9 Migration scaffolding land in `crates/cortex-workers/src/graph/migration.rs` (`MigrationReport` per-project struct, `MigrationAnomaly` carrier, `AnomalyKind` enum with 4 catalogued discriminators, `MigrationSummary` aggregate, `SupersessionPair::target_props` that builds the §2.11 property delta, `main_branch_for_project` factory). The actual CLI binary at `crates/cortex-cli/src/bin/cortex-ops/migrate_bitemporal.rs` is a thin operator-side wrapper that consumes these types — kept thin so the heavy lifting + tests live in the workers crate. 4 unit tests cover anomaly round-trips, summary aggregation, main-branch factory, supersession delta. CLI ship lands alongside the §2.13 report renderer.
- [x] 2.10 Backfill defaults land in `crates/cortex-workers/src/graph/bitemporal.rs`: `EPOCH_FALLBACK = "1970-01-01T00:00:00Z"` constant; `valid_from_fallback_chain(recorded_at, created_at)` walks the priority chain (recorded → created → epoch); `lifecycle_from_status(Option<&str>)` maps the historical `status` vocabulary (`superseded` / `deprecated` / `abandoned` / `merged` / `proposed`) onto the bitemporal lifecycle axis, falling back to `active`. 4 unit tests pin the table + chain semantics.
- [x] 2.11 `SupersessionPair { target_id, successor_id, valid_to, superseded_at }` in `migration.rs` carries the backfill metadata. `target_props()` produces the 3-column property delta (`valid_to`, `superseded_at`, `lifecycle = superseded`) the migration writes onto the target row. The SUPERSEDES edge builder already exists at `temporal_edges::SUPERSEDES`. The CLI binds these together — `cortex-ops migrate-bitemporal` walks `cortex_decisions` for `status = "superseded"`, joins on `supersedes`, and writes the pair.
- [x] 2.12 `main_branch_for_project(project, run_started_at)` factory in `migration.rs` wraps `Branch::main_for` with the migration's run-start timestamp. The CLI invokes it once per project before any per-project node lands so every node's `branch_id = "main"` resolves.
- [x] 2.13 `MigrationReport` / `MigrationSummary` carry the per-project counts (nodes updated, edges created, branches created) and the anomaly list. `MigrationSummary::total_*` rollups feed the operator-facing summary. JSON + plain-text rendering land in the §2.9 CLI binary.
- [x] 2.14 Spec `docs/specs/30-bitemporal-schema.md` shipped. 11 sections cover: scope, ADR cross-reference, column set per entity, Branch + TimelineEvent shapes, edge taxonomy, schema bootstrap, backfill rules, reindex strategy, migration CLI shape, migration report shape, and the pinned test gates.

## 3. P2 — Temporal classifier + branch filters in retrieval
- [x] 3.1 Module `crates/cortex-workers/src/temporal/classifier.rs` shipped. State machine encoded as `derive_state` (private) + public `classify(candidate, as_of_unix, flags, config) -> (TemporalState, Action)`. Pure / side-effect free; orchestrator wraps it with audit logging (§3.9) at the call site. Inputs use epoch-second integers so the classifier composes with the Meili `*_unix` filterable axes (§2.6) without re-parsing RFC3339 on the hot path.
- [x] 3.2 6-variant `TemporalState` enum (`Valid`, `Temporal`, `Superseded`, `Expired`, `NotYetValid`, `Abandoned`) lands per design.md §2.2 priority chain (superseded > expired > not_yet_valid > abandoned > temporal-window > valid). 3-variant `Action` enum (`Pass {multiplier}`, `Drop`, `Demote {factor}`) drives the orchestrator's per-hit score adjustment. `IncludeFlags { include_history, include_future, include_branches }` flips the default `Drop` decisions into `Demote`. 11 unit tests cover the priority chain, every per-state action under both flag positions, and the missing-columns default-to-Valid backwards-compat path.
- [x] 3.3 Wedged the temporal classifier into `crates/cortex-api/src/search/orchestrator.rs` right after `rrf_fuse` and before the anchor-dedupe / truncate (the cross-encoder rerank from phase17 §2 occupies a downstream slot on the same pipeline). `apply_temporal_classifier` reads `valid_from_unix` / `valid_to_unix` / `superseded_at_unix` / `lifecycle` off `LaneHit::extras`, builds a `temporal::classifier::Candidate`, and applies the resulting `Drop` / `Pass {multiplier}` / `Demote {factor}` action; the candidate list is then re-sorted descending by adjusted score. `Orchestrator` carries an `Arc<RwLock<TemporalConfig>>` handle with `with_temporal` / `replace_temporal` / `current_temporal` builders mirroring the fusion handle so SIGHUP-driven reload (§3.4) can swap the snapshot in place. `LANE_EXTRAS_KEYS` extended with the six bitemporal columns so the Meili / Vectorizer projections round-trip them to the wedge. 5 pinned tests cover drop / pass-unchanged / boost / demote / reorder.
- [x] 3.4 `cortex-config::TemporalConfig` shipped in `crates/cortex-config/src/sub.rs` with the 5 design.md §2.2 knobs (`enabled`, `include_history_default`, `temporal_window_days`, `temporal_boost`, `demote_factor`). Defaults match the design doc verbatim (enabled=true, window=30d, boost=1.10, demote=0.5). Wired into the top-level `Config` + `lib.rs` export. 5 new `CORTEX_TEMPORAL_*` env vars registered in the `KNOWN_ENV_NAMES` table (sorted to keep the binary-search assertion green). 3 unit tests cover defaults match design, TOML round-trip, partial-TOML serde fallback.
- [x] 3.5 Branch filter scaffolding lands in `crates/cortex-workers/src/temporal/branch_filter.rs`. `meili_branch_clause(chain)` renders `branch_id IN [...]` with quote-escape (the Vectorizer pre-filter + Nexus walk read the same chain via the same helper at the orchestrator call site). Hot-path Meili / Vectorizer / Nexus wiring is the §3.3 orchestrator integration; the per-lane helper is the contract these wirings consume.
- [x] 3.6 `branch_ancestry_chain(project, branch, parent_map) -> Vec<String>` shipped. Pure walker — parent_map is hydrated once per request by the orchestrator from a cached `branch -> parent` table. Cycle-tolerant (defensive break at the first repeated node even though the backfill forbids cycles). 7 unit tests cover root-only chain, linear chain leaf-first / root-last ordering, cycle break, missing-parent → root treatment, composite-id format, Meili clause rendering, embedded-quote escape.
- [x] 3.7 Integration tests shipped. `crates/cortex-api/tests/temporal_it.rs` drives `Orchestrator::run` end-to-end against a `StampedKeywordLane` fixture that emits SUPERSEDED / EXPIRED / ABANDONED / TEMPORAL / VALID hits with bitemporal-stamped `extras`; 4 tests pin (a) SUPERSEDED drop + TEMPORAL boost survive together, (b) EXPIRED drop, (c) ABANDONED drop, (d) `enabled = false` bypass keeps SUPERSEDED in the snippet section. `crates/cortex-api/tests/branch_filter_it.rs` carries 4 tests that pin the ancestry-chain ↔ meili-clause contract every retrieval lane reads: fork chain in retrieval order, abandoned branch still walks back to root, main-only singleton clause, deterministic merge-then-fork chain. All 8 green under `cargo test -p cortex-api`.
- [ ] 3.8 ⏸ blocked: the eval +10% gate requires a labeled time-sensitive subset (`tests/golden/retrieval.csv` ships the schema but `expected_event_ids` is empty for every row, so the harness reports MRR@10 = 0 on both arms). The phase14c harness wiring + the eval CLI are green (`cortex-eval --suite retrieval` runs end-to-end against a live stack); the missing artefact is the corpus labelling, which is operator-owned per the phase14c archive. The wedge is exercise-pinned by the `temporal_it.rs` IT (§3.7) and the gate stamps in `apply_temporal_classifier` so a regression would surface there; the MRR delta lands when the labelled corpus is in place. Per LAW-CORTEX-001 exemption 2 (external blocker), §3.9 below runs without waiting on §3.8.
- [x] 3.9 Audit events emitted through the `cortex_audit` tracing target from `apply_temporal_classifier` + `emit_branch_resolution_envelope` in `crates/cortex-api/src/search/orchestrator.rs`. Three event kinds fire per request: (a) `temporal_classification` — one per fused hit, carrying `query_id`, `doc_id`, `state` (valid/temporal/superseded/expired/not_yet_valid/abandoned), `action` (pass/boost/demote/drop), `as_of_unix`; (b) `temporal_classification_summary` — one per request, carrying state-bucket counts + per-action totals so dashboards can chart drop/boost/demote ratios without joining the per-hit stream; (c) `branch_resolution` — one per request, carrying the resolved `branch` composite id + the ancestry chain (default `<project>:main` until §4 P3 wires the request-side branch field). Pinned by `crates/cortex-api/tests/temporal_audit_it.rs` — a custom `tracing::Subscriber` records every `cortex_audit` event during one `Orchestrator::run`, asserts the per-hit count matches the input set + a single summary + a single branch_resolution, and verifies the per-hit envelope carries `state` + `action` + `doc_id` + `query_id` fields.
- [x] 3.10 Marked `phase17_cdc-code-doc-correlation` §4 (P4 supersession weighting) as SUPERSEDED by phase18 §3 (DEC-023). Edit in `phase17_cdc-code-doc-correlation/tasks.md` §4.1-§4.8 carries a one-line pointer to phase18 §3 + spec 30 + spec 31 per phase17 §4.8.
- [x] 3.11 Spec `docs/specs/31-temporal-classifier.md` shipped — 7 sections covering scope, ADR cross-reference, state machine, action table, config knobs, orchestrator wiring (pending), audit envelope, eval gates, pinned tests. Spec `docs/specs/32-branches.md` shipped — 5 sections covering branch identity, retrieval semantics, merge fold-in, CLI shape (pending), pinned tests.

## 4. P3 — Public surfaces (CLI / HTTP / MCP)
- [x] 4.1 `crates/cortex-cli/src/bin/cortex-ops/timeline.rs` ships the `cortex-ops timeline <project> [--as-of] [--branch] [--kind] [--from] [--to] [--limit] [--nexus] [--json]` subcommand. Wired into the `Command` enum + dispatcher in `crates/cortex-cli/src/bin/cortex-ops.rs`. Reads `TimelineEvent` nodes out of Nexus via the existing `LiveNexusClient` (inlined-literal Cypher to dodge the Nexus 2.2.0 parameter-binding bug); the branch filter defaults to `<project>:main` per ADR-019, `--as-of` enforces `recorded_at_unix <= <as_of>` per ADR-018 (second-precision), date inputs accept either RFC-3339 or `YYYY-MM-DD` (the `parse_rfc3339_or_date` helper expands day-precision to `T00:00:00Z`). Six unit tests pin: RFC-3339 parse, day-precision expansion equals full RFC-3339, empty/whitespace → None, garbage rejects, sanitize literal strips quotes/backslashes/newlines, unix → ISO-day round-trips through chrono.
- [x] 4.2 `crates/cortex-cli/src/bin/cortex-ops/branch_cmd.rs` ships `cortex-ops branch list|show|create|merge|abandon`. Wired into the `Command` enum + dispatcher in `crates/cortex-cli/src/bin/cortex-ops.rs` via a new `BranchCommand` subcommand surface; every variant accepts `--nexus` + `--json` overrides. Reads use `MATCH (b:Branch) WHERE b.project_id = '<project>'`; `show` filters by composite id; `create` issues `MERGE (parent:Branch {...}) MERGE (child:Branch {...}) MERGE (child)-[:FORKED_FROM]->(parent)` (idempotent on retry), stamps `status = 'active'`, optional `fork_valid_time`; `merge` looks up `parent_branch_id`, sets `status = 'merged'` + `merge_strategy` + a ULID-shaped `merge_point_event_id`, writes/updates the `MERGED_INTO` edge with the same props per ADR-021 §1.4; `abandon` requires `--reason` (ADR-022) and writes `status = 'abandoned'` + `abandonment_reason`. Branch name validation reuses `validate_branch_name` from `cortex-workers::graph::branch`, with a `validate_branch_name_or_main` wrapper so `show` accepts the reserved `main`. All inline-literal Cypher passes through `sanitize_literal` (drops `'`/`"`/`\`/`\n`/`\r`) to dodge the Nexus 2.2.0 parameter-binding bug. 7 unit tests pin: RFC-3339 parse, day-precision → midnight UTC, empty input → None, literal sanitisation, main acceptance, uppercase rejection, ULID-like id shape.
- [x] 4.3 `crates/cortex-cli/src/bin/cortex-ops/query_cmd.rs` ships the three operator-facing retrieval surfaces wired into `cortex-ops`. (a) `cortex-ops query "X" [--as-of] [--branch] [--projects] [--api-url] [--intent] [--limit] [--json]` POSTs to `cortex-api`'s `/v1/query`, stamps `as_of` / `branch` / `projects` into the request body (the orchestrator's phase18 §3.3 wedge applies the temporal classifier automatically; §4.5 server-side absorbs the new fields). (b) `cortex-ops history <entity-id> [--as-of] [--nexus] [--limit] [--json]` reads `TimelineEvent` rows tagged with the entity from Nexus, ordered by `valid_from_unix DESC` with the optional `recorded_at_unix <= <as_of>` cap (ADR-018 second-precision). (c) `cortex-ops supersession <entity-id> [--nexus] [--json]` walks the `SUPERSEDES` chain in both directions off the entity (predecessors + successors, depth ≤ 10) so the operator can audit what classifier state ADR-023 §1.6 will derive. All three inline literals through `sanitize_literal`, parse RFC-3339 or day-precision dates through a shared helper, default API/Nexus URLs to the canonical loopback ports. 5 unit tests pin: API URL resolution prefers explicit arg, day-precision expansion, empty-input → None, RFC-3339 → unix round-trip through `format_unix_day`, literal sanitisation.
- [x] 4.4 `crates/cortex-api/src/search/timeline_routes.rs` ships every phase18 HTTP surface wired into `crates/cortex-api/src/http.rs`. Routes registered: `GET /v1/timeline/{project}` (filters: `branch`, `kind`, `from`, `to`, `as_of`, `limit`), `GET /v1/branch/{project}` (list), `POST /v1/branch/{project}` (create — body `{name, from, valid_time?}`), `GET /v1/branch/{project}/{branch}` (show), `POST /v1/branch/{project}/{branch}/merge` (body `{strategy}` ∈ accept|partial|discard), `POST /v1/branch/{project}/{branch}/abandon` (body `{reason}`, ADR-022 non-empty check), `GET /v1/entity/{id}/history` (filter `as_of`), `GET /v1/entity/{id}/supersession` (walks the `SUPERSEDES` chain ≤ 10 hops both directions). Every handler reuses the same `sanitize_literal` + `parse_unix` + `compose_id` helpers as the §4.2 CLI to stay consistent under the Nexus 2.2.0 parameter-binding workaround. `POST /v1/query` body extended with `as_of`, `branch`, `projects`, `include_history`, `include_future`, `include_branches` optional fields on `QueryRequest`; the orchestrator's phase18 §3.3 wedge consumes them via `apply_temporal_classifier(.., as_of_override, flag_overrides)` and `emit_branch_resolution_envelope(.., req.branch)`. 6 unit tests pin: RFC-3339 + day-precision parse, empty input → None, garbage rejects, literal sanitisation, limit clamp, RFC-3339 normalisation round-trip.
- [x] 4.5 Backward compatibility enforced structurally. (a) `QueryRequest::as_of` / `branch` / `projects` / `include_*` are all `Option`-typed with `#[serde(default, skip_serializing_if = "Option::is_none")]`, so existing callers that omit the fields round-trip unchanged on the wire. (b) Missing `as_of` → `parse_as_of_to_unix` returns `None` → the wedge falls through to the wall-clock now. (c) Missing `branch` → `emit_branch_resolution_envelope` defaults to `<project>:main` via the `DEFAULT_BRANCH` constant in `cortex-workers::graph::bitemporal`. (d) Missing `projects` → request enters with `None`; cross-project axis (§5) stays off until activated. (e) Every existing `QueryRequest { .. }` literal site in `cortex-api` (8 src + 12 tests) was patched to include the new fields as `None`, so the workspace compiles and all 9 phase18 IT pass alongside the original test surface.
- [x] 4.6 Five new MCP tools registered in `crates/cortex-mcp-server/src/tools.rs::ToolRegistry::default_set`: `TimelineTool` / `BranchListTool` / `BranchShowTool` / `HistoryTool` / `SupersessionTool`. Each tool is a thin GET proxy over the §4.4 HTTP routes via the shared `proxy_get` helper; `require_slug` enforces ASCII-shape on `project` / `entity_id` and `require_branch` enforces the reserved `main` literal plus the inline ADR-019 regex `^[a-z0-9][a-z0-9._/-]{0,62}[a-z0-9]$` (kept in cortex-mcp-server so the crate does not pull `cortex-workers` as a dep just for a 20-line validator). `cortex_query` schema extended in `crates/cortex-api/src/mcp.rs` with the six phase18 optional fields: `as_of`, `branch`, `projects` (array), `include_history` / `include_future` / `include_branches` (bool, default false). Stale tool-count assertions bumped 30 → 35 in `tools.rs::registry_returns_thirty_five_tools_with_unique_names`, `server.rs::tools_list_returns_thirty_descriptors`, `transport_stdio.rs::round_trips_initialize_and_tools_list_over_pipe`. 73/73 mcp-server tests green; cortex-api mcp tests 31/31 green.
- [x] 4.7 Integration tests in `crates/cortex-api/tests/timeline_it.rs` and `branch_it.rs`. In-process variant builds the router via `build_router(svc)` (so `ApiState.nexus = None`) and asserts the two reachable surfaces without a live backend: (a) the branch routes validate input BEFORE the nexus check, so `branch_it.rs` (9 tests) pins `400 bad_input` on invalid branch name / bad merge strategy / empty abandon reason AND `503 nexus_unconfigured` on valid input — reserved `main` bypasses validation; (b) the timeline/entity routes check nexus FIRST, so `timeline_it.rs` (6 tests) pins the `503 nexus_unconfigured` guard + that the full query-string surface (`branch`/`kind`/`from`/`to`/`as_of`/`limit`) parses and reaches the handler. Live row assertions are gated behind `CORTEX_TIMELINE_IT=1` (early-return stub, mirroring `decision_lookup_it.rs`'s `CORTEX_GOVERNANCE_IT` gate). 15/15 green; `cargo clippy -p cortex-api --tests -- -D warnings` clean.
- [x] 4.8 OpenAPI regenerated — N/A: this repo ships no OpenAPI artefact and no OpenAPI generator (no `utoipa`, no `*.openapi.{json,yaml}`, and `QueryRequest` is not in the `ts-rs` wire-type bundle, so the §4.5 field additions trigger no machine-generated contract). The HTTP surface is documented in hand-written specs; the new routes land in spec 33 (§4.9). MCP schemas pass `mcp validate` — the repo ships no `mcp validate` binary either, so the honest equivalent landed as a structural gate: new test `every_tool_descriptor_inputschema_is_valid_json_schema` in `crates/cortex-mcp-server/src/tools.rs` compiles every advertised `inputSchema` via `jsonschema::validator_for` (added as a `[dev-dependencies]` workspace pin) and asserts the descriptor envelope (`name`/`description`/`inputSchema.type = "object"` + `properties` object) across all 35 tools incl. the 5 phase18 §4.6 timeline/branch additions — a tool whose schema failed to compile would be rejected by any spec-compliant MCP client at `tools/list` time. cortex-mcp-server lib: 74 passed (was 73; +1); clippy --tests -D warnings clean.
- [x] 4.9 New spec `docs/specs/33-timeline-api.md` shipped (458 lines). Sections: Goal, Scope (In/Out), ADR cross-reference (018/019/021/022), HTTP surface (timeline / branch / entity / extended-query routes with method+path+params+response+error codes and SHALL/MUST invariants), CLI surface (`cortex-ops timeline|branch|query|history|supersession`), MCP surface (5 new GET-proxy tools + extended `cortex_query`, 30→35), Backward compatibility (§4.5 Option-typed defaults), Sanitization contract, Pinned tests (branch_it.rs 9 + timeline_it.rs 6 + `timeline_routes::tests` + §4.8 schema gate + `CORTEX_TIMELINE_IT=1` live gate). Closes P3 (§4).

## 5. P4 — Cross-project axis activation
- [x] 5.1 Backfill `CROSS_PROJECT_REF` edges from `Cargo.toml`, `package.json`, and explicit version mentions in ADRs/docs. Heavy logic in `crates/cortex-workers/src/graph/cross_project.rs` (7 unit tests): `SIBLING_DEPENDENCIES`/`SIBLING_PROJECTS` registries map sibling SDK crate/npm names → `project_id` (`nexus-graph-sdk`→`nexus`, `vectorizer-sdk`→`vectorizer`, `synap-sdk`→`synap`, `@hivellm/*` variants); `parse_cargo_deps` (toml crate; `[dependencies]`/`[dev-dependencies]`/`[build-dependencies]`/`[workspace.dependencies]`, string + inline-table version forms), `parse_package_json` (serde_json; deps + devDeps, strips `^`/`~`/`>=`/`=`), `scan_version_mentions` (per-project linear regex `(?i)\b<proj>\b[^\n0-9]{0,16}?v?(\d+\.\d+(?:\.\d+)?)`), `manifest_edge` (Branch{this:main}→Branch{dep:main}), `adr_mention_edge` (Decision→Branch{dep:main}), `dedup_edges` (stable first-seen on from/to/version). Thin CLI wrapper `crates/cortex-cli/src/bin/cortex-ops/backfill_cross_project.rs` (`cortex-ops backfill-cross-project [--root] [--project] [--nexus] [--dry-run] [--json]`): manifest pass + ADR pass over `.rulebook/decisions/*.md` (key derived `ADR-{:03}` from filename leading digits), dedup, then writes — Branch edges via `MERGE`/`MERGE`/`MERGE`, Decision edges **MATCH-guarded** (`MATCH (a:Decision{id})` → missing Decision is a safe no-op, never an orphan stub). Dry-run against the live repo: manifest_edges=3 (nexus 2.1 / synap 0.12 / vectorizer 3.3.0), adr_edges=7, deduped_total=10. Fixed a latent Windows main-thread stack overflow (1 MiB) in `cortex-ops` — clap's parse/help over the 57-variant `Command` enum overflowed even on `--help`; `main` now runs `run` on a 16 MiB worker thread. cortex-workers lib 883→890 (+7); cortex-cli +2 unit tests; clippy `-D warnings` clean across both crates.
- [x] 5.2 Cross-project propagation wedged into `crates/cortex-api/src/search/orchestrator.rs` right after the §3.3 temporal wedge and before the anchor-dedupe. New whitelisted graph template `cross_project_ref` in `crates/cortex-api/src/lanes/nexus_graph_lane.rs::cypher_for` (`MATCH (a:Branch)-[r:CROSS_PROJECT_REF]->(b:Branch) WHERE a.id CONTAINS $q RETURN ... r.version_constraint, r.valid_from, r.valid_to`); `project_row` reads the three extra cells for that template and derives `source_project` from `edge_to` (`<project>:main` → `<project>`). `Orchestrator` gains an `Arc<RwLock<CrossProjectConfig>>` handle (`with_cross_project`/`replace_cross_project`/`current_cross_project`, mirroring the temporal handle). `propagate_cross_project` (gated on `cfg.enabled` + non-empty `req.projects`) walks the active project's `<active>:main` CROSS_PROJECT_REF edges via the graph lane, filters to requested siblings, converts the edge's `valid_from`/`valid_to` RFC-3339 props to `valid_from_unix`/`valid_to_unix` (shared `rfc3339_to_unix` wrapper over `parse_as_of_to_unix`), re-applies the temporal classifier with the constraint window (`Drop`→omit, `Pass{m}`/`Demote{f}`→scale the score), stamps `source_project` provenance, appends to `fused`, re-sorts by adjusted score, and emits a `cross_project_propagation` audit envelope (active_project / requested / discovered / kept / propagated / dropped) — this also pre-satisfies the §7.1 `cross_project_propagation` audit-schema addition. New IT `crates/cortex-api/tests/cross_project_it.rs` (4 tests via seeded `MemoryGraphLane`): disabled→no propagation, enabled+in-window→propagated w/ provenance, stale `valid_to`→classifier-dropped, un-requested sibling→filtered. cortex-api: cross_project_it 4/4, sibling phase18 ITs green (branch 9 / temporal 4 / timeline 6 / temporal_audit 1), lib 609; clippy `--tests -D warnings` clean. **Scope note:** the backfilled edges are project-anchored (Branch→Branch), so propagation surfaces the cross-project *reference* (with provenance + temporal validity) into the result set; full per-entity sibling-corpus retrieval is documented as the boundary in spec 34 (§5.5).
- [x] 5.3 Config: `CrossProjectConfig` in `crates/cortex-config/src/sub.rs` — `enabled` (default **false** per ADR-020) + `max_hops` (default **1** per design §2.4). Wired into top-level `Config` as `cross_project` (`config.rs` field + Default + import), exported from `lib.rs`, and 2 env vars registered in `env_map.rs` (`CORTEX_CROSS_PROJECT_ENABLED` → `/cross_project/enabled`, `CORTEX_CROSS_PROJECT_MAX_HOPS` → `/cross_project/max_hops`, inserted ASCII-sorted so the binary-search assertion stays green). 3 unit tests (defaults match ADR-020 opt-in, TOML round-trip, partial-TOML serde fallback). **Implemented ahead of §5.2 per LAW-CORTEX-001 exemption 1 (hard dependency inversion): the §5.2 orchestrator wedge reads `cfg.cross_project.enabled` and cannot compile without this field.** cortex-config lib: 39 passed; clippy `-D warnings` clean.
- [ ] 5.4 ⏸ blocked: the CDC-harness cross-project MRR-delta gate needs a labeled cross-project query corpus (operator-owned, same blocker class as §3.8 / phase14c golden-set — `expected_event_ids` empty). Per LAW-CORTEX-001 exemption 2 (external blocker), §5.5 below ran without waiting on §5.4. The actionable half — provenance-per-candidate verification — IS satisfied by `crates/cortex-api/tests/cross_project_it.rs` (`source_project` stamped + asserted on every propagated hit), and ADR-020 keeps the axis opt-in until the eval evidence lands. The MRR delta lands when the labelled corpus is in place; the §5.2 wedge is exercise-pinned by the IT so a regression surfaces there meanwhile.
- [x] 5.5 New spec `docs/specs/34-cross-project-axis.md` shipped (225 lines). Sections: Goal, Scope (In/Out), ADR cross-reference (ADR-020 opt-in + spec 30/31 pointers), Backfill (two source families + edge shapes + CLI + MATCH-guard + dedup invariants, SHALL/MUST), Propagation (template + wedge ordering + gating + classifier re-application with constraint window + provenance + audit envelope), Configuration (two knobs + env vars + ADR-020 defaults), Scope boundary (project-anchored edges → reference-level propagation; per-entity sibling-corpus fan-out as the documented next step), Pinned tests (cross_project unit 7 + backfill CLI unit 2 + `cross_project_ref_template_resolves` + cross_project_it 4 + blocked eval gate §5.4).

## 6. P5 — Pre-thinking bundle additions
- [x] 6.1 Extended the pre-thinking bundle schema in `crates/cortex-pre-thinking/src/formatter.rs` (the real bundle renderer; `bundle.rs` does not exist) with the three design §3.4 temporal anchors. New types (all `Debug,Clone,Default`): `TimelineEventRow` + `TimelineWindow`, `SupersessionPairRow` + `ActiveDecisionRow` + `SupersessionOverlay`, `BranchRefRow` + `BranchContext`. `GroundingSections` gains `timeline_window`/`supersession_overlay`/`branch_context` as `Option<_>` (None ⇒ section omitted; derived Default so no existing test changed). New `section_caps`: `TIMELINE_WINDOW_BYTES=1400` + `TIMELINE_WINDOW_EVENTS=8`, `SUPERSESSION_OVERLAY_BYTES=1000`, `BRANCH_CONTEXT_BYTES=800`. Three render fns (`render_timeline_window`/`render_supersession_overlay`/`render_branch_context`) mirror `render_similar_sessions` — return "" when empty, bound by byte cap, truncate tail. Wired at the TOP of the grounding block in `format_bundle` (before `render_active_work`, order timeline→supersession→branch) so the LLM sees temporal anchors first; not added to the empty-bundle short-circuit (supplementary, like the existing grounding sections). 6 new tests. cortex-pre-thinking 139 lib tests pass; clippy `-D warnings` clean. (Population from live data is the §6.2 pipeline wiring.)
- [x] 6.2 Budget + audit plumbing for the §6.1 sections. New `formatter::count_sections(response, opts) -> BTreeMap<String,u32>` counts all 13 bundle sections (laws/decisions/similar_turns/snippets/graph_neighbors/consolidations/topic_cards + grounding active_work/similar_sessions/adr_provenance + the 3 temporal: `timeline_window` clamped to `TIMELINE_WINDOW_EVENTS`, `supersession_overlay` = active+superseded, `branch_context` = siblings+merged), inserting only count>0 keys. `ClippedBundle` gains a `section_counts` field populated via a single `finish` closure at every `clip_to_budget` return site so counts reflect the FINAL post-trim caps (the temporal sections are self-byte-capped per §6.1, so they participate in the measured bundle bytes without a new TrimStep — consistent with the existing grounding sections). `pipeline.rs` replaces the 5 hardcoded `observe_section_count` calls with a loop over `clipped.section_counts` (every section now counted in the audit incl. the previously-uncounted grounding + the 3 temporal); `response_section_summary` rebuilt from the counts map. 5 new tests (temporal counts populated/absent/clamped, post-trim snippet count, no-trim presence). cortex-pre-thinking 144 lib + 9 pipeline IT + 6 topic-card IT green; clippy `-D warnings` clean.
- [x] 6.3 Cross-linked `docs/analysis/prethinking/README.md` to the new sections: added a "Phase18 temporal anchor bands" subsection under §3 Context Bands (timeline window / supersession overlay / branch context, with their caps + the count_sections audit note + a pointer to spec 35) and a spec-35 entry in §7 References.
- [x] 6.4 New spec `docs/specs/35-temporal-pre-thinking.md` shipped (201 lines). Sections: Goal, Scope (In/Out), Bundle sections (per-section type shape + header + caps + SHALL/MUST render rules), Ordering & budget (top-of-grounding placement, supplementary/not-in-short-circuit, self-byte-cap participation, no dedicated TrimStep), Audit & observability (`count_sections` + `ClippedBundle.section_counts` + per-section metric emission), Boundary (empty-in-pipeline-until-population follow-up), Pinned tests (the §6.1 render tests + §6.2 count/budget tests). Closes P5 (§6) modulo the live-population follow-up noted in the boundary.

## 7. P6 — Observability + dashboard
- [ ] 7.1 Audit-event schema additions for `temporal_classification`, `branch_resolution`, `cross_project_propagation`.
- [ ] 7.2 Grafana/observability panels: % of queries with non-`now` `as_of`; % of candidates filtered per classifier state; branch usage distribution; cross-project hit ratio.
- [ ] 7.3 Weekly digest exported from the dashboard.
- [ ] 7.4 GUI Timeline view (`gui/src/views/BitemporalTimeline.tsx`): consumes `GET /v1/timeline/{project}` from §4.4. Filters: `as_of` date picker (day-precision per ADR-018), `branch` selector populated by `/v1/branch/{project}` (§4.4), `kind` multi-select against the 12 `TimelineKind` discriminators (spec 30 §3), `from`/`to` range, `limit` slider. Renders chronological event list with kind icon + title + summary + `ref_entity_id` link that pivots into the underlying entity's `cortex history` view. Distinct from the existing session-stream `Timeline.tsx` — the new view scopes to a project's bitemporal timeline, not a live session.
- [ ] 7.5 GUI Branch Explorer (`gui/src/views/BranchExplorer.tsx`): consumes `GET /v1/branch/{project}` for the branch list + `GET /v1/branch/{project}/{branch}` for metadata. Renders a tree (root `main` → children via `parent_branch_id`) with per-node status badge (`active` / `merged` / `abandoned`), fork point, merge point + strategy, abandonment reason. Click-through opens the branch's `BitemporalTimeline` filtered to `branch_id = <selected>`.
- [ ] 7.6 GUI History panel (`gui/src/views/EntityHistory.tsx`): consumes `GET /v1/entity/{id}/history` + `GET /v1/entity/{id}/supersession`. Bitemporal trail surface — shows every `valid_from` / `valid_to` / `superseded_at` event the entity went through, plus the SUPERSEDES / EVOLVES_FROM / OBSOLETES chain walks rendered as a directed graph. Reachable from the `Decisions` view (existing) via a new `[history]` chip per row.
- [ ] 7.7 Type generation — extend `gui/src/lib/api.ts` with the new HTTP envelopes (`TimelineEvent`, `BranchMetadata`, `EntityHistoryEntry`, `SupersessionChainNode`). The generator stays handwritten today; phase18 §7 ships the types alongside the view code.
- [ ] 7.8 Tests in `gui/src/views/*.test.tsx` (vitest + jsdom): one per view covering happy path, empty-state, error-state, filter persistence to localStorage (matching the pattern in `Tasks.tsx`).

## 8. Cross-cutting
- [x] 8.1 Knowledge captured inline per commit (the P0 ADR set is itself the load-bearing knowledge artefact; commit messages of §2.1–§2.14 record the deltas + design rationale). Post-P2 metric deltas land via `rulebook_knowledge_add` once the temporal classifier ships and the CDC harness gives a measurable baseline (blocked on §3 → phase14c).
- [x] 8.2 Memory carriers — the per-section commit messages + the §2.13 `MigrationReport` JSON shape carry the durable state. Operator-driven memory save fires after the §2.9 migration CLI runs; the report bytes drop into `rulebook_memory_save` with `kind = phase18-migration-count`.
- [x] 8.3 Phase17 coordination — phase17_cdc-code-doc-correlation P2 (cross-encoder rerank) and phase18 P2 (temporal classifier) share the fusion-pipeline refactor. Sequencing: rerank lands first; classifier wedges in front of rerank. The phase18 §3 design.md §2.2 already lists this dependency; phase17 P4 (supersession weighting) is now explicitly superseded by phase18 §3 (ADR-023 §1.6) — phase17 tasks.md tail will reference DEC-023 when phase17 §4 closes.

## 99. Mandatory tail (rulebook v5.3.0)
- [x] 99.1 Update or create documentation covering the implementation — Spec 30 (`docs/specs/30-bitemporal-schema.md`) shipped for P0+P1. Specs 31-35 (classifier / branches / timeline API / cross-project / pre-thinking) land alongside their implementing phases (P2-P5). 6 ADRs DEC-018..023 in `.rulebook/decisions/` document the design lock. Inline doc comments at every code site (`graph/bitemporal.rs`, `graph/branch.rs`, `graph/temporal_edges.rs`, `graph/schema.rs`, `graph/reindex_alias.rs`, `graph/migration.rs`, `fulltext/document.rs`, `fulltext/builders.rs`, `fulltext/settings.rs`, `embedder/chunker.rs`).
- [x] 99.2 Write tests covering the new behavior — 31 new unit tests across the P0+P1 surface: `graph::bitemporal::*` (9), `graph::branch::*` (6), `graph::temporal_edges::*` (6), `graph::reindex_alias::*` (3), `graph::migration::*` (4), `graph::schema::*` (2 new pins), `fulltext::settings::*` (1 new axis pin). Test gates pinned in spec 30 §11. Classifier state-machine unit tests + migration dry-run integration tests land with §3 (P2) when phase14c eval harness goes green.
- [x] 99.3 Run tests and confirm they pass — `cargo test -p cortex-workers --lib`: 867 passed (was 836 pre-phase18; +31). `cargo clippy -p cortex-workers -- -D warnings`: clean. `cargo check --workspace`: clean. The retrieval eval suite (`cortex-eval --suite retrieval`) is the P2 gate, not the P0+P1 gate — it runs when §3 lands with phase14c golden-set.
