[package]
name = "cortex-fulltext"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Cortex full-text indexer: maps enriched events to Meilisearch documents (spec 08)."

[lib]
path = "src/lib.rs"

[[bin]]
name = "cortex-fulltext-worker"
path = "src/main.rs"

[dependencies]
cortex-core = { path = "../cortex-core" }
cortex-classifier = { path = "../cortex-classifier" }
cortex-embedder = { path = "../cortex-embedder" }
cortex-storage = { path = "../cortex-storage" }

serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
chrono = { workspace = true }
anyhow = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
async-trait = { workspace = true }
reqwest = { workspace = true }

synap-sdk = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
wiremock = "0.6"
