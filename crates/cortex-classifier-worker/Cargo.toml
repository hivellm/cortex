[package]
name = "cortex-classifier-worker"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Cortex classifier worker: bridges cortex.events.{raw,bootstrap} to cortex.events.enriched."

[lib]
path = "src/lib.rs"

[[bin]]
name = "cortex-classifier-worker"
path = "src/main.rs"

[dependencies]
cortex-core = { path = "../cortex-core" }
cortex-classifier = { path = "../cortex-classifier" }
cortex-embedder = { path = "../cortex-embedder" }

serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
async-trait = { workspace = true }
chrono = { workspace = true }

synap-sdk = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
